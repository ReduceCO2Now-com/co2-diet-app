import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'ConfidenceChip',
    skip: 'ConfidenceChip widget not yet implemented',
    () {
      testWidgets(
        'renders green chip for high confidence band',
        (tester) async {},
      );

      testWidgets(
        'renders amber chip for medium confidence band',
        (tester) async {},
      );

      testWidgets(
        'chip is tappable and opens explanation sheet',
        (tester) async {},
      );
    },
  );
}
