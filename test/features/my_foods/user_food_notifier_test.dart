import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'UserFoodNotifier',
    skip: 'UserFoodNotifier not yet implemented',
    () {
      test(
        'saveCustomFood persists and refreshes the My Foods list',
        () {},
      );

      test(
        'saveOverride persists an override tied to '
        'overrideOfRef/overrideOfSource',
        () {},
      );

      test(
        'revertOverride removes the override and the original reappears',
        () {},
      );
    },
  );
}
