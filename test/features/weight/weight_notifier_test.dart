import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'WeightNotifier',
    skip: 'WeightNotifier not yet implemented',
    () {
      test(
        'build() loads all weight entries and current goal/reminder '
        'settings together',
        () {},
      );

      test(
        'logWeight appends a new entry and refreshes the '
        'chart-backing list',
        () {},
      );
    },
  );
}
