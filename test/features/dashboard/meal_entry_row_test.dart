import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'MealEntryRow',
    skip: 'MealEntryRow not yet implemented',
    () {
      testWidgets(
        'swipe reveals Edit, Duplicate, Delete actions',
        (tester) async {},
      );

      testWidgets(
        'row shows name, quantity, calories, and CO2 (not just '
        'name+quantity)',
        (tester) async {},
      );

      testWidgets(
        'a slot with zero entries hides its header entirely',
        (tester) async {},
      );
    },
  );
}
