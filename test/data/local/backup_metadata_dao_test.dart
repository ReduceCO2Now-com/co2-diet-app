import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'BackupMetadataDao',
    skip: 'BackupMetadataDao not yet implemented',
    () {
      test(
        'getMetadata/saveMetadata round-trip autoBackupFrequency and '
        'lastBackupAt',
        () {},
      );
    },
  );
}
