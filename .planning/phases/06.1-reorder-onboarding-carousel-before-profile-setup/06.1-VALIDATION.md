---
phase: 06.1
slug: reorder-onboarding-carousel-before-profile-setup
status: planned
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-04
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `flutter_test` (widget/unit) + `integration_test` (device-level) — both already configured, no setup needed |
| **Config file** | none dedicated — standard `flutter test` / `flutter test integration_test/` per existing project convention |
| **Quick run command** | `flutter test test/features/onboarding/onboarding_gate_test.dart` |
| **Full suite command** | `flutter test` |
| **Estimated runtime** | ~30-60 seconds (consistent with prior phases' suite size) |

---

## Sampling Rate

- **After every task commit:** `flutter test test/features/onboarding/onboarding_gate_test.dart` (fastest targeted feedback for the trigger-relocation behavior) — or the specific new/changed test file for that task.
- **After every plan wave:** `flutter test` (full suite — this phase touches shared router/gate code, cheap insurance against unrelated regressions).
- **Before `/gsd:verify-work`:** Full suite must be green, plus the manual flow-level re-verification checkpoint (redirect guard / bottom-nav-hide / screen-reader read-order across the new Carousel→Profile→Dashboard sequence).
- **Max feedback latency:** ~60 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|-----------|--------------------|-------------|--------|
| 06.1-01-T1 | 06.1-01 | 1 | Carousel exit buttons pass through to /profile without calling completeOnboarding(); last-slide button relabeled "Set Up Profile" | widget (tdd) | `flutter test test/features/onboarding/onboarding_carousel_screen_test.dart` | ❌ → created by this task | ⬜ pending |
| 06.1-01-T2 | 06.1-01 | 1 | Profile Setup forward button becomes the terminal completeOnboarding() trigger; stale test renamed; Carousel nav-hide test added | widget/integration (tdd) | `flutter test test/features/onboarding/onboarding_gate_test.dart` | ✅ existing, extended | ⬜ pending |
| 06.1-01-T3 | 06.1-01 | 1 | Flow-order code comments in app_router.dart/onboarding_gate_provider.dart updated (no logic change) | static + regression | `test -z "$(grep -rl 'Profile Setup → Carousel' lib/core/router/app_router.dart lib/features/onboarding/providers/onboarding_gate_provider.dart)" && flutter test test/features/onboarding/` | n/a (comment-only) | ⬜ pending |
| 06.1-02-T1 | 06.1-02 | 2 | Manual flow-level re-verification (redirect guard / bottom-nav-hide / screen-reader read-order) | manual (checkpoint:human-verify) | N/A — human checkpoint, "approved" resume-signal | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `test/features/onboarding/onboarding_carousel_screen_test.dart` — new widget test file, created and made GREEN by Plan 06.1-01 Task 1. Asserts both button labels ("Skip intro" unchanged, last-slide → "Set Up Profile") and that neither button triggers `completeOnboarding()`. Closes the pre-existing (not newly introduced) coverage gap.
- [x] `test/features/onboarding/onboarding_gate_test.dart` lines 236-274 — renamed test + updated its misleading comment (it previously described `completeOnboarding()` firing "as the Carousel does"; no longer true post-reorder), handled by Plan 06.1-01 Task 2 alongside the new button-tap and Carousel nav-hide tests. Same-file edit, no new fixture work.

*No test framework installation or shared-fixture work needed — `flutter_test`/`integration_test` are already fully configured project-wide.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Flow-level re-verification: redirect guard + bottom-nav-hide + screen-reader read-order across the new sequence | 06.1-CONTEXT.md "Verification Scope" decision | Per-screen accessibility properties (contrast, tap targets, dark mode) are unaffected by reorder and don't need re-auditing, but the *sequence* itself is exactly what this phase changes — a real screen-reader traversal of Carousel→Profile→Dashboard needs a human, not a widget test | Single scaled-down checkpoint (not a full 06-10-style 3-checkpoint audit), captured as Plan 06.1-02's sole task: on a real device, run VoiceOver/TalkBack through Splash→Welcome→Legal Consent→Carousel→Profile Setup→Dashboard once; confirm focus order reads sensibly, bottom nav stays hidden until Profile Setup's forward action, no redirect loop occurs, and onboarding-complete state persists across an app restart |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (06.1-02-T1 is the sole exception, correctly categorized as Manual-Only above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (T1/T2/T3 are all automated; only the final checkpoint is manual)
- [x] Wave 0 covers all MISSING references (2 gaps identified above, both closed by Plan 06.1-01)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
