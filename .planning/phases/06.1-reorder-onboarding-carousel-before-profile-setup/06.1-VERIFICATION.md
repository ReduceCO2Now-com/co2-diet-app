---
phase: 06.1-reorder-onboarding-carousel-before-profile-setup
verified: 2026-09-07T00:00:00Z
status: passed
score: 8/8 must-haves verified
---

# Phase 06.1: Reorder onboarding — Carousel before Profile Setup Verification Report

**Phase Goal:** Reorder the onboarding flow so the Carousel appears before Profile Setup, with onboarding-completion now triggered by Profile Setup's terminal action instead of the Carousel's, while preserving the Phase 6 (06-10) bottom-nav-hide fix and redirect-guard behavior intact. Shipped sequence: Splash → Welcome → Legal Consent → (Connectivity Choice) → Carousel → Profile Setup → Dashboard.

**Verified:** 2026-09-07
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Plan 06.1-01)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Carousel's "Skip intro" and last-slide buttons navigate to `/profile` and never call `completeOnboarding()` | ✓ VERIFIED | `onboarding_carousel_screen.dart:74-76,95,164` — both buttons call `_goToProfile()`, a synchronous method that only does `context.go('/profile')`. No `onboarding_gate_provider` import remains in the file. 3 widget tests in `onboarding_carousel_screen_test.dart` assert `hasCompletedOnboarding` stays non-true after both exit paths — all pass. |
| 2 | Carousel's last-slide button is labeled "Set Up Profile", not "Go to Dashboard"; "Skip intro" label unchanged | ✓ VERIFIED | `onboarding_carousel_screen.dart:96` ("Skip intro"), `:165` ("Set Up Profile"). Widget test asserts `find.text('Set Up Profile')` found, `find.text('Go to Dashboard')` absent — passes. |
| 3 | Profile Setup's forward button ("Go to Dashboard") is the only call site that triggers `completeOnboarding()`, then navigates to `/dashboard` | ✓ VERIFIED | `profile_screen.dart:41-44` `_finishOnboarding()` awaits `completeOnboarding()` then `if (mounted) context.go('/dashboard')`, wired to the `FilledButton.onPressed` at line 147. Grepped entire `lib/` — this is the sole call site of `completeOnboarding()`. |
| 4 | Bottom nav stays hidden on both `/onboarding-carousel` and `/profile` until onboarding completes | ✓ VERIFIED | `app_router.dart:105-106` `AppShell`'s `!hasOnboarded ? null : NavigationBar(...)` gate is generic and unchanged. Widget tests in `onboarding_gate_test.dart` (lines 161-183 for `/profile`, 185-202 for `/onboarding-carousel`) both assert `NavigationBar` absent pre-completion — pass. |
| 5 | Code comments in `app_router.dart` and `onboarding_gate_provider.dart` describe the new Carousel-before-Profile-Setup order, not the old order | ✓ VERIFIED | `app_router.dart:71-88` `AppShell` doc comment attributes `completeOnboarding()` to Profile Setup's "Go to Dashboard" button. `onboarding_gate_provider.dart:19-26,31-32,48` all reference `ProfileScreen` as current call site and "(Splash → Welcome → Legal Consent → Carousel → Profile Setup)" order. Grep for stale phrase "Profile Setup → Carousel" returns zero matches in either file. |

### Observable Truths (Plan 06.1-02)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 6 | A real-device screen-reader pass confirms Carousel → Profile Setup → Dashboard reads in a sensible focus order | ✓ VERIFIED (human-approved) | `06.1-02-SUMMARY.md` "Result: Pass" — Part B of `06.1-02-CHECKLIST.md` confirmed by user's "approved" resume signal per the plan's own resume-signal contract. |
| 7 | A real-device pass confirms the bottom nav bar stays hidden through both Carousel and Profile Setup and appears only after Profile Setup's terminal action | ✓ VERIFIED (human-approved) | `06.1-02-SUMMARY.md` Part A observations; same "approved" gate. |
| 8 | A real-device pass confirms no redirect loop occurs anywhere in the new sequence and that onboarding-complete state persists across an app restart | ✓ VERIFIED (human-approved) | `06.1-02-SUMMARY.md` Part C observations (force-quit/relaunch, background/return both land on Dashboard); same "approved" gate. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/features/onboarding/screens/onboarding_carousel_screen.dart` | Pure pass-through exit handlers (no `completeOnboarding` call) | ✓ VERIFIED | `StatefulWidget` (not `ConsumerStatefulWidget`); `_goToProfile()` is the sole handler for both exit buttons; no Riverpod import. |
| `lib/features/profile/screens/profile_screen.dart` | Terminal `completeOnboarding()` trigger on the onboarding-only forward button | ✓ VERIFIED | `_finishOnboarding()` (lines 41-44) wired to `FilledButton.onPressed` (line 147), gated by unchanged `if (!hasOnboarded)` (line 142). |
| `test/features/onboarding/onboarding_carousel_screen_test.dart` | New widget test: button labels + non-invocation of `completeOnboarding()` | ✓ VERIFIED | 112 lines (>40 min), 3 tests, all pass (`flutter test` run confirmed). |
| `test/features/onboarding/onboarding_gate_test.dart` | Updated/renamed redirect-guard tests + button-tap trigger test + carousel nav-hide test | ✓ VERIFIED | Contains renamed test ("as Profile Setup does", line 256), new button-tap trigger test (line 336), new carousel nav-hide test (line 185). All 12 tests in file pass. |
| `.planning/phases/06.1-reorder-onboarding-carousel-before-profile-setup/06.1-02-SUMMARY.md` | Recorded pass/fail outcome of the manual flow-level re-verification checkpoint | ✓ VERIFIED | Exists, records "Result: Pass" for all 9 manual steps (Parts A/B/C). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `onboarding_carousel_screen.dart` | `/profile` | `context.go('/profile')` in both exit handlers | ✓ WIRED | Line 75, both call sites (95, 164) route through `_goToProfile()`. |
| `profile_screen.dart` | `onboarding_gate_provider.dart` | `ref.read(onboardingGateProvider.notifier).completeOnboarding()` in forward button's `onPressed` | ✓ WIRED | Line 42, inside `_finishOnboarding()`, assigned to `onPressed` at line 147. |
| `profile_screen.dart` | `/dashboard` | `context.go('/dashboard')` after `completeOnboarding()` resolves, guarded by `mounted` | ✓ WIRED | Line 43: `if (mounted) context.go('/dashboard');`. |
| `legal_consent_screen.dart` | `/connectivity-choice` → `/onboarding-carousel` | `context.go(...)` chain (fixed during 06.1-02) | ✓ WIRED | `legal_consent_screen.dart:70` goes to `/connectivity-choice`; `connectivity_choice_screen.dart:46` goes to `/onboarding-carousel` — closes the full Legal Consent → Connectivity Choice → Carousel chain that was found broken (orphaned Carousel) during 06.1-02 prep and fixed in commit `7ea47aa`. |
| `app_router.dart` redirect guard | `onboardingOnlyRoutes` / `allowedPreOnboarding` | Route-membership lists | ✓ UNCHANGED (as required) | `/profile` remains outside `onboardingOnlyRoutes` and inside `allowedPreOnboarding` (lines 157-175) — CONTEXT.md's "no routing-logic change" requirement holds. `/connectivity-choice` and `/onboarding-carousel` both present in `onboardingOnlyRoutes`. |

### Requirements Coverage

Per `06.1-CONTEXT.md` and `ROADMAP.md` (line 233, line 355), this phase is explicitly context-driven with **no REQUIREMENTS.md IDs assigned** — it originates from a deferred UX todo raised during Phase 6's `06-10` verification, not a tracked requirement. The plans' own pseudo-requirement tags (`CTX-TRIGGER`, `CTX-COPY`, `CTX-ROUTE-GUARD` in Plan 01; `CTX-VERIFY` in Plan 02) are traceable to `06.1-CONTEXT.md`'s locked decisions rather than to `REQUIREMENTS.md` entries.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CTX-TRIGGER | 06.1-01 | Completion trigger moves from Carousel to Profile Setup | ✓ SATISFIED | See Truths 1, 3 above. |
| CTX-COPY | 06.1-01 | Button label matrix ("Set Up Profile" / "Go to Dashboard" / "Skip intro" unchanged) | ✓ SATISFIED | See Truth 2 above. |
| CTX-ROUTE-GUARD | 06.1-01 | Redirect guard / bottom-nav-hide behavior preserved, comments updated | ✓ SATISFIED | See Truths 4, 5 above. |
| CTX-VERIFY | 06.1-02 | Real-device flow-level re-verification | ✓ SATISFIED | See Truths 6-8 above. |

Confirmed absence of REQUIREMENTS.md IDs is intentional and documented (`ROADMAP.md:233,355`, `06.1-CONTEXT.md`) — not a gap. `grep` of `REQUIREMENTS.md` for any `06.1`/`CTX-*` reference returns zero matches, consistent with this being deliberately out-of-scope for the requirements ledger.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `test/features/onboarding/onboarding_gate_test.dart` | 165-168 | Stale docstring on the pre-existing `/profile` nav-hide test still reads "...bypassing the Carousel, the only call site that invokes completeOnboarding()" | ℹ️ Info | Factually inaccurate post-reorder (Profile Setup, not the Carousel, is now that call site) but is a test-file docstring, not application code — outside the scope of Task 3's grep check (`app_router.dart`/`onboarding_gate_provider.dart` only) and does not affect test correctness or any assertion. Cosmetic only. |

`flutter analyze` on all 5 phase-touched source files (`onboarding_carousel_screen.dart`, `profile_screen.dart`, `app_router.dart`, `onboarding_gate_provider.dart`, `legal_consent_screen.dart`): **No issues found.** No blocker or warning-level anti-patterns detected.

### Human Verification Required

None outstanding. The one item requiring human verification (real-device flow/screen-reader/persistence pass) was already executed and approved as part of Plan 06.1-02's blocking `checkpoint:human-verify` task — recorded in `06.1-02-SUMMARY.md` ("Result: Pass", all 9 steps confirmed via the user's "approved" resume signal per the plan's own resume-signal contract).

### Gaps Summary

No gaps found. All 8 must-have truths across both plans are verified: automated widget tests confirm the Carousel is a pure pass-through with correct labels and no `completeOnboarding()` invocation; Profile Setup is confirmed as the sole terminal trigger; the bottom-nav-hide gate and redirect-guard route-membership lists are confirmed unchanged in logic; code comments are confirmed updated with zero stale "Profile Setup → Carousel" references; and the full `flutter test test/features/onboarding/ test/features/profile/ test/features/legal/legal_consent_screen_test.dart` suite (38 tests) passes with `flutter analyze` clean.

One noteworthy finding surfaced during Plan 06.1-02's own prep work (not a residual gap, already fixed and covered by an updated test before the device checkpoint ran): Legal Consent originally routed straight to `/profile`, leaving the reordered Carousel unreachable in the shipped build despite Plan 06.1-01's widget tests all passing (none asserted on cross-screen route sequence). This was found and fixed (`legal_consent_screen.dart` now routes to `/connectivity-choice` → `/onboarding-carousel` → `/profile`), confirmed present in the current source, covered by `test/features/legal/legal_consent_screen_test.dart`, and independently re-confirmed by this verification's own read of the router chain and full test run.

---

*Verified: 2026-09-07*
*Verifier: Claude (gsd-verifier)*
