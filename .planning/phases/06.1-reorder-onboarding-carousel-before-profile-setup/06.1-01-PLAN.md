---
phase: 06.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/onboarding/screens/onboarding_carousel_screen.dart
  - test/features/onboarding/onboarding_carousel_screen_test.dart
  - lib/features/profile/screens/profile_screen.dart
  - test/features/onboarding/onboarding_gate_test.dart
  - lib/core/router/app_router.dart
  - lib/features/onboarding/providers/onboarding_gate_provider.dart
autonomous: true
requirements: [CTX-TRIGGER, CTX-COPY, CTX-ROUTE-GUARD]

must_haves:
  truths:
    - "Carousel's 'Skip intro' and last-slide buttons navigate to /profile and never call completeOnboarding()"
    - "Carousel's last-slide button is labeled 'Set Up Profile', not 'Go to Dashboard'; 'Skip intro' label is unchanged"
    - "Profile Setup's forward button ('Go to Dashboard') is the only call site that triggers completeOnboarding(), then navigates to /dashboard"
    - "Bottom nav stays hidden on both /onboarding-carousel and /profile until onboarding completes"
    - "Code comments in app_router.dart and onboarding_gate_provider.dart describe the new Carousel-before-Profile-Setup order, not the old order"
  artifacts:
    - path: "lib/features/onboarding/screens/onboarding_carousel_screen.dart"
      provides: "Pure pass-through exit handlers (no completeOnboarding call)"
    - path: "lib/features/profile/screens/profile_screen.dart"
      provides: "Terminal completeOnboarding() trigger on the onboarding-only forward button"
    - path: "test/features/onboarding/onboarding_carousel_screen_test.dart"
      provides: "New widget test: button labels + non-invocation of completeOnboarding()"
      min_lines: 40
    - path: "test/features/onboarding/onboarding_gate_test.dart"
      provides: "Updated/renamed redirect-guard tests + new button-tap trigger test + new carousel nav-hide test"
  key_links:
    - from: "lib/features/onboarding/screens/onboarding_carousel_screen.dart"
      to: "/profile"
      via: "context.go('/profile') in both exit handlers"
      pattern: "context\\.go\\('/profile'\\)"
    - from: "lib/features/profile/screens/profile_screen.dart"
      to: "lib/features/onboarding/providers/onboarding_gate_provider.dart"
      via: "ref.read(onboardingGateProvider.notifier).completeOnboarding() in the forward button's onPressed"
      pattern: "onboardingGateProvider\\.notifier\\)\\.completeOnboarding\\(\\)"
    - from: "lib/features/profile/screens/profile_screen.dart"
      to: "/dashboard"
      via: "context.go('/dashboard') after completeOnboarding() resolves, guarded by mounted"
      pattern: "context\\.go\\('/dashboard'\\)"
---

<objective>
Relocate the onboarding-completion trigger from the Carousel to Profile Setup so the shipped flow becomes Splash → Welcome → Legal Consent → Carousel → Profile Setup → Dashboard, per `06.1-CONTEXT.md`'s locked "Onboarding-Completion Trigger" and "Screen CTAs & Copy" decisions. The Carousel becomes a pure pass-through to `/profile` (no `completeOnboarding()` call, on either exit button); Profile Setup's forward button becomes the terminal action that calls `completeOnboarding()` then navigates to `/dashboard`. This preserves the Phase 6 (06-10) bottom-nav-hide bug fix intact, since `AppShell`'s nav-hide gate is generic and keyed off `onboardingGateProvider` regardless of which screen flips it.

Purpose: Deliberate UX reconsideration (explain the app's purpose via the Carousel before asking for personal data in Profile Setup) without reintroducing the exact bottom-nav-bypass bug 06-10 fixed.
Output: Edited `onboarding_carousel_screen.dart` + `profile_screen.dart`; comment-only updates to `app_router.dart` + `onboarding_gate_provider.dart`; a new dedicated Carousel widget test file (Wave 0 gap closed); `onboarding_gate_test.dart` extended with a button-tap trigger test and a Carousel nav-hide test, and its one stale test renamed.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-reorder-onboarding-carousel-before-profile-setup/06.1-CONTEXT.md
@.planning/phases/06.1-reorder-onboarding-carousel-before-profile-setup/06.1-RESEARCH.md

@lib/features/onboarding/screens/onboarding_carousel_screen.dart
@lib/features/profile/screens/profile_screen.dart
@lib/core/router/app_router.dart
@lib/features/onboarding/providers/onboarding_gate_provider.dart
@test/features/onboarding/onboarding_gate_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carousel becomes a pure pass-through to /profile</name>
  <files>lib/features/onboarding/screens/onboarding_carousel_screen.dart, test/features/onboarding/onboarding_carousel_screen_test.dart</files>
  <behavior>
    - "Skip intro" button navigates to /profile without completing onboarding: pump `OnboardingCarouselScreen` inside `ProviderScope` (override `sharedPreferencesProvider` with a mock instance via `SharedPreferences.setMockInitialValues({})` then `getInstance()`) wrapped in `MaterialApp.router` using a minimal `GoRouter` (`initialLocation: '/onboarding-carousel'`, routes: `/onboarding-carousel` -> `OnboardingCarouselScreen()`, `/profile` -> a throwaway `Scaffold(body: Text('PROFILE_ROUTE_MARKER'))`). Tap "Skip intro"; `pumpAndSettle`; expect `find.text('PROFILE_ROUTE_MARKER')` `findsOneWidget` and expect the container's `sharedPreferencesProvider` read `.getBool('hasCompletedOnboarding')` to still be null/false (never true).
    - Last-slide button is labeled "Set Up Profile", not "Go to Dashboard": same harness; drag the `PageView` left (e.g. `tester.drag(find.byType(PageView), const Offset(-2000, 0))` with a `pump` between each drag, twice, to reach the 3rd/last slide); `pumpAndSettle`; expect `find.text('Set Up Profile')` `findsOneWidget` and `find.text('Go to Dashboard')` `findsNothing`.
    - Last-slide button navigates to /profile without completing onboarding: same last-slide setup; tap "Set Up Profile"; `pumpAndSettle`; expect `find.text('PROFILE_ROUTE_MARKER')` `findsOneWidget` and `hasCompletedOnboarding` still null/false (same assertion shape as case 1).
  </behavior>
  <action>
    Per CONTEXT.md's "Onboarding-Completion Trigger" and "Screen CTAs & Copy" decisions. Follow RED-then-GREEN: first create test/features/onboarding/onboarding_carousel_screen_test.dart implementing the <behavior> cases above and run it — it must fail against the current implementation (old "Go to Dashboard" label, current completeOnboarding() call on both exit buttons). Then edit lib/features/onboarding/screens/onboarding_carousel_screen.dart: rename the private async _finishOnboarding() method (lines 74-77) to a synchronous _goToProfile() that only calls context.go('/profile') — remove the await ref.read(onboardingGateProvider.notifier).completeOnboarding() call entirely (no mounted guard needed once there is no async gap before navigation). Update both call sites to the renamed method: the "Skip intro" TextButton's onPressed (line 96) and the last-slide FilledButton's onPressed (line 165). Relabel the last-slide FilledButton's Text from "Go to Dashboard" to "Set Up Profile" (line 166); leave "Skip intro" unchanged (per CONTEXT.md it describes skipping the slides, not a destination). Per CONTEXT.md's "Carousel Content" decision, do not touch the `_slides` list's literal heading/body text — only the method, its call sites, and the last-slide button label change. Since the widget no longer reads any Riverpod provider, convert OnboardingCarouselScreen from ConsumerStatefulWidget to StatefulWidget and _OnboardingCarouselScreenState from ConsumerState<OnboardingCarouselScreen> to State<OnboardingCarouselScreen>; remove the now-unused imports of onboarding_gate_provider.dart and flutter_riverpod/flutter_riverpod.dart. Update the class doc comment (lines 48-53) to describe the Carousel's new position (runs between Legal Consent and Profile Setup, not after Profile Setup) and remove the sentence claiming both exit paths persist completeOnboarding — exact wording is your discretion per CONTEXT.md. Re-run the test file to confirm GREEN.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/onboarding_carousel_screen_test.dart</automated>
  </verify>
  <done>Both Carousel exit buttons navigate to /profile via context.go without invoking completeOnboarding(); last-slide button reads "Set Up Profile"; all 3 new widget tests pass; OnboardingCarouselScreen no longer imports onboarding_gate_provider.dart or flutter_riverpod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile Setup becomes the terminal onboarding-completion trigger</name>
  <files>lib/features/profile/screens/profile_screen.dart, test/features/onboarding/onboarding_gate_test.dart</files>
  <behavior>
    - Tapping "Go to Dashboard" on /profile before onboarding completes triggers completion and navigates to Dashboard: using onboarding_gate_test.dart's existing pumpApp harness with hasCompletedOnboarding:false, navigate to /profile, find and tap the FilledButton labeled "Go to Dashboard", pumpAndSettle; expect the Dashboard AppBar text found (same assertion pattern as the file's existing "completed onboarding redirects away from /welcome to /dashboard" test) and expect container.read(sharedPreferencesProvider).getBool('hasCompletedOnboarding') to be true.
    - Bottom navigation bar is hidden on /onboarding-carousel before onboarding completes: mirroring the file's existing "/profile" equivalent test (lines 161-183), using pumpApp with hasCompletedOnboarding:false, navigate to /onboarding-carousel, pumpAndSettle; expect find.text('Skip intro') findsOneWidget (sanity-checks the right screen rendered) and find.byType(NavigationBar) findsNothing.
    - Stale test renamed: the test currently named "calling completeOnboarding() mid-session (as the Carousel does) then navigating to /dashboard produces no redirect back to /splash" (lines 236-274) is renamed to reference Profile Setup as the new trigger site (e.g. "as Profile Setup does"), and its in-body comment no longer references OnboardingCarouselScreen._finishOnboarding(); its existing assertions (call completeOnboarding() directly on the notifier, navigate to /dashboard, expect no redirect) remain byte-for-byte unchanged — this test's logic is generic to the redirect guard, not to which screen calls the method.
  </behavior>
  <action>
    Per CONTEXT.md's "Onboarding-Completion Trigger" decision and RESEARCH.md's Pattern 1 (safe async trigger-then-navigate with mounted guard). Follow RED-then-GREEN: first extend test/features/onboarding/onboarding_gate_test.dart per <behavior> above — add the button-tap trigger test and the /onboarding-carousel nav-hide test, and rename the stale test (lines 236-274) plus its in-body comment without touching its assertions (Pitfall 2 in RESEARCH.md). Run the file; the new button-tap test must fail against the current implementation ("Continue" label, no completeOnboarding() call). Then edit lib/features/profile/screens/profile_screen.dart's onboarding-only forward button (lines 132-141): change the Text label from "Continue" to "Go to Dashboard"; change onPressed from the synchronous () => context.go('/onboarding-carousel') to an async handler mirroring OnboardingCarouselScreen's pre-edit _finishOnboarding() pattern — await ref.read(onboardingGateProvider.notifier).completeOnboarding(), then if (mounted) context.go('/dashboard'). Update the comment at lines 129-131 ("Onboarding-only 'Continue' button...") to describe this as the onboarding flow's terminal action — exact wording your discretion. Per CONTEXT.md's "Skip Affordance" decision, do not add any new "Skip for now" link to Profile Setup — the existing forward button already functions as the skip path since every field is optional and auto-saves. Leave the surrounding `if (!hasOnboarded) ...` conditional gate exactly as-is (Pitfall 4 in RESEARCH.md — only the FilledButton's onPressed/child change, never the condition). Re-run the full file to confirm GREEN.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/onboarding_gate_test.dart</automated>
  </verify>
  <done>ProfileScreen's forward button is labeled "Go to Dashboard", awaits completeOnboarding() then navigates to /dashboard with a mounted guard; the if (!hasOnboarded) gate is unchanged; onboarding_gate_test.dart's stale test is renamed/re-commented with zero assertion changes; two new tests (button-tap trigger, carousel nav-hide) pass; full file green.</done>
</task>

<task type="auto">
  <name>Task 3: Update flow-order code comments in router and gate provider</name>
  <files>lib/core/router/app_router.dart, lib/features/onboarding/providers/onboarding_gate_provider.dart</files>
  <action>
    Comment-only updates, zero logic changes (per CONTEXT.md's "Route Guard" decision: "mechanical, not a routing-logic change"). In lib/core/router/app_router.dart, update AppShell's class doc comment (lines 70-83) — it currently narrates the pre-reorder order ("before the Carousel/completeOnboarding() has ever run", attributing the completeOnboarding() call to the Carousel) — reword to describe Profile Setup as the step that now owns completeOnboarding(), and the Carousel as the earlier pass-through step; keep the historical 06-10 bug-fix narrative (why the bar is hidden generically off onboardingGateProvider rather than a fixed per-screen list) intact since it remains accurate. Do NOT touch the redirect guard's onboardingOnlyRoutes/allowedPreOnboarding lists or their membership (Pitfall 3 in RESEARCH.md — /profile must stay excluded from onboardingOnlyRoutes; this is unchanged by the reorder). In lib/features/onboarding/providers/onboarding_gate_provider.dart, update: (1) OnboardingGateNotifier's class doc comment (line 31), which lists the flow order "(Splash → Welcome → Legal Consent → Profile Setup → Carousel)" — change to "(Splash → Welcome → Legal Consent → Carousel → Profile Setup)"; (2) sharedPreferencesProvider's doc comment (lines 19-25), which currently attributes the historical UnmountedRefException bug to "OnboardingCarouselScreen ... calling completeOnboarding() from its Go to Dashboard button" — reword to reference ProfileScreen as the current call site (the keepAlive: true rationale itself is unchanged, only which screen it now protects). After editing, grep both files for the literal stale phrase "Profile Setup → Carousel" and confirm zero matches in each.
  </action>
  <verify>
    <automated>test -z "$(grep -rl 'Profile Setup → Carousel' lib/core/router/app_router.dart lib/features/onboarding/providers/onboarding_gate_provider.dart)" && flutter test test/features/onboarding/</automated>
  </verify>
  <done>Both files' doc comments describe the new Carousel-before-Profile-Setup order; no occurrences of the stale "Profile Setup → Carousel" phrase remain; onboardingOnlyRoutes/allowedPreOnboarding membership is unchanged; full onboarding test directory is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Device-local UI navigation | User-triggered button taps drive `go_router` navigation; no network or external input crosses this boundary — this phase is entirely client-side |
| On-device `SharedPreferences` gate flag | `hasCompletedOnboarding` stored in plaintext `SharedPreferences`; a rooted/jailbroken device could tamper with it directly (pre-existing, documented risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06.1-01 | Tampering | `SharedPreferences` `hasCompletedOnboarding` flag (`onboarding_gate_provider.dart`) | accept | Pre-existing accepted risk, documented since Phase 6 (`onboarding_gate_provider.dart` line 41: "a plain on-device SharedPreferences value ... accepted risk"). This phase's trigger relocation does not change the underlying trust model — still a plain on-device boolean enforced only for normal-navigation UX, not tamper resistance. |
| T-06.1-02 | Elevation of Privilege (UX bypass) | `AppShell` bottom-nav visibility gate (`app_router.dart`) | mitigate | Verified `AppShell`'s `!hasOnboarded ? null : NavigationBar(...)` gate is left logically unchanged and generically covers both pre-completion screens under the new trigger location (Carousel now first, Profile Setup now terminal). Task 2 adds an explicit widget test (`onboarding_gate_test.dart`) asserting `NavigationBar` is absent on `/onboarding-carousel` pre-completion, in addition to the pre-existing `/profile` coverage — closing the exact regression risk CONTEXT.md flagged as the reason for relocating the trigger (rather than leaving it at Carousel-exit and just swapping screen order, which was explicitly rejected). |

This phase introduces no new external attack surface — it moves a UX-gate trigger between two already-trusted, already-reviewed client-side screens within the same trust boundary Phase 6 established.
</threat_model>

<verification>
Run the full onboarding/profile test suites: `flutter test test/features/onboarding/ test/features/profile/`. Grep confirms zero remaining occurrences of the stale "Profile Setup → Carousel" order phrase in `app_router.dart`/`onboarding_gate_provider.dart`. `flutter analyze` should be clean (no unused-import warnings from the `ConsumerStatefulWidget` → `StatefulWidget` conversion in Task 1).
</verification>

<success_criteria>
- Carousel's two exit buttons never call `completeOnboarding()` and both navigate to `/profile`.
- Carousel's last-slide button reads "Set Up Profile"; "Skip intro" is unchanged.
- Profile Setup's forward button reads "Go to Dashboard", awaits `completeOnboarding()`, then navigates to `/dashboard` with a `mounted` guard.
- Bottom nav stays hidden on both `/onboarding-carousel` and `/profile` pre-completion (proven by widget tests).
- All updated/created tests pass; no regressions in the wider onboarding/profile test suites.
- No stale "Profile Setup → Carousel" order references remain in code comments.
</success_criteria>

<output>
Create `.planning/phases/06.1-reorder-onboarding-carousel-before-profile-setup/06.1-01-SUMMARY.md` when done
</output>
