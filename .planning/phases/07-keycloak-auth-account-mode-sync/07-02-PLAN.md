---
phase: 07-keycloak-auth-account-mode-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - android/app/build.gradle.kts
  - ios/Runner/Info.plist
  - lib/domain/entities/auth_state.dart
  - lib/domain/services/keycloak_config.dart
  - lib/domain/services/backend_config.dart
  - lib/core/di/auth_providers.dart
autonomous: false
requirements:
  - AUTH-10

user_setup: []

must_haves:
  truths:
    - "pubspec.yaml declares flutter_appauth 12.0.2 and flutter_secure_storage 11.0.0 as direct dependencies with dated pub.dev-verification comment blocks, matching this project's existing convention"
    - "No Firebase or Supabase package appears anywhere in pubspec.yaml/pubspec.lock after this plan (AUTH-10) -- dart run tool/check_privacy_deps.dart stays green"
    - "AuthState is a plain Dart sealed class (not Freezed) with AuthUnauthenticated/AuthAuthenticated variants, mirroring FoodSearchState's established pattern"
    - "KeycloakConfig and BackendConfig centralize every realm/client/redirect/backend-URL value in one place each, each value marked [ASSUMED] pending Tomris's real realm/deployment"
    - "The Android manifestPlaceholders redirect scheme and iOS CFBundleURLTypes scheme are byte-for-byte identical to KeycloakConfig.redirectUrl's scheme"
  artifacts:
    - path: "lib/domain/entities/auth_state.dart"
      provides: "AuthState sealed class (AuthUnauthenticated{sessionExpired}, AuthAuthenticated{email,accessToken})"
    - path: "lib/domain/services/keycloak_config.dart"
      provides: "KeycloakConfig: issuer, clientId, redirectUrl, scopes, appleIdpAlias, googleIdpAlias"
    - path: "lib/domain/services/backend_config.dart"
      provides: "BackendConfig: baseUrl [ASSUMED placeholder]"
    - path: "lib/core/di/auth_providers.dart"
      provides: "secureStorageProvider, appAuthProvider, authHttpClientProvider (keepAlive wrapper providers)"
  key_links:
    - from: "android/app/build.gradle.kts"
      to: "lib/domain/services/keycloak_config.dart"
      via: "manifestPlaceholders redirect scheme matches KeycloakConfig.redirectUrl's scheme"
      pattern: "appAuthRedirectScheme"
    - from: "ios/Runner/Info.plist"
      to: "lib/domain/services/keycloak_config.dart"
      via: "CFBundleURLSchemes entry matches KeycloakConfig.redirectUrl's scheme"
      pattern: "CFBundleURLSchemes"

install_reference:
  packages:
    - name: flutter_appauth
      version: "12.0.2"
      pub_score: "160/160"
      publisher: "dexterx.dev (github.com/MaikuB/flutter_appauth)"
    - name: flutter_secure_storage
      version: "11.0.0"
      pub_score: "160/160"
      publisher: "steenbakker.dev (github.com/juliansteenbakker/flutter_secure_storage)"
---

<objective>
Install the two new packages every later Phase 7 plan depends on (flutter_appauth, flutter_secure_storage) behind a single package-legitimacy checkpoint, wire the native Android/iOS redirect-URI scheme they require, and create the shared contracts (AuthState, KeycloakConfig, BackendConfig, and the thin DI wrapper providers) that Plans 07-03 through 07-08 all build on. Building contracts first (interface-first ordering) means no later plan has to guess at these shapes.

Purpose: centralizing every Keycloak/backend value in KeycloakConfig/BackendConfig -- each explicitly [ASSUMED] per 07-RESEARCH.md's Assumptions Log (A1, A2, A3, A4) -- means the eventual real-realm handoff from Tomris is a one-file change, not a scattered find-and-replace (07-RESEARCH.md Anti-Patterns to Avoid).

Output: two new pubspec dependencies (human-approved), native redirect-scheme wiring on both platforms, AuthState, KeycloakConfig, BackendConfig, and lib/core/di/auth_providers.dart.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-keycloak-auth-account-mode-sync/07-CONTEXT.md
@.planning/phases/07-keycloak-auth-account-mode-sync/07-RESEARCH.md
@lib/features/food_search/providers/food_search_state.dart
@lib/core/di/providers.dart
@lib/core/di/legal_providers.dart
@pubspec.yaml
@android/app/build.gradle.kts
@ios/Runner/Info.plist
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Package legitimacy checkpoint: flutter_appauth, flutter_secure_storage</name>
  <what-built>Nothing yet -- this checkpoint precedes installing two new pubspec.yaml dependencies: flutter_appauth 12.0.2, flutter_secure_storage 11.0.0.</what-built>
  <how-to-verify>
    pub.dev/Dart is not a slopcheck-supported ecosystem, so this follows the same independent-signal review + human-approval precedent as Plans 04-11/05-08/05-09/06-02. Please check each package directly on pub.dev before approving:
    1. https://pub.dev/packages/flutter_appauth -- confirm 160/160 pub score, publisher dexterx.dev, first published 2019 (7 years active maintenance), latest release 2026-06-26, github.com/MaikuB/flutter_appauth. This is the project's own locked decision (STATE.md: "Auth: Keycloak OIDC + PKCE via flutter_appauth") -- confirming legitimacy, not evaluating alternatives.
    2. https://pub.dev/packages/flutter_secure_storage -- confirm 160/160 pub score, verified publisher steenbakker.dev, 3.47M 30-day downloads, 4.47k likes, github.com/juliansteenbakker/flutter_secure_storage.
    3. http is already a transitive dependency (visible in pubspec.lock today) -- this plan only promotes it to a direct dependency (matching the project's `path`-package precedent for depend_on_referenced_packages); no new package identity is being introduced.
    Neither flutter_appauth nor flutter_secure_storage phones home or bundles analytics -- flutter_appauth only talks to the system browser plus the configured OIDC issuer (a first-party endpoint once Tomris's realm exists); flutter_secure_storage is purely on-device Keychain/Keystore access. Confirms AUTH-07/AUTH-10/PRIV-08 compliance.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `flutter pub add flutter_appauth flutter_secure_storage http`, or describe concerns.</resume-signal>
  <action>No automated action in this task -- it is a blocking human-verify checkpoint. The actual `flutter pub add` install is the first line of the next task's action, which only runs after this checkpoint is approved.</action>
</task>

<task type="auto">
  <name>Task 1: Install packages + native redirect-scheme wiring</name>
  <files>pubspec.yaml, android/app/build.gradle.kts, ios/Runner/Info.plist</files>
  <action>
    Run `flutter pub add flutter_appauth flutter_secure_storage http` (only after the checkpoint above is approved). Add a version-comment block to each new pubspec.yaml entry matching this project's existing convention (see flutter_slidable's or fl_chart's comment block) citing the pub score, publisher, and this session's verification date. http needs only a one-line comment noting it was already transitive (via openfoodfacts) and is now declared directly per depend_on_referenced_packages, since lib/domain/services/backend_config.dart and the realm-discovery check both import it directly.

    Android: in android/app/build.gradle.kts's defaultConfig block, add a manifestPlaceholders assignment setting "appAuthRedirectScheme" to "com.reduceco2now.co2diet.auth" (per flutter_appauth's README setup instructions, 07-RESEARCH.md Pitfall 2 -- lowercase scheme, no other plugin currently contributes this placeholder key so a plain assignment is safe here).

    iOS: in ios/Runner/Info.plist, add a CFBundleURLTypes array entry (sibling to the existing top-level keys, containing a dict with CFBundleURLSchemes) whose single scheme string is "com.reduceco2now.co2diet.auth" -- matches the Android scheme exactly, and matches KeycloakConfig.redirectUrl created in Task 2 ("com.reduceco2now.co2diet.auth://callback").
  </action>
  <verify>
    <automated>flutter pub get; dart run tool/check_privacy_deps.dart; grep -c "appAuthRedirectScheme" android/app/build.gradle.kts; grep -c "com.reduceco2now.co2diet.auth" ios/Runner/Info.plist</automated>
  </verify>
  <done>flutter_appauth and flutter_secure_storage appear in pubspec.yaml/pubspec.lock. The privacy blocklist script still exits 0 (AUTH-10). The redirect scheme string appears in both the Android manifest-placeholder assignment and the iOS Info.plist.</done>
</task>

<task type="auto">
  <name>Task 2: AuthState, KeycloakConfig, BackendConfig, auth_providers.dart</name>
  <files>
    lib/domain/entities/auth_state.dart,
    lib/domain/services/keycloak_config.dart,
    lib/domain/services/backend_config.dart,
    lib/core/di/auth_providers.dart
  </files>
  <action>
    Create lib/domain/entities/auth_state.dart: a plain Dart `sealed class AuthState` (not Freezed -- mirrors FoodSearchState's established pattern: `const AuthState()` base constructor, two static ergonomic factories `AuthState.unauthenticated({bool sessionExpired = false})` and `AuthState.authenticated({required String email, String? accessToken})`) with two `final class` variants: `AuthUnauthenticated` (field `sessionExpired`, defaults false -- true only right after a genuine server-side session invalidation, per CONTEXT.md's "one-time notice" decision) and `AuthAuthenticated` (fields `email` required, `accessToken` nullable -- null immediately after an optimistic/offline-cached restore before a fresh token exchange resolves).

    Create lib/domain/services/keycloak_config.dart: `class KeycloakConfig` with static const fields `issuer` (`'http://localhost:8081/realms/co2diet'`), `clientId` (`'co2diet-mobile'`), `redirectUrl` (`'com.reduceco2now.co2diet.auth://callback'`), `scopes` (`['openid', 'profile', 'email', 'offline_access']`), `appleIdpAlias` (`'apple'`), `googleIdpAlias` (`'google'`). Every field gets an inline `// [ASSUMED]` doc comment citing 07-RESEARCH.md's Assumptions Log entry (A1/A3/A4/A5) it corresponds to, and a class-level doc comment stating every value is a placeholder pending Tomris's real realm export and MUST NOT be treated as a production contract.

    Create lib/domain/services/backend_config.dart: `class BackendConfig` with a single static const `baseUrl` (`'http://localhost:8080'`), doc-commented `// [ASSUMED]` per 07-RESEARCH.md Assumption A2/Open Question #1 -- the real backend base URL is unknown until Tomris deploys it; this is the one place Plan 07-03's deleteAccount() call reads from.

    Create lib/core/di/auth_providers.dart with three `@Riverpod(keepAlive: true)` providers, mirroring providers.dart's doc-comment style: `secureStorage(Ref ref) => const FlutterSecureStorage();`, `appAuth(Ref ref) => const FlutterAppAuth();`, and `authHttpClient(Ref ref)` returning `http.Client()` with `ref.onDispose(client.close)` (mirrors appDatabaseProvider's onDispose pattern). keepAlive on all three because AuthNotifier (Plan 07-03, itself keepAlive) reads them via bare `ref.read` from mutation methods that may run after their own widget disposes -- the same UnmountedRefException risk class documented for OnboardingGateNotifier/mealEntryProvider precedents.
  </action>
  <verify>
    <automated>flutter analyze lib/domain/entities/auth_state.dart lib/domain/services/keycloak_config.dart lib/domain/services/backend_config.dart lib/core/di/auth_providers.dart</automated>
  </verify>
  <done>All four files exist, compile clean under flutter analyze (zero lint errors), and AuthState/KeycloakConfig/BackendConfig/auth_providers.dart contain nothing but pure contracts -- no flutter_appauth calls, no network calls, no UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| pub.dev package installs | Third-party code entering the build via `flutter pub add` |
| Native redirect-URI scheme | OS-level intent-filter (Android) / URL scheme (iOS) that any other app could theoretically register the same string for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-SC | Tampering | pub.dev installs (flutter_appauth, flutter_secure_storage) | mitigate | Blocking human-verify checkpoint before install; both packages carry verified-publisher/160-pub-score/long-history signals |
| T-07-02-01 | Spoofing | Custom redirect-URI scheme (com.reduceco2now.co2diet.auth://) | accept | A malicious app registering the identical scheme could intercept the redirect intent, but cannot complete the token exchange without the PKCE code_verifier (generated and held only by flutter_appauth inside this app's process) -- standard RFC 8252 mitigation, no additional app-side control needed this phase |
</threat_model>

<verification>
`flutter pub get` succeeds. `dart run tool/check_privacy_deps.dart` exits 0. `flutter analyze` reports zero new lint errors for the four new files. Both native redirect-scheme entries are present and identical to `KeycloakConfig.redirectUrl`'s scheme.
</verification>

<success_criteria>
flutter_appauth and flutter_secure_storage are installed and human-approved; native platform redirect-URI wiring is in place on both Android and iOS; AuthState/KeycloakConfig/BackendConfig/auth_providers.dart exist as pure, dependency-free contracts ready for Plan 07-03 to build AuthNotifier against.
</success_criteria>

<output>
Create `.planning/phases/07-keycloak-auth-account-mode-sync/07-02-SUMMARY.md` when done
</output>
