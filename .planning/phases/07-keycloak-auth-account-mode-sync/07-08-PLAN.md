---
phase: 07-keycloak-auth-account-mode-sync
plan: 08
type: execute
wave: 4
depends_on:
  - 07-05
  - 07-06
  - 07-07
files_modified:
  - lib/core/router/app_router.dart
  - lib/features/dashboard/widgets/mode_indicator.dart
  - lib/features/dashboard/screens/placeholder_dashboard_screen.dart
  - lib/features/legal/screens/legal_hub_screen.dart
  - docs/legal/terms.md
  - docs/backend-contracts/gdpr-account-deletion.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - AUTH-06
  - PRIV-05
  - AUTH-10

must_haves:
  truths:
    - "Dashboard's mode indicator reflects real auth state -- 'Account Mode: Data still stored locally (sync coming soon)' when authenticated, 'Stored on this device' when not -- never the old hardcoded 'Synced across devices' copy, which would falsely imply sync exists"
    - "/auth and /check-email are reachable routes; visiting them requires no onboarding-gate bypass since AUTH-related routes are post-onboarding-only (Account Mode is a Settings-initiated action, never part of the onboarding flow, per 07-CONTEXT.md's explicit exclusion of onboarding scaffolding)"
    - "Legal Hub's Your Rights section cross-references the new in-app Delete Account flow for Account Mode users, alongside its existing local-data references"
    - "docs/legal/terms.md's frontmatter version is bumped and contains a new Account Mode & Data section describing zero-data-movement and the account-mode-terms consent"
    - "A written GDPR account-deletion API-contract spec exists for Tomris, isolated from any assumption about it already being correct"
    - "Full test suite is green; AUTH-10's CI privacy blocklist check still passes with flutter_appauth/flutter_secure_storage installed"
  artifacts:
    - path: "docs/backend-contracts/gdpr-account-deletion.md"
      provides: "Method/path/headers/status-code spec for DELETE /me/account, marked [ASSUMED] pending Tomris confirmation"
  key_links:
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/auth/screens/auth_screen.dart"
      via: "GoRoute(path: '/auth', ...) and GoRoute(path: '/check-email', ...)"
      pattern: "path: '/auth'"
    - from: "lib/features/dashboard/screens/placeholder_dashboard_screen.dart"
      to: "lib/features/auth/providers/auth_provider.dart"
      via: "ModeIndicator(isLocalMode: ref.watch(authProvider) is! AuthAuthenticated)"
      pattern: "ref\\.watch\\(authProvider\\)"
---

<objective>
Close out Phase 7: register the new auth routes, wire the Dashboard's `ModeIndicator` to real auth state (replacing its Phase-1-era hardcoded `isLocalMode: true` and its now-inaccurate "Synced across devices" copy), cross-reference account deletion from the Legal Hub, ship the `terms.md` "Account Mode & Data" section, hand Tomris a concrete written API-contract spec for the GDPR deletion endpoint, and run the full regression suite.

Purpose: this is the plan that makes every prior Phase 7 plan's work actually reachable end-to-end and internally consistent -- without it, `AuthNotifier`/`AccountSection`/`AuthScreen` all exist but nothing routes to them and the Dashboard still lies about sync status.

Output: registered `/auth`/`/check-email` routes, real-auth-state `ModeIndicator`, updated Legal Hub, updated `terms.md`, `docs/backend-contracts/gdpr-account-deletion.md`, and a fully green `flutter test`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-keycloak-auth-account-mode-sync/07-CONTEXT.md
@.planning/phases/07-keycloak-auth-account-mode-sync/07-RESEARCH.md
@lib/core/router/app_router.dart
@lib/features/dashboard/widgets/mode_indicator.dart
@lib/features/dashboard/screens/placeholder_dashboard_screen.dart
@lib/features/legal/screens/legal_hub_screen.dart
@docs/legal/terms.md
@lib/features/auth/providers/auth_provider.dart
@lib/domain/entities/auth_state.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router wiring + ModeIndicator real-state wiring + full regression</name>
  <files>
    lib/core/router/app_router.dart,
    lib/features/dashboard/widgets/mode_indicator.dart,
    lib/features/dashboard/screens/placeholder_dashboard_screen.dart
  </files>
  <action>
    In `app_router.dart`, add two top-level `GoRoute`s (outside the `StatefulShellRoute`, alongside `/legal-hub`/`/food-search` -- Account Mode is Settings-initiated, not bottom-nav-scoped): `GoRoute(path: '/auth', builder: (context, state) => const AuthScreen())` and `GoRoute(path: '/check-email', builder: (context, state) => CheckEmailScreen(email: state.uri.queryParameters['email'] ?? ''))`. Neither route needs an entry in the `redirect` callback's `onboardingOnlyRoutes`/`allowedPreOnboarding` lists -- they are reachable only from the already-onboarded Settings screen, and a post-onboarding user is never blocked from any non-onboarding route by the existing redirect logic.

    In `mode_indicator.dart`, update the Account-Mode copy from the current placeholder "Synced across devices" to "Account Mode: Data still stored locally (sync coming soon)" (07-CONTEXT.md's exact locked wording) and update the doc comment referencing "Phase 7's future Account Mode state" to note this phase wires it for real. Keep the `isLocalMode` constructor param unchanged (still a plain bool -- the widget itself stays dependency-free and testable without Riverpod).

    In `placeholder_dashboard_screen.dart`, change the `const ModeIndicator()` call site to `ModeIndicator(isLocalMode: ref.watch(authProvider) is! AuthAuthenticated)` (no longer `const`, now reads live auth state every rebuild).

    Run the full test suite and fix any regression this wiring surfaces (e.g. any existing Dashboard widget test that asserted the old `const ModeIndicator()` construction or the old "Synced across devices" copy).
  </action>
  <verify>
    <automated>flutter test 2>&1 | tail -40</automated>
  </verify>
  <done>/auth and /check-email are reachable routes. ModeIndicator reflects real AuthNotifier state on the Dashboard with the correct locked copy. Full flutter test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Legal Hub cross-reference + terms.md Account Mode section + GDPR contract spec for Tomris</name>
  <files>
    lib/features/legal/screens/legal_hub_screen.dart,
    docs/legal/terms.md,
    docs/backend-contracts/gdpr-account-deletion.md
  </files>
  <action>
    In `legal_hub_screen.dart`'s "Your Rights" section, add one more `ListTile` (leading `Icons.person_remove_outlined`, title "Delete Account (Account Mode)", subtitle "Permanently delete your Account Mode login and backend account -- your local data stays on this device.") positioned after the existing "Consent Withdrawal" tile, navigating to `context.push('/settings')` (the actual Delete Account action lives in Settings' Account section per Plan 07-06 -- Legal Hub only cross-references it, per 07-RESEARCH.md's "Your Rights never builds a new mutation/action screen" anti-pattern).

    In `docs/legal/terms.md`, bump the frontmatter `version:` to today's date and add a new `## 9. Account Mode & Data` section (before `## Contact`, renumbering the existing `## 9. Contact` to `## 10. Contact`) covering: Account Mode is optional and separate from the App's core (Local Mode) functionality; creating or logging into an Account Mode account via Keycloak OIDC moves zero local data to any server; the user's local data remains fully under their control regardless of Account Mode status; Account Mode requires accepting this section via a separate in-app checkbox at signup, recorded as its own consent event; permanent Account Mode account deletion is available in-app (Settings -> Account) and is immediate and irreversible per GDPR Art. 17, and does not affect local data. Preserve the existing `<!-- PENDING LEGAL REVIEW -->` HTML comment (re-flagging the whole document for review is correct per STATE.md's existing pre-launch-blocker convention -- do not add a second comment).

    Create `docs/backend-contracts/gdpr-account-deletion.md` (new directory) as a concrete written spec for Tomris, per 07-RESEARCH.md Open Question #1's explicit recommendation: document the assumed contract (`DELETE {baseUrl}/me/account`, `Authorization: Bearer <access_token>` header, expects a synchronous 2xx on success meaning the Keycloak user record AND any backend-side account data are both hard-deleted in the same operation, no grace/recovery period, no request body), explicitly mark every field `[ASSUMED -- not yet confirmed with Tomris]`, list the legal requirements driving it (PRIV-05: GDPR Art. 17, App Store account-deletion requirement since June 2022), and state the client-side isolation point (`AuthNotifier.deleteAccount()` in `lib/features/auth/providers/auth_provider.dart`) so a future contract change is a one-file update.
  </action>
  <verify>
    <automated>grep -c "Account Mode &amp; Data" docs/legal/terms.md; grep -c "^version: $(date +%Y-%m-%d)$" docs/legal/terms.md; test -f docs/backend-contracts/gdpr-account-deletion.md &amp;&amp; echo "contract_doc_exists"; flutter test test/features/legal/ 2>&1 | tail -15</automated>
  </verify>
  <done>Legal Hub cross-references Delete Account. terms.md has a version-bumped frontmatter and a new Account Mode & Data section. docs/backend-contracts/gdpr-account-deletion.md exists as a concrete, clearly-[ASSUMED]-marked spec. Legal Hub/consent tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new -- this plan only wires already-built, already-threat-modeled components (Plans 07-03/07-05/07-06/07-07) together and updates documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | `/auth`/`/check-email` routes reachable via a crafted deep link before the user ever taps "Sign in / Create account" | accept | No sensitive action occurs merely by landing on `AuthScreen` -- it requires an explicit user-initiated network call (connectivity-gated) to do anything; consistent with every other non-onboarding route in this app having no deep-link guard |
</threat_model>

<verification>
`flutter test` (full suite) is green. `dart run tool/check_privacy_deps.dart` exits 0. Router exposes `/auth` and `/check-email`. `ModeIndicator` call site in `placeholder_dashboard_screen.dart` reads `ref.watch(authProvider)`.
</verification>

<success_criteria>
Phase 7 is fully wired end-to-end: every screen built in Plans 07-05/07-06/07-07 is reachable, the Dashboard tells the truth about Account Mode's current (zero-sync) state, Legal Hub and terms.md reflect the new Account Mode surface, and Tomris has a concrete written contract to build the GDPR deletion endpoint against.
</success_criteria>

<output>
Create `.planning/phases/07-keycloak-auth-account-mode-sync/07-08-SUMMARY.md` when done
</output>
