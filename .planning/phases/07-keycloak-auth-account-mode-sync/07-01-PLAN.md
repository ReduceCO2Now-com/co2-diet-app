---
phase: 07-keycloak-auth-account-mode-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/features/auth/auth_screen_test.dart
  - test/features/auth/providers/auth_provider_test.dart
  - test/features/settings/widgets/account_section_test.dart
  - test/domain/services/methodology_version_checker_test.dart
  - test/features/dashboard/widgets/co2_methodology_banner_test.dart
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - PRIV-05

must_haves:
  truths:
    - "flutter test discovers all 5 new Phase 7 test files and reports them skipped (not failing, not erroring) before any production code exists"
    - "Every later Phase 7 plan that implements one of these files removes the skip: marker as part of turning the stub into a real, green test (Nyquist contract)"
  artifacts:
    - path: "test/features/auth/auth_screen_test.dart"
      provides: "Wave 0 stub for AUTH-01/AUTH-04 (combined sign-in/create-account screen + Check Email screen)"
    - path: "test/features/auth/providers/auth_provider_test.dart"
      provides: "Wave 0 stub for AUTH-02/AUTH-05/AUTH-06/PRIV-05 (AuthNotifier + realm-discovery provider)"
    - path: "test/features/settings/widgets/account_section_test.dart"
      provides: "Wave 0 stub for AUTH-03/PRIV-05 (Account section in Settings)"
    - path: "test/domain/services/methodology_version_checker_test.dart"
      provides: "Wave 0 stub for the CO2 methodology-announcement mechanism (CO2-04, roadmap success criterion #5)"
    - path: "test/features/dashboard/widgets/co2_methodology_banner_test.dart"
      provides: "Wave 0 stub for the dismissible Dashboard banner + per-version dismissal persistence"
  key_links: []
---

<objective>
Create the five Wave 0 stub test files 07-RESEARCH.md and 07-VALIDATION.md flag as missing, following the group-level `skip:` pattern established verbatim in every prior phase (Phase 2-6 Wave 0 plans). This satisfies the Nyquist contract before any Phase 7 production code exists — every later plan in this phase fills in exactly one of these files and removes its skip marker.

Purpose: every subsequent Phase 7 plan's `<verify>` block runs `flutter test <file>` against one of these five files. Without this plan, those commands would fail with "file not found" instead of a clean, informative skip.

Output: five new test files under `test/features/auth/`, `test/features/auth/providers/`, `test/features/settings/widgets/`, `test/domain/services/`, `test/features/dashboard/widgets/` — all present, all skipped, zero failures.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-keycloak-auth-account-mode-sync/07-CONTEXT.md
@.planning/phases/07-keycloak-auth-account-mode-sync/07-RESEARCH.md
@.planning/phases/07-keycloak-auth-account-mode-sync/07-VALIDATION.md
@test/domain/services/legal_document_loader_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain/provider-layer Wave 0 stubs</name>
  <files>
    test/features/auth/providers/auth_provider_test.dart,
    test/domain/services/methodology_version_checker_test.dart
  </files>
  <action>
    Create two stub test files, each with top-level `group('<Name>', () { ... }, skip: 'Awaiting Plan 07-0X implementation');` blocks wrapping 3-6 placeholder `test('...', () {})` cases naming the exact future behavior (a `// TODO(Plan 07-0X):` comment per case is enough, no bodies needed) — mirrors the Phase 2-6 Wave 0 precedent (group-level skip, not per-test skip) documented in STATE.md's `[Phase 02-01]`/`[Phase 05-01]`/`[Phase 06-01]` decisions.

    `auth_provider_test.dart` (→ filled by Plan 07-03): two groups, `'AuthNotifier'` and `'realmDiscoveryReadyProvider'`. AuthNotifier cases: "build() returns AuthUnauthenticated with no stored refresh token", "build() optimistically returns AuthAuthenticated using a cached email when a refresh token exists, before the network call resolves", "successful silent refresh yields AuthAuthenticated with the userinfo-resolved email and persists the new refresh token", "a network-failure refresh error keeps state AuthAuthenticated and does not delete the stored refresh token (CONTEXT.md: network failure vs. real invalidation)", "a genuine invalid_grant/revoked-token refresh error yields AuthUnauthenticated(sessionExpired: true) and deletes stored refresh token + cached email", "signIn() calls authorizeAndExchangeCode with no kc_idp_hint/prompt params", "signUp() calls authorizeAndExchangeCode with additionalParameters[prompt]='create' and records an 'account_mode_terms' consent event on success", "signInWithIdp('apple') and signInWithIdp('google') pass the matching kc_idp_hint alias", "a cancelled/dismissed browser flow leaves state unchanged with no rethrown error", "logout() calls endSession, clears secure storage, and sets AuthUnauthenticated(sessionExpired: false)", "deleteAccount() on a 2xx response clears secure storage, sets AuthUnauthenticated, and records an 'account_deletion' consent event", "deleteAccount() on a non-2xx response throws AccountDeletionException and leaves state/storage untouched". realmDiscoveryReadyProvider cases: "returns true on HTTP 200 from the discovery endpoint", "returns false on a non-200 response or a thrown exception".

    `methodology_version_checker_test.dart` (→ filled by Plan 07-04): cases for `isStale` ("null snapshot is never stale", "snapshot equal to the current version is not stale", "snapshot older than the current version is stale") and `hasAnyStale` ("false when profile/meal/food versions are all null or current", "true when the profile version is stale", "true when any meal-entry snapshot version is stale", "true when any user-food version is stale").
  </action>
  <verify>
    <automated>flutter test test/features/auth/providers/auth_provider_test.dart test/domain/services/methodology_version_checker_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>Both files exist, `flutter test` reports them as skipped (0 failures), and each test name documents the exact future behavior it stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Widget-layer Wave 0 stubs</name>
  <files>
    test/features/auth/auth_screen_test.dart,
    test/features/settings/widgets/account_section_test.dart,
    test/features/dashboard/widgets/co2_methodology_banner_test.dart
  </files>
  <action>
    Create three stub test files using the same group-level skip pattern, with `testWidgets('...', (tester) async {})` bodies wrapped inside the skipped `group()` — mirrors STATE.md's `[Phase 05-01]`/`[Phase 06-01]` decision (group-level skip extended to `testWidgets`).

    `auth_screen_test.dart` (→ filled by Plan 07-05): cases for "defaults to create-account mode with social buttons above an 'or' divider above email/password fields", "Apple button only renders on iOS", "toggling 'Already have an account? Sign in' switches to sign-in mode and hides the account-mode-terms checkbox", "honesty note text is always visible", "submitting create-account with a password under 8 chars shows an inline error and does not call the notifier", "submitting create-account without the terms checkbox checked shows an inline error", "submitting valid create-account calls signUp() and navigates to /check-email", "offline (mocked connectivity none) shows an inline no-connection message and never opens the browser", "'Forgot password?' launches the reset-credentials URL via url_launcher", "tapping the Google/Apple button calls signInWithIdp with the matching alias", "CheckEmailScreen renders the entered email and a working Resend action".

    `account_section_test.dart` (→ filled by Plan 07-06): cases for "signed-out state shows a single Sign in / Create account row that navigates to /auth", "signed-in state shows email + Change password + Log out + Delete account rows", "Change password launches the Keycloak account-console URL", "Log out calls logout() immediately with no confirmation dialog", "Delete account opens a confirmation AlertDialog with the exact locked copy and no typed-word field", "confirming delete calls deleteAccount(); cancelling does not", "a failed deleteAccount() shows an inline error and leaves the signed-in UI intact", "AuthUnauthenticated(sessionExpired: true) shows a one-time 'You've been signed out' notice and then calls acknowledgeSessionExpired()".

    `co2_methodology_banner_test.dart` (→ filled by Plan 07-07): cases for "renders nothing when no stale entries exist", "renders the banner with the interpolated methodology version when stale entries exist and the version was never dismissed", "dismissing the banner persists a per-version flag so it does not reappear for the same version", "a newer stale version reappears even after a prior version was dismissed", "tapping Learn more navigates to /data-analysis".
  </action>
  <verify>
    <automated>flutter test test/features/auth/auth_screen_test.dart test/features/settings/widgets/account_section_test.dart test/features/dashboard/widgets/co2_methodology_banner_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>All three files exist, `flutter test` reports them as skipped (0 failures).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan creates only skipped test scaffolding, no production code, no data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | N/A | Test scaffolding only | accept | No production code or data path introduced by this plan |
</threat_model>

<verification>
`flutter test test/features/auth/ test/domain/services/methodology_version_checker_test.dart test/features/settings/widgets/account_section_test.dart test/features/dashboard/widgets/co2_methodology_banner_test.dart` reports 0 failures, all cases skipped with an informative reason string.
</verification>

<success_criteria>
All five Wave 0 stub files exist, are discovered by `flutter test`, and are cleanly skipped (not failing) — unblocking every later Phase 7 plan's `<verify>` command.
</success_criteria>

<output>
Create `.planning/phases/07-keycloak-auth-account-mode-sync/07-01-SUMMARY.md` when done
</output>
