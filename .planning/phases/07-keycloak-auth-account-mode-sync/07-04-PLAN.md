---
phase: 07-keycloak-auth-account-mode-sync
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/domain/services/methodology_version_checker.dart
  - test/domain/services/methodology_version_checker_test.dart
autonomous: true
requirements:
  - CO2-04

must_haves:
  truths:
    - "A row with a null co2MethodologyVersion(Snapshot) is never treated as stale -- it never had a CO2 estimate applied (existing Phase 4 rule: null means CO2-absent or manual source)"
    - "A row whose stored version differs from the current app-binary constant is stale; the current constant itself never counts as stale"
    - "The checker takes plain version strings as input -- zero Drift/DB imports, fully unit-testable without a database"
  artifacts:
    - path: "lib/domain/services/methodology_version_checker.dart"
      provides: "currentCo2MethodologyVersion constant, MethodologyVersionChecker.isStale/hasAnyStale"
  key_links: []
---

<objective>
Build the pure, DB-free comparison logic behind Phase 7's local-only CO2 methodology-update announcement (ROADMAP.md Phase 7 success criterion #5 / CO2-04's "surfaces a non-intrusive announcement" clause). This plan builds the mechanism only -- it does NOT bump `currentCo2MethodologyVersion` past `'1.0'` or touch any CO2 factor data, per 07-CONTEXT.md's explicit scope boundary.

Purpose: keeping this logic pure (no Drift row types, no DAOs) makes it a clean TDD unit and lets Plan 07-07 compose it with real DB reads without ever needing to mock a database here.

Output: `lib/domain/services/methodology_version_checker.dart` and a green, unskipped `methodology_version_checker_test.dart`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-keycloak-auth-account-mode-sync/07-CONTEXT.md
@.planning/phases/07-keycloak-auth-account-mode-sync/07-RESEARCH.md
@lib/domain/entities/user_profile.dart
@lib/domain/entities/meal_entry.dart
@lib/domain/entities/user_food.dart
@lib/domain/services/daily_totals_calculator.dart
@test/domain/services/methodology_version_checker_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MethodologyVersionChecker</name>
  <files>lib/domain/services/methodology_version_checker.dart, test/domain/services/methodology_version_checker_test.dart</files>
  <behavior>
    - `isStale(null)` returns `false`.
    - `isStale('1.0')` (equal to `currentCo2MethodologyVersion`) returns `false`.
    - `isStale('0.9')` (not equal to the current constant) returns `true`.
    - `isStale` accepts an optional `currentVersion` override parameter (defaults to `currentCo2MethodologyVersion`) so tests can probe future-version scenarios without mutating the real constant.
    - `hasAnyStale(profileVersion: null, mealVersions: [], foodVersions: [])` returns `false`.
    - `hasAnyStale(profileVersion: '1.0', mealVersions: [null, '1.0'], foodVersions: [null])` returns `false` (nothing stale anywhere).
    - `hasAnyStale(profileVersion: '0.9', mealVersions: [], foodVersions: [])` returns `true` (stale profile version alone is sufficient).
    - `hasAnyStale(profileVersion: '1.0', mealVersions: ['1.0', '0.8', null], foodVersions: [])` returns `true` (one stale meal-entry snapshot among otherwise-current/null entries is sufficient).
    - `hasAnyStale(profileVersion: '1.0', mealVersions: [], foodVersions: [null, '0.7'])` returns `true` (one stale custom-food version is sufficient).
  </behavior>
  <action>
    Remove the `skip:` marker from methodology_version_checker_test.dart (created in Plan 07-01) and replace its placeholder cases with the real behavior above (RED first, confirm failure).

    Create lib/domain/services/methodology_version_checker.dart with a top-level `const currentCo2MethodologyVersion = '1.0';` (doc-commented: "the app-binary constant this phase's mechanism compares against -- deliberately NOT bumped this phase per 07-CONTEXT.md; a future release bumps this to trigger the Dashboard banner for real users") and `class MethodologyVersionChecker` with a `const` constructor exposing `bool isStale(String? snapshotVersion, {String currentVersion = currentCo2MethodologyVersion})` (null-safe: null snapshot always returns false; otherwise simple string inequality, not a semver comparison -- versions in this codebase are simple decimal strings and the mechanism only ever needs to detect "differs from the constant") and `bool hasAnyStale({required String? profileVersion, required List<String?> mealVersions, required List<String?> foodVersions, String currentVersion = currentCo2MethodologyVersion})` composing three `isStale` checks (profile version, then `.any()` across each list).
  </action>
  <verify>
    <automated>flutter test test/domain/services/methodology_version_checker_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>methodology_version_checker_test.dart is green with zero skips, covering every behavior case above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None -- pure on-device comparison logic, zero network, zero DB access, zero external input beyond already-validated in-memory strings.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | N/A | Pure comparison function | accept | No trust boundary crossed -- no network, no DB, no user input parsing beyond already-typed Dart strings |
</threat_model>

<verification>
`flutter test test/domain/services/methodology_version_checker_test.dart` reports 0 failures, 0 skips.
</verification>

<success_criteria>
MethodologyVersionChecker correctly classifies null/current/stale version strings per every behavior case, ready for Plan 07-07 to wire against real DB reads.
</success_criteria>

<output>
Create `.planning/phases/07-keycloak-auth-account-mode-sync/07-04-SUMMARY.md` when done
</output>
