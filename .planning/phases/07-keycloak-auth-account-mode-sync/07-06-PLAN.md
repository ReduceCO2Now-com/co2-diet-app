---
phase: 07-keycloak-auth-account-mode-sync
plan: 06
type: execute
wave: 3
depends_on:
  - 07-03
files_modified:
  - lib/features/settings/widgets/account_section.dart
  - lib/features/settings/screens/settings_screen.dart
  - test/features/settings/widgets/account_section_test.dart
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - PRIV-05

must_haves:
  truths:
    - "The Account section is entirely absent from Settings until realmDiscoveryReadyProvider resolves true -- no skeleton, no loading placeholder"
    - "Logging out requires zero confirmation taps -- a single tap on Log out completes it"
    - "Deleting an account requires exactly one confirmation dialog with the exact locked copy and a destructive-styled confirm button -- no typed-word field, no re-authentication"
    - "A failed account deletion leaves the signed-in UI completely intact and shows an inline error -- never a premature signed-out flash"
    - "A successful account deletion shows a confirmation message before the section reverts to signed-out state -- deletion is never silent"
  artifacts:
    - path: "lib/features/settings/widgets/account_section.dart"
      provides: "AccountSection: signed-out CTA row, signed-in email/change-password/logout/delete rows, delete confirmation dialog, session-expired notice"
  key_links:
    - from: "lib/features/settings/screens/settings_screen.dart"
      to: "lib/features/auth/providers/realm_discovery_provider.dart"
      via: "ref.watch(realmDiscoveryReadyProvider) gates AccountSection's presence in the ListView"
      pattern: "realmDiscoveryReadyProvider"
    - from: "lib/features/settings/widgets/account_section.dart"
      to: "lib/features/auth/providers/auth_provider.dart"
      via: "ref.watch(authProvider) / ref.read(authProvider.notifier).logout()/.deleteAccount()"
      pattern: "authProvider\\.notifier\\)\\.(logout|deleteAccount)"
---

<objective>
Wire Account Mode into Settings: the realm-discovery-gated "Account" section (positioned after the existing feature rows, before Legal & Privacy, per 07-CONTEXT.md's locked placement decision), covering the full signed-out -> signed-in -> logged-out lifecycle, plus the GDPR account-deletion flow (PRIV-05) with its own deliberately-simpler confirmation dialog (distinct from Danger Zone's typed-word pattern, per 07-RESEARCH.md Pitfall 4).

Purpose: this is the only place a user manages their Account Mode session day-to-day -- email display, change password, log out, delete account all live in one section so nothing about Account Mode competes visually with the app's actual (local, functional) features above it.

Output: `AccountSection`, `SettingsScreen` updated with the gated section, and a green, unskipped `account_section_test.dart`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-keycloak-auth-account-mode-sync/07-CONTEXT.md
@.planning/phases/07-keycloak-auth-account-mode-sync/07-RESEARCH.md
@lib/features/auth/providers/auth_provider.dart
@lib/features/auth/providers/realm_discovery_provider.dart
@lib/domain/entities/auth_state.dart
@lib/domain/services/keycloak_config.dart
@lib/features/settings/screens/settings_screen.dart
@lib/core/widgets/ed_safety_net_dialog.dart
@lib/features/backup/widgets/danger_zone_section.dart
@test/features/settings/widgets/account_section_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccountSection widget</name>
  <files>lib/features/settings/widgets/account_section.dart</files>
  <action>
    Create `AccountSection` (ConsumerWidget). `build()` watches `authProvider`. Signed-out (`AuthUnauthenticated`) view: a single `ListTile` ("Sign in / Create account", subtitle "Access Account Mode -- your local data stays put") navigating to `context.push('/auth')`. If `state.sessionExpired == true`, schedule (via `WidgetsBinding.instance.addPostFrameCallback`) a one-time `ScaffoldMessenger.of(context).showSnackBar` with "You've been signed out" and immediately call `ref.read(authProvider.notifier).acknowledgeSessionExpired()` so it fires exactly once per genuine invalidation, never on rebuild.

    Signed-in (`AuthAuthenticated`) view: a section header "Account", a `ListTile` showing `state.email` (leading `Icons.account_circle_outlined`), a "Change password" `ListTile` calling `launchUrl(Uri.parse('${KeycloakConfig.issuer}/account/account-security/signing-in'), mode: LaunchMode.externalApplication)`, a "Log out" `ListTile` calling `ref.read(authProvider.notifier).logout()` directly on tap -- no confirmation dialog, no intermediate state (CONTEXT.md: "Immediate, no confirmation dialog"), and a "Delete account" `ListTile` (destructive styling -- `AppColors.error` per this project's existing Danger Zone convention) that opens a confirmation `AlertDialog` built in the style of `showEdSafetyNetDialog` (a standalone function returning `Future<bool>`, `barrierDismissible: false`, title "Delete your account?", body text exactly: "Your account will be permanently deleted. Your local data on this device will NOT be affected.", a `TextButton` "Cancel" (pop false) and a destructive-styled `FilledButton` "Delete account" (pop true) -- deliberately NOT `DangerZoneSection`'s typed-`DELETE`-word pattern, since CONTEXT.md explicitly rejects that friction level here).

    On confirm (`true` returned), call `await ref.read(authProvider.notifier).deleteAccount()` inside a `try`/`on AccountDeletionException catch` -- on success, capture the `BuildContext` before the `await` gap and, guarded by `if (context.mounted)`, show `ScaffoldMessenger.of(context).showSnackBar` with the confirmation message "Your account has been deleted." BEFORE the widget rebuilds signed-out (per 07-CONTEXT.md's locked "Post-deletion: Confirmation message, then the Account section reverts to signed-out state" decision -- the widget then rebuilds signed-out automatically on the next frame since `authProvider`'s state already flipped, but the confirmation SnackBar must be shown, not skipped); on failure, show an inline error via `ScaffoldMessenger` SnackBar ("Couldn't delete your account -- please try again.") and take no other action (state/UI stays signed-in, per the locked "no local state changes until the backend confirms success" decision).
  </action>
  <verify>
    <automated>flutter analyze lib/features/settings/widgets/account_section.dart</automated>
  </verify>
  <done>AccountSection compiles clean and implements every signed-out/signed-in/deletion/session-expired case described above, including the post-deletion success confirmation SnackBar shown before the section reverts to signed-out.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsScreen wiring + turn account_section_test.dart green</name>
  <files>lib/features/settings/screens/settings_screen.dart, test/features/settings/widgets/account_section_test.dart</files>
  <action>
    Remove the `skip:` marker from account_section_test.dart (created in Plan 07-01) and replace its placeholder cases with real widget tests covering every case named there (RED first, confirm failure), overriding `authProvider` with a controllable state and mocking `url_launcher`'s platform channel for the Change-password assertion. Add a dedicated test case (`testWidgets('shows success confirmation before reverting to signed-out on successful deletion', ...)`) that mocks `deleteAccount()` to succeed, confirms the deletion dialog, and asserts `find.text('Your account has been deleted.')` is present via `find.byType(SnackBar)` -- proves the locked post-deletion confirmation message is shown, not skipped (07-CONTEXT.md).

    Convert `SettingsScreen` from a `StatelessWidget` to a `ConsumerWidget` (mirrors `AppShell`'s existing `ConsumerWidget` pattern). Add `ref.watch(realmDiscoveryReadyProvider)`; when it resolves `true` (via `.when(data: (ready) => ready, ...)` or an `AsyncValue` pattern-match, treating `loading`/`error` as "not ready yet -- render nothing"), insert a `Divider(height: 1)` followed by `const AccountSection()` into the `ListView`'s children, positioned immediately after the existing "Backup & Restore" row's `Divider` and the `MealReminderSettingsSection` block, and before the "Legal & Privacy" row's own `Divider` -- matching 07-CONTEXT.md's locked placement ("after the core feature rows... but before Legal & Privacy").
  </action>
  <verify>
    <automated>flutter test test/features/settings/widgets/account_section_test.dart 2>&1 | tail -30</automated>
  </verify>
  <done>account_section_test.dart is green with zero skips, including the post-deletion confirmation SnackBar assertion. SettingsScreen only renders AccountSection once realm discovery succeeds, positioned per the locked placement decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings UI -> AuthNotifier.deleteAccount() -> assumed backend endpoint | User-triggered, irreversible action against an unconfirmed contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Repudiation | Account deletion with no re-authentication step | accept | Matches CONTEXT.md's explicit locked decision (same friction level as the existing local-data Danger Zone wipe); the action only affects the backend account, never local data, bounding the blast radius of an accidental tap |
| T-07-06-02 | Denial of Service (self) | A failed deleteAccount() leaving the UI in a broken/ambiguous state | mitigate | Explicit try/on AccountDeletionException catch keeps state and UI fully consistent on failure -- no partial-deletion UI state is possible |
</threat_model>

<verification>
`flutter test test/features/settings/widgets/account_section_test.dart` reports 0 failures, 0 skips.
</verification>

<success_criteria>
Settings shows the Account section only once a live realm is reachable, and every signed-out/signed-in/logout/delete-account interaction behaves exactly per 07-CONTEXT.md's locked decisions.
</success_criteria>

<output>
Create `.planning/phases/07-keycloak-auth-account-mode-sync/07-06-SUMMARY.md` when done
</output>
