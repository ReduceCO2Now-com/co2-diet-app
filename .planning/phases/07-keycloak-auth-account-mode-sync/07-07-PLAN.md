---
phase: 07-keycloak-auth-account-mode-sync
plan: 07
type: execute
wave: 3
depends_on:
  - 07-04
files_modified:
  - lib/features/dashboard/providers/methodology_banner_provider.dart
  - lib/features/dashboard/widgets/co2_methodology_banner.dart
  - lib/features/dashboard/screens/placeholder_dashboard_screen.dart
  - test/features/dashboard/widgets/co2_methodology_banner_test.dart
autonomous: true
requirements:
  - CO2-04

must_haves:
  truths:
    - "The banner never renders when no stale entries exist -- the mechanism stays fully dormant this phase since currentCo2MethodologyVersion is never bumped"
    - "Dismissing the banner persists per-version, not globally -- a future real version bump reopens the banner even after an earlier version was dismissed"
    - "The banner's Learn more link reuses the existing Estimate Transparency panel (via /data-analysis) rather than introducing new explanatory copy"
  artifacts:
    - path: "lib/features/dashboard/providers/methodology_banner_provider.dart"
      provides: "hasStaleMethodologyEntriesProvider + dismissal-state provider reading/writing SharedPreferences"
    - path: "lib/features/dashboard/widgets/co2_methodology_banner.dart"
      provides: "Co2MethodologyBanner: dismissible card, mirrors Co2ProfilePromptCard's structure"
  key_links:
    - from: "lib/features/dashboard/providers/methodology_banner_provider.dart"
      to: "lib/domain/services/methodology_version_checker.dart"
      via: "MethodologyVersionChecker().hasAnyStale(profileVersion:..., mealVersions:..., foodVersions:...)"
      pattern: "MethodologyVersionChecker\\(\\)\\.hasAnyStale"
    - from: "lib/features/dashboard/screens/placeholder_dashboard_screen.dart"
      to: "lib/features/dashboard/widgets/co2_methodology_banner.dart"
      via: "Co2MethodologyBanner rendered in the Dashboard body"
      pattern: "Co2MethodologyBanner\\("
---

<objective>
Wire `MethodologyVersionChecker` (Plan 07-04) to real database reads and a dismissible Dashboard banner, completing Phase 7's local-only CO2 methodology-update announcement mechanism (ROADMAP.md success criterion #5 / CO2-04). Since `currentCo2MethodologyVersion` stays `'1.0'` this phase, the banner is exercised only via tests and mocked staleness -- it stays dormant for real users until a future release bumps the constant, per 07-CONTEXT.md's explicit scope boundary.

Purpose: building the full mechanism now (not deferring it) means a future factor-table update only needs to change one constant to activate user-facing communication about it -- no new screens or plumbing required later.

Output: `methodology_banner_provider.dart`, `Co2MethodologyBanner`, Dashboard wiring, and a green, unskipped `co2_methodology_banner_test.dart`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-keycloak-auth-account-mode-sync/07-CONTEXT.md
@lib/domain/services/methodology_version_checker.dart
@lib/features/dashboard/widgets/co2_profile_prompt_card.dart
@lib/features/dashboard/screens/placeholder_dashboard_screen.dart
@lib/core/di/providers.dart
@lib/features/onboarding/providers/onboarding_gate_provider.dart
@test/features/dashboard/widgets/co2_methodology_banner_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: methodology_banner_provider.dart (DB reads + per-version dismissal)</name>
  <files>lib/features/dashboard/providers/methodology_banner_provider.dart</files>
  <action>
    Create `@riverpod Future&lt;bool&gt; hasStaleMethodologyEntries(Ref ref)`: reads `ref.watch(appDatabaseProvider)`, calls `db.userProfileDao.getProfile()`, `db.mealEntryDao.getAllEntries()`, and `db.userFoodDao.getAllAlphabetical()` (all three DAOs already exist -- no schema/DAO changes needed), extracts `profile?.co2MethodologyVersion`, `mealRows.map((r) => r.co2MethodologyVersionSnapshot).toList()`, and `foodRows.map((r) => r.co2MethodologyVersion).toList()`, then returns `const MethodologyVersionChecker().hasAnyStale(profileVersion: ..., mealVersions: ..., foodVersions: ...)`. This composes the pure checker with real DB access at the provider layer, mirroring `BackupExportService`'s established precedent for whole-table scans living outside the standard per-feature repository interfaces.

    Create `@Riverpod(keepAlive: true) class MethodologyBannerDismissalNotifier extends _$MethodologyBannerDismissalNotifier` holding `String? build()` (the last-dismissed version, read from `ref.watch(sharedPreferencesProvider).getString('co2MethodologyBannerDismissedVersion')`) and a `Future&lt;void&gt; dismiss(String version)` method persisting that version and updating `state`. keepAlive mirrors `OnboardingGateNotifier`'s established rationale (mutated from a widget callback that may not keep an active watcher).

    Create a small composed `@riverpod Future&lt;bool&gt; showMethodologyBanner(Ref ref)` returning `true` only when `hasStaleMethodologyEntriesProvider` resolves `true` AND `ref.watch(methodologyBannerDismissalNotifierProvider) != currentCo2MethodologyVersion` -- the single value `Co2MethodologyBanner` needs to decide whether to render.
  </action>
  <verify>
    <automated>flutter analyze lib/features/dashboard/providers/methodology_banner_provider.dart</automated>
  </verify>
  <done>methodology_banner_provider.dart compiles clean and correctly composes DB reads with the pure checker and per-version SharedPreferences dismissal.</done>
</task>

<task type="auto">
  <name>Task 2: Co2MethodologyBanner widget + Dashboard wiring + turn test green</name>
  <files>
    lib/features/dashboard/widgets/co2_methodology_banner.dart,
    lib/features/dashboard/screens/placeholder_dashboard_screen.dart,
    test/features/dashboard/widgets/co2_methodology_banner_test.dart
  </files>
  <action>
    Remove the `skip:` marker from co2_methodology_banner_test.dart (created in Plan 07-01) and replace its placeholder cases with real widget tests covering every case named there (RED first, confirm failure), overriding `showMethodologyBannerProvider`/`methodologyBannerDismissalNotifierProvider` with controllable values and a `SharedPreferences.setMockInitialValues({})` instance per this project's established onboarding-gate test convention.

    Create `Co2MethodologyBanner` (ConsumerWidget), structurally mirroring `Co2ProfilePromptCard` (Material card, `colorScheme.surfaceContainerLow` background, `InkWell` body, dismiss `IconButton`) but with an `Icons.eco_outlined` leading icon, body text "CO2 estimates updated with methodology v{currentCo2MethodologyVersion} -- your past entries keep their original estimates.", and TWO tap targets: the card body ("Learn more" -- `context.push('/data-analysis')`) and the dismiss `IconButton` (`onPressed` calls `ref.read(methodologyBannerDismissalNotifierProvider.notifier).dismiss(currentCo2MethodologyVersion)`). Watches `showMethodologyBannerProvider`; renders `SizedBox.shrink()` on `loading`/`error`/`false`.

    Wire `const Co2MethodologyBanner()` into `PlaceholderDashboardScreen`'s body, positioned above the existing `ModeIndicator` row (top of the Dashboard, above the metric cards -- a methodology change is more consequential than the mode indicator's passive status line).
  </action>
  <verify>
    <automated>flutter test test/features/dashboard/widgets/co2_methodology_banner_test.dart 2>&1 | tail -30</automated>
  </verify>
  <done>co2_methodology_banner_test.dart is green with zero skips. The banner never renders in the current dormant state (currentCo2MethodologyVersion never bumped this phase) but is fully exercised and correct under mocked staleness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None -- entirely local: SQLite reads via existing DAOs, SharedPreferences reads/writes, zero network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | N/A | Local-only DB scan + SharedPreferences flag | accept | No trust boundary crossed -- identical risk profile to every other on-device Dashboard widget already shipped in Phase 5 |
</threat_model>

<verification>
`flutter test test/features/dashboard/widgets/co2_methodology_banner_test.dart` reports 0 failures, 0 skips.
</verification>

<success_criteria>
The CO2 methodology-announcement mechanism is fully built and tested end-to-end (DB scan -> staleness check -> dismissible banner -> per-version persistence), dormant for real users until a future release bumps `currentCo2MethodologyVersion`.
</success_criteria>

<output>
Create `.planning/phases/07-keycloak-auth-account-mode-sync/07-07-SUMMARY.md` when done
</output>
