---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 08
type: execute
wave: 3
depends_on:
  - 05-05
files_modified:
  - pubspec.yaml
  - lib/domain/entities/notification_prefs.dart
  - lib/domain/repositories/i_notification_prefs_repository.dart
  - lib/data/repositories/notification_prefs_repository.dart
  - lib/core/di/notification_providers.dart
  - lib/domain/services/notification_service.dart
  - lib/core/router/app_router.dart
  - lib/main.dart
autonomous: false
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03

user_setup: []

must_haves:
  truths:
    - "flutter_local_notifications is called with named parameters only (v22.2.0 API), never the pre-v20 positional shape"
    - "AndroidScheduleMode.inexactAllowWhileIdle is used for all meal/weigh-in reminders -- never exactAllowWhileIdle/alarmClock (RESEARCH.md Pitfall 3)"
    - "Notification permission is requested only when a reminder toggle is first enabled, never during service initialization"
    - "Tapping a scheduled notification navigates via the captured rootNavigatorKey, with no BuildContext/ref required inside the callback"
  artifacts:
    - path: "lib/domain/services/notification_service.dart"
      provides: "NotificationService -- init, JIT permission request, per-slot + weigh-in scheduling, tap navigation"
    - path: "lib/domain/entities/notification_prefs.dart"
      provides: "NotificationPrefs entity (4 meal-slot enabled+time pairs)"
  key_links:
    - from: "lib/domain/services/notification_service.dart"
      to: "lib/core/router/app_router.dart"
      via: "rootNavigatorKey.currentState?.context -> GoRouter.of(context).push(payload)"
      pattern: "rootNavigatorKey"
    - from: "lib/core/di/notification_providers.dart"
      to: "lib/domain/services/notification_service.dart"
      via: "notificationServiceProvider -- the single authoritative DI-access path consumed by 05-13/05-14/05-18"
      pattern: "notificationServiceProvider"

install_reference:
  packages:
    - name: fl_chart
      version: "1.2.0"
      pub_score: "150/160"
      publisher: "imaNNeo (github.com/imaNNeo/fl_chart)"
    - name: flutter_local_notifications
      version: "22.2.0"
      pub_score: "150/160"
      publisher: "MaikuB (github.com/MaikuB/flutter_local_notifications)"
    - name: timezone
      version: "0.11.1"
      pub_score: "150/160"
      publisher: "required transitive of flutter_local_notifications"
    - name: flutter_timezone
      version: "5.1.0"
      pub_score: "150/160"
      publisher: "tjarvstrand (github.com/tjarvstrand/flutter_timezone)"
---

<objective>
Install the four new packages this phase's charting and notification sub-domains need (`fl_chart`, `flutter_local_notifications`, `timezone`, `flutter_timezone`) behind a single package-legitimacy checkpoint (pub.dev is not a slopcheck-supported ecosystem — same precedent as Plan 04-11's `flutter_slidable`), then build the `NotificationPrefs` domain layer and the `NotificationService` wrapper that every meal-reminder and weigh-in-reminder feature in this phase depends on.

Purpose: `fl_chart` is needed by the Dashboard sparkline (05-11), Weight Tracking chart (05-13), and Data Analysis trend section (05-15) — installing it here, in the earliest wave that needs any new package, avoids repeated `pubspec.yaml` conflicts across parallel Wave 5 plans. `NotificationService` is the sole code path that talks to the OS notification plugin (Architectural Responsibility Map: "Notification scheduling/permission — Local domain service — OS talks to the OS AlarmManager/UNUserNotificationCenter; app code only calls the service wrapper").

Output: Four new pubspec dependencies (human-approved), `NotificationPrefs` entity/repository/notifier-ready DI, `NotificationService` with `scheduleMealReminder`/`cancelMealReminder`/`scheduleWeighInReminder`/`cancelWeighInReminder`/`requestPermissionIfNeeded`, a `rootNavigatorKey` wired into `GoRouter`, and cold-start/warm notification-tap navigation wired in `main.dart`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@lib/data/local/daos/notification_prefs_dao.dart
@lib/core/router/app_router.dart
@lib/main.dart
@lib/data/local/daos/user_profile_dao.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Package legitimacy checkpoint: fl_chart, flutter_local_notifications, timezone, flutter_timezone</name>
  <what-built>Nothing yet -- this checkpoint precedes installing four new pubspec.yaml dependencies: fl_chart 1.2.0, flutter_local_notifications 22.2.0, timezone 0.11.1, flutter_timezone 5.1.0.</what-built>
  <how-to-verify>
    pub.dev/Dart is not a slopcheck-supported ecosystem, so this follows the same independent-signal review + human-approval precedent as Plan 04-11's `flutter_slidable`. Please check each package directly on pub.dev before approving:
    1. https://pub.dev/packages/fl_chart -- confirm ~150/160 pub score, BSD-3 license, `imaNNeo/fl_chart` repo, no newer breaking major version since RESEARCH.md's 2026-07-27 verification.
    2. https://pub.dev/packages/flutter_local_notifications -- confirm ~150/160 pub score, `MaikuB/flutter_local_notifications` repo, still at v22.x (named-parameter API).
    3. https://pub.dev/packages/timezone -- confirm still the standard `flutter_local_notifications` companion (mandatory transitive dependency).
    4. https://pub.dev/packages/flutter_timezone -- confirm ~150/160 pub score, `tjarvstrand/flutter_timezone` repo.
    None of these four packages make any network call themselves (chart rendering and local OS notification scheduling only) -- confirms AUTH-07/PRIV-08 compliance.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `flutter pub add fl_chart flutter_local_notifications timezone flutter_timezone`, or describe concerns.</resume-signal>
  <action>No automated action in this task -- it is a blocking human-verify checkpoint. The actual `flutter pub add` install is the first line of the next task's &lt;action&gt;, which only runs after this checkpoint is approved.</action>
</task>

<task type="auto">
  <name>Task 1: Install dependencies and build the NotificationPrefs domain layer</name>
  <files>
    pubspec.yaml,
    lib/domain/entities/notification_prefs.dart,
    lib/domain/repositories/i_notification_prefs_repository.dart,
    lib/data/repositories/notification_prefs_repository.dart,
    lib/core/di/notification_providers.dart
  </files>
  <action>
    Run `flutter pub add fl_chart flutter_local_notifications timezone flutter_timezone` (only after the checkpoint above is approved). Add a version-comment block to each new `pubspec.yaml` entry matching this project's existing convention (see `flutter_slidable`'s comment block) citing the pub score and verification date from RESEARCH.md's Package Legitimacy Audit.

    Create `lib/domain/entities/notification_prefs.dart`: immutable `NotificationPrefs` with four `(bool enabled, String? time)` pairs, one per `MealSlot` (breakfast/lunch/dinner/snack) — model as a `Map<MealSlot, MealReminderConfig>`-free flat class with named fields `breakfastEnabled`/`breakfastTime`/`lunchEnabled`/`lunchTime`/`dinnerEnabled`/`dinnerTime`/`snackEnabled`/`snackTime` (mirrors the table layout directly — avoids an extra Map-based indirection layer for a fixed 4-slot set). Add a convenience method `bool enabledFor(MealSlot slot)` and `String? timeFor(MealSlot slot)` via a `switch`. Sentinel `copyWith`.

    Create `lib/domain/repositories/i_notification_prefs_repository.dart`: `abstract interface class INotificationPrefsRepository { Future<NotificationPrefs> getPrefs(); Future<void> savePrefs(NotificationPrefs prefs); }`.

    Create `lib/data/repositories/notification_prefs_repository.dart`: `final class NotificationPrefsRepository implements INotificationPrefsRepository` backed by `NotificationPrefsDao`, returning an all-disabled `const NotificationPrefs()` default when no row exists yet, following the same single-row-id/HLC-placeholder convention as `Co2SettingsRepository`.

    Create `lib/core/di/notification_providers.dart`: `notificationPrefsDaoProvider`, `notificationPrefsRepositoryProvider` (both keepAlive), mirroring `co2_settings_providers.dart`.
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; flutter analyze lib/domain/entities/notification_prefs.dart lib/data/repositories/notification_prefs_repository.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All four packages appear in `pubspec.yaml`/`pubspec.lock`. `NotificationPrefs`/`NotificationPrefsRepository` compile and cover all four meal slots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationService, rootNavigatorKey, and main.dart wiring</name>
  <files>
    lib/domain/services/notification_service.dart,
    lib/core/di/notification_providers.dart,
    lib/core/router/app_router.dart,
    lib/main.dart,
    test/domain/services/notification_service_test.dart
  </files>
  <behavior>
    - `scheduleMealReminder(slot, time)` calls the plugin's `zonedSchedule` with `androidScheduleMode: AndroidScheduleMode.inexactAllowWhileIdle` and `matchDateTimeComponents: DateTimeComponents.time`, using a stable per-slot notification id so re-scheduling replaces rather than duplicates.
    - `scheduleWeighInReminder(frequency, weekday, time)` uses `matchDateTimeComponents: DateTimeComponents.dayOfWeekAndTime` for weekly/custom frequencies.
    - `scheduleWeighInReminder` is idempotent/safe to call repeatedly (always uses the same stable weigh-in notification id, so a repeat call simply replaces the pending schedule with a freshly recomputed one) -- this is the mechanism biweekly/monthly recurrence relies on, and it must be re-invoked on every app-foreground event (`AppLifecycleState.resumed`), not only when the Weight Tracking screen happens to be opened (05-CONTEXT.md Planning Addendum -- locked decision, resolving a checker-flagged gap).
    - `requestPermissionIfNeeded()` is called explicitly by the caller (a settings toggle handler) -- never invoked automatically by `initialize()`.
    - On permission denial, scheduling is skipped and the method returns a typed result the caller uses to revert its toggle -- no exception thrown for the expected "denied" case.
  </behavior>
  <action>
    Add a top-level `final rootNavigatorKey = GlobalKey&lt;NavigatorState&gt;();` to `lib/core/router/app_router.dart` and pass it as `GoRouter(navigatorKey: rootNavigatorKey, ...)`.

    Create `lib/domain/services/notification_service.dart`:
    - `class NotificationService` wrapping a `FlutterLocalNotificationsPlugin` instance (constructor-injected for testability, following this project's existing DI-by-constructor convention rather than a static singleton).
    - `Future<void> initialize()`: configures `timezone`/`flutter_timezone` (`tz.initializeTimeZones()`, `tz.setLocalLocation(tz.getLocation(await FlutterTimezone.getLocalTimezone()))`), calls `_plugin.initialize(...)` with `onDidReceiveNotificationResponse` set to a method that reads `response.payload` and, via `rootNavigatorKey.currentState?.context`, calls `GoRouter.of(context).push(payload)` when the context is non-null and the payload matches one of this app's known route prefixes (`/food-search`, `/weight-tracking`) — reject/ignore any other payload shape (RESEARCH.md Security Domain: notification payloads are always app-generated, but the parser still validates the shape defensively).
    - `Future<bool> requestPermissionIfNeeded()`: checks `permission_handler`'s `Permission.notification.status`; if already granted, returns `true` without prompting; otherwise requests and returns the granted boolean. Never called from `initialize()`.
    - `Future<bool> scheduleMealReminder(MealSlot slot, String time)`: parses `time` (`'HH:mm'`) into hour/minute, computes the next `TZDateTime` occurrence, calls `zonedSchedule` per the `<behavior>` bullets with `payload: '/food-search?slot=${slot.name}'` (this exact query-param contract is what Plan 05-18's food-search `initialSlot` wiring implements later — cite this cross-plan contract in a doc comment). Uses a stable id via `slot.index` offset into a dedicated notification-id range (e.g. `100 + slot.index`).
    - `Future<void> cancelMealReminder(MealSlot slot)`.
    - `Future<bool> scheduleWeighInReminder({required String frequency, int? weekday, required String time})`: for `'weekly'`/`'custom'`, uses `matchDateTimeComponents: DateTimeComponents.dayOfWeekAndTime`; for `'biweekly'`/`'monthly'`, schedules the next single occurrence only (`flutter_local_notifications` has no native biweekly/monthly recurrence primitive), computed fresh from `DateTime.now()` every time this method runs — payload `'/weight-tracking'`. Uses a single fixed weigh-in notification id distinct from the meal-slot id range, so every call (whether the first enable or a later re-arm) replaces the pending schedule rather than duplicating it.

    **Locked decision (05-CONTEXT.md Planning Addendum):** because there is no native recurrence for biweekly/monthly, this method must be re-invoked periodically to keep the "next occurrence" fresh, and the correct trigger for that is every app-foreground event, not merely whenever the Weight Tracking screen happens to be opened (the original design's flaw: infrequent-checkin users choosing Monthly might never reopen that screen before the single scheduled occurrence fires and isn't replaced). This method itself stays a pure, stateless scheduling call with no knowledge of *when* it's invoked from — Plan 05-18 adds the actual `AppLifecycleState.resumed` observer at the app root (`lib/app.dart`) that calls this method (via the Weight feature's own settings) on every foreground event, in addition to Plan 05-13's existing screen-open call (belt-and-suspenders, not a replacement).
    - `Future<void> cancelWeighInReminder()`.
    - On any `PlatformException` from the plugin (permission denial, etc.), catch and return `false`/no-op rather than propagating — callers (settings toggle handlers, built in later plans) interpret a `false` result as "revert the toggle, show Open Settings link" per CONTEXT.md's permission-denied UX.

    **Register the DI provider in this same task, in this same plan** (fixes a dependency-correctness gap the plan-checker caught: `NotificationService` must have exactly one authoritative provider, created where the class itself is created, not left for a same-wave sibling plan to add): add `@Riverpod(keepAlive: true) NotificationService notificationService(Ref ref) => NotificationService(FlutterLocalNotificationsPlugin());` to `lib/core/di/notification_providers.dart` (this file already exists from this same plan's Task 1, which added `notificationPrefsDaoProvider`/`notificationPrefsRepositoryProvider` — this task adds one more provider to it). Every later plan that needs `NotificationService` (05-13, 05-14, 05-18) consumes `ref.read(notificationServiceProvider)` / `ref.watch(notificationServiceProvider)` — none of them may declare a competing provider for it.

    Wire `lib/main.dart`: after `runApp`, call `flutterLocalNotificationsPlugin.getNotificationAppLaunchDetails()` and, if the app was cold-launched via a notification tap, schedule the same navigation via `WidgetsBinding.instance.addPostFrameCallback` (per RESEARCH.md Pattern 4's cold-start handling). Call `NotificationService.initialize()` before `runApp`, mirroring the existing `configureOff()`/`ensureOffReferenceDb()` sequencing (non-fatal on any initialization exception, following this file's existing `try`/`on Exception catch` pattern).

    De-skip `test/domain/services/notification_service_test.dart` with real assertions using a mocktail mock of `FlutterLocalNotificationsPlugin` (verify `zonedSchedule` is called with the expected named-argument values via mocktail's `verify(() => mockPlugin.zonedSchedule(...))` capturing argument matchers), covering the `<behavior>` bullets.
  </action>
  <verify>
    <automated>flutter test test/domain/services/notification_service_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`NotificationService` compiles and its de-skipped test passes, proving named-parameter usage, `inexactAllowWhileIdle`, JIT permission timing, and denial handling. `notificationServiceProvider` is registered in `lib/core/di/notification_providers.dart` — the single authoritative DI-access path every later plan consumes. `rootNavigatorKey` is wired into `GoRouter`. `main.dart` initializes the service and handles cold-start launch details without crashing when notifications are unavailable (e.g. simulator/CI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notification tap -> deep link | Payload is always app-generated (never network/user-supplied), but the tap handler still validates payload shape before calling `router.push` |
| App <-> OS notification scheduler | Local-only; no FCM/APNs, no server-side infrastructure (NOTIF-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | Notification payload used as a route string | mitigate | Payload always constructed by `NotificationService` itself; tap handler rejects any payload not matching a known route prefix before calling `router.push` (RESEARCH.md Security Domain) |
| T-05-08-02 | Elevation of Privilege | `SCHEDULE_EXACT_ALARM` special permission | mitigate | Never requested -- `AndroidScheduleMode.inexactAllowWhileIdle` used exclusively (RESEARCH.md Pitfall 3) |
| T-05-08-SC | Tampering | pub.dev package installs (fl_chart, flutter_local_notifications, timezone, flutter_timezone) | mitigate | Blocking human-verify checkpoint completed above per Package Legitimacy Audit before any install |
</threat_model>

<verification>
```bash
flutter test test/domain/services/notification_service_test.dart && flutter analyze lib/domain/services/notification_service.dart lib/core/router/app_router.dart lib/main.dart
```
</verification>

<success_criteria>
- Four new packages installed and human-approved
- `NotificationPrefs`/`NotificationPrefsRepository` cover all four meal slots
- `NotificationService` uses named parameters, `inexactAllowWhileIdle`, and JIT permission requesting only
- `rootNavigatorKey` wired into `GoRouter`; notification-tap navigation validated against known route prefixes
- De-skipped `notification_service_test.dart` passes
- `notificationServiceProvider` is registered exactly once, in this plan, in `lib/core/di/notification_providers.dart`
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-08-SUMMARY.md` when done.
</output>
