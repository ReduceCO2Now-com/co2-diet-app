---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
task: uat
total_tasks: 9
status: in_progress
last_updated: "2026-08-03T13:35:00.000Z"
---

<current_state>
Still inside the Phase 5 real-device UAT pass (see `05-UAT.md`). Tests 1-6
of 9 pass. Mid-Test-7 (Backup & Restore / Export Data): the export
field-leak bug found earlier this session is now **fixed, tested, and
committed** (`8545e1e`) — see `completed_work` below. What's left for
Test 7 is the actual on-device walkthrough (native share sheet, real
exported files), which hasn't happened yet.
</current_state>

<completed_work>

- UAT Tests 1-6 of 9 done and passing (see `05-UAT.md` for full detail;
  also summarized in the phase's prior `.continue-here.md` history).
- Test 7 (Backup & Restore) started: walked through
  `backup_export_service.dart`'s `exportData()`/`createBackup()`/
  `storageStatus()`, all of which funnel through the shared
  `_readCategoryRows()` (line 442) to build both backup and user-facing
  export files.
- **Root cause found:** `_readCategoryRows()` (backup_export_service.dart:442-468)
  calls `row.toJson(serializer: _serializer)` on the *raw Drift row* for
  every `ExportCategory`, with no field filtering. Every one of the 7
  categories' backing tables (`user_profile_table.dart`,
  `meal_entry_table.dart`, `favorite_table.dart`, `user_food_table.dart`,
  `weight_entry_table.dart`, `co2_settings_table.dart`,
  `notification_prefs_table.dart`) mixes in `SyncSafeTable`
  (`lib/data/local/mixins/sync_safe_table.dart`), which injects 6
  internal sync-machinery columns onto every table: `id` (UUID v7),
  `hlcMillis`, `hlcCounter`, `hlcNodeId` (HLC clock components — Phase-1
  placeholders per `backup_export_service.dart`'s own comments),
  `dirty` (local-changes flag), `deletedAt` (soft-delete tombstone).
  Confirmed via `grep -l "with SyncSafeTable" lib/data/local/tables/*.dart`
  — all 7 export categories' tables are on the list. So this is not a
  Profile-specific bug — it's a shared-helper bug that leaks the same 6
  internal columns into every category's CSV/Excel/JSON export and into
  every backup zip.
- **Fix implemented and committed (`8545e1e`):** checked `_restoreCategory()`
  first and confirmed `applyRestore()`'s `*Row.fromJson()` genuinely needs
  every `SyncSafeTable` column to reconstruct a valid row — so the fix
  had to diverge backup fidelity from human-export fidelity, not apply
  one filter to both. Added `includeInternalFields` param to
  `exportData()` (default `false`), backed by a new
  `_stripInternalFields()` helper keyed off a `_internalFieldNames`
  constant (`id`/`hlcMillis`/`hlcCounter`/`hlcNodeId`/`dirty`/
  `deletedAt`). `createBackup()` now passes `includeInternalFields: true`
  so restore is unaffected. `BackupNotifier.shareExport()` (the actual
  "Export Data"/"Share export" UI path) gets the stripped default
  automatically — no caller-side change needed there.
- Wrote 2 new regression tests in `backup_export_service_test.dart`
  first, confirmed the leak test genuinely failed against pre-fix code
  (`row.containsKey('id')` was `true`), then implemented the fix and
  reverified both pass. Full suite (379 tests) green, `flutter analyze`
  clean on the touched file.
- Wrote up the bug as a `resolved` gap entry in `05-UAT.md`'s Gaps
  section (same YAML format as the other 6 entries), `fix_commit:
  "8545e1e"`. **Real-device confirmation of an actual exported CSV/JSON
  file is explicitly flagged as still outstanding** in that entry — this
  was a code-reading find, not yet verified against a real exported file
  on hardware.
</completed_work>

<remaining_work>

- **Resume Test 7's actual on-device walkthrough**: from Settings, open
  "Backup & Restore", confirm Current Storage Status/Automatic
  Backups/Export Data/Restore Data/Privacy statement/Danger Zone are all
  present, tap "Create backup" and confirm the native OS share sheet
  opens with a real archive attached, then "Share export" — and this
  time, actually open/inspect an exported CSV or JSON file to visually
  confirm the internal sync columns are really gone and the human-facing
  data still looks right. This on-device confirmation is the one thing
  not yet done for the export-leak fix.
- Then Tests 8-9 (Restore document picker, Danger Zone typed
  confirmation).
- Bug #4 (Profile Daily Targets crash) is still DEFERRED — do not resume
  investigating it as part of this UAT pass (see `deferred-items.md`).
- iOS re-confirmation of the text-field-focus-loss bug (#3) is
  nice-to-have, not blocking.
- Once UAT complete: diagnose remaining issues → `/gsd:plan-phase 5
  --gaps` → `/gsd:execute-phase 5 --gaps-only` (standard gap-closure
  flow). Phase 5 is already marked complete in ROADMAP.md/STATE.md — no
  need to re-run `phase complete`, just get the gap fixes committed.
</remaining_work>

<decisions_made>

- User wanted this export-leak bug treated as a full systemic fix
  touching all 7 categories, done with complete context in a fresh
  session — that's why the prior session was paused rather than rushed.
- Confirmed (by reading `_restoreCategory()`/`fromJson()`) that backup
  and human-facing export genuinely need *different* field fidelity —
  restore needs every `SyncSafeTable` column, plain export doesn't. Went
  with a single `exportData()` parameter (`includeInternalFields`)
  rather than two separate methods, since the only real difference is
  which fields survive encoding — everything else (zip structure,
  manifest, per-format encoding) is identical between backup and export.
- Every bug fix this UAT session has followed strict discipline: find
  root cause by reading real code, write a regression test, verify it
  fails pre-fix, then fix and reverify. Followed the same discipline
  here — the leak test was confirmed failing against pre-fix code before
  the fix was written.
- `excel` (not `excel_plus`) is locked in for Excel export (Plan 05-09 /
  `05-CONTEXT.md` Planning Addendum) — unrelated to this bug, unaffected
  by the fix (still runs through the same `_encodeRows()`).
</decisions_made>

<blockers>
None. Code fix is done, tested, and committed. Only remaining step is
the real-device walkthrough for Test 7 itself.
</blockers>

<context>
This UAT pass has been unusually productive at catching real bugs
automated tests never caught (three stacked notification bugs, two
fl_chart rendering bugs, a two-layer text-focus bug, and now this export
leak — see `05-UAT.md` for full history). The export-field-leak bug fit
the same pattern: something structurally wrong that was invisible to
automated tests because no existing test asserted on export *content*,
only on file existence/row counts — now two new tests do assert on
content.

The fix is code-complete and unit-tested, but **not yet confirmed on a
real device** — no one has actually opened an exported CSV/JSON file on
hardware to eyeball that the leak is really gone and the remaining data
still looks right. Do that as part of resuming Test 7's walkthrough,
with the same rigor as every other bug this session: don't consider this
gap entry "fully closed" (as opposed to its current "resolved" status
based on unit-test verification) until the user has actually seen it
work on-device, per this session's established pattern (e.g. bug #2's
"RESOLVED" only came after user confirmation on Tab S7 FE, not just
after tests passed).
</context>

<next_action>
Start with: walk the user through Test 7 (Backup & Restore) on their
Tab S7 FE — from Settings, open "Backup & Restore", tap "Create backup"
(confirm native OS share sheet, real archive attached), then "Share
export" for at least one category/format combo, then actually open the
exported CSV or JSON file and confirm the 6 internal sync columns
(`id`/`hlcMillis`/`hlcCounter`/`hlcNodeId`/`dirty`/`deletedAt`) are gone
and the real data looks right. Then continue to Tests 8-9 (Restore
document picker, Danger Zone typed confirmation).
</next_action>
