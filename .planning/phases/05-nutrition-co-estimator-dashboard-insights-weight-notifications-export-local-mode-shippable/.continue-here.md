---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
task: uat
total_tasks: 9
status: in_progress
last_updated: "2026-08-03T12:37:34.230Z"
---

<current_state>
Phase 5 is fully executed and passed automated goal-verification (35/35 must-haves). Now running a manual UAT pass (`.../05-UAT.md`) at the user's explicit request, since Phase 5 had zero real-device checkpoints (unlike Phases 2/3/4, which each had one and each caught real bugs automated tests missed). This UAT session found real bugs automated tests never caught, confirming the user's instinct was right.

Tests 1-4 of 9 pass. **Test 5/6 (notifications) required THREE stacked root-cause fixes, all now committed (`025bc55`, `973a9eb`), one final user-side interactive step away from real-device confirmation.** The earlier "Samsung sleeping apps" hypothesis was superseded -- the real causes were (1) timezone never initialized, (2) required plugin manifest receivers never declared (confirmed via the actual merged manifest that shipped in the tested APK), (3) inexact alarm scheduling mode, which the user correctly identified as wrong for a time-anchored feature after their own sharp "Alarms & reminders special access" observation. Now uses exact alarm scheduling. **Blocked on: user needs to enable a meal or weigh-in reminder once more in the app** -- this triggers the new just-in-time SCHEDULE_EXACT_ALARM permission request, which opens a system Settings screen only a human can tap through (adb cannot grant this on this Knox-managed device -- confirmed while investigating, pm/dumpsys package commands are OS-restricted for third-party packages specifically on this device).
</current_state>

<completed_work>

UAT tests 1-4 done (9 total), all pass: (1) Dashboard general composition. (2) CO2 Calculation Settings screen. (3) Data Analysis screen, including the supplementary remaining-scope check. (4) Weight Tracking -- log weigh-in, range switching, touch-and-drag tooltip, and goal reference line all confirmed working; range labels relabeled to plain language (Week/Month/3 Months/Year/All, commit `3d8cf19`). The range-switching "bug" turned out not to be one -- user's test data was all within one week, so every range genuinely looked identical; a new widget test (`weight_screen_test.dart`, entries spread 3/20/60/200/400 days apart) confirms the underlying mechanism works correctly. One non-blocking polish item logged in `deferred-items.md`: the Weight chart has no visible axis/date labels at all, making it harder to read at a glance (data/interaction still correct).

**Test 5/6 (meal + weigh-in reminders) -- THREE stacked root causes found and fixed, one final interactive step from real-device confirmation:**

- **Root cause 1 (commit `025bc55`):** `NotificationService.initialize()` never called `tz_data.initializeTimeZones()`/`tz.setLocalLocation(...)` despite its own doc comment claiming it does -- `tz.local` is a package-global `late` field with no default, so the first read of it threw `LateInitializationError`, uncaught (not a `PlatformException`), silently preventing any scheduling. Reproduced via a widget test isolated from `notification_service_test.dart` (whose `setUpAll` pre-seeds `tz.setLocalLocation`, masking this gap). Fixed by actually calling `tz_data.initializeTimeZones()` + `FlutterTimezone.getLocalTimezone()`.
- User did a genuinely fresh rebuild+restart of that fix and STILL saw nothing fire, and explicitly asked for real device evidence rather than another theory. Ran `integration_test/notification_scheduling_test.dart` (real, non-mocked plugin) on the user's connected Tab S7 FE (`R52RB0FSSAX`), commit `2285c60`: `tz.local` resolved correctly, scheduling succeeded, `pendingNotificationRequests()` showed 3 pending notifications -- but waiting past the scheduled time in real time via `adb` showed the alarm never fired.
- **User independently found a second, sharp, concrete clue:** the app was entirely absent from Settings > Apps > Special access > Alarms & reminders. That page only lists apps that declared `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM`. Investigating led to **root cause 2 (commit `973a9eb`):** `android/app/src/main/AndroidManifest.xml` never declared the `ScheduledNotificationReceiver`/`ScheduledNotificationBootReceiver` that `flutter_local_notifications` (>= v16) requires the CONSUMING app to declare itself (the plugin stopped bundling these in its own manifest). Without the receiver, `AlarmManager` had no registered component to deliver the broadcast to -- confirmed by directly inspecting the actual merged manifest baked into the previously-tested APK (receivers genuinely absent). Added them, rebuilt, confirmed present, then confirmed via `adb shell dumpsys alarm` that `AlarmManager` now holds a validly-targeted alarm.
- Still wasn't firing after ~8 minutes of real-time waiting (checked via `adb shell dumpsys alarm`/`dumpsys notification` directly). Agreed with the user this pointed to **root cause 3:** `AndroidScheduleMode.inexactAllowWhileIdle` is structurally the wrong choice for a time-anchored reminder feature (not a workaround to switch away from -- exact scheduling is the product-correct behavior here). User explicitly approved switching to `exactAllowWhileIdle` + `SCHEDULE_EXACT_ALARM` (commit `973a9eb`), with the SAME just-in-time, clearly-explained permission pattern already used for camera/notification permission (`NotificationService.requestExactAlarmPermissionIfNeeded()`, wired into both `MealReminderSettingsSection`'s and `WeighInReminderSection`'s toggle handlers -- deny reverts the toggle + shows "Open Settings").
- Full suite (377 tests, 2 new) green, `flutter analyze` clean.
- **Could not complete final real-device verification myself:** granting `SCHEDULE_EXACT_ALARM` requires a human to tap through a system Settings screen (the plugin's `requestExactAlarmsPermission()` launches `ACTION_REQUEST_SCHEDULE_EXACT_ALARM` and blocks on the result) -- `adb` cannot grant this programmatically on this specific device, since `pm`/`dumpsys package` commands are OS/MDM-restricted for third-party packages on this Samsung Knox-managed tablet (confirmed while investigating: `pm path com.android.settings` works, `pm path com.reduceco2now.co2diet` fails with a permission/visibility error). `adb shell dumpsys alarm` (a different service) DOES still work for reading alarm state without that restriction.

**Bugs found and their status (all in `05-UAT.md`'s Gaps section, YAML):**

1. **Android build failure** (core library desugaring missing for flutter_local_notifications) -- RESOLVED, commit `08e7bc1`. Confirmed via successful `flutter build apk --debug`.
2. **Two fl_chart rendering bugs on Data Analysis** (Y-axis label overlap on the stacked bar chart; X-axis showing raw decimal indices instead of dates on the trend chart) -- RESOLVED, commit `c3cbd84`, user-confirmed on Android Tab S7 FE 2026-07-29. Root cause for #2a: fl_chart's `SideTitles.maxIncluded` default forces an extra label at the exact max value, colliding with the last regular-interval label when max isn't a clean multiple of the interval -- fixed via `maxIncluded: false`. Root cause for #2b: `trend_section.dart`'s `LineChartData` had no `titlesData` at all (not a misconfigured `getTitlesWidget` as originally guessed) -- added a real bottomTitles date mapping. Both reproduced via a widget test that failed against pre-fix code, then verified passing after -- test lives in `data_analysis_screen_test.dart`.
3. **Text-field focus loss on every keystroke** (Profile Setup age/height/weight, CO2 Settings location/region, Weight Tracking goal) -- RESOLVED, user-confirmed on Android Tab S7 FE 2026-07-29 (fast typing across Profile/CO2 Settings/Weight, no drops). **iOS iPhone re-confirmation still outstanding** -- this bug was originally reported cross-device (both Android and iOS); only Android has been re-tested so far. Flag this if it resurfaces, but it's not blocking further UAT progress.
   - Fix 1 (commit `1f58cf1`): removed value-tied `ValueKey`s on TextFormFields in `profile_form.dart`, `co2_settings_screen.dart`, `weight_screen.dart` that caused per-keystroke widget remount.
   - Fix 2 (commit `147f1f1`): removed `state = AsyncValue.loading()` mid-save from `ProfileNotifier.saveProfile`/`Co2SettingsNotifier.saveSettings`, which was swapping the ENTIRE screen body to a spinner on every keystroke.
4. **Profile screen crash tapping any Daily Targets card** (`_dependents.isEmpty` Flutter framework assertion) -- **DEFERRED 2026-07-29, root cause still unknown.** This is Phase 1 code (PROF-01-05), not Phase 5 scope, and does not block Phase 5's checkpoint -- user explicitly decided to stop investigating (four attempts total) and track it as debt instead. Full writeup: `deferred-items.md`'s "Profile 'Daily Targets' crash (bug #4)" section. Do NOT resume investigating this as part of Phase 5 UAT -- pick it up later as its own bugfix session or when Phase 6 touches the Profile/onboarding area again. Deferral + evidence committed at `1f534c1`.
</completed_work>

<remaining_work>

- Ask the user to enable a meal reminder or weigh-in reminder in the app once more (fresh install already has all 3 fixes: `025bc55`, `2285c60`, `973a9eb`). This will trigger the NEW just-in-time exact-alarm permission request, which navigates to a system Settings screen -- have them grant it there and return to the app. This is the one remaining step only a human can do (see current_state for why).
- Once granted: verify via `adb shell dumpsys alarm | grep co2diet` that a new exact alarm is registered, wait past the scheduled time, and check `adb shell dumpsys notification --noredact | grep -A20 co2diet` for the actual posted notification -- this is a stronger, more direct check than `pendingNotificationRequests()` (which only reflects the plugin's own bookkeeping, not genuine OS delivery).
- Once actual OS-level firing is confirmed: mark Test 5 pass and the "Meal and weigh-in reminders actually fire" gap entry `resolved` (currently `partial`), then continue to Tests 7-9 (Backup share sheet, Restore document picker, Danger Zone typed confirmation). Test 6 (re-arm behavior) should be retested at the same time.
- Bug #4 (crash) is DEFERRED -- do not resume investigating it as part of this UAT pass (see `deferred-items.md`).
- iOS re-confirmation of bug #3 (focus loss) is nice-to-have but not blocking -- mention if the opportunity arises, don't hold up the UAT sequence for it.
- Once UAT testing is complete: diagnose remaining issues -> `/gsd:plan-phase 5 --gaps` -> `/gsd:execute-phase 5 --gaps-only` per the standard gap-closure flow.
- After all gaps closed and phase re-verified: Phase 5 was already marked complete in ROADMAP.md/STATE.md before this UAT session started (that happened before the user asked for device verification) -- no need to re-run `phase complete`, just confirm the gap fixes are committed.
</remaining_work>

<decisions_made>

- User explicitly required a dedicated real-device UAT pass for Phase 5, matching Phases 2/3/4's precedent, rather than folding it into Phase 6 -- Phase 5's automated-only closure was judged insufficient.
- Every requirement-completion marking this session was scrutinized against actual reachability (not just partial-layer completion) after two premature completions (CO2-02/CO2-03) were caught and reverted early in execution (commit `746dbf8`).
- `excel` (not `excel_plus`) locked in for Plan 05-09's Excel export -- recorded in `05-CONTEXT.md`'s Planning Addendum.
- Every bug fix this session followed a strict discipline: identify root cause via reading real code (not guessing), write a regression test, verify via git-stash that the test actually fails against the pre-fix code, then confirm it passes after the fix. This same discipline is why bug #4's third theory was correctly NOT turned into a fix -- the test didn't fail, so the fix wasn't applied.
</decisions_made>

<blockers>
- None currently active. Bug #4 (Profile screen crash) has no confirmed root cause but is formally deferred (not a Phase 5 blocker) -- see `deferred-items.md`.
</blockers>

<context>
This session has been unusually deep for a single UAT pass -- three real, previously-invisible bugs found and (mostly) fixed, all because 366+ existing automated tests never caught any of them. The user has been an excellent bug reporter: precise reproduction steps, cross-device confirmation, and correctly pushing back when a fix claimed "resolved" without real verification. Match that rigor when resuming -- do not declare bug #3 "resolved" without the user's explicit real-device confirmation, and do not attempt a fix for bug #4 without a failing test first.
</context>

<next_action>
Start with: run `/gsd:resume-work` or continue directly. Ask the user to open the app (already rebuilt+installed with all 3 notification fixes) and enable a meal or weigh-in reminder -- this triggers the new exact-alarm Settings screen, which they need to grant manually (adb cannot do this on their device). Once granted and they confirm, use `adb shell dumpsys alarm`/`dumpsys notification` (both confirmed working without the pm/dumpsys-package restriction hit earlier) to independently verify real firing. Bug #4 is deferred -- do NOT reopen it here.
</next_action>
