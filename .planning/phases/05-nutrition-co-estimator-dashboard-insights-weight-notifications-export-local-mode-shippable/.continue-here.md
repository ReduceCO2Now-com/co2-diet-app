---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
task: uat
total_tasks: 18
status: in_progress
last_updated: "2026-08-03T15:00:00.000Z"
---

<current_state>
Phase 5's Android UAT pass is **complete: 9/9 tests pass** (Tab S7 FE).
Per the user's explicit call, the same 9-test script now needs an
**independent iOS pass** on a real iPhone before Phase 5 is considered
genuinely done — not just the one previously-outstanding iOS
re-confirmation item (bug #3, text-field focus loss), the *entire*
script, since this project's standing precedent (Phase 3's
camera-permission crash, caught only by a dedicated iOS real-device
pass) is that no platform is assumed to work because the other one
does. This is especially true for Phase 5's notification system
(Tests 5-6), which is built on `UNUserNotificationCenter` on iOS vs.
`AlarmManager` on Android — genuinely different OS subsystems, not a
shared code path that Android testing could have exercised.

`05-UAT.md` has been restructured to hold both passes: the existing
Android results are preserved as a labeled historical section
("Android Pass — complete, 9/9"), and a fresh "iOS Pass — in progress"
section with all 9 tests reset to `[pending]` has been appended,
mirroring the same script with iOS-specific callouts on the tests most
likely to diverge (Tests 2/4's focus-loss re-confirmation, Tests 5/6's
independent notification subsystem, Test 7's export field-leak
regression check). Frontmatter now tracks `platforms_completed:
[android]` / `current_platform: ios`. No iOS testing has actually
started yet — this is purely the setup/restructuring step.
</current_state>

<completed_work>

- **Android pass: 9/9 tests pass.** Full detail (root causes, fixes,
  commits, real-device confirmations) lives in `05-UAT.md`'s "Android
  Pass" section and its Gaps section — not repeated here to keep this
  handoff focused on what's next. Six real bugs found and fixed this
  session (three stacked notification root causes, two fl_chart
  rendering bugs, a two-layer text-focus bug, an export field leak), one
  correctly-diagnosed non-bug (Test 8's stale test artifact), all with
  real-device confirmation, not just passing unit tests.
- **`05-UAT.md` restructured for the iOS pass** (this step, just done):
  Android results preserved as a labeled historical section; a fresh
  "iOS Pass — in progress" section added with all 9 tests reset to
  `[pending]`, same script, iOS-specific callouts added where the two
  platforms are most likely to diverge (Tests 2/4's focus-loss
  re-confirmation was already flagged outstanding for iOS; Tests 5/6 run
  on an entirely different OS notification subsystem; Test 7 needs a
  spot-check that the export field-leak fix holds on iOS too). No
  Gaps-section entries changed — those stay as Android-confirmed
  history; new iOS-only bugs (if any) get a `platform:` tag when logged.
</completed_work>

<remaining_work>

- **Run the full 9-test script on a real iPhone**, starting at Test 1
  (Dashboard). Track results in `05-UAT.md`'s "iOS Pass" section as you
  go, same format as the Android pass (`result: pass/fail`, `note:`
  with real-device evidence). Pay particular attention to:
  - **Tests 5/6 (notifications):** genuinely independent of the Android
    fix — iOS uses `UNUserNotificationCenter`, not `AlarmManager`. Don't
    assume this works because Android's three-root-cause fix landed.
  - **Tests 2/4 (text-field focus loss):** this bug was originally
    reported on iOS too, only re-confirmed on Android so far — this is
    the one specific carry-over item from the Android pass.
  - **Test 7 (export field leak):** the fix is platform-agnostic Dart
    code, so it should hold, but worth an actual spot-check of an
    exported file on iOS rather than assuming.
- Any new iOS-only bugs found: same discipline as the Android pass —
  real root cause, regression test that fails pre-fix, fix, real-device
  re-confirmation, logged as a Gaps entry with `platform: ios`.
- Bug #4 (Profile Daily Targets crash) remains formally DEFERRED,
  cross-device (Android AND iOS both crash) but out of Phase 5's scope —
  do not resume investigating it as part of this pass (see
  `deferred-items.md`).
- Once iOS's 9 tests are done: update `05-UAT.md`'s frontmatter
  (`platforms_completed: [android, ios]`, `status: complete`), then
  confirm whether any iOS-only gaps need closing before Phase 5 is
  genuinely done. Only then move to Phase 6.

<decisions_made>

- **User explicitly rejected treating Phase 5 as done after the Android
  pass alone.** Standing project rule: no platform is assumed to work
  because the other one does — established precedent is Phase 3's
  camera-permission crash, which only a dedicated iOS real-device pass
  caught (Android testing had missed it entirely). Applies with extra
  force to Phase 5 since notifications are built on genuinely different
  OS subsystems per platform (`UNUserNotificationCenter` vs.
  `AlarmManager`), not a shared code path.
- Reused the same `05-UAT.md` file for both platforms (labeled sections)
  rather than creating a second file, to keep one source of truth per
  phase — no established convention existed for a second-platform pass,
  so this was a judgment call, not a repo-wide pattern.
- Every bug fix this UAT session (Android pass) followed strict
  discipline: find root cause by reading real code, write a regression
  test, verify it fails pre-fix, then fix and reverify, then real-device
  confirm. Apply the identical discipline to whatever the iOS pass
  surfaces — don't relax it just because Android is done.
</decisions_made>

<blockers>
None. iOS pass hasn't started yet — purely needs the user to actually
run the 9 tests on a real iPhone.
</blockers>

<context>
The Android pass was unusually productive at catching real bugs
automated tests never caught — see `05-UAT.md`'s Android section for
full history. The user's insistence on an independent iOS pass here is
consistent with how this project has always treated cross-platform
correctness (Phase 3's camera bug is the standing example) — don't
skip or abbreviate the iOS script just because Android went well.
</context>

<next_action>
Start with: walk the user through iOS Test 1 (Dashboard — general
composition) on a real iPhone, then continue sequentially through
Test 9, updating `05-UAT.md`'s "iOS Pass" section's `result:`/`note:`
fields as each one is confirmed — same rigor as the Android pass (real
root cause, failing test before any fix, real-device re-confirmation
for anything found broken).
</next_action>
