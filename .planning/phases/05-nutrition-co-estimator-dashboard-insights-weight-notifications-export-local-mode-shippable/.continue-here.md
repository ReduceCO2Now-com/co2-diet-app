---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
task: uat
total_tasks: 9
status: in_progress
last_updated: "2026-08-03T13:11:17.089Z"
---

<current_state>
Still inside the Phase 5 real-device UAT pass (see `05-UAT.md`). Tests 1-6
of 9 pass (dashboard, CO2 settings, data analysis, weight tracking,
meal+weigh-in notifications — the last one closed after a 3-root-cause
investigation, commits `025bc55`/`973a9eb`/`4d373c2`). Mid-Test-7 (Backup
& Restore / Export Data), inspecting `lib/domain/services/
backup_export_service.dart` found a real, systemic bug in the export
pipeline that is NOT yet written up in `05-UAT.md`'s Gaps section or
`deferred-items.md`, and NOT yet fixed. This session was paused (per
user's explicit request) before writing up or fixing it, specifically to
tackle it properly with full context next time rather than rushing.

**No code has been changed for this bug yet — `git status`/`git diff`
are clean, everything up to `4d373c2` is committed.** This is purely an
investigation finding that needs to be acted on next session.
</current_state>

<completed_work>

- UAT Tests 1-6 of 9 done and passing (see `05-UAT.md` for full detail;
  also summarized in the phase's prior `.continue-here.md` history).
- Test 7 (Backup & Restore) started: walked through
  `backup_export_service.dart`'s `exportData()`/`createBackup()`/
  `storageStatus()`, all of which funnel through the shared
  `_readCategoryRows()` (line 442) to build both backup and user-facing
  export files.
- **Root cause found:** `_readCategoryRows()` (backup_export_service.dart:442-468)
  calls `row.toJson(serializer: _serializer)` on the *raw Drift row* for
  every `ExportCategory`, with no field filtering. Every one of the 7
  categories' backing tables (`user_profile_table.dart`,
  `meal_entry_table.dart`, `favorite_table.dart`, `user_food_table.dart`,
  `weight_entry_table.dart`, `co2_settings_table.dart`,
  `notification_prefs_table.dart`) mixes in `SyncSafeTable`
  (`lib/data/local/mixins/sync_safe_table.dart`), which injects 6
  internal sync-machinery columns onto every table: `id` (UUID v7),
  `hlcMillis`, `hlcCounter`, `hlcNodeId` (HLC clock components — Phase-1
  placeholders per `backup_export_service.dart`'s own comments),
  `dirty` (local-changes flag), `deletedAt` (soft-delete tombstone).
  Confirmed via `grep -l "with SyncSafeTable" lib/data/local/tables/*.dart`
  — all 7 export categories' tables are on the list. So this is not a
  Profile-specific bug — it's a shared-helper bug that leaks the same 6
  internal columns into every category's CSV/Excel/JSON export and into
  every backup zip.
- Confirmed nothing has been fixed or committed yet for this — it was
  caught by reading the code during Test 7, not by a failing test or a
  user report on-device yet.
</completed_work>

<remaining_work>

- **Decide and implement the fix for the `_readCategoryRows()` leak**,
  likely a field-allowlist (or blocklist) applied per category after
  `toJson()`, or a custom serializer that excludes `SyncSafeTable`'s 6
  columns from the human-facing export while still including them in
  `createBackup()`'s zips if `applyRestore()` actually needs them to
  round-trip correctly (**check `_restoreCategory()`/`fromJson()` calls
  around line 474+ before stripping fields — restore may depend on `id`
  at least for `upsertProfile`/`restoreEntries` etc., so backup exports
  and human-readable exports likely need *different* filtering, not the
  same one**).
- Whichever approach is picked, it needs to apply uniformly across all 7
  categories (profile, mealEntries, favorites, customFoods,
  weightEntries, co2Settings, notificationPrefs), not just Profile.
- Write a regression test that fails against current code (asserts an
  exported row's JSON does NOT contain `hlcMillis`/`hlcCounter`/
  `hlcNodeId`/`dirty`/`deletedAt` for at least one, ideally all 7,
  categories) before writing the fix — same discipline used for every
  other bug this session (see `decisions_made` below).
- After the fix: write up the bug in `05-UAT.md`'s Gaps section (YAML,
  same format as the other 5 resolved entries) and resume Test 7 proper
  (confirm native share sheet opens for "Create backup" and "Share
  export" with a real archive attached).
- Then Tests 8-9 (Restore document picker, Danger Zone typed
  confirmation).
- Bug #4 (Profile Daily Targets crash) is still DEFERRED — do not resume
  investigating it as part of this UAT pass (see `deferred-items.md`).
- iOS re-confirmation of the text-field-focus-loss bug (#3) is
  nice-to-have, not blocking.
- Once UAT complete: diagnose remaining issues → `/gsd:plan-phase 5
  --gaps` → `/gsd:execute-phase 5 --gaps-only` (standard gap-closure
  flow). Phase 5 is already marked complete in ROADMAP.md/STATE.md — no
  need to re-run `phase complete`, just get the gap fixes committed.
</remaining_work>

<decisions_made>

- User wants this export-leak bug treated as a full systemic fix
  touching all 7 categories, done with complete context in a fresh
  session, rather than rushed under the previous session's context
  limit — that's the entire reason this pause was requested.
- Every bug fix this UAT session has followed strict discipline: find
  root cause by reading real code, write a regression test, verify via
  git-stash that it actually fails pre-fix, then fix and reverify. Apply
  the same discipline here — don't fix before a failing test exists.
- `excel` (not `excel_plus`) is locked in for Excel export (Plan 05-09 /
  `05-CONTEXT.md` Planning Addendum) — unrelated to this bug but keep it
  in mind since `_encodeRows()` touches the same rows this bug affects.
</decisions_made>

<blockers>
None. This is a fresh finding with a clear next step (pick the
allowlist/blocklist approach, verify restore's field needs first, write
a failing test, fix, verify).
</blockers>

<context>
This UAT pass has been unusually productive at catching real bugs
automated tests never caught (three stacked notification bugs, two
fl_chart rendering bugs, a two-layer text-focus bug — see `05-UAT.md`
for full history). This export-field-leak bug fits the same pattern:
something structurally wrong that's invisible to automated tests because
no existing test asserts on export *content*, only on file
existence/row counts. Treat it with the same rigor as the rest of this
session — real root cause, failing test first, then fix, then real
verification (ideally opening an exported CSV/JSON on the real device or
at least asserting file contents in a test) before marking it resolved
in `05-UAT.md`.

The one nuance to get right immediately on resuming: **do not blindly
strip all 6 `SyncSafeTable` columns from both backup AND export paths
without first checking whether `applyRestore()`/`_restoreCategory()`
needs any of them (`id` especially) to reconstruct rows correctly.**
`createBackup()` and human-facing `exportData()` share `_readCategoryRows()`
today; the fix may need to diverge them (full-fidelity for backup/restore,
filtered for human export) rather than applying one filter to both.
</context>

<next_action>
Start with: read `lib/domain/services/backup_export_service.dart` in
full (especially `_restoreCategory()`, `_encodeRows()`, and each
`*Row.fromJson()`/`.toCompanion()` call around lines 470-560+) to confirm
exactly which of the 6 `SyncSafeTable` columns restore actually depends
on, before deciding whether one shared filter or two divergent filters
(backup vs. human export) is the right shape. Then write the failing
regression test, then implement the fix across all 7 categories, then
resume Test 7's on-device walkthrough.
</next_action>
