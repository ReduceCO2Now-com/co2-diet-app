---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
task: uat
total_tasks: 9
status: in_progress
last_updated: "2026-07-29T08:40:12.678Z"
---

<current_state>
Phase 5 is fully executed and passed automated goal-verification (35/35 must-haves). Now running a manual UAT pass (`.../05-UAT.md`) at the user's explicit request, since Phase 5 had zero real-device checkpoints (unlike Phases 2/3/4, which each had one and each caught real bugs automated tests missed). This UAT session found real bugs automated tests never caught, confirming the user's instinct was right.

**Currently stuck on a real-device crash with an unknown root cause.** Two fix attempts have failed to resolve it. A third attempt was ruled out because it couldn't be reproduced in a widget test (user's explicit instruction: never fix a theory without first proving a test fails against current code). Session paused here due to context exhaustion (81% used), not because the work is done.
</current_state>

<completed_work>

UAT tests 1-2 done (9 total): Dashboard general composition = pass. CO2 Settings screen = issue (see below, believed fixed but not re-confirmed). Test 3 (Data Analysis) supplementary check still awaiting the user's answer (charts already logged as issues; rest of screen -- weekly total, contributors, goal comparison, transparency, improvement opportunities, insights timeline -- never confirmed).

**Bugs found and their status (all in `05-UAT.md`'s Gaps section, YAML, ready for `/gsd:plan-phase 5 --gaps` once UAT itself is finished):**

1. **Android build failure** (core library desugaring missing for flutter_local_notifications) -- FIXED, commit `08e7bc1`. Confirmed via successful `flutter build apk --debug`.
2. **Two fl_chart rendering bugs on Data Analysis** (Y-axis label overlap on the stacked bar chart; X-axis showing raw decimal indices instead of dates on the trend chart) -- NOT YET FIXED. Root cause identified as likely `titlesData`/`getTitlesWidget` config in `today_breakdown_bar_chart.dart` and `trend_section.dart`, but no fix attempted yet.
3. **Text-field focus loss on every keystroke** (Profile Setup age/height/weight, CO2 Settings location/region, Weight Tracking goal) -- TWO real, stacked root causes found and fixed, but user has only re-confirmed neither is fully solved on-device yet (reported "still partial" after fix 1, hasn't retested since fix 2):
   - Fix 1 (commit `1f58cf1`): removed value-tied `ValueKey`s on TextFormFields in `profile_form.dart`, `co2_settings_screen.dart`, `weight_screen.dart` that caused per-keystroke widget remount. Regression tests added and verified (via git-stash) to actually catch the bug.
   - Fix 2 (commit `147f1f1`): removed `state = AsyncValue.loading()` mid-save from `ProfileNotifier.saveProfile`/`Co2SettingsNotifier.saveSettings`, which was swapping the ENTIRE screen body to a spinner on every keystroke (much more severe than fix 1's per-field issue). Regression tests added and verified via git-stash.
   - **Still needs real-device re-confirmation** -- this is the very next thing to ask the user about when resuming.
4. **Profile screen crash on tapping empty target values** (`_dependents.isEmpty` Flutter framework assertion) -- **UNRESOLVED, root cause unknown**. Two theories tried and both wrong:
   - Theory A: same infinite-loop bug as #3 (a real, separate bug in `profile_screen.dart`'s locale-detection effect -- FIXED at commit `c6697a3`, but user re-tested and got the IDENTICAL crash, so this was not the cause of THIS bug, even though it was a real bug worth fixing).
   - Theory B: `MissingTargetDash`'s `Tooltip` wrapper (only on the empty-value path) conflicting with `showDialog`'s route insertion. Built `test/features/profile/profile_screen_crash_test.dart` to test this -- it reproduces the exact real interaction (tall viewport, real `ProfileScreen`, confirmed tap lands) and does NOT throw. Falsified.
   - **Do not attempt a third fix without first getting a test to fail against current code** (explicit, repeated user instruction). Next steps to try: get the FULL crash stack trace/frame list (only the one-line assertion message has been captured so far -- ask user to scroll up further in logcat/Xcode console for the frames above/below the assertion line), or try an `integration_test` (real device/emulator) instead of a `flutter_test` widget test, since Overlay/Route timing for this specific assertion may not fully replicate in the widget-test harness.
</completed_work>

<remaining_work>

- Get real-device re-confirmation from the user on bug #3 (focus loss) after fix 2 -- ask this first when resuming.
- Continue investigating bug #4 (crash) -- get the full stack trace, or try integration_test. Do NOT fix without reproduction.
- Fix bug #2 (fl_chart axis labels) -- not yet attempted at all.
- Resume UAT at Test 3's supplementary check (Data Analysis remaining scope), then Tests 4-9 (Weight chart touch/drag, meal reminder firing+tap, weigh-in reminder firing+re-arm, Backup share sheet, Restore document picker, Danger Zone typed confirmation).
- Once UAT testing is complete: diagnose remaining issues -> `/gsd:plan-phase 5 --gaps` -> `/gsd:execute-phase 5 --gaps-only` per the standard gap-closure flow.
- After all gaps closed and phase re-verified: Phase 5 was already marked complete in ROADMAP.md/STATE.md before this UAT session started (that happened before the user asked for device verification) -- no need to re-run `phase complete`, just confirm the gap fixes are committed.
</remaining_work>

<decisions_made>

- User explicitly required a dedicated real-device UAT pass for Phase 5, matching Phases 2/3/4's precedent, rather than folding it into Phase 6 -- Phase 5's automated-only closure was judged insufficient.
- Every requirement-completion marking this session was scrutinized against actual reachability (not just partial-layer completion) after two premature completions (CO2-02/CO2-03) were caught and reverted early in execution (commit `746dbf8`).
- `excel` (not `excel_plus`) locked in for Plan 05-09's Excel export -- recorded in `05-CONTEXT.md`'s Planning Addendum.
- Every bug fix this session followed a strict discipline: identify root cause via reading real code (not guessing), write a regression test, verify via git-stash that the test actually fails against the pre-fix code, then confirm it passes after the fix. This same discipline is why bug #4's third theory was correctly NOT turned into a fix -- the test didn't fail, so the fix wasn't applied.
</decisions_made>

<blockers>
- Bug #4 (Profile screen crash) has no confirmed root cause. Two theories eliminated. Needs either a fuller stack trace from the user or an integration_test-based reproduction attempt.
</blockers>

<context>
This session has been unusually deep for a single UAT pass -- three real, previously-invisible bugs found and (mostly) fixed, all because 366+ existing automated tests never caught any of them. The user has been an excellent bug reporter: precise reproduction steps, cross-device confirmation, and correctly pushing back when a fix claimed "resolved" without real verification. Match that rigor when resuming -- do not declare bug #3 "resolved" without the user's explicit real-device confirmation, and do not attempt a fix for bug #4 without a failing test first.
</context>

<next_action>
Start with: run `/gsd:resume-work` or continue directly. First ask the user: "Have you had a chance to retest the keyboard/focus issue after the second fix (147f1f1)?" Then continue bug #4 investigation (ask for the fuller stack trace, or propose writing an integration_test). Then resume the UAT test sequence at Test 3's supplementary check.
</next_action>
