---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 14
type: execute
wave: 5
depends_on:
  - 05-08
files_modified:
  - lib/features/notifications/providers/notification_prefs_notifier.dart
  - lib/features/notifications/widgets/meal_reminder_settings_section.dart
autonomous: true
requirements:
  - NOTIF-01

must_haves:
  truths:
    - "Each of the four meal slots (Breakfast/Lunch/Dinner/Snack) has its own independent enable toggle and time picker"
    - "Enabling a slot's reminder for the first time requests notification permission just-in-time -- never during app startup/onboarding"
    - "Permission denial reverts that slot's toggle to off and shows an inline Open Settings recovery link, not a full-screen block"
  artifacts:
    - path: "lib/features/notifications/widgets/meal_reminder_settings_section.dart"
      provides: "MealReminderSettingsSection -- standalone widget, embedded into General Settings by Plan 05-18"
  key_links:
    - from: "lib/features/notifications/widgets/meal_reminder_settings_section.dart"
      to: "lib/domain/services/notification_service.dart"
      via: "scheduleMealReminder/cancelMealReminder per MealSlot"
      pattern: "scheduleMealReminder"
---

<objective>
Build the meal-reminder settings section (NOTIF-01): one independently configurable enable-toggle + time-picker pair per `MealSlot`. Built as a standalone, embeddable widget (`MealReminderSettingsSection`) rather than edited directly into `SettingsScreen` — Plan 05-18 embeds it into the General Settings screen once every Wave 5 output exists, avoiding a `settings_screen.dart` file conflict with the other Wave 5 plans.

Purpose: NOTIF-01 requires per-slot granularity (not one generic daily reminder). This widget is a thin UI layer over `NotificationPrefsRepository` (05-08) and `NotificationService` (05-08) — it does not duplicate any scheduling logic itself.

Output: `NotificationPrefsNotifier` (Riverpod glue between the repository and this widget) and `MealReminderSettingsSection`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@lib/domain/entities/notification_prefs.dart
@lib/core/di/notification_providers.dart
@lib/domain/services/notification_service.dart
@lib/domain/entities/meal_slot.dart
@lib/features/weight/widgets/weigh_in_reminder_section.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationPrefsNotifier</name>
  <files>
    lib/features/notifications/providers/notification_prefs_notifier.dart,
    test/features/notifications/notification_prefs_ui_test.dart
  </files>
  <behavior>
    - `build()` loads the current `NotificationPrefs`.
    - `setSlotEnabled(slot, enabled, time)` persists via the repository, then calls `NotificationService.scheduleMealReminder`/`cancelMealReminder` for that slot -- notifier is the single place that keeps stored prefs and live-scheduled OS notifications in sync.
  </behavior>
  <action>
    Create `lib/features/notifications/providers/notification_prefs_notifier.dart`: `@riverpod class NotificationPrefsNotifier extends _$NotificationPrefsNotifier` with `Future<NotificationPrefs> build() => ref.watch(notificationPrefsRepositoryProvider).getPrefs();`. Add `Future<bool> setSlotEnabled(MealSlot slot, bool enabled, {String? time}) async { ... }`: when `enabled` is `true`, calls `NotificationService.requestPermissionIfNeeded()` first; if denied, returns `false` without persisting or scheduling (caller reverts its own toggle UI). If granted (or `enabled == false`), persists the updated `NotificationPrefs` via the repository, calls `scheduleMealReminder`/`cancelMealReminder` accordingly, and re-runs `build()`. Generated provider name: `notificationPrefsProvider`. Inject `NotificationService` via a new `@Riverpod(keepAlive: true) NotificationService notificationService(Ref ref)` provider added to `lib/core/di/notification_providers.dart` (this file already exists from Plan 05-08 — this task adds one provider to it, not a conflicting rewrite, since 05-08 already landed in an earlier wave).

    De-skip the notifier-facing half of `test/features/notifications/notification_prefs_ui_test.dart` covering the `<behavior>` bullets with a mocktail-mocked `NotificationService`.
  </action>
  <verify>
    <automated>flutter test test/features/notifications/ 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`notificationPrefsProvider` builds and its `setSlotEnabled` correctly gates on permission before persisting/scheduling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MealReminderSettingsSection widget</name>
  <files>
    lib/features/notifications/widgets/meal_reminder_settings_section.dart,
    test/features/notifications/notification_prefs_ui_test.dart
  </files>
  <behavior>
    - Four independent rows (Breakfast/Lunch/Dinner/Snack), each with its own `Switch` + time picker, driven by `notificationPrefsProvider`.
    - A denied-permission attempt reverts that row's switch to off and shows an inline "Notifications are disabled — Open Settings" `TextButton` (using `app_settings`' `AppSettings.openAppSettings()`, matching the existing camera-permission-denied pattern's lightweight variant, not `CameraPermissionDeniedWidget`'s full-screen block).
  </behavior>
  <action>
    Create `lib/features/notifications/widgets/meal_reminder_settings_section.dart`: `class MealReminderSettingsSection extends ConsumerWidget` rendering a `Column` of four rows, one per `MealSlot.values`, each a `ListTile`-like row with a time-picker `TextButton` (opens `showTimePicker`, formats to `'HH:mm'`) and a trailing `Switch` whose `onChanged` calls `ref.read(notificationPrefsProvider.notifier).setSlotEnabled(slot, value, time: ...)`; on a `false` return, show the inline Open Settings recovery message beneath that specific row (row-scoped, not app-wide) and ensure the `Switch` visually reflects the reverted-to-off state (re-read `notificationPrefsProvider` after the call, since `build()` isn't re-run on a denied/no-op path).

    Complete the de-skip of `test/features/notifications/notification_prefs_ui_test.dart`'s widget-level assertions (four independent rows; denial-reverts-toggle-and-shows-link).
  </action>
  <verify>
    <automated>flutter test test/features/notifications/ 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`MealReminderSettingsSection` renders four independent slot rows; a denied permission reverts only that row's toggle with an inline recovery link. De-skipped test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Delegates entirely to `NotificationService` (05-08), which owns the OS-boundary threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/features/notifications/ && flutter analyze lib/features/notifications/
```
</verification>

<success_criteria>
- Four independently configurable meal-slot reminder rows
- Permission requested just-in-time only, never eagerly
- Denial reverts only the affected row with an inline recovery link
- De-skipped test passes
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-14-SUMMARY.md` when done.
</output>
