---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 19
type: execute
wave: 8
depends_on:
  - 05-18
files_modified:
  - test/core/offline_phase5_test.dart
  - lib/features/data_analysis/widgets/estimate_transparency_panel.dart
  - lib/features/co2_settings/screens/co2_settings_screen.dart
autonomous: true
requirements:
  - AUTH-07
  - PRIV-08
  - INS-04
  - NFR-05

must_haves:
  truths:
    - "No Phase-5-introduced code path (CO2 settings, Weight, Notifications, Backup/Export, Dashboard, Data Analysis) ever instantiates OffApiClient or calls Connectivity"
    - "Every new CO2/nutrition display surface this phase added uses formatCo2Display/ConfidenceChip conventions -- no single false-precision number anywhere"
    - "The full test suite (Phase 1 through 5) is green"
  artifacts:
    - path: "test/core/offline_phase5_test.dart"
      provides: "Runtime proof that Phase 5 code never touches the network, extending offline_logging_test.dart's Phase 4 pattern"
  key_links:
    - from: "test/core/offline_phase5_test.dart"
      to: "lib/domain/services/"
      via: "constructs every new Phase 5 service/repository against in-memory fixtures with no network mocks provided, asserting no MissingPluginException/SocketException occurs"
      pattern: "OffApiClient"
---

<objective>
Close out Phase 5 with the offline-proof test extending Plan 04-12's `offline_logging_test.dart` pattern to every new Phase 5 code path (AUTH-07/PRIV-08), an explicit NFR-05 audit of every new CO2/nutrition display surface this phase added, and a full-suite regression run.

Purpose: This is the phase's final gate before Local Mode is declared shippable (ROADMAP.md's Phase 5 success criterion). AUTH-07/PRIV-08 are invariants enforced from Phase 1 by never integrating backend code, but this phase adds enough new surface area (Backup export, CO2 Settings, Weight, Notifications) that an explicit runtime proof — not just "we didn't add a network client" by inspection — is warranted, mirroring Phase 4's `offline_logging_test.dart` precedent exactly.

Output: `test/core/offline_phase5_test.dart` de-skipped with real runtime assertions; any NFR-05 gaps found during the audit fixed directly; full suite green.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-VALIDATION.md
@test/features/meal_logging/offline_logging_test.dart
@lib/features/barcode_scan/utils/co2_formatter.dart
@lib/features/barcode_scan/widgets/confidence_chip.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline-proof test for every Phase 5 code path</name>
  <files>test/core/offline_phase5_test.dart</files>
  <behavior>
    - Building/exercising `Co2SettingsRepository`, `WeightRepository`, `NotificationService`, `BackupExportService`, `DailyTotalsCalculator`, `PersonalCo2MultiplierCalculator`, `ImprovementOpportunityFinder`, and `InsightsTimelineRuleEngine` against in-memory/mocked dependencies -- with no `OffApiClient`/`Connectivity` mock provided at all -- completes without any `MissingPluginException`/`SocketException`/network-channel invocation, proving none of these classes reach for the network path even indirectly.
  </behavior>
  <action>
    De-skip `test/core/offline_phase5_test.dart`, following `offline_logging_test.dart`'s exact structure (an in-memory `AppDatabase`, real DAOs/repositories wired without any `offApiClientProvider`/`connectivity_plus` override present at all): construct each of the eight Phase 5 services/repositories listed in `<behavior>` and exercise one representative method on each (e.g. `Co2SettingsRepository.getSettings`, `WeightRepository.logWeight`, `NotificationService.scheduleMealReminder` with a mocked plugin, `BackupExportService.exportData` against a temp dir, `DailyTotalsCalculator.compute` with fixture entries, `PersonalCo2MultiplierCalculator.compute`, `ImprovementOpportunityFinder.findOpportunities`, `InsightsTimelineRuleEngine.evaluate`). Assert each completes successfully. Add a final assertion (static import check via a simple `dart:io` file-content scan, mirroring how `blocklist_test.dart` performs a static config check) confirming none of `lib/domain/services/daily_totals_calculator.dart`, `personal_co2_multiplier_calculator.dart`, `notification_service.dart`, `backup_export_service.dart`, `improvement_opportunity_finder.dart`, `insights_timeline_rule_engine.dart` contain the string `OffApiClient` or `Connectivity(`.
  </action>
  <verify>
    <automated>flutter test test/core/offline_phase5_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>De-skipped test passes, proving every new Phase 5 domain service/repository is network-free both at runtime and by static source inspection.</done>
</task>

<task type="auto">
  <name>Task 2: NFR-05 display audit and full-suite regression</name>
  <files>
    lib/features/data_analysis/widgets/estimate_transparency_panel.dart,
    lib/features/co2_settings/screens/co2_settings_screen.dart
  </files>
  <action>
    Review every new CO2/nutrition-displaying widget this phase added (`MetricCard`, `TrendSparkline`, `EstimateTransparencyPanel`, `GoalComparisonBar`, `DetailedFoodAnalysisPanel`, `ImprovementOpportunities`, `WeightChart`) against NFR-05 ("CO2 data presented with honest uncertainty -- confidence bands and ranges, never false-precision numbers"). Specifically confirm:
    - Any CO2 value rendered anywhere reuses `formatCo2Display` (Phase 3's existing rounding-to-1-2-significant-figures helper) rather than a raw unrounded double -- if any widget from Plans 05-11/05-13/05-15/05-17 was written with a raw `.toStringAsFixed(1)` or similar ad hoc formatting instead, fix it in place to call `formatCo2Display`.
    - `EstimateTransparencyPanel`'s confidence-mix summary and `Co2SettingsScreen`'s Data Quality Indicator both use existing established terminology (`'High'`/`'Medium'` confidence, `'Basic'`/`'Good'`/`'Detailed'` estimate) consistently, with no numeric confidence percentage invented that AGRIBALYSE data doesn't actually support.
    Fix any gap found directly in the listed files (or note in the summary if no gap was found and no fix was needed).

    Run the full regression suite.
  </action>
  <verify>
    <automated>flutter test test/ 2>&amp;1 | tail -30</automated>
  </verify>
  <done>Every new CO2-displaying surface uses `formatCo2Display`/established confidence-band terminology; full `flutter test test/` suite (Phase 1 through 5) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan; no new production surface introduced beyond targeted NFR-05 fixes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/ && flutter analyze lib/
```
Expected: full suite green (Phase 1-5), zero analyzer issues project-wide.
</verification>

<success_criteria>
- `offline_phase5_test.dart` de-skipped and green, proving AUTH-07/PRIV-08 for every new Phase 5 code path
- NFR-05 audit complete; any gaps fixed
- Full `flutter test` suite green
- Phase 5 (Local Mode) is shippable per ROADMAP.md's success criteria
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-19-SUMMARY.md` when done.
</output>
