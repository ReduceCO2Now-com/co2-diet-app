---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 11
type: execute
wave: 5
depends_on:
  - 05-10
  - 05-08
files_modified:
  - lib/features/dashboard/widgets/metric_card.dart
  - lib/features/dashboard/widgets/trend_sparkline.dart
  - lib/features/dashboard/widgets/quick_insight_line.dart
  - lib/features/dashboard/widgets/mode_indicator.dart
  - lib/features/dashboard/widgets/co2_profile_prompt_card.dart
  - lib/features/dashboard/widgets/macro_split_bar.dart
autonomous: true
requirements:
  - DASH-01
  - DASH-04
  - DASH-05
  - DASH-06
  - NUTR-02
  - NUTR-03
  - NUTR-04
  - NFR-05

must_haves:
  truths:
    - "All three metric cards (CO2, calories, protein) always render, with the one matching the user's Profile goal ordered/sized first"
    - "The 7-day sparkline plots exactly one metric at a time (CO2/Calories/Protein), switchable via a segmented control"
    - "The quick insight line names whichever of CO2/calories/protein is most notable today plus its contributing meal slot, in factual (non-judgmental) tone"
    - "The CO2 profile prompt card only renders when Co2Settings.dataQuality is basic, and is dismissible"
  artifacts:
    - path: "lib/features/dashboard/widgets/metric_card.dart"
      provides: "MetricCard -- value vs target display, goal-aware ordering hook"
    - path: "lib/features/dashboard/widgets/trend_sparkline.dart"
      provides: "TrendSparkline -- fl_chart compact 7-day sparkline with metric toggle"
    - path: "lib/features/dashboard/widgets/quick_insight_line.dart"
      provides: "QuickInsightLine -- single most-notable-metric-today sentence"
  key_links:
    - from: "lib/features/dashboard/widgets/trend_sparkline.dart"
      to: "package:fl_chart/fl_chart.dart"
      via: "LineChart with titlesData: FlTitlesData(show: false)"
      pattern: "LineChart"
---

<objective>
Build the standalone widgets the Dashboard needs: three metric cards (CO2/calories/protein vs target, goal-aware ordering), a switchable 7-day `fl_chart` sparkline, a factual quick-insight sentence, a mode indicator, a macro-split bar (NUTR-04), and the dismissible "Complete your CO2 profile" prompt card. Each widget is self-contained and independently testable via direct `pumpWidget` — this plan does not touch `placeholder_dashboard_screen.dart` or `app_router.dart`; Plan 05-18 assembles all of Wave 5's screen/widget output into the final wired Dashboard.

Purpose: DASH-01/04/05/06 and NUTR-02/03/04 are all Dashboard-surface requirements built directly on `DailyTotalsCalculator` (05-10)'s output. Building widgets standalone here (rather than inline in the screen) lets every Wave 5 plan run in parallel with zero file conflicts on the shared screen/router files.

Output: Six new widget files under `lib/features/dashboard/widgets/`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@lib/domain/services/daily_totals_calculator.dart
@lib/domain/entities/calc_targets.dart
@lib/domain/entities/co2_settings.dart
@lib/core/theme/color_tokens.dart
@lib/features/barcode_scan/utils/co2_formatter.dart
@lib/features/barcode_scan/widgets/confidence_chip.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MetricCard, MacroSplitBar, ModeIndicator, QuickInsightLine</name>
  <files>
    lib/features/dashboard/widgets/metric_card.dart,
    lib/features/dashboard/widgets/macro_split_bar.dart,
    lib/features/dashboard/widgets/mode_indicator.dart,
    lib/features/dashboard/widgets/quick_insight_line.dart,
    test/features/dashboard/metric_card_test.dart
  </files>
  <behavior>
    - `MetricCard` renders a value, a target (or `'—'` when target is null, per the existing no-fake-precision convention), and a `remaining`/`consumed` framing; an `isEmphasized` flag controls larger sizing/first-position styling.
    - `ModeIndicator` renders `'Stored on this device'` for Local Mode (the only mode that exists until Phase 7 wires Account Mode) — accepts an `isLocalMode` bool param (default `true`) so this widget compiles correctly against Phase 7's future Account Mode state without needing a rewrite then.
    - `QuickInsightLine` accepts a pre-computed `String? insightText` (the "most notable metric" selection logic lives in Plan 05-18's Dashboard-assembly wiring, which has access to the user's targets and goal; this widget is a pure display component) and renders nothing (`SizedBox.shrink()`) when null.
  </behavior>
  <action>
    Create `lib/features/dashboard/widgets/metric_card.dart`: `class MetricCard extends StatelessWidget` with constructor params `label` (String), `value` (double?), `target` (double?), `unit` (String, e.g. `'kcal'`/`'g'`/`'kg CO2e'`), `isEmphasized` (bool, default false). Displays `label`, formatted `value` (or `'—'` when null, reusing the existing "no fake precision" convention already established for `CalcTargets`), a `'of {target}'` suffix (or omitted when target is null), and a linear progress indicator when both are non-null. `isEmphasized` increases font size / adds a border accent color (`AppColors.primaryContainer`) — this is the "goal-matching metric ordered/sized first" visual hook; the actual ordering decision (which metric matches the user's goal) is Plan 05-18's concern, this widget only renders whatever emphasis flag it's given.

    Create `lib/features/dashboard/widgets/macro_split_bar.dart`: `class MacroSplitBar extends StatelessWidget` taking a nullable `MacroSplit` (from `DailyTotalsCalculator`'s `DailyTotals.macroSplit`) and rendering three proportional colored segments (protein/carbs/fat) in a single horizontal bar with a legend; renders an empty-state message when `null` (NUTR-04, viewable from Dashboard).

    Create `lib/features/dashboard/widgets/mode_indicator.dart` per `<behavior>` above.

    Create `lib/features/dashboard/widgets/quick_insight_line.dart` per `<behavior>` above — plain `Text` styled as a caption, no icon (matches the existing "Lunch contributed most CO2 today" factual-tone example from CONTEXT.md verbatim in a doc comment).

    De-skip `test/features/dashboard/metric_card_test.dart` with real `pumpWidget` assertions covering `MetricCard`'s null-target dash rendering and `ModeIndicator`'s text.
  </action>
  <verify>
    <automated>flutter test test/features/dashboard/metric_card_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All four widgets compile and render correctly in isolation; de-skipped test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrendSparkline (fl_chart) and CO2 profile prompt card</name>
  <files>
    lib/features/dashboard/widgets/trend_sparkline.dart,
    lib/features/dashboard/widgets/co2_profile_prompt_card.dart,
    test/features/dashboard/trend_sparkline_test.dart
  </files>
  <behavior>
    - A segmented control (`SegmentedButton<DashboardMetric>` or equivalent) switches which of CO2/Calories/Protein the sparkline plots; the chart itself has no visible axes/titles/grid (compact sparkline per RESEARCH.md Pattern 2).
    - Tapping anywhere on the card (not the chart's internal touch handling) invokes an `onTap` callback — the sparkline itself has `lineTouchData: LineTouchData(enabled: false)` since this is a compact card-tap-to-navigate pattern, not an interactive tooltip chart (that richer interaction belongs to the Weight/Data-Analysis charts, Plans 05-13/05-15).
    - `Co2ProfilePromptCard` renders only when `show` is `true` and calls `onDismiss`/`onTap` appropriately; renders `SizedBox.shrink()` otherwise.
  </behavior>
  <action>
    Create `lib/features/dashboard/widgets/trend_sparkline.dart`: define `enum DashboardMetric { co2, calories, protein }`. `class TrendSparkline extends StatefulWidget` (needs local state for which metric is selected before notifying the parent, OR accept `selectedMetric`/`onMetricChanged` as controlled-component params — prefer the controlled pattern, matching this codebase's existing Riverpod-driven state conventions rather than introducing local widget state for a value the parent screen needs to read anyway for the "tap opens Data Analysis pre-set to this metric" behavior). Constructor: `selectedMetric` (`DashboardMetric`), `onMetricChanged` (`ValueChanged<DashboardMetric>`), `spots` (`Map<DashboardMetric, List<FlSpot>>`, 7 points each), `onTap` (`VoidCallback`). Renders the segmented control above a `GestureDetector(onTap: onTap, child: LineChart(...))` using RESEARCH.md's Pattern 2 compact-sparkline configuration verbatim (`titlesData: FlTitlesData(show: false)`, `gridData: FlGridData(show: false)`, `borderData: FlBorderData(show: false)`, `lineTouchData: LineTouchData(enabled: false)`, `belowBarData` filled at `alpha: 0.12`).

    Create `lib/features/dashboard/widgets/co2_profile_prompt_card.dart`: `class Co2ProfilePromptCard extends StatelessWidget` with `show` (bool), `onDismiss` (VoidCallback), `onTap` (VoidCallback). Renders exactly the copy `'Complete your CO2 profile for better estimates'` (CONTEXT.md's specified framing, verbatim) with a dismiss (X) icon button and a tap target that navigates to CO2 Calculation Settings (wired by Plan 05-18).

    De-skip `test/features/dashboard/trend_sparkline_test.dart` with real assertions covering the segmented-control metric switch and the tap-to-invoke-callback behavior (not the internal fl_chart rendering itself, which is exercised visually).
  </action>
  <verify>
    <automated>flutter test test/features/dashboard/trend_sparkline_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`TrendSparkline` renders a compact, axis-free chart and its segmented control switches the plotted metric; `Co2ProfilePromptCard` renders/hides per `show` and calls its callbacks. De-skipped test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure presentation widgets; no I/O, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (fl_chart already installed by Plan 05-08) |
</threat_model>

<verification>
```bash
flutter test test/features/dashboard/ && flutter analyze lib/features/dashboard/widgets/
```
</verification>

<success_criteria>
- Six widgets exist, compile, and are independently testable
- `TrendSparkline` uses the compact-sparkline `fl_chart` configuration (no axes/grid/touch tooltip)
- `Co2ProfilePromptCard` renders CONTEXT.md's exact copy
- Both de-skipped tests pass
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-11-SUMMARY.md` when done.
</output>
