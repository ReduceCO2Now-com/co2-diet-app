---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/domain/services/daily_totals_calculator_test.dart
  - test/domain/services/personal_co2_multiplier_test.dart
  - test/domain/services/notification_service_test.dart
  - test/domain/services/backup_export_service_test.dart
  - test/domain/services/improvement_opportunity_finder_test.dart
  - test/domain/entities/meal_entry_nutrient_test.dart
  - test/data/local/co2_settings_dao_test.dart
  - test/data/local/weight_dao_test.dart
  - test/data/local/notification_prefs_dao_test.dart
  - test/data/local/backup_metadata_dao_test.dart
  - test/data/local/food_catalog_cache_co2_test.dart
  - test/data/repositories/co2_settings_repository_test.dart
  - test/data/repositories/weight_repository_test.dart
  - test/features/co2_settings/co2_settings_notifier_test.dart
  - test/features/weight/weight_notifier_test.dart
  - test/features/dashboard/metric_card_test.dart
  - test/features/dashboard/trend_sparkline_test.dart
  - test/features/co2_settings/co2_settings_screen_test.dart
  - test/features/weight/weight_screen_test.dart
  - test/features/notifications/notification_prefs_ui_test.dart
  - test/features/data_analysis/data_analysis_screen_test.dart
  - test/features/data_analysis/improvement_opportunities_test.dart
  - test/features/data_analysis/insights_timeline_test.dart
  - test/features/backup/backup_restore_screen_test.dart
  - test/core/offline_phase5_test.dart
autonomous: true
requirements:
  - NUTR-01
  - NUTR-02
  - NUTR-03
  - NUTR-04
  - CO2-02
  - CO2-03
  - CO2-05
  - CO2-06
  - DASH-01
  - DASH-04
  - INS-01
  - INS-03
  - WT-01
  - WT-02
  - WT-03
  - WT-04
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04
  - PRIV-08
  - AUTH-07

must_haves:
  truths:
    - "flutter test exits 0 on the full Wave 0 stub suite (all tests skip with clear labels, nothing fails)"
    - "Every subsequent Phase 5 plan has a named test file to target in its <verify> block"
    - "No production code is modified in this plan"
  artifacts:
    - path: "test/domain/services/daily_totals_calculator_test.dart"
      provides: "Stubs for NUTR-01/CO2-02 aggregation, including null-snapshot handling"
    - path: "test/core/offline_phase5_test.dart"
      provides: "Stub for the AUTH-07/PRIV-08 no-network-code-path proof"
    - path: "test/data/local/food_catalog_cache_co2_test.dart"
      provides: "Regression stub for the Phase-4 CO2 cache-path gap fix"
  key_links:
    - from: "test/domain/services/daily_totals_calculator_test.dart"
      to: "lib/domain/services/daily_totals_calculator.dart"
      via: "group-level skip referencing production class name"
      pattern: "skip:.*DailyTotalsCalculator"
---

<objective>
Create Wave 0 test stubs for every automated test file the Phase 5 plan set will need. Every stub file must compile, run with `flutter test`, and exit 0 — tests skip atomically at the group level when the production class/screen is absent (established Phase 2-4 pattern: group-level `skip:` for unit/widget tests).

Purpose: Satisfy the Nyquist rule — every execute plan in Phase 5 has a named test file in its `<verify>` block from the start. Stubs prevent a mid-phase execute from producing "file not found" errors and give this plan set a concrete, pre-agreed coverage map across all six sub-domains (CO2 Settings, Dashboard, Weight, Insights, Notifications, Export/Backup) plus the two foundational fixes (nutrient-snapshot schema gap, CO2 cache-path gap).

**Revision note (plan-checker scope_sanity finding, resolved):** the original draft of this plan crammed 25 files into 3 tasks (11/4/10 files per task), exceeding this project's scope-sanity thresholds even though the content is mechanical group-level `skip:` stubs. This revision spreads the same 25 files across 5 smaller tasks (6/5/4/5/5) grouped by architectural layer, so no single task touches more than 6 files.

Output: 25 new test stub files. Zero production code changes.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-VALIDATION.md

<!-- Phase 4 stub pattern reference: group-level skip -->
@test/features/meal_logging/offline_logging_test.dart
@.planning/phases/04-meal-logging-core-10s-target/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain service stubs (6 files)</name>
  <files>
    test/domain/services/daily_totals_calculator_test.dart,
    test/domain/services/personal_co2_multiplier_test.dart,
    test/domain/services/notification_service_test.dart,
    test/domain/services/backup_export_service_test.dart,
    test/domain/services/improvement_opportunity_finder_test.dart,
    test/domain/entities/meal_entry_nutrient_test.dart
  </files>
  <action>
    Create `test/domain/services/` directory. Write all six files below using the established group-level skip pattern: `group('ClassName', skip: 'ClassName not yet implemented', () { test(...); });`. Import only `package:flutter_test/flutter_test.dart` — no production imports (the classes do not exist yet).

    `test/domain/services/daily_totals_calculator_test.dart` — `group('DailyTotalsCalculator', skip: ...)`:
    - `test('sums calories/protein/carbs/fat across today\'s weight-based-unit entries', () {})` — NUTR-01
    - `test('sugar/fiber/sodium totals are null when zero logged entries have that nutrient snapshot (no false precision)', () {})` — NUTR-01, Pitfall 1
    - `test('sugar/fiber/sodium totals sum only entries that have a non-null snapshot; entries with null snapshot are skipped, not treated as zero', () {})` — NUTR-01
    - `test('applies the personal CO2 multiplier only to the aggregate total, never per-entry', () {})` — CO2-02
    - `test('piece/cup/portion-unit entries are excluded from every numeric total (documented limitation, matches MealEntryRow/RecentRow precedent)', () {})` — CO2-02/NUTR-01
    - `test('macro split (protein/carbs/fat) percentages sum to ~100% when all three are non-null', () {})` — NUTR-04
    - `test('computing over a pooled trailing-7-day entry list yields an explicit weekly total distinct from a single day\'s total', () {})` — CO2-02 weekly total

    `test/domain/services/personal_co2_multiplier_test.dart` — `group('PersonalCo2MultiplierCalculator', skip: ...)`:
    - `test('returns a neutral 1.0 multiplier when Co2Settings has every field unset (regional-average fallback)', () {})` — CO2-03
    - `test('multiplier changes deterministically when purchasingSource/shoppingTransport/foodWasteLevel are set', () {})` — CO2-02/CO2-03
    - `test("never reads or mutates any food's own co2e100g/confidenceBand -- pure settings-to-multiplier function", () {})` — CO2-02 (separate-layer invariant)
    - `test('dataQuality classification is basic/good/detailed based on how many optional fields are set', () {})` — CO2-03
    - `test('foodStorage/householdSize/locationCountry/locationRegion produce no numeric effect -- confirmed v1 scope narrowing per 05-CONTEXT.md addendum, not a gap', () {})` — CO2-02

    `test/domain/services/notification_service_test.dart` — `group('NotificationService', skip: ...)`:
    - `test('scheduleMealReminder calls zonedSchedule with matchDateTimeComponents: time and androidScheduleMode: inexactAllowWhileIdle', () {})` — NOTIF-01, RESEARCH.md Pitfall 3
    - `test('cancelMealReminder cancels the stable per-slot notification id', () {})` — NOTIF-01
    - `test('scheduleWeighInReminder for Custom frequency uses matchDateTimeComponents: dayOfWeekAndTime with the chosen weekday', () {})` — NOTIF-02
    - `test('scheduleWeighInReminder is safe to call repeatedly -- a second call replaces rather than duplicates the pending schedule', () {})` — NOTIF-02, biweekly/monthly re-arm
    - `test('requestPermissionIfNeeded is only invoked when a toggle is first enabled, never on service construction', () {})` — NOTIF-01/NOTIF-02 just-in-time permission
    - `test('permission denied leaves the reminder disabled and surfaces a recoverable state (no full-screen block)', () {})` — NOTIF-01/02

    `test/domain/services/backup_export_service_test.dart` — `group('BackupExportService', skip: ...)`:
    - `test('exportData produces a zip containing manifest.json plus one file per selected category/format', () {})` — PRIV-01
    - `test('manifest.json includes a formatVersion field alongside the per-file category/format/row-count entries', () {})` — PRIV-01, encryption-forward-compatibility decision
    - `test('previewRestore rejects a manifest.json with an unrecognized formatVersion before any write', () {})` — PRIV-04
    - `test('restore preview lists what would change before any write occurs', () {})` — PRIV-04
    - `test('restore rejects a zip entry whose normalized path escapes the target extraction directory (zip-slip)', () {})` — PRIV-04, RESEARCH.md Pitfall 6
    - `test('createBackup writes to the app documents directory; automatic backup frequency off/daily/weekly is honored', () {})` — PRIV-02/PRIV-03

    `test/domain/services/improvement_opportunity_finder_test.dart` — `group('ImprovementOpportunityFinder', skip: ...)`:
    - `test('suggests a same-cluster protein alternative (e.g. red meat -> poultry/fish/legumes) with a quantified CO2 delta', () {})` — CO2-06
    - `test('never suggests a cross-cluster swap (e.g. beef -> lettuce)', () {})` — CO2-06
    - `test('returns no suggestions when today has no high-CO2 entries (opt-in, never forced)', () {})` — CO2-06

    `test/domain/entities/meal_entry_nutrient_test.dart` — `group('MealEntry nutrient snapshot', skip: 'sugar/fiber/salt snapshot fields not yet added', ...)`:
    - `test('scaled() returns sugar/fiber/salt scaled the same way as calories/protein/carbs/fat', () {})` — NUTR-01
    - `test('scaled() returns null sugar/fiber/salt when the source snapshot is null (off_ref-sourced entries have no such data)', () {})` — NUTR-01, Pitfall 1
    - `test('copyWith sentinel pattern covers the three new nullable fields', () {})`

    All six files: no imports beyond `flutter_test`.
  </action>
  <verify>
    <automated>flutter test test/domain/services/ test/domain/entities/meal_entry_nutrient_test.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All six test files compile and report 0 failures (tests are skipped, not failing). `flutter test` on these paths exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: DAO stubs (5 files)</name>
  <files>
    test/data/local/co2_settings_dao_test.dart,
    test/data/local/weight_dao_test.dart,
    test/data/local/notification_prefs_dao_test.dart,
    test/data/local/backup_metadata_dao_test.dart,
    test/data/local/food_catalog_cache_co2_test.dart
  </files>
  <action>
    Write all five files below using the same group-level skip pattern. No imports beyond `flutter_test`.

    `test/data/local/co2_settings_dao_test.dart` — `group('Co2SettingsDao', skip: 'Co2SettingsDao not yet implemented', ...)`:
    - `test('getSettings returns null before any save; upsertSettings enforces the single-row convention', () {})` — CO2-03

    `test/data/local/weight_dao_test.dart` — `group('WeightDao', skip: 'WeightDao not yet implemented', ...)`:
    - `test('logWeight inserts a new WeightEntryRow; getEntriesInRange filters by [from, to] inclusive', () {})` — WT-01/WT-02
    - `test('getSettings/saveSettings round-trip the target weight, target date, and reminder fields', () {})` — WT-03/WT-04

    `test/data/local/notification_prefs_dao_test.dart` — `group('NotificationPrefsDao', skip: 'NotificationPrefsDao not yet implemented', ...)`:
    - `test('getPrefs returns all-disabled defaults before any save; savePrefs upserts the single row', () {})` — NOTIF-01

    `test/data/local/backup_metadata_dao_test.dart` — `group('BackupMetadataDao', skip: 'BackupMetadataDao not yet implemented', ...)`:
    - `test('getMetadata/saveMetadata round-trip autoBackupFrequency and lastBackupAt', () {})` — PRIV-03

    `test/data/local/food_catalog_cache_co2_test.dart` — `group('FoodCatalogDao user_food_cache_fts CO2 enrichment', skip: 'cache-path CO2 join not yet fixed', ...)`:
    - `test('searchAndCache stores the primary (most-specific) category tag into categoriesTags instead of NULL', () {})` — RESEARCH.md Pitfall 4
    - `test('searchLocalFoods LEFT JOINs off_ref.co2_factors for user_food_cache_fts results using the stored categoriesTags', () {})` — RESEARCH.md Pitfall 4
    - `test('a previously API-cached product now shows a CO2 estimate on a subsequent offline local search', () {})` — CO2-02/NFR-05
  </action>
  <verify>
    <automated>flutter test test/data/local/co2_settings_dao_test.dart test/data/local/weight_dao_test.dart test/data/local/notification_prefs_dao_test.dart test/data/local/backup_metadata_dao_test.dart test/data/local/food_catalog_cache_co2_test.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All five DAO stub files compile and pass (skipped, not failing).</done>
</task>

<task type="auto">
  <name>Task 3: Repository and notifier stubs (4 files)</name>
  <files>
    test/data/repositories/co2_settings_repository_test.dart,
    test/data/repositories/weight_repository_test.dart,
    test/features/co2_settings/co2_settings_notifier_test.dart,
    test/features/weight/weight_notifier_test.dart
  </files>
  <action>
    Create `test/features/co2_settings/` and `test/features/weight/` directories. Write four stub files using the same group-level skip pattern.

    `test/data/repositories/co2_settings_repository_test.dart` — `group('Co2SettingsRepository', skip: 'Co2SettingsRepository not yet implemented', ...)`:
    - `test('getSettings returns a default all-unset Co2Settings when no row has ever been saved', () {})` — CO2-03
    - `test('saveSettings never rewrites any already-logged MealEntry (forward-only, no retroactive recalculation)', () {})` — CO2-02 forward-only invariant

    `test/data/repositories/weight_repository_test.dart` — `group('WeightRepository', skip: 'WeightRepository not yet implemented', ...)`:
    - `test('logWeight persists a new entry; getEntriesInRange(range) supports 7d/30d/90d/1yr/all', () {})` — WT-01/WT-02
    - `test('saveGoal persists targetWeightKg + targetDate independently of reminder settings', () {})` — WT-03

    `test/features/co2_settings/co2_settings_notifier_test.dart` — `group('Co2SettingsNotifier', skip: 'Co2SettingsNotifier not yet implemented', ...)`:
    - `test('build() loads current settings; saveSettings persists and refreshes state', () {})` — CO2-03

    `test/features/weight/weight_notifier_test.dart` — `group('WeightNotifier', skip: 'WeightNotifier not yet implemented', ...)`:
    - `test('build() loads all weight entries and current goal/reminder settings together', () {})` — WT-01/WT-03/WT-04
    - `test('logWeight appends a new entry and refreshes the chart-backing list', () {})` — WT-01

    All four files: no imports beyond `flutter_test`.
  </action>
  <verify>
    <automated>flutter test test/data/repositories/co2_settings_repository_test.dart test/data/repositories/weight_repository_test.dart test/features/co2_settings/ test/features/weight/ 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All four stub files exist and pass (skipped, not failing).</done>
</task>

<task type="auto">
  <name>Task 4: Dashboard, CO2 Settings, Weight, and Notifications UI stubs (5 files)</name>
  <files>
    test/features/dashboard/metric_card_test.dart,
    test/features/dashboard/trend_sparkline_test.dart,
    test/features/co2_settings/co2_settings_screen_test.dart,
    test/features/weight/weight_screen_test.dart,
    test/features/notifications/notification_prefs_ui_test.dart
  </files>
  <action>
    Create `test/features/notifications/` directory. Write five stub files using `testWidgets(..., skip: true)` bodies or group-level skip, matching Phase 4's widget-test stub convention.

    `test/features/dashboard/metric_card_test.dart` — `group('MetricCard', skip: 'MetricCard not yet implemented', ...)`:
    - `testWidgets('renders value vs target with the goal-matching metric ordered/sized first', (tester) async {})` — DASH-01, NUTR-02/03
    - `testWidgets('shows the mode indicator text for Local vs Account mode', (tester) async {})` — DASH-06
    - `testWidgets('quick-log buttons push /food-search?slot=<slot>; Quick Add pushes /food-search with no slot param', (tester) async {})` — DASH-02
    - `testWidgets('CO2 profile prompt card renders only when data quality is basic and is dismissible', (tester) async {})` — Dashboard prompt card

    `test/features/dashboard/trend_sparkline_test.dart` — `group('TrendSparkline', skip: 'TrendSparkline not yet implemented', ...)`:
    - `testWidgets('segmented control switches which metric (CO2/Calories/Protein) the 7-day sparkline plots', (tester) async {})` — DASH-04
    - `testWidgets('tapping the chart opens Data Analysis pre-set to the currently selected metric', (tester) async {})` — DASH-08

    `test/features/co2_settings/co2_settings_screen_test.dart` — `group('Co2SettingsScreen', skip: 'Co2SettingsScreen not yet implemented', ...)`:
    - `testWidgets('all seven fields are optional; saving with everything empty succeeds', (tester) async {})` — CO2-03
    - `testWidgets('data quality indicator updates as fields are filled in', (tester) async {})` — CO2-03

    `test/features/weight/weight_screen_test.dart` — `group('WeightScreen', skip: 'WeightScreen not yet implemented', ...)`:
    - `testWidgets('logging a weight entry appends a point to the chart', (tester) async {})` — WT-01/WT-02
    - `testWidgets('goal line renders as a dashed horizontal line with no pace/projection text anywhere nearby', (tester) async {})` — WT-03
    - `testWidgets('reminder frequency Custom reveals a weekday + time picker', (tester) async {})` — WT-04

    `test/features/notifications/notification_prefs_ui_test.dart` — `group('Meal reminder settings section', skip: 'meal reminder settings UI not yet implemented', ...)`:
    - `testWidgets('each of the four meal slots has an independent enable toggle and time picker', (tester) async {})` — NOTIF-01
    - `testWidgets('toggling a reminder on when permission is denied reverts the toggle and shows an Open Settings link', (tester) async {})` — NOTIF-01

    All five files: widget-test files import `package:flutter_test/flutter_test.dart` only; no production imports.
  </action>
  <verify>
    <automated>flutter test test/features/dashboard/metric_card_test.dart test/features/dashboard/trend_sparkline_test.dart test/features/co2_settings/co2_settings_screen_test.dart test/features/weight/weight_screen_test.dart test/features/notifications/ 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All five stub files exist and pass (skipped, not failing).</done>
</task>

<task type="auto">
  <name>Task 5: Data Analysis, Backup, and offline-proof stubs (5 files)</name>
  <files>
    test/features/data_analysis/data_analysis_screen_test.dart,
    test/features/data_analysis/improvement_opportunities_test.dart,
    test/features/data_analysis/insights_timeline_test.dart,
    test/features/backup/backup_restore_screen_test.dart,
    test/core/offline_phase5_test.dart
  </files>
  <action>
    Create `test/features/data_analysis/` and `test/features/backup/` directories. Write five stub files using the same conventions.

    `test/features/data_analysis/data_analysis_screen_test.dart` — `group('DataAnalysisScreen', skip: 'DataAnalysisScreen not yet implemented', ...)`:
    - `testWidgets('shows today\'s stacked-bar breakdown by meal, an explicit weekly total, ranked contributors, and a goal-comparison progress bar', (tester) async {})` — INS-01, CO2-02 weekly total
    - `testWidgets('trend section metric and range toggles are independently selectable', (tester) async {})` — INS-01
    - `testWidgets('opening from the Weight metric shows a weight trend breakdown, not calorie/protein/CO2 data', (tester) async {})` — WT-05
    - `testWidgets('Estimate Transparency shows the mix of High/Medium confidence items contributing to the aggregate total', (tester) async {})` — CO2-05/INS-02

    `test/features/data_analysis/improvement_opportunities_test.dart` — `group('ImprovementOpportunities widget', skip: 'ImprovementOpportunities widget not yet implemented', ...)`:
    - `testWidgets('is never rendered on the Dashboard screen', (tester) async {})` — CO2-06
    - `testWidgets('shows a quantified CO2 delta in non-judgmental copy', (tester) async {})` — CO2-06/NFR-02

    `test/features/data_analysis/insights_timeline_test.dart` — `group('InsightsTimeline', skip: 'InsightsTimeline not yet implemented', ...)`:
    - `testWidgets('renders zero entries when no pattern threshold is met', (tester) async {})` — INS-03
    - `testWidgets('renders a factual, non-judgmental pattern line when a rule threshold is met', (tester) async {})` — INS-03/NFR-01

    `test/features/backup/backup_restore_screen_test.dart` — `group('BackupRestoreScreen', skip: 'BackupRestoreScreen not yet implemented', ...)`:
    - `testWidgets('Danger Zone delete button stays disabled until the exact word DELETE is typed', (tester) async {})` — PRIV-09
    - `testWidgets('Restore Data opens a real OS file picker and can import a backup zip from outside the app\'s own documents directory', (tester) async {})` — PRIV-04
    - `testWidgets('Restore requires an explicit confirmation step after showing the preview', (tester) async {})` — PRIV-04
    - `testWidgets('Privacy & Ownership statement discloses that shared backups are not encrypted by the app', (tester) async {})` — PRIV-02

    `test/core/offline_phase5_test.dart` — `group('Phase 5 offline proof', skip: 'Phase 5 code paths not yet implemented', ...)`:
    - `test('DailyTotalsCalculator, PersonalCo2MultiplierCalculator, NotificationService, BackupExportService never call OffApiClient or Connectivity', () {})` — AUTH-07/PRIV-08
  </action>
  <verify>
    <automated>flutter test test/features/data_analysis/ test/features/backup/ test/core/offline_phase5_test.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All five stub files exist and pass (skipped, not failing). `flutter test test/` exits 0 (full suite including all 25 new Phase 5 stubs, plus all pre-existing Phase 1-4 tests still green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production | Test files must not import production code that does not exist yet -- stubs use only flutter_test imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Wave 0 stub plan -- no new dependencies |
</threat_model>

<verification>
```bash
flutter test test/
```
Expected: all Phase 1-4 tests still green; twenty-five new Phase 5 stubs skipped (not failing). Exit code 0.

```bash
flutter analyze test/domain/ test/data/ test/features/dashboard/ test/features/co2_settings/ test/features/weight/ test/features/data_analysis/ test/features/notifications/ test/features/backup/ test/core/
```
Expected: zero analyzer issues in the new stub files.
</verification>

<success_criteria>
- Twenty-five stub files exist at exact paths listed in files_modified, spread across 5 tasks (6/5/4/5/5 files) -- no task touches more than 6 files
- `flutter test test/` exits 0
- No production files modified
- All stubs use group-level skip or `skip: true`/skipped testWidgets bodies
- No imports beyond flutter_test in stub files
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-01-SUMMARY.md` when done.
</output>
