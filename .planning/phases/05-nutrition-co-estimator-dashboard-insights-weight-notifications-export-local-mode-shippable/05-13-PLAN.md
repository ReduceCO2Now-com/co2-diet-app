---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 13
type: execute
wave: 5
depends_on:
  - 05-07
  - 05-08
files_modified:
  - lib/features/weight/screens/weight_screen.dart
  - lib/features/weight/widgets/weight_chart.dart
  - lib/features/weight/widgets/weight_entry_form.dart
  - lib/features/weight/widgets/weigh_in_reminder_section.dart
autonomous: true
requirements:
  - WT-01
  - WT-02
  - WT-03
  - WT-04
  - WT-05
  - NOTIF-02

must_haves:
  truths:
    - "Logging a weight entry (value, unit, date, optional note) appends a point to the chart immediately"
    - "The chart's goal line (when a target weight is set) is a static dashed reference line -- no 'on pace'/projection text renders anywhere on this screen"
    - "The 7d/30d/90d/1yr/all range filter is available, defaulting to 30d on screen open"
    - "Enabling the weigh-in reminder for the first time triggers NotificationService.requestPermissionIfNeeded before scheduling; denial reverts the toggle with an inline Open Settings link"
  artifacts:
    - path: "lib/features/weight/screens/weight_screen.dart"
      provides: "WeightScreen -- record, history+chart, goal, reminders (no Learn More section)"
    - path: "lib/features/weight/widgets/weight_chart.dart"
      provides: "WeightChart -- fl_chart interactive multi-range chart with goal HorizontalLine"
  key_links:
    - from: "lib/features/weight/widgets/weigh_in_reminder_section.dart"
      to: "lib/domain/services/notification_service.dart"
      via: "scheduleWeighInReminder/cancelWeighInReminder"
      pattern: "scheduleWeighInReminder"
---

<objective>
Build the Weight Tracking screen: Record (log a weigh-in), History + interactive chart (7d/30d/90d/1yr/all, default 30d), Goal (target weight + target date, static dashed reference line only), and Reminders (Never/Weekly/2-Weekly/Monthly/Custom day+time). This screen is the sole logging surface for weight per CONTEXT.md's "Settings-primary" placement decision (WT-05) — not yet linked from Settings or wired into `app_router.dart`; Plan 05-18 adds both.

Purpose: Delivers WT-01 through WT-04 end-to-end against the 05-07 domain layer and 05-08's `NotificationService`. The "Learn More" section (guide/diet book/Discord) is explicitly excluded per CONTEXT.md's Deferred Ideas — do not add it.

Output: `WeightScreen`, `WeightChart` (fl_chart with `ExtraLinesData.horizontalLines` goal line), `WeightEntryForm`, `WeighInReminderSection`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@lib/features/weight/providers/weight_notifier.dart
@lib/domain/entities/weight_entry.dart
@lib/domain/entities/weight_settings.dart
@lib/domain/services/notification_service.dart
@lib/features/profile/widgets/profile_form.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WeightEntryForm and WeightChart</name>
  <files>
    lib/features/weight/widgets/weight_entry_form.dart,
    lib/features/weight/widgets/weight_chart.dart,
    test/features/weight/weight_screen_test.dart
  </files>
  <behavior>
    - Logging an entry via the form calls `WeightNotifier.logWeight` and the chart reflects the new point without a manual refresh.
    - The chart's goal line renders only when `targetWeightKg` is non-null, as a dashed `HorizontalLine` with a `'Goal: {value} {unit}'` label -- no derived pace/projection text anywhere in this widget tree.
    - Range tabs (7d/30d/90d/1yr/all) re-query `WeightNotifier.entriesForRange` and re-plot; default selection on first build is 30d.
  </behavior>
  <action>
    Create `lib/features/weight/widgets/weight_entry_form.dart`: a compact form/bottom-sheet-style widget (value field, kg/lb unit toggle, date picker defaulting to today, optional note field, Save button) calling `ref.read(weightProvider.notifier).logWeight(...)` on save. No blocking validation beyond requiring a numeric value (mirrors LOG-10's minimal-required-field philosophy).

    Create `lib/features/weight/widgets/weight_chart.dart`: `class WeightChart extends ConsumerStatefulWidget` (needs local state for the selected `WeightRange` tab). Uses RESEARCH.md Pattern 3 verbatim: `LineChartData(lineBarsData: [...], extraLinesData: ExtraLinesData(horizontalLines: [HorizontalLine(y: targetWeightKg, dashArray: [6, 4], label: HorizontalLineLabel(show: true, labelResolver: (line) => 'Goal: ${line.y} kg'))]), lineTouchData: LineTouchData(enabled: true, touchTooltipData: ...))`. Range tabs above the chart call `ref.read(weightProvider.notifier).entriesForRange(range)` and rebuild the plotted spots. Add an explicit code comment: "No pace/projection value is ever computed or rendered here — CONTEXT.md explicitly rejects it; the HorizontalLine is the only goal-related rendering."

    Start de-skipping `test/features/weight/weight_screen_test.dart` with assertions for the entry-form-appends-a-point and goal-line-renders-only-when-set behaviors (full-screen assertions land in Task 2).
  </action>
  <verify>
    <automated>flutter analyze lib/features/weight/widgets/weight_entry_form.dart lib/features/weight/widgets/weight_chart.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Both widgets compile; the chart's goal line uses only `ExtraLinesData`, never a computed pace value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WeighInReminderSection and WeightScreen assembly</name>
  <files>
    lib/features/weight/widgets/weigh_in_reminder_section.dart,
    lib/features/weight/screens/weight_screen.dart,
    test/features/weight/weight_screen_test.dart
  </files>
  <behavior>
    - Reminder frequency options are exactly Never/Weekly/2-Weekly/Monthly/Custom; selecting Custom reveals a weekday + time picker.
    - Toggling the reminder on for the first time calls `NotificationService.requestPermissionIfNeeded()` before `scheduleWeighInReminder`; if denied, the toggle reverts to off and an inline "Open Settings" link/message renders (no full-screen block, per CONTEXT.md's lightweight permission-denied pattern).
  </behavior>
  <action>
    Create `lib/features/weight/widgets/weigh_in_reminder_section.dart`: frequency dropdown (Never/Weekly/2-Weekly/Monthly/Custom), conditional weekday+time picker for Custom, enable toggle. On enable: `final granted = await notificationService.requestPermissionIfNeeded(); if (!granted) { revert toggle; show inline "Open Settings" TextButton calling app_settings' AppSettings.openAppSettings(); return; } await notificationService.scheduleWeighInReminder(...); await ref.read(weightProvider.notifier).saveReminderSettings(...);`. On disable: `cancelWeighInReminder()` + `saveReminderSettings(enabled: false)`.

    Create `lib/features/weight/screens/weight_screen.dart`: `class WeightScreen extends ConsumerWidget` assembling, in order: `WeightChart` (with range tabs), a "Log weight" button opening `WeightEntryForm` (bottom sheet, mirroring `showFoodDetailSheet`'s `showModalBottomSheet` pattern), a Goal section (target weight + target date fields, auto-saving via `saveGoal`), `WeighInReminderSection`, and a brief static "Best Practices" tips section (Claude's discretion per CONTEXT.md — harmless static copy, no new capability). Do NOT include any "Learn More" section (guide/diet book/Discord) — explicitly excluded.

    Complete `test/features/weight/weight_screen_test.dart`'s de-skip with the Custom-reveals-weekday-picker assertion.
  </action>
  <verify>
    <automated>flutter test test/features/weight/ 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`WeightScreen` renders Record/History+Chart/Goal/Reminders (no Learn More section); reminder permission-denial reverts the toggle with an inline recovery link. De-skipped test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Local-only screen; notification scheduling delegates entirely to `NotificationService` (05-08), which already owns the OS-boundary threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (fl_chart, flutter_local_notifications already installed by Plan 05-08) |
</threat_model>

<verification>
```bash
flutter test test/features/weight/ && flutter analyze lib/features/weight/
```
</verification>

<success_criteria>
- `WeightScreen` covers Record/History+Chart/Goal/Reminders only, no Learn More section
- Chart goal line never accompanied by derived pace/projection text
- Reminder permission-denial reverts the toggle with an inline recovery link
- De-skipped screen test passes
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-13-SUMMARY.md` when done.
</output>
