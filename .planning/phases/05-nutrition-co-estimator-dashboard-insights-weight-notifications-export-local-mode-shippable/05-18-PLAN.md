---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 18
type: execute
wave: 7
depends_on:
  - 05-11
  - 05-12
  - 05-13
  - 05-14
  - 05-15
  - 05-16
  - 05-17
  - 05-08
files_modified:
  - lib/core/router/app_router.dart
  - lib/features/dashboard/screens/placeholder_dashboard_screen.dart
  - lib/features/food_search/screens/food_search_screen.dart
  - lib/features/settings/screens/settings_screen.dart
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-06
  - DASH-07
  - DASH-08
  - WT-05
  - NOTIF-01

must_haves:
  truths:
    - "Dashboard is reachable as the default post-onboarding screen (unchanged initialLocation behavior, already /dashboard branch) and shows metric cards, sparkline, quick insight, mode indicator, quick-log buttons, today's meal list, and the CO2 profile prompt card at the bottom"
    - "Tapping any Dashboard metric or the trend chart opens Data Analysis pre-set to that metric"
    - "CO2 Calculation Settings, Weight Tracking, and Backup & Restore are all reachable from Settings within the existing 2-tap Legal-Hub-style navigation budget"
    - "Tapping a per-slot quick-log button or Quick Add opens food search pre-set to that meal slot, identical to the auto-detected-slot flow"
  artifacts:
    - path: "lib/features/dashboard/screens/placeholder_dashboard_screen.dart"
      provides: "Fully composed Dashboard: metric cards, sparkline, quick insight, mode indicator, quick-log buttons, meal list, CO2 prompt card"
    - path: "lib/core/router/app_router.dart"
      provides: "New routes: /co2-settings, /weight-tracking, /data-analysis, /backup-restore"
  key_links:
    - from: "lib/features/dashboard/screens/placeholder_dashboard_screen.dart"
      to: "lib/core/router/app_router.dart"
      via: "context.push('/data-analysis?metric=co2') on metric-card/chart tap"
      pattern: "/data-analysis"
---

<objective>
Wire every standalone Wave 5/6 screen into the app: add the four new go_router routes, assemble the Dashboard from Plan 05-11's widgets plus quick-log buttons (DASH-02) and the metric-tap-to-Data-Analysis behavior (DASH-08), add General Settings entry points for CO2 Calculation Settings / Weight Tracking / Backup & Restore, embed Plan 05-14's meal reminder settings section into General Settings, and extend `FoodSearchScreen` to accept a pre-selected meal slot (shared by both the Dashboard's quick-log buttons and Plan 05-08's notification-tap payload contract).

Purpose: Every prior Wave 5/6 plan deliberately built its screen/widget standalone to maximize parallelism with zero `app_router.dart`/`settings_screen.dart` conflicts. This plan is the single integration point that makes all of it reachable — the last piece before Phase 5's Local Mode is genuinely shippable.

Output: Four new routes, a fully composed Dashboard, three new Settings entry points plus the embedded meal-reminder section, and slot-aware `FoodSearchScreen`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@lib/core/router/app_router.dart
@lib/features/dashboard/screens/placeholder_dashboard_screen.dart
@lib/features/food_search/screens/food_search_screen.dart
@lib/features/settings/screens/settings_screen.dart
@lib/domain/entities/meal_slot.dart
@lib/domain/entities/user_profile.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router wiring and food-search slot pre-selection</name>
  <files>
    lib/core/router/app_router.dart,
    lib/features/food_search/screens/food_search_screen.dart
  </files>
  <action>
    Edit `lib/core/router/app_router.dart`: add four new `GoRoute`s at the top level (alongside `/food-search`, `/barcode-scan`, `/methodology`):
    - `GoRoute(path: '/co2-settings', builder: (context, state) => const Co2SettingsScreen())`
    - `GoRoute(path: '/weight-tracking', builder: (context, state) => const WeightScreen())`
    - `GoRoute(path: '/data-analysis', builder: (context, state) => DataAnalysisScreen(initialMetric: DashboardMetric.values.firstWhereOrNull((m) => m.name == state.uri.queryParameters['metric']) ?? DashboardMetric.co2))` (fall back to `co2` when the query param is absent/unrecognized — never crash on a malformed deep link).
    - `GoRoute(path: '/backup-restore', builder: (context, state) => const BackupRestoreScreen())`
    Update the existing `/food-search` route: `builder: (context, state) => FoodSearchScreen(initialSlot: MealSlot.values.firstWhereOrNull((s) => s.name == state.uri.queryParameters['slot']))`.

    Edit `lib/features/food_search/screens/food_search_screen.dart`: add an `initialSlot` (`MealSlot?`, default `null`) constructor parameter. Thread it through to every `showFoodDetailSheet(...)` call site in this file as the `initialSlot:` argument (the sheet already accepts this parameter per Plan 04-09/04-10 — no change needed there), so a search result tapped after arriving via a slot-preset route pre-fills that slot in `PortionSlotForm` exactly like editing an existing entry does today.
  </action>
  <verify>
    <automated>flutter analyze lib/core/router/app_router.dart lib/features/food_search/screens/food_search_screen.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Four new routes exist and resolve to the correct screens with query-param fallbacks; `/food-search?slot=breakfast` pre-fills the Breakfast slot when a search result is tapped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard assembly (metric cards, sparkline, quick insight, mode indicator, quick-log buttons, CO2 prompt)</name>
  <files>
    lib/features/dashboard/screens/placeholder_dashboard_screen.dart,
    test/features/dashboard/metric_card_test.dart
  </files>
  <behavior>
    - The metric card matching the user's Profile `goal` (reduce_co2/lose_weight/gain_muscle -> co2/calories/protein respectively; any other goal falls back to calories as the default emphasis) is rendered with `isEmphasized: true` and ordered first.
    - Quick-log buttons for Breakfast/Lunch/Dinner/Snack, plus a "+ Quick Add Food" secondary shortcut, each push `/food-search?slot=<slot>` (Quick Add omits the query param entirely, letting the existing time-of-day auto-detect apply, per CONTEXT.md: "behaves exactly like the per-slot quick-log buttons... editable inside the sheet").
    - Tapping any metric card or the trend chart pushes `/data-analysis?metric=<metric>`.
  </behavior>
  <action>
    Edit `lib/features/dashboard/screens/placeholder_dashboard_screen.dart`: above the existing slot-grouped meal list (unchanged from Phase 4), insert: a `ref.watch(profileProvider)` + `ref.watch(mealEntryProvider)`-driven `Row` of three `MetricCard`s built from `DailyTotalsCalculator.compute(entries, co2Multiplier: PersonalCo2MultiplierCalculator.compute(co2Settings))` (watch `co2SettingsProvider` for the settings input), with goal-to-emphasis mapping per `<behavior>`; a `TrendSparkline` sourcing its 7-day spots from a small helper that groups the last 7 days' entries by day and re-runs `DailyTotalsCalculator` per day (document this as an O(7) repeated aggregation, acceptable given the small daily entry counts this app expects); a `QuickInsightLine` fed by a small selection function comparing each metric's percent-of-target and picking the most notable (largest deviation) one, naming its top-contributing meal slot; a `ModeIndicator` (hardcoded `isLocalMode: true` until Phase 7); the four quick-log buttons + Quick Add row (DASH-02); the existing meal list (unchanged); and, at the very bottom (per CONTEXT.md's explicit placement decision), a `Co2ProfilePromptCard` shown only when `co2Settings.dataQuality == 'basic'` and not previously dismissed this session (track dismissal via simple local widget state, not persisted — re-appears next cold start, which is acceptable for a low-priority nudge). Keep the existing empty-state (DASH-07) and swipe-to-edit/duplicate/delete (DASH-03) bodies exactly as Phase 4 built them.

    Complete `test/features/dashboard/metric_card_test.dart`'s remaining assertions (or add a new `dashboard_composition_test.dart` if cleaner — Claude's discretion) covering the goal-to-emphasis mapping and the quick-log-button route-push behavior.
  </action>
  <verify>
    <automated>flutter test test/features/dashboard/ 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Dashboard renders every DASH-01/02/03/04/05/06/07/08 element; goal-matching metric is emphasized and ordered first; quick-log buttons and metric taps navigate correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Settings screen entry points and embedded meal reminder section</name>
  <files>lib/features/settings/screens/settings_screen.dart</files>
  <action>
    Edit `lib/features/settings/screens/settings_screen.dart`: add three new `ListTile` entries (CO2 Calculation Settings -> `context.push('/co2-settings')`; Weight Tracking -> `context.push('/weight-tracking')`, satisfying WT-05's Settings-primary placement; Backup & Restore -> `context.push('/backup-restore')`), and embed `MealReminderSettingsSection` (Plan 05-14) directly in this screen's body under a "Meal Reminders" section header (NOTIF-01's settings-location decision: "meal-reminder config lives in the existing General Settings screen"). Keep the existing "Search foods" / "My Foods" / "Open source licenses" tiles unchanged.
  </action>
  <verify>
    <automated>flutter test test/features/notifications/ 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Settings screen lists CO2 Calculation Settings, Weight Tracking, Backup & Restore, and renders the embedded meal reminder section, all alongside the existing Phase 1-4 entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure navigation/composition wiring; no new I/O surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18-01 | Tampering | `/data-analysis?metric=` and `/food-search?slot=` query params | mitigate | Both fall back to a safe default (`co2`/`null`) via `firstWhereOrNull` when the param is absent or doesn't match a known enum value -- never crashes on a malformed deep link |
| T-05-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/ && flutter analyze lib/
```
Expected: full suite green (every Wave 0-6 stub now de-skipped and real, every Wave 5 screen now reachable); zero analyzer issues.
</verification>

<success_criteria>
- All four new routes resolve correctly with safe query-param fallbacks
- Dashboard is fully composed per DASH-01 through DASH-08
- Settings screen links to all three new screens plus the embedded meal reminder section
- Food search accepts and pre-fills a slot from either a quick-log-button tap or a notification-tap deep link
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-18-SUMMARY.md` when done.
</output>
