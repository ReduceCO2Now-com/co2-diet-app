---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 05
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - lib/data/local/daos/co2_settings_dao.dart
  - lib/data/local/daos/weight_dao.dart
  - lib/data/local/daos/notification_prefs_dao.dart
  - lib/data/local/daos/backup_metadata_dao.dart
  - lib/data/local/app_database.dart
autonomous: true
requirements:
  - CO2-03
  - WT-01
  - WT-02
  - WT-03
  - WT-04
  - NOTIF-01
  - PRIV-02
  - PRIV-03

must_haves:
  truths:
    - "Each singleton settings table (Co2Settings, WeightSettings, NotificationPrefs, BackupMetadata) enforces its single-row invariant via insertOnConflictUpdate, matching UserProfileDao's established pattern"
    - "WeightDao's range query supports the 7d/30d/90d/1yr/all filter set required by WT-02"
  artifacts:
    - path: "lib/data/local/daos/co2_settings_dao.dart"
      provides: "Co2SettingsDao -- getSettings/upsertSettings"
    - path: "lib/data/local/daos/weight_dao.dart"
      provides: "WeightDao -- weight entry CRUD + goal/reminder settings, covering both WeightEntryTable and WeightSettingsTable"
    - path: "lib/data/local/daos/notification_prefs_dao.dart"
      provides: "NotificationPrefsDao -- getPrefs/savePrefs"
    - path: "lib/data/local/daos/backup_metadata_dao.dart"
      provides: "BackupMetadataDao -- getMetadata/saveMetadata"
  key_links:
    - from: "lib/data/local/app_database.dart"
      to: "lib/data/local/daos/co2_settings_dao.dart"
      via: "@DriftDatabase(daos: [...]) registration"
      pattern: "Co2SettingsDao"
---

<objective>
Create the four DAOs backing Phase 5's five new tables (05-03) and register them in `AppDatabase`'s `daos:` list. `WeightDao` covers both `WeightEntryTable` (log rows) and `WeightSettingsTable` (goal + reminder config) in a single DAO, mirroring `MealEntryDao`'s established precedent of one DAO owning multiple closely-related tables.

Purpose: These DAOs are the sole data-access surface every later repository/notifier/service plan (CO2 Settings, Weight, Notifications, Backup) depends on. Singleton tables reuse `UserProfileDao`'s `select(table)..limit(1)` / `insertOnConflictUpdate` pattern exactly — no new singleton-row idiom is introduced.

Output: Four new DAO files, `AppDatabase.daos` updated, `app_database.g.dart` regenerated.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@lib/data/local/daos/user_profile_dao.dart
@lib/data/local/daos/meal_entry_dao.dart
@lib/data/local/tables/co2_settings_table.dart
@lib/data/local/tables/weight_entry_table.dart
@lib/data/local/tables/weight_settings_table.dart
@lib/data/local/tables/notification_prefs_table.dart
@lib/data/local/tables/backup_metadata_table.dart
@lib/data/local/app_database.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Co2SettingsDao and NotificationPrefsDao (singleton-row DAOs)</name>
  <files>
    lib/data/local/daos/co2_settings_dao.dart,
    lib/data/local/daos/notification_prefs_dao.dart,
    test/data/local/co2_settings_dao_test.dart,
    test/data/local/notification_prefs_dao_test.dart
  </files>
  <action>
    `co2_settings_dao.dart` — `@DriftAccessor(tables: [Co2SettingsTable]) class Co2SettingsDao extends DatabaseAccessor<AppDatabase> with _$Co2SettingsDaoMixin`:
    - `Future<Co2SettingsRow?> getSettings() => (select(co2SettingsTable)..limit(1)).getSingleOrNull();`
    - `Future<void> upsertSettings(Co2SettingsTableCompanion entry) => into(co2SettingsTable).insertOnConflictUpdate(entry);`

    `notification_prefs_dao.dart` — `@DriftAccessor(tables: [NotificationPrefsTable]) class NotificationPrefsDao extends DatabaseAccessor<AppDatabase> with _$NotificationPrefsDaoMixin`:
    - `Future<NotificationPrefsRow?> getPrefs() => (select(notificationPrefsTable)..limit(1)).getSingleOrNull();`
    - `Future<void> savePrefs(NotificationPrefsTableCompanion entry) => into(notificationPrefsTable).insertOnConflictUpdate(entry);`

    Both files follow `UserProfileDao`'s exact doc-comment style (single-row constraint enforced by upsert on PK).

    De-skip `test/data/local/co2_settings_dao_test.dart` and `test/data/local/notification_prefs_dao_test.dart`: remove `skip:`, replace stub bodies with real assertions using an in-memory `AppDatabase(NativeDatabase.memory())` (no `offRefPath`, matching every existing Phase 1-4 DAO test's isolation pattern) — assert `getSettings()`/`getPrefs()` returns `null` before any save, and that `upsertSettings`/`savePrefs` called twice with the same generated id updates rather than duplicates the row (`select(...).get()` returns exactly one row).
  </action>
  <verify>
    <automated>flutter test test/data/local/co2_settings_dao_test.dart test/data/local/notification_prefs_dao_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Both DAOs compile and their de-skipped tests pass, proving the single-row upsert invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Create WeightDao and BackupMetadataDao</name>
  <files>
    lib/data/local/daos/weight_dao.dart,
    lib/data/local/daos/backup_metadata_dao.dart,
    test/data/local/weight_dao_test.dart,
    test/data/local/backup_metadata_dao_test.dart
  </files>
  <action>
    `weight_dao.dart` — `@DriftAccessor(tables: [WeightEntryTable, WeightSettingsTable]) class WeightDao extends DatabaseAccessor<AppDatabase> with _$WeightDaoMixin`:
    - `Future<WeightEntryRow> logWeight(WeightEntryTableCompanion draft) => into(weightEntryTable).insertReturning(draft);` — WT-01.
    - `Future<List<WeightEntryRow>> getEntriesInRange({DateTime? from, DateTime? to}) async { ... }` — `SELECT * FROM weight_entry_table WHERE deleted_at IS NULL AND (? IS NULL OR logged_at >= ?) AND (? IS NULL OR logged_at <= ?) ORDER BY logged_at ASC`, parameterized via `Variable.withDateTime`/nullable variables (mirror `UserFoodDao.getAllAlphabetical`'s `Variable<String>(filter)` nullable-parameter idiom for the nullable `from`/`to` bounds). `from`/`to` both `null` returns the full "all-time" set — WT-02's 7d/30d/90d/1yr/all filter is a caller-side date-range computation (`DateTime.now().subtract(...)`), not a DAO concern.
    - `Future<void> deleteEntry(String id) => (delete(weightEntryTable)..where((t) => t.id.equals(id))).go();` — hard delete (a mis-logged weigh-in has no sync-relevant tombstone need distinct from Phase 4's `UserFoodDao.revert` precedent; document this deviation from the soft-delete convention the same way).
    - `Future<WeightSettingsRow?> getSettings() => (select(weightSettingsTable)..limit(1)).getSingleOrNull();` — WT-03/WT-04.
    - `Future<void> saveSettings(WeightSettingsTableCompanion entry) => into(weightSettingsTable).insertOnConflictUpdate(entry);`

    `backup_metadata_dao.dart` — `@DriftAccessor(tables: [BackupMetadataTable]) class BackupMetadataDao extends DatabaseAccessor<AppDatabase> with _$BackupMetadataDaoMixin`:
    - `Future<BackupMetadataRow?> getMetadata() => (select(backupMetadataTable)..limit(1)).getSingleOrNull();`
    - `Future<void> saveMetadata(BackupMetadataTableCompanion entry) => into(backupMetadataTable).insertOnConflictUpdate(entry);`

    De-skip `test/data/local/weight_dao_test.dart` and `test/data/local/backup_metadata_dao_test.dart` with real assertions covering the behaviors above (insert + range filter + settings round-trip for `WeightDao`; metadata round-trip for `BackupMetadataDao`), using in-memory `AppDatabase` fixtures matching every other Phase 1-4 DAO test.
  </action>
  <verify>
    <automated>flutter test test/data/local/weight_dao_test.dart test/data/local/backup_metadata_dao_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Both DAOs compile; `getEntriesInRange` correctly filters by `[from, to]`; both de-skipped tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Register all four DAOs in AppDatabase</name>
  <files>lib/data/local/app_database.dart</files>
  <action>
    Add `Co2SettingsDao`, `WeightDao`, `NotificationPrefsDao`, `BackupMetadataDao` to the `@DriftDatabase(daos: [...])` list (alongside the existing `UserProfileDao, ConsentRecordsDao, FoodCatalogDao, MealEntryDao, UserFoodDao`). Add the corresponding imports. Run `dart run build_runner build --delete-conflicting-outputs` to regenerate `app_database.g.dart` with the new `db.co2SettingsDao`, `db.weightDao`, `db.notificationPrefsDao`, `db.backupMetadataDao` accessors.
  </action>
  <verify>
    <automated>flutter test test/data/local/schema_test.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <done>`AppDatabase` exposes all four new DAO accessors. `flutter analyze lib/data/local/app_database.dart` reports zero issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App <-> local SQLite (co2diet.sqlite) | Trusted — single-user local device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | `WeightDao.getEntriesInRange`'s date-bound parameters | mitigate | `Variable.withDateTime`/nullable-`Variable<T>` parameterization only, no string interpolation, per T-04-04-01 precedent |
| T-05-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/data/local/ && flutter analyze lib/data/local/
```
Expected: full suite green including four newly de-skipped DAO tests; zero analyzer issues.
</verification>

<success_criteria>
- Four new DAOs exist and are registered in `AppDatabase.daos`
- Each singleton DAO enforces single-row semantics via `insertOnConflictUpdate`
- `WeightDao.getEntriesInRange` supports nullable `from`/`to` bounds
- All four de-skipped DAO tests pass
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-05-SUMMARY.md` when done.
</output>
