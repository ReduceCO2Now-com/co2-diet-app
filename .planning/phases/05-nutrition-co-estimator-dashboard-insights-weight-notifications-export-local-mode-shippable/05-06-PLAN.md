---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 06
type: execute
wave: 3
depends_on:
  - 05-05
files_modified:
  - lib/domain/entities/co2_settings.dart
  - lib/domain/repositories/i_co2_settings_repository.dart
  - lib/data/repositories/co2_settings_repository.dart
  - lib/core/di/co2_settings_providers.dart
  - lib/features/co2_settings/providers/co2_settings_notifier.dart
autonomous: true
requirements:
  - CO2-03

must_haves:
  truths:
    - "Saving CO2 Calculation Settings with every field empty succeeds (all fields optional, regional-average fallback)"
    - "Co2Settings.dataQuality reflects how many optional fields are set (basic/good/detailed), matching the CO2 Calculation Settings screen's Data Quality Indicator"
    - "Co2SettingsRepository never reads or writes any MealEntryTable row -- this is a separate settings layer, not a per-food modifier"
  artifacts:
    - path: "lib/domain/entities/co2_settings.dart"
      provides: "Co2Settings entity with copyWith + dataQuality getter"
    - path: "lib/features/co2_settings/providers/co2_settings_notifier.dart"
      provides: "Co2SettingsNotifier -- loads/saves settings, feeds co2SettingsProvider"
  key_links:
    - from: "lib/data/repositories/co2_settings_repository.dart"
      to: "lib/data/local/daos/co2_settings_dao.dart"
      via: "getSettings/upsertSettings"
      pattern: "Co2SettingsDao"
---

<objective>
Build the CO2 Settings domain layer: the `Co2Settings` entity, its repository interface + Drift-backed implementation, DI providers, and the `Co2SettingsNotifier` — following `UserProfile`/`ProfileNotifier`'s exact established shape (per RESEARCH.md: "same pattern as ProfileScreen/ProfileNotifier").

Purpose: CO2-03 requires a personal-footprint settings screen (location, purchasing source, shopping transport, cooking method, storage, household size, waste level) that is a strictly separate layer from any food's own CO2 data (CONTEXT.md: "forward-only... never rewrites already-logged meals"). This plan delivers everything below the screen; Plan 05-12 builds the screen itself against this contract.

Output: `Co2Settings` entity (with a `dataQuality` getter classifying Basic/Good/Detailed), `ICo2SettingsRepository`, `Co2SettingsRepository`, `co2SettingsRepositoryProvider`, and `Co2SettingsNotifier` (generated provider: `co2SettingsProvider`).
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@lib/domain/entities/user_profile.dart
@lib/features/profile/providers/profile_notifier.dart
@lib/core/di/providers.dart
@lib/data/local/daos/co2_settings_dao.dart
@lib/data/local/tables/co2_settings_table.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Co2Settings entity and repository</name>
  <files>
    lib/domain/entities/co2_settings.dart,
    lib/domain/repositories/i_co2_settings_repository.dart,
    lib/data/repositories/co2_settings_repository.dart,
    test/data/repositories/co2_settings_repository_test.dart
  </files>
  <behavior>
    - `getSettings()` returns an all-`null`-fields `Co2Settings` (not an exception, not a fabricated default) before any save.
    - `saveSettings` persists via `upsertSettings` and never touches any other table.
    - `Co2Settings.dataQuality` returns `'basic'` for 0-2 non-null optional fields set, `'good'` for 3-5, `'detailed'` for 6-7.
  </behavior>
  <action>
    Create `lib/domain/entities/co2_settings.dart` — plain immutable Dart class (no Freezed, matching `FoodItem`/`UserFood`'s sentinel `copyWith` style since this entity has no discriminated-union needs): fields `locationCountry`, `locationRegion`, `purchasingSource`, `shoppingTransport`, `cookingMethod`, `foodStorage` (all `String?`), `householdSize` (`int?`), `foodWasteLevel` (`String?`). Add a `dataQuality` getter counting non-null fields among the 7 optional ones (exclude none — all 7 are optional) and classifying per the `<behavior>` thresholds above. Add a doc comment citing CONTEXT.md: this entity is a separate personal-footprint layer, never joined against any food's own `co2e100g`/`confidenceBand`.

    Create `lib/domain/repositories/i_co2_settings_repository.dart`: `abstract interface class ICo2SettingsRepository { Future<Co2Settings> getSettings(); Future<void> saveSettings(Co2Settings settings); }`.

    Create `lib/data/repositories/co2_settings_repository.dart`: `final class Co2SettingsRepository implements ICo2SettingsRepository` backed by `Co2SettingsDao`. `getSettings()` calls `_dao.getSettings()`; returns `const Co2Settings()` (all-null) when the DAO returns `null`, otherwise maps the row's fields 1:1. `saveSettings` builds a `Co2SettingsTableCompanion` using a stable generated id (mirror `DriftProfileRepository`'s single-row-id convention — read the existing row first to reuse its id, or generate a UUID v7 via `Uuid().v7()` on first save) plus Phase-1-placeholder HLC fields (`hlcNodeId: 'local'`, `hlcCounter: 0`, matching every other Phase 1-4 repository).

    Create `test/data/repositories/co2_settings_repository_test.dart` (de-skip the Wave 0 stub): real assertions using a mocktail mock of `Co2SettingsDao` (follow `meal_entry_repository_test.dart`'s mocktail-mocked-DAO convention), covering the `<behavior>` bullets above.
  </action>
  <verify>
    <automated>flutter test test/data/repositories/co2_settings_repository_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`Co2Settings`, `ICo2SettingsRepository`, `Co2SettingsRepository` compile; the de-skipped repository test passes, proving default-on-empty, dataQuality classification, and single-table isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DI providers and Co2SettingsNotifier</name>
  <files>
    lib/core/di/co2_settings_providers.dart,
    lib/features/co2_settings/providers/co2_settings_notifier.dart,
    test/features/co2_settings/co2_settings_notifier_test.dart
  </files>
  <behavior>
    - `build()` returns the current persisted settings (or the all-null default).
    - `saveSettings` persists and re-runs `build()` so the notifier reflects the freshly saved state.
  </behavior>
  <action>
    Create `lib/core/di/co2_settings_providers.dart` mirroring `meal_logging_providers.dart`'s exact shape: `@Riverpod(keepAlive: true) Co2SettingsDao co2SettingsDao(Ref ref) => ref.watch(appDatabaseProvider).co2SettingsDao;` and `@Riverpod(keepAlive: true) ICo2SettingsRepository co2SettingsRepository(Ref ref) => Co2SettingsRepository(ref.watch(co2SettingsDaoProvider));`.

    Create `lib/features/co2_settings/providers/co2_settings_notifier.dart`: `@riverpod class Co2SettingsNotifier extends _$Co2SettingsNotifier` with `Future<Co2Settings> build() => ref.watch(co2SettingsRepositoryProvider).getSettings();` and `Future<void> saveSettings(Co2Settings settings) async { state = const AsyncValue.loading(); state = await AsyncValue.guard(() async { await ref.read(co2SettingsRepositoryProvider).saveSettings(settings); return settings; }); ref.invalidateSelf(); }` — mirrors `ProfileNotifier.saveProfile` exactly. Generated provider name: `co2SettingsProvider`.

    De-skip `test/features/co2_settings/co2_settings_notifier_test.dart` with a real assertion using `ProviderContainer` + an override of `co2SettingsRepositoryProvider` (mocktail mock), following `ProfileNotifier`'s existing test conventions if a comparable test exists, otherwise the `ProviderContainer.listen + Completer` pattern established in [Phase 02-07].
  </action>
  <verify>
    <automated>flutter test test/features/co2_settings/ 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`co2SettingsProvider` builds and saves correctly; de-skipped notifier test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App <-> local SQLite | Trusted, single-user local device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | `Co2Settings` free-text fields | mitigate | Persisted only via typed Drift Companion, never raw SQL |
| T-05-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/data/repositories/co2_settings_repository_test.dart test/features/co2_settings/ && flutter analyze lib/domain/entities/co2_settings.dart lib/data/repositories/co2_settings_repository.dart lib/core/di/co2_settings_providers.dart lib/features/co2_settings/
```
</verification>

<success_criteria>
- `Co2Settings` entity with `dataQuality` classification exists
- `Co2SettingsRepository`/`Co2SettingsNotifier` compile and pass their de-skipped tests
- Zero references to `MealEntryTable`/`FoodItem` anywhere in this plan's files (separate-layer invariant)
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-06-SUMMARY.md` when done.
</output>
