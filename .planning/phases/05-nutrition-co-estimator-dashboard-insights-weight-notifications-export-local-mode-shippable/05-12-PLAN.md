---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 12
type: execute
wave: 5
depends_on:
  - 05-06
files_modified:
  - lib/features/co2_settings/screens/co2_settings_screen.dart
  - lib/features/co2_settings/widgets/data_quality_indicator.dart
autonomous: true
requirements:
  - CO2-03

must_haves:
  truths:
    - "Every one of the 7 CO2 Calculation Settings fields is optional -- saving with all fields empty succeeds"
    - "The Data Quality Indicator (Basic/Good/Detailed) updates live as fields are filled in, sourced from Co2Settings.dataQuality"
    - "Auto-saves on field change, matching ProfileScreen's established no-blocking-validation convention"
  artifacts:
    - path: "lib/features/co2_settings/screens/co2_settings_screen.dart"
      provides: "Co2SettingsScreen -- 7 optional fields, Data Quality Indicator, auto-save"
  key_links:
    - from: "lib/features/co2_settings/screens/co2_settings_screen.dart"
      to: "lib/features/co2_settings/providers/co2_settings_notifier.dart"
      via: "ref.watch(co2SettingsProvider) / ref.read(co2SettingsProvider.notifier).saveSettings"
      pattern: "co2SettingsProvider"
---

<objective>
Build the CO2 Calculation Settings screen (CO2-03): location, purchasing source, shopping transport, cooking method, food storage, household size, food waste level — all optional, auto-saving, with a live Data Quality Indicator (Basic/Good/Detailed) sourced from `Co2Settings.dataQuality`. Not yet wired into `app_router.dart` or linked from any other screen — Plan 05-18 adds the route and the Settings-screen/prompt-card entry points once every Wave 5 screen exists.

Purpose: This is the screen `ProfileScreen`/`ProfileNotifier`'s pattern was explicitly cited as the template for (RESEARCH.md: "same pattern as ProfileScreen/ProfileNotifier"). No blocking validation, no required fields — mirrors `ONBD-04`'s profile-setup philosophy applied to this settings surface.

Output: `Co2SettingsScreen` and a small `DataQualityIndicator` widget.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@lib/features/profile/screens/profile_screen.dart
@lib/features/profile/widgets/profile_form.dart
@lib/features/co2_settings/providers/co2_settings_notifier.dart
@lib/domain/entities/co2_settings.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DataQualityIndicator widget</name>
  <files>
    lib/features/co2_settings/widgets/data_quality_indicator.dart,
    test/features/co2_settings/co2_settings_screen_test.dart
  </files>
  <behavior>
    - Renders one of three labeled states (`'Basic Estimate'`/`'Good Estimate'`/`'Detailed Estimate'`) from a `Co2Settings.dataQuality` string input.
    - Purely presentational -- takes the classification as a param, does not recompute it.
  </behavior>
  <action>
    Create `lib/features/co2_settings/widgets/data_quality_indicator.dart`: `class DataQualityIndicator extends StatelessWidget` taking `dataQuality` (String: `'basic'`/`'good'`/`'detailed'`) and rendering a labeled chip (reuse `ConfidenceChip`'s pill styling conventions where sensible, but this is a distinct concept from the per-food confidence chip per CONTEXT.md — "a different concept" — so do not reuse `ConfidenceChip` itself, build a small sibling widget with its own three-state color mapping: basic=neutral gray, good=`AppColors.warningAmber`, detailed=`AppColors.primaryContainer`).

    Start `test/features/co2_settings/co2_settings_screen_test.dart`'s de-skip here with the `DataQualityIndicator` rendering assertions (full screen assertions land in Task 2).
  </action>
  <verify>
    <automated>flutter analyze lib/features/co2_settings/widgets/data_quality_indicator.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <done>`DataQualityIndicator` renders the correct label/color for each of the three states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Co2SettingsScreen</name>
  <files>
    lib/features/co2_settings/screens/co2_settings_screen.dart,
    test/features/co2_settings/co2_settings_screen_test.dart
  </files>
  <behavior>
    - All 7 fields render as optional inputs (text fields for location country/region, dropdowns for the 5 enum-valued fields, a number field for household size); none has a required-field validator.
    - Every field's `onChanged` triggers an auto-save via `Co2SettingsNotifier.saveSettings` with a debounce (mirror `ProfileNotifier`'s auto-save-on-blur/change convention — check `ProfileForm`'s exact trigger point and replicate it, e.g. `onChanged` for dropdowns, `onEditingComplete`/`onChanged` with debounce for text fields).
    - `DataQualityIndicator` at the top of the screen reflects the live (not-yet-saved-to-disk, if using local form state) or freshly-saved `Co2Settings.dataQuality`.
  </behavior>
  <action>
    Create `lib/features/co2_settings/screens/co2_settings_screen.dart`: `class Co2SettingsScreen extends ConsumerWidget` (or `ConsumerStatefulWidget` if local `TextEditingController`s are needed for the two free-text location fields, following `ProfileForm`'s existing controller-management pattern). Layout: `AppBar(title: Text('CO2 Calculation Settings'))`, `DataQualityIndicator` at top, then a `ListView` of: location country (text field), location region (text field), purchasing source (dropdown: Supermarket/Local farm/Mix), shopping transport (dropdown: Car/Public transport/Walk or bike), cooking method (dropdown: Electric/Gas/Induction), food storage (dropdown: Small fridge/Large fridge+freezer), household size (number field), food waste level (dropdown: Low/Medium/High). Every dropdown includes an explicit "Not set" option (maps to `null`) — CO2-03 requires every field to be genuinely optional, not defaulted to the first enum value.

    Auto-save: on any field change, call `ref.read(co2SettingsProvider.notifier).saveSettings(updated)` where `updated` is the current form state's `Co2Settings` (built via `copyWith` from `ref.read(co2SettingsProvider).value ?? const Co2Settings()`), matching `ProfileNotifier.updateField`'s single-field auto-save convenience pattern.

    Complete `test/features/co2_settings/co2_settings_screen_test.dart`'s de-skip: assert saving with every field empty succeeds (no validator blocks it), and that `DataQualityIndicator` updates as fields are filled in during a widget test (pump, enter text/select dropdown values, verify the indicator's rendered label changes).
  </action>
  <verify>
    <automated>flutter test test/features/co2_settings/ 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`Co2SettingsScreen` renders all 7 optional fields, auto-saves on change, and its Data Quality Indicator updates live. De-skipped test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Local-only settings form; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/features/co2_settings/ && flutter analyze lib/features/co2_settings/
```
</verification>

<success_criteria>
- All 7 CO2 Calculation Settings fields are genuinely optional and auto-save
- Data Quality Indicator reflects live `Co2Settings.dataQuality`
- De-skipped screen test passes
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-12-SUMMARY.md` when done.
</output>
