---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 17
type: execute
wave: 6
depends_on:
  - 05-15
files_modified:
  - lib/domain/services/improvement_opportunity_finder.dart
  - lib/domain/services/insights_timeline_rule_engine.dart
  - lib/features/data_analysis/widgets/improvement_opportunities.dart
  - lib/features/data_analysis/widgets/insights_timeline.dart
  - lib/features/data_analysis/screens/data_analysis_screen.dart
autonomous: true
requirements:
  - CO2-06
  - INS-03

must_haves:
  truths:
    - "Improvement Opportunities never renders on the Dashboard or as a notification -- Data Analysis screen only"
    - "Every suggestion stays within a hand-authored protein-for-protein substitution cluster (e.g. red meat <-> poultry <-> fish <-> legumes) -- never a cross-cluster swap"
    - "Every suggestion states a quantified CO2 delta in non-judgmental, factual copy"
    - "Insights Timeline renders zero entries when no rule threshold is met -- never a forced/fabricated pattern"
  artifacts:
    - path: "lib/domain/services/improvement_opportunity_finder.dart"
      provides: "Pure function: today's high-CO2 entries -> quantified same-cluster substitution suggestions"
    - path: "lib/domain/services/insights_timeline_rule_engine.dart"
      provides: "Small fixed rule set producing factual pattern-observation strings from historical entries"
  key_links:
    - from: "lib/features/data_analysis/screens/data_analysis_screen.dart"
      to: "lib/features/data_analysis/widgets/improvement_opportunities.dart"
      via: "rendered only inside DataAnalysisScreen's body"
      pattern: "ImprovementOpportunities"
---

<objective>
Build `ImprovementOpportunityFinder` (CO2-06) and `InsightsTimelineRuleEngine` (INS-03) as pure domain services, plus their two widgets, and wire both into `DataAnalysisScreen` (05-15) — the only place either is ever rendered.

Purpose: CO2-06 is explicit that suggestions are "optional and never shown unsolicited on every screen" — CONTEXT.md locks this down further to Data-Analysis-screen-only, never the Dashboard, never a notification. `ImprovementOpportunityFinder` reuses the existing `co2_factors` category-average data from Phase 3 (via `FoodCatalogDao.getAvailableCo2Categories`/`getCo2ForCategory`, already built) — no new data source. `InsightsTimelineRuleEngine` is a small, bounded, hand-authored rule set (CONTEXT.md: "not a single CO2-only rule and not open-ended ML"), testable and extensible.

Output: Two domain services and two widgets, wired into the existing `DataAnalysisScreen` body.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@lib/features/data_analysis/screens/data_analysis_screen.dart
@lib/data/local/daos/food_catalog_dao.dart
@lib/domain/entities/meal_entry.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImprovementOpportunityFinder and InsightsTimelineRuleEngine</name>
  <files>
    lib/domain/services/improvement_opportunity_finder.dart,
    lib/domain/services/insights_timeline_rule_engine.dart,
    test/domain/services/improvement_opportunity_finder_test.dart
  </files>
  <behavior>
    - Given today's entries, `ImprovementOpportunityFinder.findOpportunities` returns suggestions only for entries whose category belongs to a known substitution cluster and whose CO2 contribution is meaningfully high within that cluster; each suggestion names the same-cluster alternative and a quantified CO2 delta (e.g. "Replacing today's beef meal with chicken would save approximately 1.2 kg CO2").
    - `InsightsTimelineRuleEngine.evaluate` returns an empty list when no rule's threshold is met over the supplied historical window; each returned entry is a factual, non-judgmental sentence.
  </behavior>
  <action>
    Create `lib/domain/services/improvement_opportunity_finder.dart`: define a hand-authored `const Map<String, List<String>> _substitutionClusters` keyed by a representative AGRIBALYSE category tag with a list of alternative category tags within the same protein-for-protein band (Claude's discretion per CONTEXT.md — e.g. a red-meat cluster mapping to poultry/fish/legume category tags; document the exact tag strings used, sourced from `tools/off_to_agribalyse_map.csv`'s existing category vocabulary, not invented ad hoc). `class ImprovementOpportunityFinder` constructor-injected with `FoodCatalogDao` (for `getCo2ForCategory`). `Future<List<ImprovementOpportunity>> findOpportunities(List<MealEntry> todayEntries)`: for each entry with a known category and a co2e contribution above a documented threshold (e.g. top contributor(s) only, not every entry — Claude's discretion, document the exact rule), look up the cluster, compute `getCo2ForCategory` for each alternative, and return the alternative with the largest quantified delta as an `ImprovementOpportunity` (`{originalName, alternativeName, deltaKg}`) with factual (non-judgmental) copy assembled in the widget layer, not baked into this service's return strings (keeps the pure-Dart service free of UI copy concerns).

    Create `lib/domain/services/insights_timeline_rule_engine.dart`: `class InsightsTimelineRuleEngine`, `List<String> evaluate(List<MealEntry> historicalEntries)` implementing a small fixed rule set (Claude's discretion on exact thresholds, document each): (1) "CO2 consistently higher on {weekday} evenings" when a specific weekday+dinner-slot combination's average CO2 exceeds the overall daily average by a documented margin across at least N occurrences; (2) "Protein consistently below target on {N} days" when protein total is below the user's target on a majority of the trailing 7 days; (3) "CO2 trending {up/down} over the trailing week" when a simple linear trend across the trailing 7 daily totals exceeds a documented slope threshold. Returns `[]` when no rule fires — never forces an entry.

    De-skip `test/domain/services/improvement_opportunity_finder_test.dart` covering the `<behavior>` bullets (same-cluster-only, quantified delta, no-suggestions-when-nothing-high).
  </action>
  <verify>
    <automated>flutter test test/domain/services/improvement_opportunity_finder_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Both services compile and pass their de-skipped assertions; no cross-cluster suggestion is ever produced; Insights Timeline returns `[]` when no threshold is met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImprovementOpportunities/InsightsTimeline widgets, wired into DataAnalysisScreen</name>
  <files>
    lib/features/data_analysis/widgets/improvement_opportunities.dart,
    lib/features/data_analysis/widgets/insights_timeline.dart,
    lib/features/data_analysis/screens/data_analysis_screen.dart,
    test/features/data_analysis/improvement_opportunities_test.dart,
    test/features/data_analysis/insights_timeline_test.dart
  </files>
  <behavior>
    - `ImprovementOpportunities` renders nothing when the finder returns `[]`; each rendered suggestion states the quantified delta in factual copy (e.g. "Replacing {original} with {alternative} would save approximately {delta} kg CO2 today").
    - `InsightsTimeline` renders nothing when the rule engine returns `[]`; each rendered line is presented as a neutral observation, never phrased as a directive or judgment.
    - Neither widget is reachable from `PlaceholderDashboardScreen` -- only from `DataAnalysisScreen`.
  </behavior>
  <action>
    Create `lib/features/data_analysis/widgets/improvement_opportunities.dart`: `class ImprovementOpportunities extends StatelessWidget` taking `List<ImprovementOpportunity>`, rendering `SizedBox.shrink()` when empty, otherwise a card per suggestion with the factual copy template from `<behavior>`.

    Create `lib/features/data_analysis/widgets/insights_timeline.dart`: `class InsightsTimeline extends StatelessWidget` taking `List<String>` (the rule engine's output), rendering `SizedBox.shrink()` when empty, otherwise a simple chronological list.

    Edit `lib/features/data_analysis/screens/data_analysis_screen.dart`: add `ImprovementOpportunities` (fed by `ImprovementOpportunityFinder`, injected via a new keepAlive DI provider mirroring existing conventions) and `InsightsTimeline` (fed by `InsightsTimelineRuleEngine` over a trailing-7-day entry query) to the screen's body, positioned after `DetailedFoodAnalysisPanel` per CONTEXT.md's screen-content ordering. Confirm via a code comment that neither widget is imported or referenced anywhere in `lib/features/dashboard/`.

    De-skip `test/features/data_analysis/improvement_opportunities_test.dart` and `test/features/data_analysis/insights_timeline_test.dart` covering the `<behavior>` bullets above, plus a static-analysis-style assertion (a simple `grep`-equivalent Dart test reading `lib/features/dashboard/` file contents, or a straightforward widget-tree assertion on `PlaceholderDashboardScreen` proving neither widget appears) confirming CO2-06's "never on the Dashboard" invariant.
  </action>
  <verify>
    <automated>flutter test test/features/data_analysis/ 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Both widgets render correctly and only inside `DataAnalysisScreen`; empty-suggestion states render nothing; both de-skipped tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure in-memory computation over already-local data; no new I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/domain/services/improvement_opportunity_finder_test.dart test/features/data_analysis/ && flutter analyze lib/domain/services/improvement_opportunity_finder.dart lib/domain/services/insights_timeline_rule_engine.dart lib/features/data_analysis/
```
</verification>

<success_criteria>
- `ImprovementOpportunityFinder` only ever suggests same-cluster alternatives with a quantified delta
- `InsightsTimelineRuleEngine` returns `[]` when no threshold is met
- Neither widget appears anywhere in `lib/features/dashboard/`
- Both de-skipped tests pass
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-17-SUMMARY.md` when done.
</output>
