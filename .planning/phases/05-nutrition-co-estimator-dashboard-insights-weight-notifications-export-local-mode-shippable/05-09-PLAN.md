---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 09
type: execute
wave: 4
depends_on:
  - 05-08
  - 05-04
  - 05-05
files_modified:
  - pubspec.yaml
  - lib/domain/entities/backup_metadata.dart
  - lib/data/repositories/backup_metadata_repository.dart
  - lib/core/di/backup_providers.dart
  - lib/domain/services/backup_export_service.dart
autonomous: false
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04
  - PRIV-08

user_setup: []

must_haves:
  truths:
    - "exportData produces a single zip containing manifest.json plus one file per selected category/format -- CSV, Excel (.xlsx), and JSON are all genuinely valid in their respective formats"
    - "manifest.json includes a formatVersion field (starting at 1) so a future encryption or format change can be detected and handled without breaking compatibility with older backups"
    - "Restore rejects any zip entry whose normalized extraction path escapes the target directory (zip-slip) before writing anything to disk"
    - "BackupExportService never instantiates OffApiClient or checks Connectivity -- fully offline (AUTH-07/PRIV-08)"
  artifacts:
    - path: "lib/domain/services/backup_export_service.dart"
      provides: "BackupExportService -- export, manual/automatic backup, restore preview + apply with zip-slip guard"
  key_links:
    - from: "lib/domain/services/backup_export_service.dart"
      to: "lib/data/local/daos/backup_metadata_dao.dart"
      via: "reads/writes autoBackupFrequency + lastBackupAt via BackupMetadataDao"
      pattern: "BackupMetadataDao"

install_reference:
  packages:
    - name: share_plus
      version: "13.3.0"
      pub_score: "150/160"
      publisher: "fluttercommunity (github.com/fluttercommunity/plus_plugins)"
    - name: csv
      version: "8.0.0"
      pub_score: "150/160"
      publisher: "close2 (github.com/close2/csv)"
    - name: excel
      version: "4.0.6"
      pub_score: "115/160 (SUS-equivalent: last published ~2 years ago as of RESEARCH.md's date -- re-verify at install time)"
      publisher: "justkawal.dev (verified publisher, github.com/justkawal/excel)"
---

<objective>
Install the three remaining new packages this phase needs (`share_plus`, `csv`, `excel`) behind a blocking human-verify checkpoint, then build `BackupMetadata` domain storage and `BackupExportService` — the pure-Dart service that generates export/backup zips (CSV/Excel/JSON + `manifest.json`), previews and applies restores with zip-slip protection, and tracks automatic-backup scheduling metadata.

Purpose: PRIV-01 through PRIV-04 require export/backup/restore covering every category of locally stored data; this service is the single place that reads every DAO, encodes rows, and packages them — the Backup & Restore screen (Plan 05-16) is a thin UI over this service, per the Architectural Responsibility Map ("Export/Backup file generation — Local domain service — Zip/CSV/Excel generation is pure Dart; OS involvement limited to share sheet and documents-directory file I/O").

Output: `share_plus`/`csv`/`excel` installed (human-approved, `excel`'s staleness explicitly re-checked), `BackupMetadata` entity + repository, and `BackupExportService` with `exportData`, `createBackup`, `previewRestore`, `applyRestore`, and zip-slip path validation.

**Locked decision (05-CONTEXT.md Planning Addendum, resolving RESEARCH.md Open Question 1):** No encryption in v1. This is an explicit, user-confirmed decision, not a silent default -- the Backup & Restore screen (Plan 05-16) discloses it plainly. To keep the door open for encryption in a future phase without breaking already-created backups, `manifest.json` includes a `formatVersion` field (starting at `1`) from day one.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@lib/data/local/daos/backup_metadata_dao.dart
@lib/core/assets/first_launch_extractor.dart
@lib/data/local/app_database.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Package legitimacy checkpoint: share_plus, csv, excel</name>
  <what-built>Nothing yet -- this checkpoint precedes installing three new pubspec.yaml dependencies: share_plus 13.3.0, csv 8.0.0, excel 4.0.6.</what-built>
  <how-to-verify>
    1. https://pub.dev/packages/share_plus -- confirm ~150/160 pub score, `fluttercommunity/plus_plugins` repo, current major version still exposes `SharePlus.instance.share(ShareParams(...))`.
    2. https://pub.dev/packages/csv -- confirm ~150/160 pub score, `close2/csv` repo.
    3. https://pub.dev/packages/excel -- **pay special attention here**: RESEARCH.md flags this package as SUS-equivalent (pub score 115/160, last published ~2 years before this research, vs. 150/160 for every other candidate). Re-check https://pub.dev/packages/excel directly for: (a) whether a newer release has landed since, (b) the verified-publisher badge (`justkawal.dev`) is still present, (c) no open critical security issues in the repo. If a more actively maintained `.xlsx`-writing alternative has since become the ecosystem standard, flag it instead of approving `excel`.
    None of these three packages make any network call (`share_plus` only invokes the OS share sheet; `csv`/`excel` are pure in-memory encoders) -- confirms AUTH-07/PRIV-08 compliance.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `flutter pub add share_plus csv excel`, or describe concerns / a preferred alternative.</resume-signal>
  <action>No automated action in this task -- it is a blocking human-verify checkpoint. The actual `flutter pub add` install is the first line of the next task's &lt;action&gt;, which only runs after this checkpoint is approved.</action>
</task>

<task type="auto">
  <name>Task 1: Install dependencies and build BackupMetadata domain storage</name>
  <files>
    pubspec.yaml,
    lib/domain/entities/backup_metadata.dart,
    lib/data/repositories/backup_metadata_repository.dart,
    lib/core/di/backup_providers.dart
  </files>
  <action>
    Run `flutter pub add share_plus csv excel` (only after the checkpoint above is approved). Add version-comment blocks matching this project's existing `pubspec.yaml` convention, explicitly noting `excel`'s lower pub score and re-verification date.

    Create `lib/domain/entities/backup_metadata.dart`: immutable `BackupMetadata` (`autoBackupFrequency` String default `'off'`, `lastBackupAt` DateTime?, `lastBackupPath` String?) with sentinel `copyWith`.

    Create `lib/data/repositories/backup_metadata_repository.dart`: thin repository over `BackupMetadataDao` (`getMetadata`/`saveMetadata`), default `const BackupMetadata()` when no row exists, matching `Co2SettingsRepository`'s established shape (no separate interface file needed here — this is a simple single-table read/write consumed only by `BackupExportService`, not by any UI notifier directly, so an abstract interface would add indirection with no second implementation ever expected).

    Create `lib/core/di/backup_providers.dart`: `backupMetadataDaoProvider`, `backupMetadataRepositoryProvider` (both keepAlive).
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; flutter analyze lib/domain/entities/backup_metadata.dart lib/data/repositories/backup_metadata_repository.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All three packages appear in `pubspec.yaml`/`pubspec.lock`. `BackupMetadata`/`BackupMetadataRepository` compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackupExportService -- export, backup, restore with zip-slip protection</name>
  <files>
    lib/domain/services/backup_export_service.dart,
    test/domain/services/backup_export_service_test.dart
  </files>
  <behavior>
    - `exportData({required Set<ExportCategory> categories, required Set<ExportFormat> formats})` returns a `File` (zip) containing `manifest.json` plus one encoded file per category x format combination requested.
    - `manifest.json` lists every included file's category, format, and row count.
    - `createBackup()` writes a full-data zip (all categories, JSON format) to the app documents directory and records `lastBackupAt`/`lastBackupPath` via `BackupMetadataRepository`.
    - `previewRestore(File zip)` parses and returns a summary of what the zip contains (categories, row counts, backup date from `manifest.json`) without writing anything.
    - `applyRestore(File zip)` re-validates every `ArchiveFile.name` resolves within the target extraction directory (rejects any entry whose normalized path starts with `..` or is absolute) before writing a single byte; a malicious/malformed entry aborts the entire restore with a clear typed exception, consistent with this app's no-silent-failure tone.
  </behavior>
  <action>
    Define `enum ExportCategory { profile, mealEntries, favorites, customFoods, weightEntries, co2Settings, notificationPrefs }` and `enum ExportFormat { csv, excel, json }` in this file (or a small sibling `export_category.dart` if cleaner — Claude's discretion per CONTEXT.md).

    Implement `class BackupExportService` constructor-injected with every DAO it reads from (`MealEntryDao`, `UserFoodDao`, `WeightDao`, `Co2SettingsDao`, `NotificationPrefsDao`, `UserProfileDao`, `BackupMetadataDao`) plus a `Directory` documents-dir provider (constructor param, testable without `path_provider`).

    `exportData`: for each requested category, read the relevant rows; for each requested format, encode via `const ListToCsvConverter().convert(rows)` (csv), an `Excel.createExcel()` + `appendRow` sheet (excel), or `jsonEncode` (json/`dart:convert`, already a project convention per `tool/generate_schema_v1.dart`'s `JsonEncoder.withIndent`). Build the manifest map with a `formatVersion: 1` field at the top level (per 05-CONTEXT.md's Planning Addendum -- locked decision, not Claude's discretion: this field exists from v1 specifically so a future encrypted or restructured backup format can be detected and migrated/rejected cleanly rather than silently misparsed) alongside the existing per-file category/format/row-count entries. Write each encoded file plus the in-memory `manifest.json` (built via `ArchiveFile.string(...)`, matching RESEARCH.md's `archive_io` code example) into a `ZipFileEncoder` (`package:archive/archive_io.dart`, already a project dependency — see `first_launch_extractor.dart`'s existing import). Return the resulting `File`.

    `createBackup`: calls `exportData` with all categories + `{ExportFormat.json}` only (a backup is a full-fidelity restore source, not a human-readable export — JSON is sufficient and simplest to round-trip), writes to a fixed filename pattern in the documents directory (e.g. `co2diet_backup_<timestamp>.zip`), then calls `BackupMetadataRepository.saveMetadata` to record `lastBackupAt`/`lastBackupPath`.

    `previewRestore`: opens the zip read-only via `ZipDecoder().decodeBytes(...)`, locates and parses `manifest.json`, reads `formatVersion` and rejects (with a clear typed exception, not a silent crash) any value other than the one this plan produces (`1`) -- forward compatibility for a future format bump, not merely a schema courtesy. Returns a plain `RestorePreview` value object (categories + row counts + backup timestamp + `formatVersion`) — no DAO writes.

    `applyRestore`: re-opens the zip, validates every entry's `ArchiveFile.name` via a private `_isPathSafe(String entryName, Directory targetDir)` helper (normalize via `path.normalize`, reject `..`-escaping or absolute paths) BEFORE writing any file; if any entry fails, throw a typed `ZipSlipException` and write nothing at all (all-or-nothing restore, not partial). On success, parse each category's JSON payload and re-insert rows via the corresponding DAO's insert/upsert method (mirrors the export encoding in reverse).

    De-skip `test/domain/services/backup_export_service_test.dart` with real assertions covering every `<behavior>` bullet, including a fixture zip built in-memory with a `../../../etc/passwd`-style malicious entry name to prove `applyRestore` rejects it before any write (mocktail-mocked DAOs, in-memory `Directory` via a temp dir from `dart:io`'s `Directory.systemTemp.createTempSync()`).
  </action>
  <verify>
    <automated>flutter test test/domain/services/backup_export_service_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`BackupExportService` compiles; de-skipped test passes, including the zip-slip rejection case. No network client (OffApiClient/Connectivity) is imported or instantiated anywhere in this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-provided restore zip | Untrusted input -- may come from any share-sheet/file-picker source; archive entry names must never be trusted for path construction |
| App documents directory | Trusted, app-sandboxed; automatic backups write only here, never to a user-chosen external path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | Zip-slip / path traversal on restore | mitigate | Every `ArchiveFile.name` validated to resolve within the target directory before any write; malformed/malicious entries abort the whole restore (RESEARCH.md Pitfall 6) |
| T-05-09-02 | Information Disclosure | Unencrypted export/backup zip leaving the device via share sheet | accept | Locked decision (05-CONTEXT.md Planning Addendum, resolving RESEARCH.md Open Question 1): no encryption in v1, `manifest.json`'s `formatVersion` field keeps a future encrypted format addable without breaking old backups; disclosed explicitly in the Backup & Restore screen's Privacy & Ownership statement (Plan 05-16) |
| T-05-09-SC | Tampering | pub.dev package installs (share_plus, csv, excel) | mitigate | Blocking human-verify checkpoint completed above, with explicit re-verification of `excel`'s staleness signal |
</threat_model>

<verification>
```bash
flutter test test/domain/services/backup_export_service_test.dart && flutter analyze lib/domain/services/backup_export_service.dart lib/domain/entities/backup_metadata.dart lib/data/repositories/backup_metadata_repository.dart
```
</verification>

<success_criteria>
- Three new packages installed and human-approved, with `excel`'s staleness explicitly re-checked
- `BackupExportService.exportData` produces a valid zip + manifest across CSV/Excel/JSON
- `applyRestore` rejects zip-slip entries before any write
- De-skipped `backup_export_service_test.dart` passes
- Zero network client usage anywhere in this plan's files
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-09-SUMMARY.md` when done.
</output>
