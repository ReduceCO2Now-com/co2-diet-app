---
phase: 05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable
plan: 16
type: execute
wave: 5
depends_on:
  - 05-09
files_modified:
  - lib/features/backup/providers/backup_notifier.dart
  - lib/features/backup/screens/backup_restore_screen.dart
  - lib/features/backup/widgets/danger_zone_section.dart
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04
  - PRIV-08
  - PRIV-09

must_haves:
  truths:
    - "Export Data lets the user pick categories and formats (CSV/Excel/JSON), producing a single zip via share_plus"
    - "Automatic Backups offers exactly Off/Daily/Weekly, writing to the app's own documents directory"
    - "Restore Data shows a preview of what will change and requires an explicit confirmation step before any data is overwritten"
    - "Danger Zone delete stays disabled until the user types the exact word DELETE"
    - "Privacy & Ownership statement discloses that shared backups/exports are not encrypted by the app itself"
  artifacts:
    - path: "lib/features/backup/screens/backup_restore_screen.dart"
      provides: "BackupRestoreScreen -- storage status, create backup, automatic backups, restore, export, privacy statement, danger zone"
  key_links:
    - from: "lib/features/backup/screens/backup_restore_screen.dart"
      to: "lib/domain/services/backup_export_service.dart"
      via: "exportData/createBackup/previewRestore/applyRestore"
      pattern: "BackupExportService"
---

<objective>
Build the Backup & Restore screen per the Full Reference doc's existing spec (CONTEXT.md): Current Storage Status, Create Backup, Automatic Backups, Restore Data, Export Data, Privacy & Ownership statement, and a typed-confirmation Danger Zone. Not yet linked from `SettingsScreen` or wired into `app_router.dart` — Plan 05-18 adds both.

Purpose: This is the UI layer over `BackupExportService` (05-09) — all zip/CSV/Excel/JSON/zip-slip logic already exists; this plan is presentation + `share_plus` invocation + the Danger Zone's typed-confirmation gate (PRIV-09).

Output: `BackupNotifier`, `BackupRestoreScreen`, `DangerZoneSection`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-CONTEXT.md
@.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-RESEARCH.md
@lib/domain/services/backup_export_service.dart
@lib/domain/entities/backup_metadata.dart
@lib/core/di/backup_providers.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BackupNotifier</name>
  <files>
    lib/features/backup/providers/backup_notifier.dart,
    test/features/backup/backup_restore_screen_test.dart
  </files>
  <behavior>
    - `build()` loads current `BackupMetadata` (storage status card source).
    - `exportData`/`createBackup` delegate to `BackupExportService` and, on `share`, hand the resulting file to `SharePlus.instance.share(ShareParams(files: [XFile(...)]))`.
    - `previewRestore`/`applyRestore` delegate directly; `applyRestore` is only ever called after the caller has already shown the preview and the user has confirmed.
  </behavior>
  <action>
    Create `lib/features/backup/providers/backup_notifier.dart`: `@riverpod class BackupNotifier extends _$BackupNotifier` with `Future<BackupMetadata> build() => ref.watch(backupMetadataRepositoryProvider).getMetadata();`. Add `Future<void> shareExport({required Set<ExportCategory> categories, required Set<ExportFormat> formats})` (calls the service then `SharePlus.instance.share(...)`), `Future<void> createAndShareBackup()`, `Future<RestorePreview> previewRestore(File zip)`, `Future<void> applyRestore(File zip)` (re-runs `build()` after, since a restore can change every table's data), and `Future<void> saveAutoBackupFrequency(String frequency)`. Inject `BackupExportService` via a new provider added to `lib/core/di/backup_providers.dart` (extends the file created in Plan 05-09, adding one provider rather than a conflicting rewrite).

    De-skip the notifier-facing assertions in `test/features/backup/backup_restore_screen_test.dart`.
  </action>
  <verify>
    <automated>flutter test test/features/backup/ 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`backupNotifierProvider` (or generated equivalent name) builds and its export/backup/restore methods delegate correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackupRestoreScreen and DangerZoneSection</name>
  <files>
    lib/features/backup/screens/backup_restore_screen.dart,
    lib/features/backup/widgets/danger_zone_section.dart,
    test/features/backup/backup_restore_screen_test.dart
  </files>
  <behavior>
    - Danger Zone's delete action stays disabled until the typed text field's value exactly equals `'DELETE'`.
    - Restore Data: file picker (or share-sheet receive, whichever this platform's conventions favor -- use `file_picker`-free approach via a simple file-path input/share-intent receiver is out of scope; use the OS document picker via `share_plus`'s companion or a minimal `showModalBottomSheet` file-path entry if no picker package is already a dependency -- prefer reusing `path_provider`'s existing documents-dir listing to let the user pick among their own prior local backups, avoiding a new file-picker dependency entirely) shows `previewRestore`'s summary, then requires a second explicit "Confirm Restore" tap before `applyRestore` runs.
    - Privacy & Ownership statement text explicitly states shared backups/exports are not encrypted by the app.
  </behavior>
  <action>
    Create `lib/features/backup/widgets/danger_zone_section.dart`: `class DangerZoneSection extends StatefulWidget` with a `TextEditingController`, a delete button whose `onPressed` is `null` unless the controller's text is exactly `'DELETE'` (case-sensitive, per CONTEXT.md's fixed-word decision), and an `onConfirmedDelete` callback (wired by the screen to a full local-data-wipe method — reuse each DAO's existing delete/clear capability; if no "delete everything" DAO method exists yet, add a `Future<void> clearAllLocalData()` directly in this task to `BackupExportService` that truncates every table via `AppDatabase.transaction` + `delete(table).go()` per table, since Danger Zone's scope is broader than Export/Backup but this service already has every DAO injected).

    Create `lib/features/backup/screens/backup_restore_screen.dart`: `class BackupRestoreScreen extends ConsumerWidget` assembling, in order: Current Storage Status (row counts per category, read via the same DAOs `BackupExportService` already holds — expose a small `Future<Map<ExportCategory,int>> storageStatus()` method on the service for this), Create Backup (button -> `createAndShareBackup`), Automatic Backups (segmented Off/Daily/Weekly -> `saveAutoBackupFrequency`), Export Data (category + format multi-select -> `shareExport`), Restore Data (pick from documents-dir backup files -> preview -> explicit "Confirm Restore" button -> `applyRestore`), Privacy & Ownership statement (static text disclosing no built-in encryption, per RESEARCH.md Open Question 1's recommended disclosure), and `DangerZoneSection` at the bottom.

    Complete `test/features/backup/backup_restore_screen_test.dart`'s de-skip covering the Danger Zone typed-confirmation gate, the Restore preview-then-confirm two-step flow, and the Privacy statement's disclosure text presence.
  </action>
  <verify>
    <automated>flutter test test/features/backup/ 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`BackupRestoreScreen` renders every required section; Danger Zone requires the exact typed word; Restore requires an explicit confirmation after the preview. De-skipped test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-provided restore zip | Untrusted -- delegated entirely to `BackupExportService.applyRestore`'s zip-slip validation (05-09); this screen never bypasses it |
| Danger Zone full local-data wipe | Destructive, irreversible -- typed-confirmation gate is the only safeguard, matching PRIV-09's literal requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16-01 | Repudiation/Tampering | Danger Zone accidental deletion | mitigate | Delete button disabled until exact-match typed confirmation (PRIV-09) |
| T-05-16-02 | Information Disclosure | Unencrypted export/backup shared externally | accept | Disclosed explicitly in this screen's Privacy & Ownership statement (RESEARCH.md Open Question 1) |
| T-05-16-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (share_plus/csv/excel already installed by Plan 05-09) |
</threat_model>

<verification>
```bash
flutter test test/features/backup/ && flutter analyze lib/features/backup/
```
</verification>

<success_criteria>
- All seven Backup & Restore sections render
- Danger Zone gated by exact-match typed "DELETE"
- Restore requires preview then an explicit second confirmation
- Privacy & Ownership statement discloses the no-encryption posture
- De-skipped test passes
</success_criteria>

<output>
Create `.planning/phases/05-nutrition-co-estimator-dashboard-insights-weight-notifications-export-local-mode-shippable/05-16-SUMMARY.md` when done.
</output>
