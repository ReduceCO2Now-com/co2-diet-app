---
phase: 09-reference-data-delivery-full-off-pack
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - pubspec.yaml
  - lib/data/local/reference_pack/checksum_verifier.dart
  - lib/data/local/reference_pack/disk_space_checker.dart
  - test/data/local/reference_pack/checksum_verifier_test.dart
  - test/data/local/reference_pack/disk_space_check_test.dart
  - lib/domain/entities/reference_pack_status.dart
  - lib/domain/entities/reference_pack_manifest.dart
  - lib/domain/repositories/i_reference_pack_repository.dart
  - lib/data/local/reference_pack/reference_pack_version_store.dart
autonomous: false
requirements: []

must_haves:
  truths:
    - "background_downloader, crypto, and storage_space are installed and human-approved before any code depends on them; the privacy blocklist stays green"
    - "ChecksumVerifier and DiskSpaceChecker are real, tested, on-device implementations, not stubs, satisfying CONTEXT.md's checksum-before-trust and disk-preflight-before-download decisions"
    - "Every later Phase 9 plan reads ReferencePackStatus/ReferencePackManifest/ReferencePackSchedule/IReferencePackRepository from one shared contract file each, so no plan invents its own ad hoc shape"
  artifacts:
    - path: "lib/domain/entities/reference_pack_status.dart"
      provides: "ReferencePackStatus sealed class (seed/downloading/full/updateAvailable/failed) + ReferencePackSchedule enum (manual/weekly/monthly)"
    - path: "lib/domain/entities/reference_pack_manifest.dart"
      provides: "ReferencePackManifest + ReferencePackDeltaInfo entities with fromJson + sanity-bound validation"
    - path: "lib/domain/repositories/i_reference_pack_repository.dart"
      provides: "IReferencePackRepository interface every later data-layer plan implements against"
    - path: "lib/data/local/reference_pack/checksum_verifier.dart"
      provides: "SHA-256 chunked-read verification (crypto package)"
    - path: "lib/data/local/reference_pack/disk_space_checker.dart"
      provides: "Free-disk-space preflight check (storage_space package), Pitfall-4-aware sizing"
    - path: "lib/data/local/reference_pack/reference_pack_version_store.dart"
      provides: "On-disk .version marker for the downloaded full pack, independent of the bundled seed's own version file"
  key_links: []

install_reference:
  packages:
    - name: background_downloader
      version: "9.5.7"
      pub_score: "160/160"
      publisher: "bbflight.com (github.com/781flyingdutchman/background_downloader)"
    - name: crypto
      version: "3.0.7"
      pub_score: "160/160"
      publisher: "dart.dev (official Dart-team package)"
    - name: storage_space
      version: "1.2.0"
      pub_score: "150/160"
      publisher: "oodavid.com"
---

<objective>
Install the three new packages every later Phase 9 plan depends on (background_downloader, crypto, storage_space) behind a single package-legitimacy checkpoint, then create the shared contracts (ReferencePackStatus, ReferencePackSchedule, ReferencePackManifest, IReferencePackRepository) and two small, fully-testable local utilities (ChecksumVerifier, DiskSpaceChecker) that Plans 09-03 through 09-06 all build on. Building contracts first (interface-first ordering) means no later plan has to guess at these shapes, mirroring 07-02-PLAN.md's established structure for this exact kind of plan.

Purpose: CONTEXT.md locks two non-negotiable safety gates before any download starts or is trusted -- a disk-space preflight check and a mandatory checksum verification -- both belong in the local utility layer so every later orchestration step can call them without re-implementing the logic (09-RESEARCH.md's "Don't Hand-Roll" table).

Output: three new pubspec dependencies (human-approved), ReferencePackStatus/ReferencePackSchedule/ReferencePackManifest/IReferencePackRepository contracts, ChecksumVerifier, DiskSpaceChecker, and ReferencePackVersionStore -- with disk_space_check_test.dart and checksum_verifier_test.dart turned green.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reference-data-delivery-full-off-pack/09-CONTEXT.md
@.planning/phases/09-reference-data-delivery-full-off-pack/09-RESEARCH.md
@lib/core/assets/first_launch_extractor.dart
@lib/domain/entities/backup_metadata.dart
@lib/features/food_search/providers/food_search_state.dart
@pubspec.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Package legitimacy checkpoint: background_downloader, crypto, storage_space</name>
  <what-built>Nothing yet -- this checkpoint precedes installing three new pubspec.yaml dependencies: background_downloader 9.5.7, crypto 3.0.7, storage_space 1.2.0.</what-built>
  <how-to-verify>
    pub.dev/Dart is not a slopcheck-supported ecosystem, so this follows the same independent-signal review + human-approval precedent as Plans 04-11/05-08/05-09/06-02/07-02. Please check each package directly on pub.dev before approving:
    1. pub.dev/packages/background_downloader -- confirm 160/160 pub score, verified publisher bbflight.com, 165k downloads, 499 likes, github.com/781flyingdutchman/background_downloader. Replaces the now-unmaintained flutter_downloader (09-RESEARCH.md State of the Art) and is the only package in this plan doing native platform-channel work (iOS URLSession / Android WorkManager background downloads) -- confirm it does not bundle any analytics or ad SDK.
    2. pub.dev/packages/crypto -- confirm 160/160 pub score, official dart.dev publisher (the Dart team itself), 10.4M downloads. Lowest-risk of the three.
    3. pub.dev/packages/storage_space -- confirm 150/160 pub score, verified publisher oodavid.com. Used only for a local free-disk-space query -- no network calls.
    None of the three phone home or contact any server other than the CDN URL this app itself supplies to background_downloader. Confirms AUTH-07/PRIV-08 compliance (no backend contact without explicit user action) stays intact.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with flutter pub add background_downloader crypto storage_space, or describe concerns.</resume-signal>
  <action>No automated action in this task -- it is a blocking human-verify checkpoint. The actual pub-add install is the first line of the next task's action, which only runs after this checkpoint is approved.</action>
</task>

<task type="auto">
  <name>Task 1: Install packages + ChecksumVerifier + DiskSpaceChecker</name>
  <files>
    pubspec.yaml,
    lib/data/local/reference_pack/checksum_verifier.dart,
    lib/data/local/reference_pack/disk_space_checker.dart,
    test/data/local/reference_pack/checksum_verifier_test.dart,
    test/data/local/reference_pack/disk_space_check_test.dart
  </files>
  <action>
    Run flutter pub add background_downloader crypto storage_space (only after the checkpoint above is approved). Add a version-comment block to each new pubspec.yaml entry matching this project's existing convention (see flutter_slidable's or fl_chart's comment block) citing the pub score, publisher, and this session's verification date.

    Create lib/data/local/reference_pack/checksum_verifier.dart: class ChecksumVerifier with Future&lt;bool&gt; verify(File file, String expectedSha256Hex). Implementation uses crypto's sha256.bind(file.openRead()) chunked-stream API (09-RESEARCH.md Pattern 2) -- never reads the whole file into memory (files here are 300-800MB). Returns false (does not throw) if the file does not exist, caught via on FileSystemException. Compares the computed digest's toString() against expectedSha256Hex case-insensitively.

    Create lib/data/local/reference_pack/disk_space_checker.dart: class DiskSpaceChecker wrapping the storage_space package's free-bytes query, with Future&lt;int&gt; freeBytes() and Future&lt;bool&gt; hasEnoughSpace(int compressedDownloadBytes, int decompressedFinalBytes, {double safetyMultiplierPercent = 1.15}). The required-bytes calculation must be (compressedDownloadBytes + decompressedFinalBytes) -- both must coexist on disk briefly during decompression, per 09-RESEARCH.md Pitfall 4, not just the final installed footprint -- multiplied by safetyMultiplierPercent as a margin. Document this reasoning inline since it is a codebase-specific, non-obvious sizing rule. The real storage_space plugin call is wrapped behind a constructor-injectable function seam so tests never touch the real platform channel, mirroring this project's existing seam-injection convention for platform-channel-backed values (e.g. BackupNotifier's filePickerProvider seam).

    Turn the two Wave 0 stub test files green: replace each skip group with real test() cases exercising the exact behaviors already named in 09-01's stub (match / mismatch / missing-file for checksum; sufficient / insufficient / Pitfall-4-aware sizing for disk space). ChecksumVerifier's tests use real temp files via Directory.systemTemp.createTemp() (no need to mock crypto itself, it is pure computation over real bytes). DiskSpaceChecker's tests inject a fake free-bytes function through the constructor seam described above.
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; dart run scripts/check_privacy_deps.dart pubspec.lock .privacy-blocklist.yaml &amp;&amp; flutter test test/data/local/reference_pack/checksum_verifier_test.dart test/data/local/reference_pack/disk_space_check_test.dart</automated>
  </verify>
  <done>background_downloader, crypto, and storage_space appear in pubspec.yaml/pubspec.lock. The privacy blocklist script still exits 0. checksum_verifier_test.dart and disk_space_check_test.dart are green with zero skips.</done>
</task>

<task type="auto">
  <name>Task 2: Domain contracts + version store</name>
  <files>
    lib/domain/entities/reference_pack_status.dart,
    lib/domain/entities/reference_pack_manifest.dart,
    lib/domain/repositories/i_reference_pack_repository.dart,
    lib/data/local/reference_pack/reference_pack_version_store.dart
  </files>
  <action>
    Create lib/domain/entities/reference_pack_status.dart: a plain Dart sealed class ReferencePackStatus (not Freezed -- mirrors AuthState/FoodSearchState's established pattern) with five final-class variants: ReferencePackSeed (no fields -- starter pack only, the default state), ReferencePackDownloading (bytesDownloaded, bytesTotal, both int), ReferencePackFull (installedVersion String, productCount int, installedAt DateTime), ReferencePackUpdateAvailable (currentVersion String, newVersion String, waitingForWifi bool), ReferencePackFailed (message String, canResume bool -- canResume distinguishes an interruption with a resumable partial file from a genuine terminal failure). Also declare enum ReferencePackSchedule { manual, weekly, monthly } (mirrors BackupMetadata.autoBackupFrequency's off/daily/weekly string-set pattern, but as a real enum since this value never round-trips through Drift).

    Create lib/domain/entities/reference_pack_manifest.dart: class ReferencePackManifest with fields currentVersion (String), packUrl (String), packSizeBytes (int), packSha256 (String), productCount (int), deltaFrom (Map&lt;String, ReferencePackDeltaInfo&gt;). Factory ReferencePackManifest.fromJson(Map&lt;String, dynamic&gt; json) parses the shape documented in 09-RESEARCH.md's Code Examples section (current_version, pack_url, pack_size_bytes, pack_sha256, product_count, delta_from: {old_version: {url, size_bytes, sha256}}). fromJson throws FormatException when pack_url does not start with "https://" (T-09-02-tampering mitigation -- HTTPS-only CDN URL, never plain HTTP) and when pack_size_bytes exceeds a documented sanity-bound maximum (const maxPlausiblePackSizeBytes = 2 * 1024 * 1024 * 1024, i.e. 2GB -- well above the documented 300-800MB range but low enough to reject an obviously-malicious or corrupted manifest value per the DoS-via-implausible-size threat in 09-RESEARCH.md's Known Threat Patterns table). Also create class ReferencePackDeltaInfo (url, sizeBytes, sha256) with its own fromJson and the same https-only + sanity-bound validation.

    Create lib/domain/repositories/i_reference_pack_repository.dart: abstract interface class IReferencePackRepository declaring the full contract every later plan implements/consumes: Future&lt;ReferencePackManifest&gt; fetchManifest(); Future&lt;int&gt; localProductCount(); Future&lt;bool&gt; checkDiskSpace(ReferencePackManifest manifest); Future&lt;void&gt; startFullDownload({required bool allowCellular}); Future&lt;void&gt; startDeltaDownload({required bool allowCellular}); Stream&lt;ReferencePackStatus&gt; watchStatus(); Future&lt;bool&gt; isDownloadInProgress(); Future&lt;void&gt; cancelDownload(); Future&lt;void&gt; resumeDownload(); Future&lt;void&gt; revertToSeed(); Future&lt;String?&gt; installedVersion(). Doc-comment each method citing which CONTEXT.md decision it satisfies (disk preflight, Wi-Fi override, resumable, revert, etc.) so Plan 09-03's implementer does not have to re-derive intent from CONTEXT.md line-by-line.

    Create lib/data/local/reference_pack/reference_pack_version_store.dart: class ReferencePackVersionStore mirroring first_launch_extractor.dart's version-marker pattern, but for the downloaded full pack, not the bundled seed. Uses a distinct filename (reference_pack_full.version) in getApplicationDocumentsDirectory() -- T-02-03-02 mitigation extended: the output path is always derived from getApplicationDocumentsDirectory, never from a server-supplied string. Methods: Future&lt;String?&gt; read(); Future&lt;void&gt; write(String version); Future&lt;void&gt; clear() (called by revertToSeed).
  </action>
  <verify>
    <automated>flutter analyze lib/domain/entities/reference_pack_status.dart lib/domain/entities/reference_pack_manifest.dart lib/domain/repositories/i_reference_pack_repository.dart lib/data/local/reference_pack/reference_pack_version_store.dart</automated>
  </verify>
  <done>All four files exist, compile clean under flutter analyze (zero lint errors), and contain nothing but pure contracts/local file I/O -- no network calls, no background_downloader usage, no UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| pub.dev package installs | Third-party code entering the build via flutter pub add |
| ReferencePackManifest.fromJson | Parses an untrusted server response (the CDN's manifest.json) before any of its values are trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-SC | Tampering | pub.dev installs (background_downloader, crypto, storage_space) | mitigate | Blocking human-verify checkpoint before install; all three carry verified-publisher/150-160-pub-score signals |
| T-09-02-01 | Tampering | ReferencePackManifest.fromJson (untrusted CDN response) | mitigate | Reject non-https pack_url/delta URLs; reject implausible pack_size_bytes beyond a hardcoded sanity bound, before the value is ever used for a disk-space calculation |
| T-09-02-02 | Denial of Service | DiskSpaceChecker sizing | mitigate | Sanity-bound manifest size feeds the preflight calculation only after T-09-02-01's validation passes, preventing a malicious/corrupted manifest from tricking the preflight check into approving an unbounded download |
</threat_model>

<verification>
flutter pub get succeeds. dart run scripts/check_privacy_deps.dart pubspec.lock .privacy-blocklist.yaml exits 0. flutter analyze reports zero new lint errors for the four new domain/version-store files. checksum_verifier_test.dart and disk_space_check_test.dart are fully green (no skips remaining).
</verification>

<success_criteria>
background_downloader, crypto, and storage_space are installed and human-approved; ChecksumVerifier and DiskSpaceChecker are real, tested implementations; ReferencePackStatus/ReferencePackSchedule/ReferencePackManifest/IReferencePackRepository/ReferencePackVersionStore exist as pure, dependency-free contracts ready for Plan 09-03 to build ReferencePackRepository against.
</success_criteria>

<output>
Create `.planning/phases/09-reference-data-delivery-full-off-pack/09-02-SUMMARY.md` when done
</output>
