---
phase: 09-reference-data-delivery-full-off-pack
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/data-contracts/reference-pack-manifest.md
  - tools/build_reference_pack_release.py
  - tools/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The manifest.json and delta-artifact contract the Flutter client (Plan 09-02's ReferencePackManifest, Plan 09-04's DeltaApplier) is built against is written down as a spec, not only implicit in code -- mirroring how docs/backend-contracts/gdpr-account-deletion.md was handed off to Tomris"
    - "A developer can run one script against the existing off_reference.sqlite build output to produce a versioned full-pack release artifact plus a manifest.json, and (given a prior version) a delta artifact -- closing 09-RESEARCH.md's Open Question 2 ('the client can't be meaningfully tested end-to-end without at least one real manifest + pack + delta artifact to point at')"
  artifacts:
    - path: "docs/data-contracts/reference-pack-manifest.md"
      provides: "Written manifest.json + delta-artifact contract spec, every field marked [ASSUMED — design proposal, not confirmed with CDN/build owner]"
    - path: "tools/build_reference_pack_release.py"
      provides: "Sibling script to tools/ingest_off.py: versioned full-pack release + manifest.json + delta artifact generation"
  key_links: []
---

<objective>
Write the manifest.json / delta-artifact contract spec this whole phase's client code is built against (09-RESEARCH.md's Code Examples proposal, formalized), and build the build-side script that actually produces a versioned full-pack release, its manifest.json, and a delta artifact against a prior version -- the "build-side/off-app" half of the Architectural Responsibility Map that 09-RESEARCH.md flags as this phase's own deliverable, not purely external.

Purpose: CDN hosting itself is explicitly out of this phase's scope (a coordination point per 09-CONTEXT.md), but the contract shape and the tooling to produce real fixtures are not -- without them, Plan 09-04's DeltaApplier and Plan 09-08's real-device verification would have nothing but hand-built fixtures to test against, and the eventual CDN/build-pipeline owner would have no written spec to implement toward (mirrors docs/backend-contracts/gdpr-account-deletion.md's precedent for Tomris).

Output: docs/data-contracts/reference-pack-manifest.md and tools/build_reference_pack_release.py (+ tools/README.md usage instructions). This plan has zero dependency on and zero file overlap with the Flutter client work in Plans 09-01 through 09-06 -- both halves are built independently against the same written contract.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reference-data-delivery-full-off-pack/09-CONTEXT.md
@.planning/phases/09-reference-data-delivery-full-off-pack/09-RESEARCH.md
@tools/ingest_off.py
@tools/README.md
@docs/backend-contracts/gdpr-account-deletion.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest/delta contract spec</name>
  <files>docs/data-contracts/reference-pack-manifest.md</files>
  <action>
    Create docs/data-contracts/reference-pack-manifest.md, a new top-level docs/data-contracts/ directory established for this kind of written spec (mirrors docs/backend-contracts/'s existing pattern for Tomris hand-offs -- per STATE.md's [Phase 07-08] decision establishing that pattern). Every field is explicitly marked "[ASSUMED — design proposal, not confirmed with CDN/build owner]" per 09-RESEARCH.md's Assumptions Log entry A3, since CDN hosting and its eventual owner are an explicit out-of-scope coordination point (09-CONTEXT.md).

    Document, verbatim to what Plan 09-02's ReferencePackManifest.fromJson and Plan 09-04's DeltaApplier actually implement: the manifest.json shape (current_version, pack_url, pack_size_bytes, pack_sha256, product_count, delta_from: {old_version: {url, size_bytes, sha256}}) with a worked JSON example; the required HTTPS-only + sanity-bound-size validation the client performs on every field before trusting it; the delta artifact's shape as a companion SQLite file with a products_delta table (the full 11-column products schema: barcode, product_name, product_name_en, brand, calories_100g, protein_100g, carbs_100g, fat_100g, categories_tags, agribalyse_food_code, primary_category_tag -- matching tools/ingest_off.py's actual DDL, not the illustrative 10-column subset from 09-RESEARCH.md's Code Examples) and a deleted_barcodes table (barcode primary key); the required apply-order (ATTACH -> explicit-column INSERT OR REPLACE -> DELETE deleted barcodes -> DETACH -> products_fts rebuild) that any build-side pipeline generating this artifact must produce data compatible with; and an explicit "CDN requirements" section documenting 09-RESEARCH.md Pitfall 3 (the eventual host must support HTTP Range requests and a stable strong ETag or Last-Modified validator, or resumable downloads will silently degrade to full restarts on every interruption) as a flagged coordination requirement for whoever picks the CDN.
  </action>
  <verify>
    <automated>test -f docs/data-contracts/reference-pack-manifest.md &amp;&amp; grep -c "ASSUMED" docs/data-contracts/reference-pack-manifest.md</automated>
  </verify>
  <done>docs/data-contracts/reference-pack-manifest.md exists, documents both the manifest.json and delta-artifact shapes with a worked example each, and every field carries an explicit [ASSUMED] marker.</done>
</task>

<task type="auto">
  <name>Task 2: tools/build_reference_pack_release.py</name>
  <files>tools/build_reference_pack_release.py, tools/README.md</files>
  <action>
    Create tools/build_reference_pack_release.py as a sibling to tools/ingest_off.py (Python 3.8+ stdlib only, matching this project's existing "no pip installs required" convention -- reuse sqlite3, gzip, hashlib, json from stdlib; no new Python dependencies). CLI usage: python3 tools/build_reference_pack_release.py &lt;input_off_reference.sqlite&gt; &lt;output_dir&gt; --version &lt;version-tag&gt; [--previous &lt;prior_version_release_dir&gt;].

    Given an already-built off_reference.sqlite (the output of tools/ingest_off.py, run against the full unfiltered OFF dump rather than the size-constrained starter-seed sample this project bundles today), the script: (1) copies/gzips it into &lt;output_dir&gt;/full_v&lt;version&gt;.sqlite.gz, computing its SHA-256 and byte size; (2) queries SELECT COUNT(*) FROM products for product_count; (3) writes &lt;output_dir&gt;/manifest.json matching docs/data-contracts/reference-pack-manifest.md's Task-1 spec exactly (current_version, pack_url as a placeholder token the CDN-owner fills in, pack_size_bytes, pack_sha256, product_count, delta_from: {}); (4) when --previous is given, diffs the previous version's products table against the current one (row-level: barcode present in current but absent/changed in previous -> products_delta; barcode present in previous but absent in current -> deleted_barcodes) into &lt;output_dir&gt;/delta_v&lt;previous&gt;_to_v&lt;version&gt;.sqlite, computes its own SHA-256/size, and adds a delta_from entry to manifest.json.

    Update tools/README.md with a new "## 6. Build a versioned release + delta (Phase 9)" section documenting this script's usage, mirroring the existing numbered-steps format, including a smoke-test invocation against the project's own small starter-seed off_reference.sqlite (no --previous, to produce a first fixture quickly) that Plan 09-08's real-device verification and any future manual testing can point DownloadManager/ReferencePackApiClient at via a local static file server.
  </action>
  <verify>
    <automated>python3 tools/build_reference_pack_release.py --help &amp;&amp; python3 tools/build_reference_pack_release.py assets/off_reference.sqlite /tmp/reference-pack-release-smoketest --version smoketest-v1 &amp;&amp; python3 -c "import json; json.load(open('/tmp/reference-pack-release-smoketest/manifest.json'))" &amp;&amp; echo OK</automated>
  </verify>
  <done>tools/build_reference_pack_release.py runs against the project's existing off_reference.sqlite and produces a valid manifest.json (parses as JSON, contains every documented field) plus a versioned full-pack artifact. tools/README.md documents its usage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build-side script output -> eventual CDN publication | This script's output becomes the CDN's served content; its own integrity is a supply-chain concern for whoever runs it and publishes the result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Tampering | tools/build_reference_pack_release.py's SHA-256 computation | mitigate | Uses Python's stdlib hashlib.sha256 exclusively (same trust level as the Dart client's crypto package) -- the published pack_sha256 in manifest.json is the same value ChecksumVerifier independently recomputes client-side, giving an end-to-end integrity chain from this script to the installed device |
</threat_model>

<verification>
docs/data-contracts/reference-pack-manifest.md exists and documents both shapes with worked examples. tools/build_reference_pack_release.py --help exits 0 and a smoke-test run against the project's existing off_reference.sqlite produces a valid, parseable manifest.json.
</verification>

<success_criteria>
A written, [ASSUMED]-flagged manifest/delta contract exists for the eventual CDN/build-pipeline owner to implement toward, and a working script exists today to produce real fixture artifacts (full pack, delta, manifest) for testing the client without needing a live CDN.
</success_criteria>

<output>
Create `.planning/phases/09-reference-data-delivery-full-off-pack/09-07-SUMMARY.md` when done
</output>
