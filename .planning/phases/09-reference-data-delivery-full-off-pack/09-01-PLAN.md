---
phase: 09-reference-data-delivery-full-off-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/data/local/reference_pack/reference_pack_repository_test.dart
  - test/data/local/reference_pack/disk_space_check_test.dart
  - test/data/local/reference_pack/checksum_verifier_test.dart
  - test/features/settings/reference_data_row_test.dart
  - test/features/reference_data/reference_data_screen_test.dart
  - test/app_lifecycle_reference_pack_test.dart
  - integration_test/reference_pack_delta_apply_test.dart
autonomous: true
# No REQ-IDs: ROADMAP.md Phase 9 explicitly states "(none — v1 launch is
# served by the Phase 2 bundled seed; this phase is v1.0.x enrichment kept
# in-roadmap for continuity)". Traceability for this phase runs against the
# ROADMAP goal + success criteria and 09-CONTEXT.md's locked decisions
# instead of REQ-IDs (see each plan's <objective>/<tasks> for citations).
requirements: []

must_haves:
  truths:
    - "flutter test discovers all 7 new Phase 9 test files and reports them skipped (not failing, not erroring) before any production code exists"
    - "Every later Phase 9 plan that implements one of these files removes the skip: marker as part of turning the stub into a real, green test (Nyquist contract)"
  artifacts:
    - path: "test/data/local/reference_pack/reference_pack_repository_test.dart"
      provides: "Wave 0 stub for manifest fetch/parse, version comparison, disk preflight orchestration, revert (→ Plan 09-03)"
    - path: "test/data/local/reference_pack/disk_space_check_test.dart"
      provides: "Wave 0 stub for DiskSpaceChecker preflight logic (→ Plan 09-02)"
    - path: "test/data/local/reference_pack/checksum_verifier_test.dart"
      provides: "Wave 0 stub for SHA-256 match/mismatch verification (→ Plan 09-02)"
    - path: "test/features/settings/reference_data_row_test.dart"
      provides: "Wave 0 stub for the Settings row subtitle states (→ Plan 09-05)"
    - path: "test/features/reference_data/reference_data_screen_test.dart"
      provides: "Wave 0 stub for the dedicated download/revert screen (→ Plan 09-05)"
    - path: "test/app_lifecycle_reference_pack_test.dart"
      provides: "Wave 0 stub for the foreground-triggered Weekly/Monthly throttle check (→ Plan 09-06)"
    - path: "integration_test/reference_pack_delta_apply_test.dart"
      provides: "Wave 0 stub proving products_fts stays in sync after a delta apply (→ Plan 09-04)"
  key_links: []
---

<objective>
Create the seven Wave 0 stub test files 09-RESEARCH.md and 09-VALIDATION.md flag as missing, following the group-level `skip:` pattern established verbatim in every prior phase (Phase 2-7 Wave 0 plans, most recently 07-01-PLAN.md). This satisfies the Nyquist contract before any Phase 9 production code exists — every later plan in this phase fills in exactly one (or two, for the repository file) of these files and removes its skip marker.

Purpose: every subsequent Phase 9 plan's `<verify>` block runs `flutter test <file>` against one of these seven files. Without this plan, those commands would fail with "file not found" instead of a clean, informative skip.

Output: seven new test files under `test/data/local/reference_pack/`, `test/features/settings/`, `test/features/reference_data/`, `test/`, and `integration_test/` — all present, all skipped, zero failures.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reference-data-delivery-full-off-pack/09-CONTEXT.md
@.planning/phases/09-reference-data-delivery-full-off-pack/09-RESEARCH.md
@.planning/phases/09-reference-data-delivery-full-off-pack/09-VALIDATION.md
@test/features/auth/providers/auth_provider_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data-layer unit-test Wave 0 stubs</name>
  <files>
    test/data/local/reference_pack/reference_pack_repository_test.dart,
    test/data/local/reference_pack/disk_space_check_test.dart,
    test/data/local/reference_pack/checksum_verifier_test.dart
  </files>
  <action>
    Create three stub test files, each with top-level `group('<Name>', () { ... }, skip: 'Awaiting Plan 09-0X implementation');` blocks wrapping placeholder `test('...', () {})` cases naming the exact future behavior (a `// TODO(Plan 09-0X):` comment per case is enough, no bodies needed) — mirrors the Phase 2-7 Wave 0 precedent (group-level skip, not per-test skip) documented in STATE.md's `[Phase 02-01]`/`[Phase 05-01]`/`[Phase 06-01]`/`[Phase 07-01]` decisions.

    `reference_pack_repository_test.dart` (→ filled by Plan 09-03): two groups. `'ReferencePackRepository'` cases: "fetchManifest() parses a valid manifest.json response into ReferencePackManifest", "fetchManifest() throws on a non-https pack_url", "fetchManifest() throws when pack_size_bytes exceeds the sanity-bound maximum", "localProductCount() returns the row count from the currently attached off_ref.products table", "checkDiskSpace() returns insufficient when free bytes are below (compressed + decompressed + safety margin)", "checkDiskSpace() returns sufficient when free bytes clear the margin", "revertToSeed() deletes the downloaded pack file, re-attaches the bundled seed, and resets the delta-refresh schedule to manual", "installedVersion() returns null before any full-pack download has ever completed". `'version comparison'` cases: "an update is available when manifest.currentVersion differs from the installed version", "no update is available when manifest.currentVersion matches the installed version".

    `disk_space_check_test.dart` (→ filled by Plan 09-02): group `'DiskSpaceChecker'` cases: "hasEnoughSpace returns true when free bytes exceed the required amount times the safety multiplier", "hasEnoughSpace returns false when free bytes are below the required amount", "hasEnoughSpace's required-bytes calculation accounts for transient 2x disk usage during gzip decompression (09-RESEARCH.md Pitfall 4), not just the final installed footprint".

    `checksum_verifier_test.dart` (→ filled by Plan 09-02): group `'ChecksumVerifier'` cases: "verify returns true when the file's SHA-256 matches the expected hex digest", "verify returns false when the file's SHA-256 does not match", "verify returns false (does not throw) when the target file does not exist".
  </action>
  <verify>
    <automated>flutter test test/data/local/reference_pack/reference_pack_repository_test.dart test/data/local/reference_pack/disk_space_check_test.dart test/data/local/reference_pack/checksum_verifier_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>All three files exist, `flutter test` reports them as skipped (0 failures), and each test name documents the exact future behavior it stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Widget-layer Wave 0 stubs</name>
  <files>
    test/features/settings/reference_data_row_test.dart,
    test/features/reference_data/reference_data_screen_test.dart
  </files>
  <action>
    Create two stub test files using the same group-level skip pattern, with `testWidgets('...', (tester) async {})` bodies wrapped inside the skipped `group()` — mirrors STATE.md's `[Phase 05-01]`/`[Phase 06-01]`/`[Phase 07-01]` decision (group-level skip extended to `testWidgets`).

    `reference_data_row_test.dart` (→ filled by Plan 09-05): group `'ReferenceDataRow'` cases: "shows 'Using starter pack' subtitle in the seed state", "shows a 'Downloading… X/Y MB' subtitle with live byte progress in the downloading state", "shows a 'Full catalog installed — N MB' subtitle in the full state", "shows an 'Update available — connect to Wi-Fi' subtitle when an update is known but the device is off Wi-Fi", "tapping the row navigates to /reference-data".

    `reference_data_screen_test.dart` (→ filled by Plan 09-05): group `'ReferenceDataScreen'` cases: "shows a determinate progress bar with percent and byte counts during an active download", "the Cancel button is visible only during an active download and calls cancelDownload() when tapped", "shows the product-count comparison (starter pack vs full catalog) using concrete numbers, not vague language", "tapping Download with insufficient disk space shows the blocking 'Not enough storage' message and never starts a download", "tapping Download while off Wi-Fi shows the 'download over cellular anyway?' prompt before starting, and starting requires an explicit confirmation", "the Revert action is disabled while a download is actively in progress", "the Revert action shows a confirmation dialog with the exact locked copy from CONTEXT.md; confirming calls revertToSeed() and cancelling does not".
  </action>
  <verify>
    <automated>flutter test test/features/settings/reference_data_row_test.dart test/features/reference_data/reference_data_screen_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>Both files exist, `flutter test` reports them as skipped (0 failures).</done>
</task>

<task type="auto">
  <name>Task 3: Lifecycle unit stub + delta-apply integration stub</name>
  <files>
    test/app_lifecycle_reference_pack_test.dart,
    integration_test/reference_pack_delta_apply_test.dart
  </files>
  <action>
    Create two stub test files using the group-level skip pattern.

    `app_lifecycle_reference_pack_test.dart` (→ filled by Plan 09-06): group `'Reference pack scheduled check'` cases: "does not check for an update on resume when the schedule is manual", "checks for an update on resume when the schedule is weekly and 7+ days have elapsed since the last check", "does not check on resume when the schedule is weekly and fewer than 7 days have elapsed", "checks for an update on resume when the schedule is monthly and 30+ days have elapsed", "a schedule check off Wi-Fi with no allowCellular override only updates the 'connect to Wi-Fi' status and never starts the multi-MB delta download".

    `reference_pack_delta_apply_test.dart` (→ filled by Plan 09-04): use `testWidgets` (this project's `integration_test` files follow the same testWidgets-based convention as the rest of the suite, per 09-RESEARCH.md's Validation Architecture) wrapped in a skipped `group('DeltaApplier', ...)`. Cases: "applying a delta with new/changed rows makes them immediately findable via products_fts text search (09-RESEARCH.md Pitfall 1)", "applying a delta with deleted_barcodes removes the corresponding row from both products and products_fts", "delta apply runs inside a single transaction — a mid-apply failure leaves the previously-attached data intact (all-or-nothing)".
  </action>
  <verify>
    <automated>flutter test test/app_lifecycle_reference_pack_test.dart integration_test/reference_pack_delta_apply_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>Both files exist, `flutter test` reports them as skipped (0 failures).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan creates only skipped test scaffolding, no production code, no data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | N/A | Test scaffolding only | accept | No production code or data path introduced by this plan |
</threat_model>

<verification>
`flutter test test/data/local/reference_pack/ test/features/settings/reference_data_row_test.dart test/features/reference_data/ test/app_lifecycle_reference_pack_test.dart integration_test/reference_pack_delta_apply_test.dart` reports 0 failures, all cases skipped with an informative reason string.
</verification>

<success_criteria>
All seven Wave 0 stub files exist, are discovered by `flutter test`, and are cleanly skipped (not failing) — unblocking every later Phase 9 plan's `<verify>` command.
</success_criteria>

<output>
Create `.planning/phases/09-reference-data-delivery-full-off-pack/09-01-SUMMARY.md` when done
</output>
