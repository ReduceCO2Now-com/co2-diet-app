---
phase: 09-reference-data-delivery-full-off-pack
plan: 06
type: execute
wave: 6
depends_on: ["09-05"]
files_modified:
  - lib/features/reference_data/providers/reference_pack_schedule_provider.dart
  - lib/app.dart
  - lib/features/reference_data/providers/reference_pack_notifier.dart
  - lib/features/reference_data/screens/reference_data_screen.dart
  - test/app_lifecycle_reference_pack_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "A user can choose Manual/Weekly/Monthly delta-refresh scheduling, honestly described as a foreground app-open check rather than a true background timer (09-CONTEXT.md: no workmanager/background_fetch/Timer.periodic exists in this app)"
    - "Weekly/Monthly checks fire on app resume only when the configured interval has actually elapsed, throttled the same way Plan 05-13's weigh-in reminder re-arm already is"
    - "Reverting to the starter pack resets the schedule back to Manual, and the delta-refresh completion path never shows a Dashboard-level banner (09-CONTEXT.md: explicitly rejected, unlike the CO2 methodology banner)"
  artifacts:
    - path: "lib/features/reference_data/providers/reference_pack_schedule_provider.dart"
      provides: "ReferencePackScheduleNotifier: SharedPreferences-backed schedule + lastCheckedAt + throttled due-check logic"
  key_links:
    - from: "lib/app.dart"
      to: "lib/features/reference_data/providers/reference_pack_schedule_provider.dart"
      via: "AppLifecycleState.resumed observer calls ReferencePackScheduleNotifier.isCheckDue then ReferencePackNotifier.checkForUpdateIfDue"
      pattern: "AppLifecycleState.resumed"
---

<objective>
Wire the foreground-triggered Weekly/Monthly delta-refresh check into Co2DietApp's existing AppLifecycleState.resumed observer (the same pattern Plan 05-13/05-18 already established for the weigh-in reminder re-arm), and make revertToSeed reset the schedule back to Manual -- closing out every remaining locked decision in 09-CONTEXT.md's "Delta Refresh: Schedule & Prompting" section.

Purpose: 09-CONTEXT.md is explicit that "no true background scheduler exists in this app" and that this is "an honest constraint to design around, not a corner cut." Reusing the exact AppLifecycleState.resumed + throttled-interval pattern this codebase already ships (rather than inventing a second scheduling mechanism) keeps that honesty and avoids Phase 5's own flagged gap (STATE.md Pre-Launch Blockers: BackupMetadata's autoBackupFrequency scheduling "was never fully wired").

Output: ReferencePackScheduleNotifier, an extended Co2DietApp lifecycle observer, a checkForUpdateIfDue() addition to ReferencePackNotifier, and a schedule-selection control on ReferenceDataScreen -- with app_lifecycle_reference_pack_test.dart turned green.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reference-data-delivery-full-off-pack/09-CONTEXT.md
@lib/app.dart
@lib/features/dashboard/providers/methodology_banner_provider.dart
@lib/features/onboarding/providers/onboarding_gate_provider.dart
@lib/domain/entities/reference_pack_status.dart
@lib/features/reference_data/providers/reference_pack_notifier.dart
@lib/features/reference_data/screens/reference_data_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReferencePackScheduleNotifier</name>
  <files>lib/features/reference_data/providers/reference_pack_schedule_provider.dart</files>
  <action>
    Create lib/features/reference_data/providers/reference_pack_schedule_provider.dart: a keepAlive Riverpod notifier class ReferencePackScheduleNotifier, mirroring MethodologyBannerDismissalNotifier's SharedPreferences-backed keepAlive pattern exactly (build() reads ref.watch(sharedPreferencesProvider) synchronously, no async work). Persists two keys: referencePackSchedule (String, one of ReferencePackSchedule.manual/weekly/monthly's name value, default manual) and referencePackLastCheckedAt (String, ISO8601, nullable/absent meaning "never checked"). build() returns a small record/class holding schedule (ReferencePackSchedule) and lastCheckedAt (DateTime, nullable).

    Method setSchedule(schedule) persists and updates state synchronously (same pattern as MethodologyBannerDismissalNotifier.dismiss). Method recordCheckedNow() persists DateTime.now() as lastCheckedAt. Method resetToManual() persists schedule back to manual -- called by revertToSeed's flow, per 09-CONTEXT.md: "Reverting to the starter pack resets the schedule setting back to 'Manual only'" (stated twice in CONTEXT.md, once under Delta Refresh and once under Revert-to-Seed Behavior -- confirming this is the same single rule, not two).

    Add a pure, side-effect-free top-level or static function isCheckDue(schedule, lastCheckedAt, now) (easily unit-testable without any SharedPreferences/widget setup) that returns false for schedule == manual always; for weekly, true when lastCheckedAt is null or the gap to now is 7 days or more; for monthly, true when lastCheckedAt is null or the gap to now is 30 days or more. This mirrors WeighInReminderSection/Co2DietApp's existing "next occurrence computed fresh from DateTime.now()" honesty framing, not a persisted next-fire-time.
  </action>
  <verify>
    <automated>flutter analyze lib/features/reference_data/providers/reference_pack_schedule_provider.dart</automated>
  </verify>
  <done>ReferencePackScheduleNotifier compiles clean, persists schedule + lastCheckedAt through SharedPreferences, and isCheckDue is a pure function with no I/O.</done>
</task>

<task type="auto">
  <name>Task 2: app.dart wiring + notifier/screen extensions + turn lifecycle stub green</name>
  <files>
    lib/app.dart,
    lib/features/reference_data/providers/reference_pack_notifier.dart,
    lib/features/reference_data/screens/reference_data_screen.dart,
    test/app_lifecycle_reference_pack_test.dart
  </files>
  <action>
    In lib/app.dart's existing didChangeAppLifecycleState override, after the weigh-in reminder re-arm block already there, add a second independent check (this feature must not depend on or interfere with the weigh-in reminder logic already present): read ReferencePackScheduleNotifier's current schedule/lastCheckedAt via ref.read, and if isCheckDue(...) returns true, call an unawaited ref.read(referencePackNotifierProvider.notifier).checkForUpdateIfDue() then ref.read(referencePackScheduleProvider.notifier).recordCheckedNow() -- record the check timestamp regardless of whether an update was found, so a manifest that returns "no update" still counts as a completed check for throttling purposes.

    Add checkForUpdateIfDue() to ReferencePackNotifier: fetches the manifest via checkForUpdate() (the lightweight, any-connection version-check ping per 09-CONTEXT.md), compares against installedVersion(); if a newer version exists, updates state to ReferencePackUpdateAvailable with waitingForWifi computed from isOnWifi(). Critically, this method never starts the actual multi-MB delta download itself when off Wi-Fi -- it only updates status to surface "Update available — connect to Wi-Fi" (09-CONTEXT.md: "only the actual multi-MB delta payload waits for Wi-Fi"). When on Wi-Fi and the schedule is weekly/monthly (i.e. this call originated from the automatic scheduled path, not a manual tap), proceed to call startDeltaDownload(allowCellular: false) directly with no per-refresh confirmation dialog (09-CONTEXT.md: "Once automatic refresh is enabled, refreshes run quietly on Wi-Fi without a per-refresh confirmation dialog"). Do not surface any Dashboard-level banner or notification on completion (09-CONTEXT.md: explicitly rejected, unlike the Phase 7 CO2 methodology banner) -- the Settings row's live-updating subtitle from Plan 09-05 is the only visibility this needs.

    In lib/features/reference_data/screens/reference_data_screen.dart, add a schedule-selection control (SegmentedButton&lt;ReferencePackSchedule&gt; with Manual/Weekly/Monthly segments, mirroring BackupRestoreScreen's existing Automatic Backups SegmentedButton exactly) bound to ref.watch(referencePackScheduleProvider) / ref.read(referencePackScheduleProvider.notifier).setSchedule(...), visible only in the Full state (scheduling a refresh only makes sense once the full catalog is actually installed -- CONTEXT.md's Delta Refresh section only ever discusses refreshing the already-downloaded full catalog). Wire the screen's existing Revert confirmation handler (Plan 09-05 Task 3) to also call ref.read(referencePackScheduleProvider.notifier).resetToManual() immediately after a successful revertToSeed(), completing CONTEXT.md's "reverting resets the schedule to Manual" rule end-to-end.

    Turn app_lifecycle_reference_pack_test.dart green: replace the skip group with real unit tests exercising isCheckDue directly (pure function, no widget pump needed) plus one integration-style test using ProviderContainer to prove Co2DietApp's didChangeAppLifecycleState calls checkForUpdateIfDue only when due, per the exact cases already named in Plan 09-01's stub.
  </action>
  <verify>
    <automated>flutter test test/app_lifecycle_reference_pack_test.dart</automated>
  </verify>
  <done>app_lifecycle_reference_pack_test.dart is green with zero skips. Co2DietApp's resumed handler checks for a reference-pack update only when the configured schedule's interval has elapsed. Reverting resets the schedule to manual. No Dashboard banner exists for this feature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new -- this plan wires existing, already-verified collaborators together on a lifecycle event. No new untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Denial of Service | Repeated app-resume triggering excessive manifest checks | accept | The lightweight manifest GET is a tiny payload per 09-CONTEXT.md's own framing; isCheckDue's interval gating (7/30 days) already prevents any realistic abuse pattern from a single local user's own app-resume cadence |
</threat_model>

<verification>
flutter test test/app_lifecycle_reference_pack_test.dart green with zero skips. flutter analyze reports zero new lint errors across all four modified files. flutter test (full suite) still green -- confirms the new lifecycle-observer addition does not regress the existing weigh-in reminder re-arm logic it sits alongside.
</verification>

<success_criteria>
Weekly/Monthly delta-refresh scheduling works end-to-end through the same honest, foreground-triggered mechanism this codebase already established for weigh-in reminders. Reverting to the starter pack always resets the schedule to Manual. No Dashboard-level interruption exists for this feature, matching 09-CONTEXT.md's explicit rejection of one.
</success_criteria>

<output>
Create `.planning/phases/09-reference-data-delivery-full-off-pack/09-06-SUMMARY.md` when done
</output>
