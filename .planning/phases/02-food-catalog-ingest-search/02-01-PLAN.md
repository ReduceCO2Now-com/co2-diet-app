---
phase: 02-food-catalog-ingest-search
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - build.yaml
  - test/data/local/food_catalog_dao_test.dart
  - test/data/repositories/food_catalog_repository_test.dart
  - test/features/food_search/food_search_notifier_test.dart
  - integration_test/food_search_benchmark_test.dart
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - NFR-06

must_haves:
  truths:
    - "flutter test test/data/local/food_catalog_dao_test.dart exits 0 (stub tests marked pending or passing)"
    - "flutter test test/data/repositories/food_catalog_repository_test.dart exits 0 (stubs)"
    - "flutter test test/features/food_search/food_search_notifier_test.dart exits 0 (stubs)"
    - "flutter test integration_test/food_search_benchmark_test.dart exits 0 on emulator/device (stubs skip if DB absent)"
    - "build.yaml exists with drift_dev options including fts5 module"
    - "integration_test/ directory exists and contains the benchmark file"
  artifacts:
    - path: "build.yaml"
      provides: "FTS5 module configuration for drift_dev code generation"
      contains: "fts5"
    - path: "test/data/local/food_catalog_dao_test.dart"
      provides: "Wave 0 stub test for FTS5 DAO — covers LOG-01"
    - path: "test/data/repositories/food_catalog_repository_test.dart"
      provides: "Wave 0 stub test for fallback + caching — covers LOG-02"
    - path: "test/features/food_search/food_search_notifier_test.dart"
      provides: "Wave 0 stub test for debounce + state transitions"
    - path: "integration_test/food_search_benchmark_test.dart"
      provides: "Wave 0 benchmark stub — covers LOG-01 timing, NFR-06a hit-rate"
  key_links:
    - from: "build.yaml"
      to: "drift_dev"
      via: "builders: drift_dev: options: sql: options: modules: [fts5]"
      pattern: "fts5"
    - from: "integration_test/food_search_benchmark_test.dart"
      to: "IntegrationTestWidgetsFlutterBinding"
      via: "IntegrationTestWidgetsFlutterBinding.ensureInitialized()"
      pattern: "IntegrationTestWidgetsFlutterBinding"
---

<objective>
Create the Wave 0 test scaffolds required by VALIDATION.md and the build.yaml FTS5 configuration that drift_dev needs for code generation. These stubs gate all subsequent plans — no Wave 1 task should start until these files exist and pass.

Purpose: VALIDATION.md lists 6 Wave 0 gaps (4 test files + integration_test/ dir + build.yaml). The stub tests use `markTestPending()` or `skip:` for behaviors that cannot be tested until the real implementation exists in later waves. The benchmark test skips gracefully when `off_reference.sqlite` is absent.

Output: `build.yaml`, 3 unit test stub files under `test/`, 1 integration test stub under `integration_test/`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-food-catalog-ingest-search/02-VALIDATION.md
@.planning/phases/02-food-catalog-ingest-search/02-RESEARCH.md
@lib/data/local/app_database.dart
@test/data/local/user_profile_dao_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: build.yaml with FTS5 module config</name>
  <files>build.yaml</files>
  <action>Create `build.yaml` in the project root. The file must configure `drift_dev` with the FTS5 SQLite module so that `.drift` files containing FTS5 virtual table declarations are parsed without errors. Use the exact structure documented in the Drift generation options reference. Set `dialect: sqlite`, `version: "3.34"`, and `modules: [fts5]` under `drift_dev` builder options. This file does not yet need to reference any new `.drift` files — it provides the global FTS5 permission for all Drift code generation going forward. Do not include any other builders; only the `drift_dev` entry is needed.</action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | grep -i "error" | grep -v "^#" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>build.yaml exists at project root; `dart run build_runner build --delete-conflicting-outputs` completes without new errors introduced by this file.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 unit test stubs (DAO, Repository, Notifier)</name>
  <files>
    test/data/local/food_catalog_dao_test.dart,
    test/data/repositories/food_catalog_repository_test.dart,
    test/features/food_search/food_search_notifier_test.dart
  </files>
  <action>Create three unit test stub files. Each file must compile cleanly and exit 0 under `flutter test` even though the production types they test do not yet exist. Use the `skip: 'Wave 0 stub — implementation pending'` argument on `group()` or individual `test()` calls to mark tests as pending without failing. Do NOT import any production files that do not exist yet (they will be created in Plans 02-02 through 02-05); instead, declare the expected class/function names as comments in the test body explaining what each test will verify once implemented.

  File 1 — `test/data/local/food_catalog_dao_test.dart`:
  - Top-level `group('FoodCatalogDao', skip: 'Wave 0 stub — FoodCatalogDao not yet implemented', () { ... })` block.
  - Inside: test('searchLocalFoods returns FoodItem list for valid query'), test('searchLocalFoods returns empty list for no-match query'), test('searchLocalFoods returns results from off_ref ATTACH alias').
  - No production imports. Import only `package:flutter_test/flutter_test.dart`.

  File 2 — `test/data/repositories/food_catalog_repository_test.dart`:
  - `group('FoodCatalogRepository', skip: 'Wave 0 stub — FoodCatalogRepository not yet implemented', () { ... })`.
  - Inside: test('searchLocal returns local results without triggering API'), test('searchAndCache fires OFF API when local results empty'), test('searchAndCache writes API results to co2diet.sqlite user-catalog tables'), test('searchAndCache returns NetworkError state on API failure').
  - No production imports.

  File 3 — `test/features/food_search/food_search_notifier_test.dart`:
  - `group('FoodSearchNotifier', skip: 'Wave 0 stub — FoodSearchNotifier not yet implemented', () { ... })`.
  - Inside: test('initial state is FoodSearchState.prompt'), test('query shorter than 2 chars emits prompt state'), test('valid query after 300ms debounce emits results state'), test('valid query with no local results and offline emits offlineNoResults'), test('valid query with no local results and online emits loading then results from API').
  - No production imports.

  Create the `test/features/food_search/` directory as needed (the file creation will create it). Create `test/data/repositories/` directory as needed.</action>
  <verify>
    <automated>flutter test test/data/local/food_catalog_dao_test.dart test/data/repositories/food_catalog_repository_test.dart test/features/food_search/food_search_notifier_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>All three test files compile and exit 0. Flutter test output shows 0 failures; skipped tests are listed as "SKIP".</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 integration test benchmark stub</name>
  <files>integration_test/food_search_benchmark_test.dart</files>
  <action>Create the `integration_test/food_search_benchmark_test.dart` file. The `integration_test/` directory was already created by `pubspec.yaml` having `integration_test: sdk: flutter` in dev_dependencies (directory may need creating manually).

  The stub must:
  1. Call `IntegrationTestWidgetsFlutterBinding.ensureInitialized()` at the start of `main()`.
  2. Declare three `testWidgets` cases matching the three benchmark cases from VALIDATION.md: BM-01 (worst-case 2-char query `"mi"` — must complete in <1s), BM-02 (full-word `"banana"` — <1s), BM-03 (no-local-result query `"xyzzy_noresult_42"` — <1s).
  3. Each test body must check `const bool dbReady = false;` and `if (!dbReady) { markTestSkipped('off_reference.sqlite not yet bundled — Wave 0 stub'); return; }` so the test exits gracefully without the DB file present.
  4. Below the skip guard, add the Stopwatch measurement pattern (commented out) showing the intent: create a `FoodCatalogRepository`, call `searchLocal(query)`, assert `elapsed < 1000ms`, assert result count as appropriate.

  Imports needed: `package:flutter_test/flutter_test.dart`, `package:integration_test/integration_test.dart`. No production imports (they do not exist yet).

  The file structure must match what `flutter test integration_test/food_search_benchmark_test.dart` expects when run against a connected device or emulator.</action>
  <verify>
    <automated>flutter test integration_test/food_search_benchmark_test.dart --plain-name "BM-01" 2>&1 | grep -E "SKIP|passed|No tests" | head -3</automated>
  </verify>
  <done>`integration_test/food_search_benchmark_test.dart` exists; running it on an emulator or device shows all three BM tests skipped (not failing) due to the `dbReady = false` guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production code | Test stubs must not import non-existent files that would cause compile errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | build.yaml (no new package installs in this plan) | accept | No packages installed; build.yaml is a config file only — no supply-chain risk |
</threat_model>

<verification>
Run after all tasks complete:

```bash
flutter test test/data/local/food_catalog_dao_test.dart \
             test/data/repositories/food_catalog_repository_test.dart \
             test/features/food_search/food_search_notifier_test.dart
```

Expected: 0 failures, all groups skipped.

```bash
cat build.yaml | grep -c fts5
```

Expected: 1 (fts5 entry present).
</verification>

<success_criteria>
- build.yaml exists with `modules: [fts5]` under drift_dev options
- 3 unit test stub files exist and pass `flutter test` (all skipped, no failures)
- integration_test/food_search_benchmark_test.dart exists and runs without crashing (tests self-skip)
- `dart run build_runner build --delete-conflicting-outputs` still exits 0
</success_criteria>

<output>
Create `.planning/phases/02-food-catalog-ingest-search/02-01-SUMMARY.md` when done.
</output>
