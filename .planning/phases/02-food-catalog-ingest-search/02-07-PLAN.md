---
phase: 02-food-catalog-ingest-search
plan: 07
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - test/data/local/food_catalog_dao_test.dart
  - test/data/repositories/food_catalog_repository_test.dart
  - test/features/food_search/food_search_notifier_test.dart
  - integration_test/food_search_benchmark_test.dart
autonomous: false
requirements:
  - LOG-01
  - LOG-02
  - NFR-06

must_haves:
  truths:
    - "flutter test test/ exits 0 — all unit tests green including food catalog DAO and repository tests"
    - "FoodCatalogDao.searchLocalFoods on an in-memory DB with test fixture returns correct FoodItem list"
    - "FoodCatalogDao._sanitizeFts5Query correctly strips metacharacters in unit test"
    - "FoodCatalogRepository fires searchAndCache when searchLocal returns empty, per mock test"
    - "FoodCatalogRepository caches API results to DB in unit test (mock OffApiClient)"
    - "FoodSearchNotifier debounce test: fast successive calls result in exactly one repository call"
    - "FoodSearchNotifier offline test: connectivity none → FoodSearchOfflineNoResults state"
    - "Benchmark integration test BM-01, BM-02, BM-03 all pass on physical Android device (< 1s each)"
    - "NFR-06a manual verification: >90% of the 200-food benchmark list returns a local result"
  artifacts:
    - path: "test/data/local/food_catalog_dao_test.dart"
      provides: "Unit tests for FTS5 DAO search (real implementation replacing Wave 0 stubs)"
    - path: "test/data/repositories/food_catalog_repository_test.dart"
      provides: "Unit tests for searchLocal + searchAndCache (mock OffApiClient)"
    - path: "test/features/food_search/food_search_notifier_test.dart"
      provides: "Unit tests for debounce, state transitions, offline, retry"
    - path: "integration_test/food_search_benchmark_test.dart"
      provides: "Benchmark tests BM-01, BM-02, BM-03 measuring real query timing"
  key_links:
    - from: "test/data/local/food_catalog_dao_test.dart"
      to: "lib/data/local/app_database.dart"
      via: "AppDatabase(NativeDatabase.memory()) — no off_ref attached in unit test"
      pattern: "NativeDatabase.memory"
    - from: "integration_test/food_search_benchmark_test.dart"
      to: "lib/data/local/app_database.dart"
      via: "AppDatabase.connect(offRefPath: ...) with real off_reference.sqlite"
      pattern: "AppDatabase.connect"
---

<objective>
Replace Wave 0 test stubs with real implementations: unit tests for `FoodCatalogDao`, `FoodCatalogRepository`, and `FoodSearchNotifier`; and real benchmark tests measuring `<1s` query time. Then checkpoint for physical-device benchmark verification (NFR-06a LOG-01 acceptance criterion).

Purpose: VALIDATION.md marks these as the final quality gate for Phase 2. The benchmark integration tests on physical Android are the ONLY valid proof that LOG-01 and NFR-06 are satisfied — emulator results are explicitly excluded per RESEARCH.md Environment Availability.

Output: 4 test files with real assertions. Human checkpoint to verify physical-device benchmark results.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-food-catalog-ingest-search/02-VALIDATION.md
@.planning/phases/02-food-catalog-ingest-search/02-RESEARCH.md
@lib/data/local/daos/food_catalog_dao.dart
@lib/data/repositories/food_catalog_repository.dart
@lib/features/food_search/providers/food_search_notifier.dart
@lib/features/food_search/providers/food_search_state.dart
@lib/domain/entities/food_item.dart
@test/data/local/user_profile_dao_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real unit tests for DAO, Repository, and Notifier</name>
  <files>
    test/data/local/food_catalog_dao_test.dart,
    test/data/repositories/food_catalog_repository_test.dart,
    test/features/food_search/food_search_notifier_test.dart
  </files>
  <behavior>
    - FoodCatalogDao._sanitizeFts5Query('"apple"') → 'apple*' (quotes stripped)
    - FoodCatalogDao._sanitizeFts5Query('apple orange') → 'apple* orange*'
    - FoodCatalogDao._sanitizeFts5Query('   ') → '' (empty after trim)
    - FoodCatalogDao._sanitizeFts5Query('appl*') → 'appl*' (existing * not doubled; cleaned first then * added)
    - FoodCatalogDao.searchLocalFoods on in-memory DB without off_ref attached returns []
    - FoodCatalogRepository.searchLocal delegates to DAO and returns its result
    - FoodCatalogRepository.searchAndCache calls OffApiClient.searchOff when local returns []
    - FoodCatalogRepository.searchAndCache writes mock API results to UserFoodCacheTable
    - FoodCatalogRepository.searchAndCache throws NetworkException when OffApiClient throws SocketException
    - FoodSearchNotifier initial state is FoodSearchPrompt
    - FoodSearchNotifier.onQueryChanged('a') emits FoodSearchPrompt immediately (no timer)
    - FoodSearchNotifier offline: searchLocal returns [], connectivity=none → FoodSearchOfflineNoResults
    - FoodSearchNotifier networkError: searchLocal returns [], online, searchAndCache throws NetworkException → FoodSearchNetworkError
  </behavior>
  <action>Replace the Wave 0 stubs in all three test files with real assertions. Follow the pattern from `test/data/local/user_profile_dao_test.dart` for Drift in-memory tests.

  FILE 1 — `test/data/local/food_catalog_dao_test.dart`:
  - Import: `package:drift/native.dart`, `package:flutter_test/flutter_test.dart`, `package:co2diet/data/local/app_database.dart`, `package:co2diet/data/local/daos/food_catalog_dao.dart`, `package:co2diet/domain/entities/food_item.dart`.
  - Use `flutter test` (not bare `dart test`) because `app_database.dart` imports `drift_flutter` which imports `dart:ui` — same pattern as Phase 1 DAO tests (see STATE.md decision: "flutter test required for Drift DAO tests").
  - Setup: `late AppDatabase db; setUp(() { db = AppDatabase(NativeDatabase.memory()); }); tearDown(() async => db.close());`
  - Test group: `FoodCatalogDao`:
    - Test `_sanitizeFts5Query strips metacharacters`: invoke via `db.foodCatalogDao.searchLocalFoods('"apple"')` (which internally sanitizes). Alternatively, if `_sanitizeFts5Query` is private, test it indirectly — a query that would be malformed if not sanitized should return [] not throw. Use `expect(() async => db.foodCatalogDao.searchLocalFoods('"apple"'), returnsNormally)`.
    - Test `searchLocalFoods returns empty on fresh DB`: `final results = await db.foodCatalogDao.searchLocalFoods('banana'); expect(results, isEmpty);` — no off_ref attached in test, so empty list is the expected result.
    - Test `searchLocalFoods returns empty for 1-char input`: test that the DAO does not crash on short input; the 2-char minimum is enforced in the notifier, not the DAO, but the DAO should handle it gracefully.

  FILE 2 — `test/data/repositories/food_catalog_repository_test.dart`:
  - Import: `package:mocktail/mocktail.dart`, `package:drift/native.dart`, `package:flutter_test/flutter_test.dart`, `package:co2diet/data/repositories/food_catalog_repository.dart`, `package:co2diet/data/remote/off_api_client.dart`, `package:co2diet/domain/entities/food_item.dart`.
  - Mock: `class MockOffApiClient extends Mock implements OffApiClient {}` (use mocktail, already in pubspec dev_dependencies).
  - Setup: create `AppDatabase(NativeDatabase.memory())`, get `foodCatalogDao` from it, create `MockOffApiClient`, create `FoodCatalogRepository(dao, mock)`.
  - Test `searchLocal delegates to DAO`: call `repo.searchLocal('banana')`, expect `isA<List<FoodItem>>()` (empty is fine — no data in test DB).
  - Test `searchAndCache fires API when local empty`: `when(() => mock.searchOff('xyz')).thenAnswer((_) async => [FoodItem(productName: 'Xylosoup', barcode: '111')]); final result = await repo.searchAndCache('xyz'); verify(() => mock.searchOff('xyz')).called(1); expect(result, hasLength(1));`
  - Test `searchAndCache writes to DB`: after `repo.searchAndCache('xyz')`, check that `db.select(db.userFoodCacheTable).get()` returns 1 row.
  - Test `searchAndCache throws NetworkException on SocketException`: `when(() => mock.searchOff(any())).thenThrow(SocketException('Connection failed')); expect(() => repo.searchAndCache('xyz'), throwsA(isA<NetworkException>()));`
  - `tearDown`: close DB.

  FILE 3 — `test/features/food_search/food_search_notifier_test.dart`:
  - Notifier testing with Riverpod 3 requires `ProviderContainer`. Use `ProviderContainer` with mock overrides for `foodCatalogRepositoryProvider` and `connectivity`.
  - Import: `package:mocktail/mocktail.dart`, `package:flutter_riverpod/flutter_riverpod.dart`, `package:flutter_test/flutter_test.dart`, `package:co2diet/features/food_search/providers/food_search_notifier.dart`, `package:co2diet/features/food_search/providers/food_search_state.dart`, `package:co2diet/domain/repositories/i_food_catalog_repository.dart`.
  - `class MockFoodCatalogRepository extends Mock implements IFoodCatalogRepository {}`
  - Test `initial state is FoodSearchPrompt`: `expect(container.read(foodSearchNotifierProvider).value, isA<FoodSearchPrompt>());`
  - Test `onQueryChanged with 1-char emits prompt`: call `notifier.onQueryChanged('a')`, immediately check state is `FoodSearchPrompt` without awaiting any timer (no `pumpEventQueue` needed — state is synchronous for prompt emission).
  - Test `offline → offlineNoResults`: set up mock repo to return `[]` for `searchLocal`; override connectivity to return `[ConnectivityResult.none]` if possible (connectivity_plus makes this hard to mock directly — use an alternative: mock the repository such that it always returns [], and test that when an injected `checkConnectivity` returns none, the state is offlineNoResults). If direct connectivity mock is complex, use a simpler integration: set up mock repo, call `notifier.onQuerySubmitted('banana')`, pump event queue, and check state matches. Note: this test may need to be adjusted based on how `Connectivity()` is called in the notifier — if it's called as `Connectivity().checkConnectivity()` directly (not injectable), the connectivity test must be done in integration rather than pure unit test. In that case, mark as `skip: 'requires connectivity mock injection — architectural note for Phase 2+'` and add a TODO to make connectivity injectable.
  - Test `networkError state on NetworkException`: mock repo's `searchLocal` to return `[]`, mock `searchAndCache` to throw `NetworkException('test error')`. Mock connectivity to return online (if possible). Fire `onQuerySubmitted('banana')`, pump queue, expect `FoodSearchNetworkError`.
  - Use `pumpEventQueue()` + `pump(Duration(milliseconds: 350))` where needed for timer-based tests.

  Run `flutter test test/` after creating all three files.</action>
  <verify>
    <automated>flutter test test/data/local/food_catalog_dao_test.dart test/data/repositories/food_catalog_repository_test.dart test/features/food_search/food_search_notifier_test.dart 2>&1 | tail -10</automated>
  </verify>
  <done>All three unit test files pass `flutter test` with 0 failures. Tests replaced the Wave 0 stubs with real assertions. Any tests that require injectable dependencies not yet available are marked `skip:` with a documented TODO, not deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Real benchmark integration tests</name>
  <files>integration_test/food_search_benchmark_test.dart</files>
  <action>Replace the Wave 0 stub in `integration_test/food_search_benchmark_test.dart` with real benchmark tests. These tests require `off_reference.sqlite` to be present (produced by `tools/ingest_off.py`). The executor must check whether the DB file exists in the assets directory before proceeding — if `assets/off_reference.sqlite.gz` does not yet exist (the OFF ingest has not been run), the tests must retain the graceful skip guard.

  UPDATED BENCHMARK FILE:

  Structure:
  1. `IntegrationTestWidgetsFlutterBinding.ensureInitialized()` at top of main.
  2. A helper `Future<FoodCatalogRepository?> _buildTestRepo()` that:
     - Calls `ensureOffReferenceDb()` to get the off_reference path.
     - Creates `AppDatabase.connect(offRefPath: offRefPath)`.
     - Returns `FoodCatalogRepository(db.foodCatalogDao, OffApiClient())`.
     - Returns `null` if `ensureOffReferenceDb()` throws (catches `StateError` with 'assets/off_reference.sqlite.gz not found').
  3. Three `testWidgets` cases with Stopwatch timing:

     BM-01 (worst-case 2-char prefix):
     ```dart
     testWidgets('BM-01: worst-case 2-char prefix query < 1s', (tester) async {
       final repo = await _buildTestRepo();
       if (repo == null) { markTestSkipped('off_reference.sqlite not available'); return; }
       final sw = Stopwatch()..start();
       final results = await repo.searchLocal('mi');
       sw.stop();
       expect(sw.elapsedMilliseconds, lessThan(1000),
           reason: 'Worst-case 2-char query must complete in < 1s (was ${sw.elapsedMilliseconds}ms)');
       expect(results, isNotEmpty, reason: '"mi" must return results in a DE-filtered OFF subset');
     });
     ```

     BM-02 (full-word query):
     ```dart
     testWidgets('BM-02: full-word query < 1s', (tester) async {
       final repo = await _buildTestRepo();
       if (repo == null) { markTestSkipped('off_reference.sqlite not available'); return; }
       final sw = Stopwatch()..start();
       final results = await repo.searchLocal('banana');
       sw.stop();
       expect(sw.elapsedMilliseconds, lessThan(1000));
     });
     ```

     BM-03 (no-local-result — measures local search time for empty-result query):
     ```dart
     testWidgets('BM-03: no-local-result query completes in < 1s (measures local path only)', (tester) async {
       final repo = await _buildTestRepo();
       if (repo == null) { markTestSkipped('off_reference.sqlite not available'); return; }
       final sw = Stopwatch()..start();
       final results = await repo.searchLocal('xyzzy_noresult_42');
       sw.stop();
       expect(sw.elapsedMilliseconds, lessThan(1000));
       expect(results, isEmpty, reason: 'This term must not match in the local DB');
     });
     ```

  4. After BM-01/02/03, add a `testWidgets('NFR-06a hit-rate benchmark', ...)` test that:
     - Skips with markTestSkipped if repo is null.
     - Defines a `const List<String> benchmarkFoods` of 20 representative EU/German foods (a proxy for the 200-food list; the executor should choose 20 realistic terms from: 'Milch', 'Butter', 'Joghurt', 'Käse', 'Brot', 'Banane', 'Apfel', 'Nudeln', 'Reis', 'Kartoffeln', 'Huhn', 'Rindfleisch', 'Lachs', 'Tofu', 'Haferflocken', 'Sonnenblumenöl', 'Tomaten', 'Gurke', 'Ei', 'Zucker').
     - For each term, calls `repo.searchLocal(term)`.
     - Counts how many return `isNotEmpty`.
     - `expect(hitCount / benchmarkFoods.length, greaterThanOrEqualTo(0.90), reason: 'NFR-06a: ≥90% hit rate required. Got ${hitCount}/${benchmarkFoods.length}')`.
     - NOTE: This test measures the subset of 20 terms, not the full 200-food benchmark. The full 200-food list is a manual pre-launch acceptance test; this automated test is an early proxy.

  Imports needed: `package:flutter_test/flutter_test.dart`, `package:integration_test/integration_test.dart`, `package:co2diet/data/local/app_database.dart`, `package:co2diet/data/repositories/food_catalog_repository.dart`, `package:co2diet/data/remote/off_api_client.dart`, `package:co2diet/core/assets/first_launch_extractor.dart`.</action>
  <verify>
    <automated>flutter analyze integration_test/food_search_benchmark_test.dart 2>&1 | grep -c " error " || true</automated>
  </verify>
  <done>Benchmark test file compiles with 0 analyzer errors. When `off_reference.sqlite.gz` is absent, tests skip gracefully. When the DB is present, tests run Stopwatch-bounded assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 2 code is complete: OFF ingest pipeline, Drift FTS5 data layer, FoodCatalogRepository with OFF API fallback and caching, FoodSearchNotifier with debounce and state transitions, search screen UI with all state variants, and benchmark integration tests.</what-built>
  <how-to-verify>
    REQUIRED: You must have run `tools/ingest_off.py` and have `assets/off_reference.sqlite.gz` available, and have a physical Android device connected (Pixel 6a or Samsung A54 class per RESEARCH.md).

    Step 1 — Confirm the DB is available:
    `ls -lh assets/off_reference.sqlite.gz` — must show a file ≤50 MB.

    Step 2 — Run the unit test suite:
    `flutter test test/`
    Expected: 0 failures, all tests green.

    Step 3 — Connect physical Android device and run benchmark integration tests:
    `flutter test integration_test/food_search_benchmark_test.dart --device-id <your-device-id>`
    (Get device ID from: `flutter devices`)

    Expected results:
    - BM-01 ("mi" query): PASS — elapsed < 1000ms, results non-empty
    - BM-02 ("banana" query): PASS — elapsed < 1000ms
    - BM-03 ("xyzzy_noresult_42"): PASS — elapsed < 1000ms, results empty
    - NFR-06a (20-food hit rate): PASS — ≥90% of benchmark foods return results

    Step 4 — Smoke-test the search UI on device:
    `flutter run`
    - Navigate to Settings → "Search foods" → `/food-search` opens with keyboard auto-focused.
    - Type "mil" → results appear instantly (NO shimmer/loading indicator).
    - Tap a result → bottom sheet shows name, brand, macros. No CO₂ row.
    - Clear field (✕ button) → prompt state returns.
    - Enable airplane mode → search a term → "No results — connect to the internet to search more" shown.
    - Re-enable network → search an unusual term with no local result → shimmer + "Searching online..." → API results appear.

    Step 5 — Confirm first-launch decompression works:
    Uninstall the app, reinstall, and confirm the "Setting up food database..." splash appears on first launch only.

    If any benchmark test FAILS timing (>1000ms):
    - Check whether the FTS5 prefix indexes are in place: `sqlite3 off_reference.sqlite "PRAGMA index_list(products_fts_data);"` — should show fts5 index entries.
    - Check DB size: a very large DB (close to 50 MB) may have slower query times on budget Android hardware.
    - Options: increase completeness threshold (0.7+) to reduce DB size, or restrict countries to DE/AT/CH/CH only.
    - Do NOT declare success if benchmark times exceed 1000ms — LOG-01 is a hard acceptance criterion.
  </how-to-verify>
  <resume-signal>Type "approved" if all benchmark tests pass on physical device and the UI smoke-test passes. Type "failed: [describe what failed]" to surface issues for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → real device hardware | Benchmark tests measure real SQLite performance on real Android hardware; emulator results not accepted |
| benchmark food list → FTS5 | Food names in the benchmark list are controlled test data; no sanitization concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | SocketException mock in repository test | accept | Only in test environment; mock validates error path without real network exposure |
| T-02-07-02 | Denial of Service | NFR-06a 20-food loop in integration test | accept | 20 sequential FTS5 queries on indexed DB; total time expected well under 20s; no risk of device overload |
| T-02-07-SC | Tampering | No new package installs in this plan | accept | Test-only code using existing test infrastructure; no supply-chain risk |
</threat_model>

<verification>
```bash
flutter test test/
```

Must exit 0, 0 failures.

```bash
flutter test integration_test/food_search_benchmark_test.dart --device-id <physical-android-device-id>
```

Must exit 0 with BM-01, BM-02, BM-03, NFR-06a all PASS on physical Android device.

(This is a required acceptance criterion per RESEARCH.md — emulator is insufficient.)
</verification>

<success_criteria>
- `flutter test test/` exits 0 — all unit tests green (DAO, repository, notifier)
- Benchmark integration tests pass on physical Android device (Pixel 6a / Samsung A54 class):
  - BM-01: 2-char "mi" query < 1000ms, non-empty results
  - BM-02: "banana" query < 1000ms
  - BM-03: no-match query < 1000ms, empty results
  - NFR-06a: ≥90% of 20-term benchmark food list returns results from local DB
- Human checkpoint approved by Ali after physical-device verification
- Phase 2 complete: food catalog search is fast, offline, and benchmarked
</success_criteria>

<output>
Create `.planning/phases/02-food-catalog-ingest-search/02-07-SUMMARY.md` when done.
</output>
