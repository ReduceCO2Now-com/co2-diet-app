---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
plan: 09
type: execute
wave: 4
depends_on:
  - 06-05
  - 06-07
  - 06-08
files_modified:
  - lib/core/router/app_router.dart
  - lib/app.dart
  - lib/features/settings/screens/settings_screen.dart
  - lib/features/profile/screens/profile_screen.dart
  - test/features/onboarding/onboarding_gate_test.dart
autonomous: true
requirements:
  - ONBD-01
  - ONBD-04
  - ONBD-05
  - LEG-01
  - ACC-02

must_haves:
  truths:
    - "initialLocation is /splash, not /profile; a fresh install with no stored onboarding flag always lands on Splash first"
    - "A direct deep link/navigation attempt to /dashboard (or any other route) before onboarding completes is redirected to /splash"
    - "Once onboarding is complete, navigating to /splash, /welcome, /legal-consent, or /onboarding-carousel redirects to /dashboard instead of re-showing onboarding"
    - "Once onboarding is complete, the /profile bottom-nav tab is reachable normally and is never redirected away"
    - "Settings contains a reachable 'Legal & Privacy' entry point into the Legal Hub, satisfying LEG-01's within-2-taps requirement end to end"
    - "During onboarding (hasCompletedOnboarding == false), Profile Setup shows a persistent 'Continue' button advancing to the Carousel; once onboarding is complete, that button is gone and Profile behaves exactly as it did in every prior phase"
    - "System font scaling is respected everywhere but clamped to a maximum of 1.6x so no key screen overflows at extreme accessibility text sizes (ACC-02)"
  artifacts:
    - path: "lib/core/router/app_router.dart"
      provides: "New routes (/splash, /welcome, /legal-consent, /onboarding-carousel, /legal-hub, /legal-hub/document, /legal-hub/consent-history) + top-level redirect gate + initialLocation: '/splash'"
    - path: "lib/app.dart"
      provides: "MediaQuery.withClampedTextScaling wrapper (ACC-02)"
  key_links:
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/onboarding/providers/onboarding_gate_provider.dart"
      via: "redirect callback reads OnboardingGateNotifier's current bool via ref.read on every navigation attempt"
      pattern: "onboardingGateNotifierProvider"
    - from: "lib/features/settings/screens/settings_screen.dart"
      to: "lib/features/legal/screens/legal_hub_screen.dart"
      via: "context.push('/legal-hub')"
      pattern: "legal-hub"
    - from: "lib/features/profile/screens/profile_screen.dart"
      to: "lib/features/onboarding/providers/onboarding_gate_provider.dart"
      via: "ref.watch(onboardingGateNotifierProvider) gates the conditional Continue-to-Carousel button"
      pattern: "onboardingGateNotifierProvider"
---

<objective>
Wire every screen built in Waves 1-3 into `app_router.dart` behind a top-level onboarding-gate redirect, apply the ACC-02 Dynamic Type clamp at the app root, add the "Legal & Privacy" Settings entry point, and add Profile Setup's onboarding-only "Continue to Carousel" button — turning six independently-built screens into one navigable, gated flow.

Purpose: This is the single integration point every prior Wave 1-3 plan was built against without touching `app_router.dart` (avoids repeated router-file conflicts across parallel plans, mirrors Plan 05-18's "final integration" precedent). Until this plan runs, none of Splash/Welcome/Legal Consent/Carousel/Legal Hub/Consent History is reachable from a cold start.

Output: `app_router.dart` with the full onboarding-gated route tree, `app.dart` with the text-scale clamp, `SettingsScreen`'s new entry point, `ProfileScreen`'s conditional Continue button, and the completed `onboarding_gate_test.dart` redirect-behavior coverage.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-CONTEXT.md
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-RESEARCH.md
@lib/core/router/app_router.dart
@lib/app.dart
@lib/features/onboarding/providers/onboarding_gate_provider.dart
@lib/features/legal/screens/legal_document_screen.dart
@lib/features/legal/screens/legal_hub_screen.dart
@lib/features/settings/screens/settings_screen.dart
@lib/features/profile/screens/profile_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router wiring -- new routes + onboarding redirect gate</name>
  <files>
    lib/core/router/app_router.dart
  </files>
  <action>
    In `lib/core/router/app_router.dart`, add these top-level `GoRoute`s (same style as the existing `/food-search`, `/barcode-scan` entries, outside the `StatefulShellRoute`): `/splash` → `SplashScreen`; `/welcome` → `WelcomeScreen`; `/legal-consent` → `LegalConsentScreen`; `/onboarding-carousel` → `OnboardingCarouselScreen`; `/legal-hub` → `LegalHubScreen`; `/legal-hub/document` → `LegalDocumentScreen(docId: LegalDocId.values.firstWhereOrNull((d) => d.name == state.uri.queryParameters['doc']) ?? LegalDocId.terms)` (same `firstWhereOrNull`-with-safe-fallback pattern already used for `AnalysisMetric`/`MealSlot` deep-link parsing, T-05-18-01 precedent); `/legal-hub/consent-history` → `ConsentHistoryScreen`.

    Change `initialLocation: '/profile'` to `initialLocation: '/splash'`.

    Add a top-level `redirect: (context, state) { ... }` callback to the `GoRouter(...)` constructor implementing exactly this logic: read `final hasOnboarded = ref.read(onboardingGateNotifierProvider);` (the `ref` already in scope inside the `@Riverpod` `appRouter` function). Define `const onboardingOnlyRoutes = ['/splash', '/welcome', '/legal-consent', '/onboarding-carousel'];` and `const allowedPreOnboarding = [...onboardingOnlyRoutes, '/profile'];`. If `!hasOnboarded` and `state.matchedLocation` does not start with any entry in `allowedPreOnboarding`, return `'/splash'`. If `hasOnboarded` and `state.matchedLocation` starts with any entry in `onboardingOnlyRoutes` (deliberately NOT including `/profile` in this second check — a completed user must still be able to visit the Profile bottom-nav tab normally), return `'/dashboard'`. Otherwise return `null` (no redirect). This closes the deep-link-skip-consent gap RESEARCH.md's Security Domain section flags (T-06-09-01 below) — the callback is evaluated on every navigation attempt including the very first one at `initialLocation`, not just explicit user taps.

    Add the necessary new imports (`SplashScreen`, `WelcomeScreen`, `OnboardingCarouselScreen`, `LegalConsentScreen`, `LegalHubScreen`, `LegalDocumentScreen`, `LegalDocId`, `ConsentHistoryScreen`, `onboardingGateNotifierProvider`).
  </action>
  <verify>
    <automated>flutter analyze lib/core/router/app_router.dart 2>&amp;1 | tail -25</automated>
  </verify>
  <done>All 7 new routes resolve. A cold start with no stored onboarding flag lands on `/splash`. A direct `context.go('/dashboard')` attempt before onboarding completes redirects to `/splash`. A completed user visiting `/welcome` redirects to `/dashboard`; the same user visiting `/profile` does not redirect.</done>
</task>

<task type="auto">
  <name>Task 2: ACC-02 text-scale clamp, Settings entry point, Profile Continue button</name>
  <files>
    lib/app.dart,
    lib/features/settings/screens/settings_screen.dart,
    lib/features/profile/screens/profile_screen.dart
  </files>
  <action>
    In `lib/app.dart`'s `MaterialApp.router(...)`, add a `builder: (context, child) => MediaQuery.withClampedTextScaling(minScaleFactor: 1.0, maxScaleFactor: 1.6, child: child!)` parameter (RESEARCH.md Pattern 5 / ACC-02) — applies system Dynamic Type scaling everywhere but clamps extreme values so no screen in the app overflows.

    In `lib/features/settings/screens/settings_screen.dart`, add a new `ListTile` (leading `Icon(Icons.gavel_outlined)` or similar, title `'Legal & Privacy'`, subtitle `'Terms, Privacy Policy, consent history, and your rights'`) immediately before the existing "Open source licenses" `ListTile`, `onTap: () => context.push('/legal-hub')` — satisfies LEG-01's "within 2 taps from any screen" via the existing bottom-nav → Settings → Legal & Privacy path.

    In `lib/features/profile/screens/profile_screen.dart`'s `_buildBody`, add `final hasOnboarded = ref.watch(onboardingGateNotifierProvider);` and, when `!hasOnboarded`, render a persistent bottom `FilledButton('Continue')` (full width, matching Welcome/Legal Consent's CTA style) below the footer text added in Plan 06-03, `onPressed: () => context.go('/onboarding-carousel')`. When `hasOnboarded` is `true`, this button is entirely absent and the screen behaves exactly as every prior phase built it — no other change to `ProfileScreen`'s existing save/override flows. Add the `onboarding_gate_provider.dart` import.
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart lib/features/settings/screens/settings_screen.dart lib/features/profile/screens/profile_screen.dart 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Text scales up to 1.6x everywhere without overflow. Settings has a working "Legal & Privacy" entry. Profile shows a "Continue" button only during onboarding, navigating to the Carousel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Onboarding redirect-gate test</name>
  <files>
    test/features/onboarding/onboarding_gate_test.dart
  </files>
  <behavior>
    - A fresh `GoRouter` built with `hasCompletedOnboarding == false` and `initialLocation: '/splash'` produces no redirect at `/splash` (it is already an allowed pre-onboarding route).
    - The same router configuration, when the app attempts to navigate directly to `/dashboard`, redirects to `/splash`.
    - The same configuration, when navigating to `/profile`, produces no redirect (allowed pre-onboarding route, per Task 1's `allowedPreOnboarding` set).
    - A router built with `hasCompletedOnboarding == true`, when navigating to `/welcome`, redirects to `/dashboard`.
    - The same completed-onboarding router, when navigating to `/profile`, produces no redirect.
  </behavior>
  <action>
    Remove the remaining `skip:` marker from `test/features/onboarding/onboarding_gate_test.dart` (the router-redirect group Plan 06-05 deliberately left skipped) and replace its placeholder cases with the real behavior above. Build a minimal test-only `GoRouter` (or exercise the real `appRouterProvider` via a `ProviderContainer` with `sharedPreferencesProvider` overridden to a `SharedPreferences` instance seeded via `SharedPreferences.setMockInitialValues({'hasCompletedOnboarding': true/false})`) and assert on `router.routerDelegate.currentConfiguration` / the resolved location after each navigation attempt, following this project's existing `flutter_test` + `mocktail` conventions (no new test dependency needed).
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/onboarding_gate_test.dart 2>&amp;1 | tail -25</automated>
  </verify>
  <done>`onboarding_gate_test.dart` is fully green with zero skips, covering every case in the behavior block above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Any navigation attempt (tap, deep link, programmatic `context.go`) → route resolution | The `redirect` callback is the single enforcement point for "consent is never skippable" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Elevation of Privilege | Deep-link/programmatic navigation attempting to reach `/dashboard` (or any post-onboarding route) before Legal Consent is accepted | mitigate | Top-level `redirect:` callback registered once on the root `GoRouter` (not per-route), evaluated on every navigation attempt including `initialLocation` itself — a crafted deep link into `/dashboard` pre-onboarding is redirected back to `/splash`, verified by Task 3's test |
</threat_model>

<verification>
`flutter test test/features/onboarding/onboarding_gate_test.dart` is fully green. `flutter analyze` reports zero new issues across all four modified files. Manual check: cold-launching the app (no stored data) lands on Splash and the full flow reaches Dashboard.
</verification>

<success_criteria>
Every screen built in Waves 1-3 is reachable through a single, consistently-enforced onboarding gate. LEG-01's "within 2 taps" requirement is satisfied end-to-end. ACC-02's text-scale clamp applies app-wide.
</success_criteria>

<output>
Create `.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-09-SUMMARY.md` when done
</output>
