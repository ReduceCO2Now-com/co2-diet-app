---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
plan: 08
type: execute
wave: 3
depends_on:
  - 06-04
  - 06-03
files_modified:
  - lib/features/legal/screens/legal_hub_screen.dart
  - lib/features/legal/screens/consent_history_screen.dart
  - test/features/legal/consent_history_screen_test.dart
autonomous: true
requirements:
  - LEG-01
  - LEG-02
  - LEG-03
  - PRIV-06

must_haves:
  truths:
    - "Legal Hub contains About, Legal Documents (Terms/Privacy/Health Disclaimer/Impressum), a Your Rights section, a View my consent history entry, a Concerned about eating entry, and a real contact email"
    - "Your Rights maps each GDPR right to an EXISTING Phase 5 feature (Export Data, Danger Zone) -- no new data-mutation screen or action is built here"
    - "Consent history renders every ConsentEvent the user has ever recorded, in plain language (timestamp, app version, policy version, what was accepted), reading from the already-existing ConsentRecordsDao data"
    - "Impressum is reachable from the Legal Hub's Legal Documents section, rendering the visible placeholder entity text drafted in Plan 06-02"
    - "No FAQ section and no Discord/community link appear anywhere in the Legal Hub"
  artifacts:
    - path: "lib/features/legal/screens/legal_hub_screen.dart"
      provides: "LegalHubScreen -- About, Legal Documents, Your Rights, consent history entry, helpline entry, contact email"
    - path: "lib/features/legal/screens/consent_history_screen.dart"
      provides: "ConsentHistoryScreen -- read-only list from IConsentRepository.watchConsents()"
  key_links:
    - from: "lib/features/legal/screens/legal_hub_screen.dart"
      to: "lib/features/legal/screens/legal_document_screen.dart"
      via: "context.push('/legal-hub/document?doc=terms|privacy|health_disclaimer|impressum')"
      pattern: "legal-hub/document"
    - from: "lib/features/legal/screens/legal_hub_screen.dart"
      to: "lib/features/backup/screens/backup_restore_screen.dart"
      via: "Your Rights -> Access & Portability tile pushes /backup-restore (existing Export Data screen), Consent withdrawal tile pushes /backup-restore (existing Danger Zone)"
      pattern: "backup-restore"
    - from: "lib/features/legal/screens/consent_history_screen.dart"
      to: "lib/data/local/daos/consent_records_dao.dart"
      via: "IConsentRepository.watchConsents() -> ConsentRecordsDao.watchConsents() (Phase 1, first UI surface)"
      pattern: "watchConsents"
---

<objective>
Build the Legal Hub (LEG-01/02/03, the central "government-style clarity" screen) and the read-only Consent History screen (PRIV-06) that finally surfaces Phase 1's `ConsentRecordsDao.watchConsents()` to a real user, plus the standalone, always-visible "Concerned about eating?" entry point CONTEXT.md locks independent of whether the reactive ED-safety-net warning has ever fired.

Purpose: LEG-01 requires the Legal Hub be reachable within 2 taps from any screen and contain all four legal documents; PRIV-06 requires GDPR rights be *explainable*, not re-implemented — this plan is explicitly forbidden from building new export/delete logic (06-CONTEXT.md/RESEARCH.md's Anti-Pattern list).

Output: `LegalHubScreen`, `ConsentHistoryScreen`, a green widget test for the consent-history read path.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-CONTEXT.md
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-RESEARCH.md
@lib/features/legal/screens/legal_document_screen.dart
@lib/domain/repositories/i_consent_repository.dart
@lib/domain/entities/consent_event.dart
@lib/core/widgets/ed_safety_net_dialog.dart
@lib/features/settings/screens/settings_screen.dart
@lib/features/backup/screens/backup_restore_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: LegalHubScreen</name>
  <files>
    lib/features/legal/screens/legal_hub_screen.dart
  </files>
  <action>
    Create `lib/features/legal/screens/legal_hub_screen.dart`: `class LegalHubScreen extends StatelessWidget` rendering a `Scaffold`/`ListView` with these sections, in order:

    1. **About** — a short static paragraph: what CO₂ Diet is (privacy-first, offline-first nutrition + CO₂ tracker), plus a one-line open-source note pointing at the existing "Open source licenses" entry (already in `SettingsScreen`, do not duplicate its content here — just reference it).
    2. **Legal Documents** — 4 `ListTile`s, one per `LegalDocId` (Terms, Privacy Policy, Health Disclaimer, Impressum), each `onTap: () => context.push('/legal-hub/document?doc=${docId.name}')`.
    3. **Your Rights** — static explanatory text mapping each GDPR right directly to an existing feature, per 06-CONTEXT.md's locked redirect-only design: "Access & Portability" → `ListTile` pushing `/backup-restore` (existing Export Data screen, Phase 5); "Rectify" → static text only, no tile ("edit any entry directly, anywhere in the app — already possible"); "Consent withdrawal" → `ListTile` pushing `/backup-restore` (existing Danger Zone section) with a one-line note that uninstalling the app is also always an option. Do NOT build any new mutation/action screen here.
    4. **View my consent history** — `ListTile` pushing `context.push('/legal-hub/consent-history')`.
    5. **Concerned about eating or your relationship with food?** — `ListTile` calling `showHelplineResourcesSheet(context)` (built in Plan 06-03) — standalone and independent of whether the reactive ED-safety-net warning has ever triggered, per 06-CONTEXT.md's locked decision.
    6. **Contact** — a static real contact email `ListTile` (`SelectableText` or a `mailto:` `url_launcher` link) — use a placeholder-but-plausible address consistent with `docs/legal/privacy.md`'s contact line drafted in Plan 06-02 (do not invent a different address).

    Deliberately DO NOT include an FAQ section or any Discord/community link anywhere in this screen — cut for v1 per 06-CONTEXT.md's Deferred Ideas (same reasoning as Phase 5's Weight Tracking "Learn More" cut).
  </action>
  <verify>
    <automated>flutter analyze lib/features/legal/screens/legal_hub_screen.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>LegalHubScreen renders all 6 sections listed above, contains zero FAQ/Discord content, and every Legal Documents tile navigates to the correct `LegalDocId`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConsentHistoryScreen</name>
  <files>
    lib/features/legal/screens/consent_history_screen.dart,
    test/features/legal/consent_history_screen_test.dart
  </files>
  <behavior>
    - Given a `ProviderScope` override of `consentRepositoryProvider` returning a fake `IConsentRepository` whose `watchConsents()` stream emits 2 `ConsentEvent`s, the screen renders 2 list rows, each showing the event's timestamp, app version, policy version, and a plain-language summary of `consentsGiven` (e.g. "Terms of Service, Privacy Policy, Health Disclaimer, Responsibility acknowledgment" — translate identifiers to human phrases, never render the raw JSON keys).
    - Given a fake repository whose stream emits an empty list, the screen renders an empty-state message (e.g. "No consent recorded yet") instead of a blank/crashing list.
  </behavior>
  <action>
    Remove the `skip:` marker from `test/features/legal/consent_history_screen_test.dart` (created in Plan 06-01) and replace its placeholder cases with the real behavior above.

    Create `lib/features/legal/screens/consent_history_screen.dart`: `class ConsentHistoryScreen extends ConsumerWidget`. `build()` uses `ref.watch(consentRepositoryProvider)` (or a thin dedicated `@riverpod Stream&lt;List&lt;ConsentEvent&gt;&gt; consentHistory(Ref ref) => ref.watch(consentRepositoryProvider).watchConsents();` StreamProvider — executor's choice, either is fine) via `.when(loading:, error:, data:)`/`AsyncValue` handling. Renders a `Scaffold(appBar: AppBar(title: Text('Consent History')))` body: for each `ConsentEvent`, a `Card`/`ListTile` showing the formatted UTC timestamp, `'App v${event.appVersion}'`, `'Policy v${event.policyVersion}'`, and a human-readable translation of `event.consentsGiven` (map each raw identifier — `terms`/`privacy`/`not_medical_advice`/`user_responsibility`/`age_16_plus` — to a short display phrase via a private lookup map). Empty-state: centered `Text('No consent recorded yet.')` when the list is empty.
  </action>
  <verify>
    <automated>flutter test test/features/legal/consent_history_screen_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`consent_history_screen_test.dart` is green with zero skips, covering both the populated and empty-state cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local `consent_records` table → Consent History UI | Read-only projection of the user's own on-device audit data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Information Disclosure | Consent history displays the user's own timestamps/versions on-device | accept | This is the user's own local data, viewed by the user, with no third-party access path — no mitigation needed beyond the app's existing on-device-only data model |
| T-06-08-02 | Elevation of Privilege | Legal Hub's "Your Rights" section accidentally reimplements a weaker/divergent export or delete path instead of redirecting to Phase 5's already-hardened screens | mitigate | Task 1's action explicitly forbids building any new mutation screen — every action tile pushes the existing `/backup-restore` route verbatim |
</threat_model>

<verification>
`flutter test test/features/legal/consent_history_screen_test.dart` is fully green. `flutter analyze` reports zero new issues in `legal_hub_screen.dart`.
</verification>

<success_criteria>
Legal Hub contains every element LEG-01/02/03 require, PRIV-06's GDPR rights are explained (not rebuilt), and Phase 1's dormant `ConsentRecordsDao.watchConsents()` finally has a real UI consumer.
</success_criteria>

<output>
Create `.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-08-SUMMARY.md` when done
</output>
