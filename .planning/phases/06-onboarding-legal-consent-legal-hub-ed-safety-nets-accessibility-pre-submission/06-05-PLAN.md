---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
plan: 05
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - lib/features/onboarding/providers/onboarding_gate_provider.dart
  - lib/features/onboarding/providers/onboarding_gate_provider.g.dart
  - lib/features/onboarding/screens/splash_screen.dart
  - lib/features/onboarding/screens/welcome_screen.dart
  - lib/features/onboarding/screens/onboarding_carousel_screen.dart
  - lib/main.dart
  - test/features/onboarding/onboarding_gate_test.dart
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-05

must_haves:
  truths:
    - "Splash auto-advances to Welcome after 2-3 seconds with no user interaction and no buttons"
    - "Welcome shows exactly one primary CTA labeled 'Continue' -- no 'Get Started'/'Use Without Account' pair (ONBD-02 is overridden by 06-CONTEXT.md this phase; the equal-weight-CTA requirement is moot until Phase 7 reintroduces a second path)"
    - "No Mode Choice screen or route exists anywhere in this codebase after this plan (ONBD-03 explicitly deferred to Phase 7 per 06-CONTEXT.md -- verified by absence, not by a placeholder screen)"
    - "The Onboarding Carousel has exactly 3 slides, is swipeable, has a 'Skip intro' link reachable from any slide that jumps straight to completion (not to the next slide), and a sticky 'Go to Dashboard' button on the last slide"
    - "Both 'Skip intro' and 'Go to Dashboard' persist hasCompletedOnboarding=true to SharedPreferences before navigating away -- an app restart after either action does not re-show onboarding"
  artifacts:
    - path: "lib/features/onboarding/providers/onboarding_gate_provider.dart"
      provides: "sharedPreferencesProvider (sync, overridden in main.dart) + OnboardingGateNotifier.completeOnboarding()"
    - path: "lib/features/onboarding/screens/splash_screen.dart"
      provides: "SplashScreen (2-3s auto-advance)"
    - path: "lib/features/onboarding/screens/welcome_screen.dart"
      provides: "WelcomeScreen (single Continue CTA)"
    - path: "lib/features/onboarding/screens/onboarding_carousel_screen.dart"
      provides: "OnboardingCarouselScreen (PageView, 3 slides, Skip intro / Go to Dashboard)"
  key_links:
    - from: "lib/main.dart"
      to: "lib/features/onboarding/providers/onboarding_gate_provider.dart"
      via: "SharedPreferences.getInstance() awaited before runApp, then sharedPreferencesProvider.overrideWithValue(prefs) in ProviderScope"
      pattern: "sharedPreferencesProvider.overrideWithValue"
    - from: "lib/features/onboarding/screens/onboarding_carousel_screen.dart"
      to: "lib/features/onboarding/providers/onboarding_gate_provider.dart"
      via: "OnboardingGateNotifier.completeOnboarding() called before navigating to /dashboard"
      pattern: "completeOnboarding"
---

<objective>
Build the three onboarding screens (Splash, Welcome, Carousel) and the `shared_preferences`-backed onboarding-gate provider that Plan 06-09's router redirect depends on, per 06-CONTEXT.md's simplified flow: Splash → Welcome (single "Continue" button) → Legal Consent → Profile Setup → 3-slide Carousel → Dashboard, with no Mode Choice screen this phase.

Purpose: ONBD-01/02/05 need real screens; ONBD-03's requirement is satisfied by *not* building a Mode Choice screen this phase (deferred to Phase 7 per locked decision) — this plan is where that absence is made concrete and verifiable.

Output: `OnboardingGateNotifier` + `sharedPreferencesProvider`, `SplashScreen`, `WelcomeScreen`, `OnboardingCarouselScreen`, `main.dart` wired to preload the SharedPreferences instance.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-CONTEXT.md
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-RESEARCH.md
@docs/CO2_Diet_Full_Reference.md
@lib/main.dart
@lib/core/theme/color_tokens.dart
@lib/core/theme/text_tokens.dart
@lib/core/theme/spacing_tokens.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OnboardingGateNotifier + main.dart wiring</name>
  <files>
    lib/features/onboarding/providers/onboarding_gate_provider.dart,
    lib/main.dart,
    test/features/onboarding/onboarding_gate_test.dart
  </files>
  <behavior>
    - `OnboardingGateNotifier`'s `build()` returns `false` when `sharedPreferencesProvider`'s overridden instance has no stored `hasCompletedOnboarding` key.
    - `build()` returns `true` when the stored key is `true`.
    - `completeOnboarding()` persists `true` to the (fake, in-memory-backed) `SharedPreferences` instance and updates the notifier's own state to `true` synchronously (readable via `ref.read` immediately after the call resolves, no separate rebuild needed by the caller).
  </behavior>
  <action>
    Add a lightweight, non-router-dependent supplement to `test/features/onboarding/onboarding_gate_test.dart` (created skipped in Plan 06-01) covering only the provider-level behavior above — remove its `skip:` only for this narrower slice; leave a second, still-skipped group for the full router-redirect behavior that Plan 06-09 fills in (do not attempt router/redirect testing here — this task has no `app_router.dart` dependency yet). Use `shared_preferences`'s own `SharedPreferences.setMockInitialValues({})` test helper via a `ProviderContainer` override, NOT the real plugin.

    Create `lib/features/onboarding/providers/onboarding_gate_provider.dart`: `@riverpod SharedPreferences sharedPreferences(Ref ref) => throw UnimplementedError('overridden in main.dart via ProviderScope');` and `@riverpod class OnboardingGateNotifier extends _$OnboardingGateNotifier { @override bool build() => ref.watch(sharedPreferencesProvider).getBool('hasCompletedOnboarding') ?? false; Future&lt;void&gt; completeOnboarding() async { await ref.read(sharedPreferencesProvider).setBool('hasCompletedOnboarding', true); state = true; } }` — a plain synchronous `Notifier&lt;bool&gt;` (not `AsyncNotifier`, since `build()` returns `bool` directly once the `SharedPreferences` instance is already loaded via the override).

    In `lib/main.dart`: add `final prefs = await SharedPreferences.getInstance();` before `runApp`, alongside the existing `offRefPath`/`notificationsPlugin` setup calls, and add `sharedPreferencesProvider.overrideWithValue(prefs)` to the `ProviderScope(overrides: [...])` list next to the existing `offRefPathProvider` override.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/onboarding_gate_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>The provider-level slice of `onboarding_gate_test.dart` is green (router-redirect cases remain skipped, owned by Plan 06-09). `main.dart` compiles and passes the loaded `SharedPreferences` instance into `ProviderScope`.</done>
</task>

<task type="auto">
  <name>Task 2: SplashScreen + WelcomeScreen</name>
  <files>
    lib/features/onboarding/screens/splash_screen.dart,
    lib/features/onboarding/screens/welcome_screen.dart
  </files>
  <action>
    Create `lib/features/onboarding/screens/splash_screen.dart`: `class SplashScreen extends StatefulWidget`, `initState` starts `Future.delayed(const Duration(seconds: 2), () { if (mounted) context.go('/welcome'); })` (2-3s window per ONBD-01; pick 2s as the concrete constant, documented as within spec's 2-3s range). Body: centered app logo/icon (reuse `Icons.eco` or the app's existing icon asset if one exists — check `assets/` first; fall back to a centered `Text('CO₂ Diet', style: AppTextTheme.displayLg)` if no logo asset exists), the tagline `'Track calories, protein, and estimated CO₂ — privately, offline.'` (per 06-CONTEXT.md: revert to the original spec's "Track calories..." framing, not the live-build's "Lose Weight. Improve Health. Reduce CO2." wording), and a small `CircularProgressIndicator`. No buttons, no navigation controls — matches ONBD-01's "auto-advances, no buttons" requirement exactly.

    Create `lib/features/onboarding/screens/welcome_screen.dart`: `class WelcomeScreen extends StatelessWidget`. Body: logo/app name, the same tagline concept as Splash (or a short variant), the supporting line `'Private. Offline-first. No ads.'` (verbatim, unchanged per 06-CONTEXT.md), and exactly ONE `FilledButton` labeled `'Continue'` (deliberately neutral — not "Get Started," since it no longer implies account creation, per 06-CONTEXT.md's locked decision) navigating to `context.go('/legal-consent')`. No second button, no "Use Without Account" — ONBD-02's equal-weight-CTA requirement is moot this phase since there is only one path; add a doc comment on the class citing this override explicitly.
  </action>
  <verify>
    <automated>flutter analyze lib/features/onboarding/screens/splash_screen.dart lib/features/onboarding/screens/welcome_screen.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Splash shows no buttons and schedules a 2s auto-advance to /welcome. Welcome renders exactly one "Continue" button and the required supporting copy.</done>
</task>

<task type="auto">
  <name>Task 3: OnboardingCarouselScreen</name>
  <files>
    lib/features/onboarding/screens/onboarding_carousel_screen.dart
  </files>
  <action>
    Create `lib/features/onboarding/screens/onboarding_carousel_screen.dart`: `class OnboardingCarouselScreen extends ConsumerStatefulWidget`, internal `PageController`, 3 fixed slides matching `docs/CO2_Diet_Full_Reference.md` §3's content (Slide 1: food choices have environmental impact; Slide 2: how CO₂ scoring works — production/transport/processing factors, framed with an honest-uncertainty tone per NFR-01/02, no alarm-red imagery, no letter grades; Slide 3: what you can do — log meals, compare choices, track progress over time). Each slide: icon/illustration placeholder + heading + short body text, using existing theme tokens (`AppTextTheme.headlineLgMobile`/`bodyLg`, `AppColors.surface`). A small 3-dot page indicator below the `PageView` (hand-rolled `AnimatedContainer` row, no new package per RESEARCH.md's "Don't Hand-Roll" table — a package-based indicator is explicitly not worth a new dependency for 3 fixed dots).

    Top-right or top-left: a `'Skip intro'` `TextButton` visible on every slide (not just the last) that — per ONBD-05's exact wording — jumps straight to Dashboard, NOT to the next slide: `onPressed: () async { await ref.read(onboardingGateNotifierProvider.notifier).completeOnboarding(); if (context.mounted) context.go('/dashboard'); }`.

    On the last slide only: a sticky, full-width `FilledButton` labeled `'Go to Dashboard'` at the bottom, calling the identical completion sequence (`completeOnboarding()` then `context.go('/dashboard')`) — both exit paths must persist the flag before navigating, so an app restart never re-shows onboarding.
  </action>
  <verify>
    <automated>flutter analyze lib/features/onboarding/screens/onboarding_carousel_screen.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Exactly 3 slides exist, are swipeable via PageView, "Skip intro" is present on every slide and exits immediately (not slide-advances), and "Go to Dashboard" appears only on slide 3. Both actions persist the onboarding-complete flag before navigating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — pure client-side UI + on-device key-value flag storage.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | `hasCompletedOnboarding` SharedPreferences flag manipulated directly (rooted/jailbroken device) to skip onboarding without ever completing Legal Consent | accept | Consistent residual risk of any on-device local-storage flag in this local-only app; the redirect gate (Plan 06-09) still enforces the flow for every normal app-navigation path, which is the actionable mitigation within this app's threat model |
</threat_model>

<verification>
`flutter test test/features/onboarding/onboarding_gate_test.dart` (provider-level cases) is green. `flutter analyze` reports zero new issues across all four new/modified files.
</verification>

<success_criteria>
All three onboarding screens exist and compile independently of routing (Plan 06-09 wires them into `app_router.dart`). No Mode Choice screen exists anywhere in the codebase.
</success_criteria>

<output>
Create `.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-05-SUMMARY.md` when done
</output>
