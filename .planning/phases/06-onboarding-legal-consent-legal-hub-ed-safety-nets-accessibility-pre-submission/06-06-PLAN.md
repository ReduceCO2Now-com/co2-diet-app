---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
plan: 06
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - ios/Runner/PrivacyInfo.xcprivacy
  - docs/PLAY_DATA_SAFETY_DRAFT.md
autonomous: true
requirements:
  - LEG-01

must_haves:
  truths:
    - "PrivacyInfo.xcprivacy is well-formed plist XML that passes plutil -lint"
    - "PrivacyInfo.xcprivacy declares NSPrivacyTracking: false and empty NSPrivacyCollectedDataTypes/NSPrivacyTrackingDomains arrays, matching the PRIV-07 'no data collected' story"
    - "docs/PLAY_DATA_SAFETY_DRAFT.md discloses Open Food Facts API queries (search terms, barcodes) as third-party data sharing, even though this occurs in Local Mode"
    - "docs/PLAY_DATA_SAFETY_DRAFT.md declares the app as NOT 'Made for Kids' / not directed at children, and targets a 16+ age rating with the 'Frequent Medical/Treatment Information' content descriptor"
  artifacts:
    - path: "ios/Runner/PrivacyInfo.xcprivacy"
      provides: "iOS Privacy Manifest declaring NSPrivacyAccessedAPICategoryFileTimestamp (statically-linked SQLite) and no tracking/collection"
    - path: "docs/PLAY_DATA_SAFETY_DRAFT.md"
      provides: "Drafted Play Console Data Safety form answers + age rating questionnaire answers, for manual copy-in at real submission time"
  key_links: []
---

<objective>
Produce the two concrete pre-submission artifacts 06-CONTEXT.md and ROADMAP.md's own success criteria call out by name: a real PrivacyInfo.xcprivacy iOS build artifact, and a drafted docs/PLAY_DATA_SAFETY_DRAFT.md answer-set for Google Play Console's Data Safety form and age-rating questionnaire.

Purpose: "Pre-Submission" is literally in this phase's name. Neither artifact requires live App Store Connect / Play Console access (out of this phase's reach per 06-CONTEXT.md) — both are drafted, reviewable documents/manifests that whoever owns the actual submission copies in.

Output: ios/Runner/PrivacyInfo.xcprivacy (real code artifact, Apple checks it at build time) and docs/PLAY_DATA_SAFETY_DRAFT.md.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-CONTEXT.md
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-RESEARCH.md
@.planning/research/PITFALLS.md
@docs/legal/privacy.md
@docs/legal/health_disclaimer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrivacyInfo.xcprivacy</name>
  <files>
    ios/Runner/PrivacyInfo.xcprivacy
  </files>
  <action>
    Create ios/Runner/PrivacyInfo.xcprivacy as a well-formed Apple Privacy Manifest plist (standard XML plist DOCTYPE/plist wrapper) declaring: NSPrivacyTracking = false; NSPrivacyTrackingDomains = empty array; NSPrivacyCollectedDataTypes = empty array (matches PRIV-07's "no data collected" story, consistent with the existing .privacy-blocklist.yaml enforcement); NSPrivacyAccessedAPITypes = one entry with NSPrivacyAccessedAPIType = NSPrivacyAccessedAPICategoryFileTimestamp and NSPrivacyAccessedAPITypeReasons = an array containing the single reason code C617.1 (per RESEARCH.md Pitfall 3: this app statically links sqlite3 via drift/FFI directly into the main binary, not via a separate pod with its own manifest, and SQLite's own C code calls fstat/stat-family functions for file locking, which Apple's static analyzer attributes to the main app binary). Add an XML comment near the top noting this is MEDIUM confidence and must be re-verified against Xcode's own Privacy Report (Product then Archive) before real submission — cite RESEARCH.md Common Pitfalls #3 for the full reasoning. Do NOT redeclare NSPrivacyAccessedAPICategoryUserDefaults — shared_preferences_foundation already ships its own manifest for that category (verified directly from github.com/flutter/packages this session per RESEARCH.md).

    Also add an XML comment noting this file's presence in ios/Runner/ has not yet been confirmed as a member of the Xcode Runner target's Copy Bundle Resources build phase — Xcode 15+ auto-detects .xcprivacy files placed correctly in most cases, but this must be confirmed via an actual flutter build ios plus Xcode archive before real submission (a pre-submission checklist item, not resolved by this task).
  </action>
  <verify>
    <automated>plutil -lint ios/Runner/PrivacyInfo.xcprivacy</automated>
  </verify>
  <done>plutil -lint reports the file as valid plist XML. The file declares zero tracking, zero data collection, and the one File Timestamp required-reason API entry with reason C617.1.</done>
</task>

<task type="auto">
  <name>Task 2: Play Data Safety and age rating draft doc</name>
  <files>
    docs/PLAY_DATA_SAFETY_DRAFT.md
  </files>
  <action>
    Create docs/PLAY_DATA_SAFETY_DRAFT.md: a structured markdown document (not filled into any live console — 06-CONTEXT.md is explicit this is a copy-in reference, not a live submission) with these sections:

    1. Data Safety form answers, mirroring the actual Play Console form structure: "Does your app collect or share any of the required user data types?" answer explains that in Local Mode all personal data (profile fields, meal logs, weight entries) stays on-device and is never transmitted; "Third-party data sharing" answer is YES — discloses Open Food Facts API queries (search terms the user types, barcodes scanned) as shared with a third party (Open Food Facts servers), even though the app itself has no account/backend contact in Local Mode, per PITFALLS.md C4's guidance that this app makes network requests only to the Open Food Facts API and no other service; "Is data encrypted in transit?" answer notes the OFF API's own HTTPS usage; "Can users request data deletion?" answer references the in-app Danger Zone (PRIV-09), noting this is a local-only deletion since there is no backend account to also delete in v1.

    2. Age rating questionnaire draft: target 16+, content descriptor "Frequent Medical/Treatment Information" (per 06-CONTEXT.md's locked MyFitnessPal-precedent decision — verified 16+, not a generic "health app = Everyone" assumption). Explicitly answer "Is this app directed at children / Made for Kids?" as NO for both stores' equivalent questionnaires, consistent with the 16+ positioning and avoiding COPPA/child-directed-app compliance triggers.

    3. Google Play "not a medical device" disclaimer line: note that this exact sentence (or a close paraphrase) is included verbatim in docs/legal/health_disclaimer.md (drafted in Plan 06-02) and should also appear in the Play Store listing description at real submission time (per RESEARCH.md Pitfall 6 / Assumption A2 — sourced from a secondary blog on Google's January 2026 policy update; flag as MEDIUM confidence and recommend re-verifying against Play Console's own Health Content policy page before real submission).

    4. Open questions carried forward from RESEARCH.md: note the unresolved "where does the Privacy Policy live for Play Console's public URL field" gap (no public website/hosting target exists in this codebase-only project) as an explicit pre-submission blocker for whoever owns the actual store listing — the same owner as the Impressum entity decision.
  </action>
  <verify>
    <automated>test -f docs/PLAY_DATA_SAFETY_DRAFT.md && grep -c "Open Food Facts" docs/PLAY_DATA_SAFETY_DRAFT.md</automated>
  </verify>
  <done>docs/PLAY_DATA_SAFETY_DRAFT.md exists with all four sections, discloses the OFF API third-party sharing explicitly, and states the 16+/not-made-for-kids age-rating position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → store metadata artifacts | Documentation/manifest only, not a runtime data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Information Disclosure | PrivacyInfo.xcprivacy under/over-declaring real API usage | mitigate | Re-verify the final declaration set against Xcode's own Privacy Report at real archive time (flagged inline in the manifest and as a pre-submission checklist item) — this task's output is a best-effort draft, not the final verified artifact |
</threat_model>

<verification>
plutil -lint ios/Runner/PrivacyInfo.xcprivacy succeeds. docs/PLAY_DATA_SAFETY_DRAFT.md exists and discloses the OFF API third-party data sharing explicitly.
</verification>

<success_criteria>
Both pre-submission artifacts exist as real, reviewable outputs — not placeholders — ready for a human to copy into the actual App Store Connect / Play Console forms at real submission time.
</success_criteria>

<output>
Create .planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-06-SUMMARY.md when done
</output>
