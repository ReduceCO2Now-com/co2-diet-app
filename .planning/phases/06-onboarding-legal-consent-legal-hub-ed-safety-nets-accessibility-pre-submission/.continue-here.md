---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
task: 06-10 (checkpoint 1 of 3 -- mid-verification, one real bug found and fixed)
total_tasks: 10
status: in_progress
last_updated: "2026-08-05T07:26:01.842Z"
---

<current_state>
Phase 6 is 9/10 plans complete (Waves 1-4 fully executed and committed). The
final plan, 06-10 (Wave 5), is three `checkpoint:human-verify` blocking
tasks requiring real iOS + real Android hardware. Both devices were
connected and Checkpoint 1 (dark mode / color-blind / tap-target pass)
began for real this session.

Getting the iPhone running at all required extensive local-environment
troubleshooting (iOS code-signing: a stale `CODE_SIGN_IDENTITY[sdk=iphoneos*]
= "iPhone Developer"` project setting no longer matched any modern
certificate; also a missing `~/Library/MobileDevice/Provisioning Profiles/`
directory). The signing-identity fix is committed (see below); ultimately
the user launched the app successfully via Xcode's own Run button, which
sidesteps a CLI-only `flutter run` limitation with Flutter's scheme
pre-action script not seeing `EXPANDED_CODE_SIGN_IDENTITY` in time.

While actually testing the flow (not yet the formal dark-mode/color-blind/
tap-target checklist -- that has NOT been completed or approved), the user
found a real, severe navigation bug on BOTH platforms: tapping any "View
Terms/Privacy Policy/Disclaimer" link on the Legal Consent screen bounced
the user all the way back to `/splash`, and the consent flow could not be
completed. Root-caused and fixed this session (see decisions_made). The fix
is tested (new regression test added, full suite green) and committed as
`d198fad`. It has NOT yet been re-verified on-device by the user -- an
Android redeploy was in progress when this pause was requested; iOS needs
a fresh Run from Xcode to pick up the fix.

The originally-reported "bounces back to /splash after accepting all 4
consent checkboxes and proceeding toward Profile Setup" (a second,
seemingly separate symptom) was NOT independently reproduced in a real-code
widget test (an Explore-agent investigation drove the full flow --
checkboxes checked, Accept tapped -- and landed cleanly on `/profile`, no
bounce). Working theory: this was very likely a downstream artifact of the
SAME `/legal-hub` redirect bug (an in-flight bad navigation racing/
retriggering), OR stale on-device `SharedPreferences` state from earlier
exploration in this same testing session. **This needs to be explicitly
re-tested after the fix** -- if it recurs even with the fix and a clean
app reinstall, it's a second, still-open bug.
</current_state>

<completed_work>

- 06-01 through 06-09: all COMPLETE (see commit list in ROADMAP.md /
  STATE.md -- unchanged since last pause). Full test suite as of 06-09:
  417 passing, 9 skipped, 0 failures.
- Bug found + fixed this session (commit `d198fad`,
  "fix(06-10): pre-onboarding redirect allowlist missing /legal-hub"):
  - `lib/core/router/app_router.dart`'s top-level redirect `allowedPreOnboarding`
    list omitted `/legal-hub` entirely, so any pre-onboarding navigation to
    `/legal-hub/document` (the "View Terms/Privacy/Disclaimer" links) got
    redirected back to `/splash`. Fixed by adding `/legal-hub` to the
    allowlist (covers `/legal-hub`, `/legal-hub/document`,
    `/legal-hub/consent-history` via the existing `startsWith` check).
  - Added a regression test to `test/features/onboarding/onboarding_gate_test.dart`
    (`navigating to /legal-hub/document before onboarding completes
    produces no redirect`) driving the real `Co2DietApp`/real router --
    passes.
  - Also fixed `ios/Runner.xcodeproj/project.pbxproj`: `CODE_SIGN_IDENTITY[sdk=iphoneos*]`
    was `"iPhone Developer"` (pre-Xcode-8 legacy wildcard, matches nothing
    in modern keychains) on all 3 Runner target configs (Debug/Release/
    Profile) -- changed to `"Apple Development"`. `DEVELOPMENT_TEAM` was
    NOT changed (stayed at the original `NS35HZLH9U`, confirmed correct
    via Xcode's own account manager after an earlier incorrect guess to
    change it was reverted).
  - Full suite re-verified green after both fixes: 418 passing, 9 skipped,
    0 failures. `flutter analyze`: no issues in touched files.
- Android redeploy (`flutter run -d R52RB0FSSAX --debug`) was launched
  with the fix and was still building when this pause was requested --
  check `/private/tmp/claude-501/-Users-alisafi-Documents-ReduceCO2-Now-Co2-diet-app/4d240880-338c-4761-8001-261697b48df8/scratchpad/flutter_android2.log`
  for its outcome, or just relaunch fresh next session (scratchpad may not
  survive across sessions).
</completed_work>

<remaining_work>

- **Immediate next step**: redeploy the fix to both devices (Android via
  `flutter run -d R52RB0FSSAX --debug`; iOS via Xcode's Run button on
  `ios/Runner.xcworkspace`, target "Kimo's 16", since that's the path that
  actually worked for iOS this session) and have the user re-test:
  1. Tapping every "View Terms/Privacy/Disclaimer" link on Legal Consent
     no longer bounces to `/splash` (this is the fixed bug -- should now
     work).
  2. Checking all 4 mandatory boxes and tapping "Accept and Continue"
     reaches and STAYS on Profile Setup (re-test the second symptom
     specifically -- not independently confirmed as fixed, needs a clean
     real-device retest, ideally after a full app uninstall/reinstall to
     rule out stale `SharedPreferences` from earlier exploration).
- Once navigation is confirmed solid, THEN do the actual Checkpoint 1
  checklist (dark mode / color-blind simulator / tap-target >=44x44pt)
  across: Splash, Welcome, Legal Consent, all 4 Legal Document screens,
  Profile Setup, Onboarding Carousel, Dashboard, Legal Hub, Consent
  History, both ED safety-net trigger points (Profile kcal override < 1200,
  Weight target implying BMI < 17.5).
- 06-10 Checkpoint 2 (ACC-03): VoiceOver (iOS) + TalkBack (Android)
  screen-reader pass. Not started.
- 06-10 Checkpoint 3 (NFR-01/02/03/04): SAM test + non-judgmental tone
  audit. Not started.
- After all 3 checkpoints approved: create `06-10-SUMMARY.md`, update
  STATE.md/ROADMAP.md to mark Phase 6 fully complete, then the
  `verify_phase_goal` step (spawn gsd-verifier) runs before
  `update_roadmap` marks the phase done.
</remaining_work>

<decisions_made>

- Confirmed via 06-CONTEXT.md (earlier this session): Mode Choice
  visual-weighting bias is RESOLVED (no Mode Choice screen exists in
  Phase 6, deferred to Phase 7); external Fachanwalt IT-Recht / LCA peer
  review is a pre-*launch* blocker, explicitly out of Phase 6's scope.
- Package-legitimacy checkpoint for `flutter_markdown_plus`/
  `shared_preferences`/`package_info_plus` -- approved by user during
  06-02.
- iOS `DEVELOPMENT_TEAM` root-cause investigation: initially guessed the
  team ID was wrong and changed it to the keychain certificate's team
  (`U9R2HU3B92`) -- this was WRONG (Xcode's own account manager only
  recognizes `NS35HZLH9U`) and was reverted. The actual, confirmed root
  cause was the stale `CODE_SIGN_IDENTITY` string, unrelated to team ID.
  Do not re-attempt a team-ID change if this resurfaces.
- `~/Library/MobileDevice/Provisioning Profiles/` didn't exist on this Mac
  (modern Xcode caches automatic profiles under
  `~/Library/Developer/Xcode/UserData/Provisioning Profiles/` instead) --
  created the legacy directory and copied the profile over as part of
  troubleshooting. Harmless, left in place.
- Recurring deviation across nearly every plan this session: STATE.md's
  `state.advance-plan`/`state.update-progress` gsd-tools SDK verbs don't
  parse this project's established custom "Current Position"/"Progress"
  field format -- every executor agent updated those STATE.md sections
  manually instead. Not a blocker.
</decisions_made>

<blockers>
- 06-10's three checkpoints require real iOS + real Android hardware and
  cannot be automated or approved by the assistant.
- The "bounces back after accepting consent" symptom is NOT yet confirmed
  fixed on real hardware -- needs explicit re-test, see remaining_work.
</blockers>

<context>
Phase 6 was planned across an earlier session (10 plans, 6 waves,
committed 7b87c2e) and executed wave-by-wave via `/gsd:execute-phase 6`.
Waves 1-4 all executed cleanly. Wave 5 (06-10) is pure manual verification
-- but the FIRST checkpoint immediately surfaced a real, user-facing bug
once actual devices were driving the actual code, which is exactly what
this manual pass exists to catch. The fix is small, well-tested, and
committed. The remaining work is confirming the fix on-device and then
proceeding through the rest of the accessibility/tone checklist.
</context>

<next_action>
Resume by redeploying to both devices (see remaining_work's first
bullet), then ask the user to re-test both the "View..." links and the
"Accept and Continue → Profile Setup" flow specifically before starting
the formal dark-mode/color-blind/tap-target checklist. If the second
symptom recurs even after the fix + a clean reinstall, treat it as a
genuinely separate bug and investigate fresh (the Explore-agent's
widget-test reproduction attempt found nothing wrong in the code as
written, so if it still happens, look for on-device state one clean
install can't be ruled out from a terminal alone -- e.g. capture device
logs live during the repro, or add print/breakpoint instrumentation to
the redirect callback temporarily).
</next_action>
