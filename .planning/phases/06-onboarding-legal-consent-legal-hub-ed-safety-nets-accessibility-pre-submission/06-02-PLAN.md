---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - pubspec.yaml
  - docs/legal/terms.md
  - docs/legal/privacy.md
  - docs/legal/health_disclaimer.md
  - docs/legal/impressum.md
  - lib/domain/services/legal_document_loader.dart
  - lib/core/di/legal_providers.dart
  - test/domain/services/legal_document_loader_test.dart
autonomous: false
requirements:
  - LEGAL-03
  - LEGAL-04
  - LEG-02
  - LEG-03

user_setup: []

must_haves:
  truths:
    - "docs/legal/terms.md, privacy.md, health_disclaimer.md, and impressum.md all contain complete, realistic drafted text (never lorem ipsum or a bare TODO stub) so the app is fully navigable and store-submittable as-is"
    - "None of the four documents contains a user-visible 'DRAFT' or 'pending review' banner in its rendered body -- the pending-legal-review flag is a code comment / tracked TODO only, per D-CONTEXT (Legal Document Content Strategy)"
    - "impressum.md renders its entity name, address, and responsible-person fields as honest, visible placeholder text (e.g. 'Legal Entity Name', 'Address TBD') -- never a fabricated or a hidden/blank value"
    - "health_disclaimer.md contains zero instances of the words diagnose/treat/cure/medical-advice in a claims-making sense, and includes the Google Play January 2026 'not a medical device' disclaimer sentence"
    - "Every one of the four markdown files carries a frontmatter version: field in YYYY-MM-DD format, and LegalDocumentLoader.load()/versionOf() correctly extracts it"
  artifacts:
    - path: "docs/legal/terms.md"
      provides: "Terms of Service full document, frontmatter version field"
    - path: "docs/legal/privacy.md"
      provides: "Privacy Policy, including the explicit OFF API network-call disclosure (PITFALLS.md C4)"
    - path: "docs/legal/health_disclaimer.md"
      provides: "Health Disclaimer, no diagnose/treat/cure language, Play Jan-2026 disclaimer sentence"
    - path: "docs/legal/impressum.md"
      provides: "TMG §5 / MStV §18 disclosures with visible placeholder entity data"
    - path: "lib/domain/services/legal_document_loader.dart"
      provides: "parseLegalDocument() frontmatter extractor + LegalDocumentLoader.load()/versionOf()"
    - path: "lib/core/di/legal_providers.dart"
      provides: "legalDocumentLoaderProvider"
  key_links:
    - from: "lib/domain/services/legal_document_loader.dart"
      to: "docs/legal/*.md"
      via: "rootBundle.loadString('docs/legal/$assetFileName')"
      pattern: "rootBundle.loadString"
    - from: "pubspec.yaml"
      to: "docs/legal/*.md"
      via: "flutter: assets: entry so the markdown files are bundled into the app package"
      pattern: "docs/legal/"

install_reference:
  packages:
    - name: flutter_markdown_plus
      version: "1.0.12"
      pub_score: "160/160"
      publisher: "foresightmobile.com (github.com/foresightmobile/flutter_markdown_plus)"
    - name: shared_preferences
      version: "2.5.5"
      pub_score: "160/160"
      publisher: "flutter.dev (official Flutter team package)"
    - name: package_info_plus
      version: "10.2.1"
      pub_score: "150/160"
      publisher: "fluttercommunity.dev (github.com/fluttercommunity/plus_plugins)"
---

<objective>
Install the three new packages this phase's legal-document, onboarding-gate, and consent-versioning work all depend on (`flutter_markdown_plus`, `shared_preferences`, `package_info_plus`) behind a single package-legitimacy checkpoint, draft the four complete legal documents (Terms, Privacy, Health Disclaimer, Impressum) as versioned markdown source files, and build the hand-rolled frontmatter-version parser that reads them.

Purpose: Every later Phase 6 plan that touches legal content, onboarding gating, or consent recording depends on these three packages and/or these four documents existing — installing/drafting them in the earliest wave that needs any of them avoids repeated `pubspec.yaml` conflicts across parallel Wave 2 plans (mirrors Plan 05-08's "earliest wave that needs any new package" precedent).

Output: Three new pubspec dependencies (human-approved), four complete `docs/legal/*.md` documents with `version:` frontmatter, `LegalDocumentLoader` (asset read + frontmatter parse), and `legalDocumentLoaderProvider`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-CONTEXT.md
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-RESEARCH.md
@.planning/research/PITFALLS.md
@docs/design/DESIGN.md
@lib/core/di/co2_settings_providers.dart
@pubspec.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Package legitimacy checkpoint: flutter_markdown_plus, shared_preferences, package_info_plus</name>
  <what-built>Nothing yet -- this checkpoint precedes installing three new pubspec.yaml dependencies: flutter_markdown_plus 1.0.12, shared_preferences 2.5.5, package_info_plus 10.2.1.</what-built>
  <how-to-verify>
    pub.dev/Dart is not a slopcheck-supported ecosystem, so this follows the same independent-signal review + human-approval precedent as Plans 04-11/05-08/05-09/05-16. Please check each package directly on pub.dev before approving:
    1. https://pub.dev/packages/flutter_markdown_plus -- confirm ~160/160 pub score, BSD-3-Clause, `foresightmobile/flutter_markdown_plus` repo, positioned as the community successor to Google's discontinued `flutter_markdown` (flutter/flutter#162966).
    2. https://pub.dev/packages/shared_preferences -- confirm official `flutter.dev` publisher, flutter-favorite badge, ~160/160 pub score.
    3. https://pub.dev/packages/package_info_plus -- confirm `fluttercommunity.dev` verified publisher (same family as already-approved `share_plus`/`connectivity_plus`), ~150/160 pub score.
    None of these three packages make any network call themselves (asset markdown rendering, on-device key-value storage, and local build-metadata reading only) -- confirms AUTH-07/PRIV-08 compliance.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `flutter pub add flutter_markdown_plus shared_preferences package_info_plus`, or describe concerns.</resume-signal>
  <action>No automated action in this task -- it is a blocking human-verify checkpoint. The actual `flutter pub add` install is the first line of the next task's &lt;action&gt;, which only runs after this checkpoint is approved.</action>
</task>

<task type="auto">
  <name>Task 1: Draft the four legal documents</name>
  <files>
    pubspec.yaml,
    docs/legal/terms.md,
    docs/legal/privacy.md,
    docs/legal/health_disclaimer.md,
    docs/legal/impressum.md
  </files>
  <action>
    Run `flutter pub add flutter_markdown_plus shared_preferences package_info_plus` (only after the checkpoint above is approved). Add a version-comment block to each new `pubspec.yaml` entry matching this project's existing convention (see `flutter_slidable`'s comment block) citing the pub score and this session's verification. Add `- docs/legal/` to the `flutter: assets:` list (alongside the existing `assets/fonts/` and `assets/off_reference.sqlite.gz` entries) so the four markdown files are bundled into the app package.

    Create `docs/legal/` and draft all four documents as complete, realistic, store-submittable markdown text (per 06-CONTEXT.md: "Claude drafts realistic, complete text now — not stub/lorem placeholders"). Each file starts with YAML frontmatter `---\nversion: YYYY-MM-DD\n---\n` using today's date (mirror `docs/design/DESIGN.md`'s frontmatter pattern), then the document body as markdown headings/paragraphs/lists. Every document must include an HTML-comment code marker near the top (`&lt;!-- PENDING LEGAL REVIEW: Fachanwalt IT-Recht sign-off tracked outside Phase 6, see STATE.md Todos --&gt;`) — this renders as nothing in `flutter_markdown_plus` (HTML comments are stripped) so it is never user-visible, satisfying "code comments and a tracked TODO only — never a user-visible banner."

    - `terms.md`: standard consumer-app Terms of Service — acceptance, description of service (free, offline-first nutrition/CO₂ tracker), user responsibilities, no-warranty/limitation-of-liability, changes to terms, governing law (Germany), contact.
    - `privacy.md`: GDPR-oriented Privacy Policy — what data is collected (all personal data on-device by default in Local Mode; zero third-party analytics/ad/tracking SDKs), the **explicit, PITFALLS.md-C4-mandated sentence**: "This app makes network requests to (1) the Open Food Facts API for food search and barcode lookups. It makes NO requests to any other service in Local Mode." — plus data subject rights summary (access/rectify/portability/erasure — cross-referencing the in-app Export Data and Danger Zone features by name, no new mechanism promised), retention, contact, and note that this markdown is the in-app copy (the public-hosting-URL gap for Play Console is a separate pre-submission blocker, do not fabricate a URL).
    - `health_disclaimer.md`: per 06-CONTEXT.md/PITFALLS.md C6 — states the app does not provide medical advice and is not a substitute for professional dietary or medical guidance; explicitly avoid "diagnose"/"treat"/"cure"/"medical advice"-as-a-service language anywhere; frame BMR/TDEE calculations as *estimates*, never precise/prescriptive; use "helps you track" not "helps you lose weight" phrasing; include the Google Play January-2026 required sentence verbatim or a close paraphrase preserving "not a medical device," "does not diagnose, treat, or prevent any condition"; end with the Germany-specific ED-helpline resources (BZgA eating-disorder line, bzga-essstoerungen.de, 0221 892031; ANAD e.V., anad.de, 089 2199730) plus the international fallback (findahelpline.com) — flag inline that these must be human-verified before real submission (per RESEARCH.md Assumption A4), do not present as unverified to the end user, only as an internal comment.
    - `impressum.md`: TMG §5 / MStV §18 required fields — Legal Entity/Operator, Address, Contact Email, Responsible Person (§18 MStV) — every one of these four fields renders literal, honest placeholder text ("Legal Entity Name — pending confirmation", "Address TBD", "contact@reduceco2now.example — placeholder", "Responsible Person — pending confirmation") per 06-CONTEXT.md's locked decision, NOT hidden and NOT fabricated with a real individual's name/address. Include an HTML-comment note that this is blocked on a decision from Dr. Thomas / ReduceCO2Now leadership (tracked pre-launch blocker, not resolved this phase).
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; grep -L "^version: [0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\}$" docs/legal/terms.md docs/legal/privacy.md docs/legal/health_disclaimer.md docs/legal/impressum.md; echo "exit=$?"</automated>
  </verify>
  <done>All four packages appear in `pubspec.yaml`/`pubspec.lock`. All four `docs/legal/*.md` files exist with a valid `version:` frontmatter line, complete drafted body text, no user-visible draft banner, and `impressum.md` shows visible placeholder entity data. The `grep -L` verify command prints nothing matched (i.e. every file has the frontmatter) and `echo "exit=$?"` prints `exit=1` (grep -L found zero non-matching files).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LegalDocumentLoader (frontmatter parser + asset loader)</name>
  <files>
    lib/domain/services/legal_document_loader.dart,
    lib/core/di/legal_providers.dart,
    test/domain/services/legal_document_loader_test.dart
  </files>
  <behavior>
    - `parseLegalDocument(raw)` on well-formed input (`---\nversion: 2026-08-04\n---\nBody text`) returns `(version: '2026-08-04', body: 'Body text')` with frontmatter stripped and leading whitespace trimmed from body.
    - `parseLegalDocument(raw)` on input with no leading `---` returns `(version: 'unknown', body: raw)` unchanged.
    - `parseLegalDocument(raw)` on input with an unterminated frontmatter block (`---` opener but no closing `\n---`) returns `(version: 'unknown', body: raw)`.
    - `parseLegalDocument(raw)` on a frontmatter block present but missing a `version:` line returns `(version: 'unknown', body: <stripped body>)`.
    - `LegalDocumentLoader.load('terms.md')` (integration-style, using `TestWidgetsFlutterBinding.ensureInitialized()` + the real bundled asset) returns the actual version/body from the real `docs/legal/terms.md` drafted in Task 1.
    - `LegalDocumentLoader.versionOf('terms.md')` returns just the version string.
  </behavior>
  <action>
    Remove the `skip:` marker from `test/domain/services/legal_document_loader_test.dart` (created in Plan 06-01) and replace its placeholder cases with the real behavior above (RED first, confirm failure, per the `behavior` block).

    Create `lib/domain/services/legal_document_loader.dart`: a top-level function `({String version, String body}) parseLegalDocument(String raw)` implementing the frontmatter scalar-extraction logic exactly as RESEARCH.md Pattern 3 documents (hand-rolled, no `yaml` package — mirrors the project's `check_privacy_deps.dart` precedent). Then `class LegalDocumentLoader` with `const LegalDocumentLoader()` constructor, `Future<({String version, String body})> load(String assetFileName) async => parseLegalDocument(await rootBundle.loadString('docs/legal/$assetFileName'))`, and `Future<String> versionOf(String assetFileName) async => (await load(assetFileName)).version`.

    Create `lib/core/di/legal_providers.dart` with `@riverpod LegalDocumentLoader legalDocumentLoader(Ref ref) => const LegalDocumentLoader();` (not keepAlive — the loader is stateless, cheap to reconstruct, holds no DB/stream resources).
  </action>
  <verify>
    <automated>flutter test test/domain/services/legal_document_loader_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`legal_document_loader_test.dart` is green with zero skips. `parseLegalDocument` handles all four cases in the behavior block. `LegalDocumentLoader.load('terms.md')` returns the actual frontmatter version drafted in Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Bundled asset → markdown renderer | `docs/legal/*.md` is a build-time bundled asset, never fetched at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-SC | Tampering | pub.dev installs (flutter_markdown_plus, shared_preferences, package_info_plus) | mitigate | Blocking human-verify checkpoint before install; all three carry official/flutter-favorite/verified-publisher status |
| T-06-02-01 | Tampering | `docs/legal/*.md` bundled asset | accept | Documents are compiled into the app package at build time, not fetched remotely — no runtime injection surface; a repackaged/sideloaded APK altering bundled assets is an accepted residual risk consistent with every other bundled asset in this project (`assets/off_reference.sqlite.gz`) |
</threat_model>

<verification>
`flutter pub get` succeeds with all three new packages resolved. `flutter test test/domain/services/legal_document_loader_test.dart` is fully green. All four `docs/legal/*.md` files parse correctly via `LegalDocumentLoader`.
</verification>

<success_criteria>
Three new packages installed and human-approved. Four complete, versioned legal documents exist as the single source of truth for all later Legal Consent / Legal Hub screens. `LegalDocumentLoader` correctly extracts each document's version for `LEGAL-03`'s `policyVersion`.
</success_criteria>

<output>
Create `.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-02-SUMMARY.md` when done
</output>
