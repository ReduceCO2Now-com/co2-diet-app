---
phase: 06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission
plan: 07
type: execute
wave: 3
depends_on:
  - 06-04
files_modified:
  - lib/features/legal/widgets/consent_checkbox_tile.dart
  - lib/features/legal/screens/legal_consent_screen.dart
  - test/features/legal/legal_consent_screen_test.dart
autonomous: true
requirements:
  - LEGAL-01
  - LEGAL-02
  - LEGAL-03
  - LEGAL-04
  - LEG-02
  - ACC-02
  - ACC-03
  - ACC-05
  - NFR-01

must_haves:
  truths:
    - "No checkbox on the Legal Consent screen starts pre-checked"
    - "Accept and Continue stays disabled until all 4 mandatory checkboxes (Terms, Privacy, not-medical-advice, user-responsibility) are checked -- the 5th (16+) checkbox is optional and never gates the button"
    - "View Terms / View Privacy Policy / View Disclaimer links are visible and reachable before any box is checked, and open the exact same LegalDocumentScreen the Legal Hub uses"
    - "Tapping Accept and Continue calls ConsentNotifier.acceptConsent with the real accepted checkbox identifiers, including age_16_plus only when that box was checked"
    - "The screen scales correctly at 1.6x text scale with no overflow, and every checkbox row has a screen-reader-discoverable label"
  artifacts:
    - path: "lib/features/legal/screens/legal_consent_screen.dart"
      provides: "LegalConsentScreen -- 4 mandatory + 1 optional checkbox, gated Accept and Continue"
    - path: "lib/features/legal/widgets/consent_checkbox_tile.dart"
      provides: "Reusable checkbox row widget with label + inline document link"
  key_links:
    - from: "lib/features/legal/screens/legal_consent_screen.dart"
      to: "lib/features/legal/providers/consent_notifier.dart"
      via: "ConsentNotifier.acceptConsent(consentsGiven: [...])"
      pattern: "acceptConsent"
    - from: "lib/features/legal/screens/legal_consent_screen.dart"
      to: "lib/features/legal/screens/legal_document_screen.dart"
      via: "context.push('/legal-hub/document?doc=terms|privacy|health_disclaimer')"
      pattern: "legal-hub/document"
---

<objective>
Build the Legal Consent screen: 4 mandatory + 1 optional checkbox, gated "Accept and Continue," and links into the shared `LegalDocumentScreen` — the screen every onboarding path (and, per 06-CONTEXT.md, every future Account Mode path in Phase 7) must pass through before reaching Profile Setup.

Purpose: LEGAL-01/02/03/04 are the GDPR-consent-validity core of this phase (PITFALLS.md C5) — legally invalid consent risks a €20M/4%-turnover GDPR Art. 83 fine exposure. This screen is where "checkbox exists" becomes "consent is freely given, specific, informed, unambiguous, granular, revocable, and demonstrable."

Output: `LegalConsentScreen`, `ConsentCheckboxTile`, a green widget test covering the gating/pre-checked/link/scaling/semantics behaviors.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-CONTEXT.md
@.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-RESEARCH.md
@.planning/research/PITFALLS.md
@lib/features/legal/providers/consent_notifier.dart
@lib/features/legal/screens/legal_document_screen.dart
@lib/core/theme/color_tokens.dart
@lib/core/theme/text_tokens.dart
@lib/core/theme/spacing_tokens.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConsentCheckboxTile + LegalConsentScreen</name>
  <files>
    lib/features/legal/widgets/consent_checkbox_tile.dart,
    lib/features/legal/screens/legal_consent_screen.dart,
    test/features/legal/legal_consent_screen_test.dart
  </files>
  <behavior>
    - Initial render: all 5 `Checkbox` widgets are unchecked; "Accept and Continue" is disabled (`onPressed: null` or an equivalent disabled visual/interactive state).
    - Checking Terms, Privacy, not-medical-advice, and user-responsibility (all 4 mandatory, leaving the optional 16+ box unchecked) enables "Accept and Continue."
    - Unchecking any one of the 4 mandatory boxes after all were checked disables the button again.
    - Checking only 3 of the 4 mandatory boxes plus the optional 16+ box leaves the button disabled (the optional box never substitutes for a mandatory one).
    - Tapping "View Terms" (before any box is checked) successfully navigates without requiring any checkbox state.
    - Tapping "Accept and Continue" with all 4 mandatory + the optional box checked calls `acceptConsent` with a 5-element list including `'age_16_plus'`; with only the 4 mandatory checked, the list has exactly 4 elements and omits `'age_16_plus'`.
    - At `MediaQuery` text scale 1.6x, the screen renders with no `RenderFlex overflowed` exception.
    - Each `ConsentCheckboxTile` exposes a `Semantics`/`mergeSemantics` label combining the checkbox state and its text, discoverable via `find.bySemanticsLabel` (ACC-03).
  </behavior>
  <action>
    Remove the `skip:` marker from `test/features/legal/legal_consent_screen_test.dart` (created in Plan 06-01) and replace its placeholder cases with the real behavior above, using a `ProviderScope` override of `consentNotifierProvider`/`consentRepositoryProvider` (mocktail or a fake in-memory `IConsentRepository`) so no real Drift database is needed.

    Create `lib/features/legal/widgets/consent_checkbox_tile.dart`: `class ConsentCheckboxTile extends StatelessWidget` taking `required bool value`, `required ValueChanged&lt;bool?&gt; onChanged`, `required String label`, `Widget? trailingLink` (optional inline "View Terms"-style link widget rendered after the label). Wraps a `CheckboxListTile` (or `Row(Checkbox + Expanded(Text) + trailingLink)`) in a `MergeSemantics`/explicit `Semantics(label: ..., checked: value)` so screen readers announce the full row as one element (ACC-03), and ensures the tappable row area is at least 44x44pt (ACC-05) via `minVerticalPadding`/a minimum-height constraint.

    Create `lib/features/legal/screens/legal_consent_screen.dart`: `class LegalConsentScreen extends ConsumerStatefulWidget`. State holds 5 `bool` fields, ALL initialized to `false` — never pre-checked (PITFALLS.md C5's hard requirement). Renders a `Scaffold` with heading "Before you start" / body copy "We need your consent to continue.", 5 `ConsentCheckboxTile` rows: Terms of Service (with a `trailingLink` `TextButton('View Terms')` → `context.push('/legal-hub/document?doc=terms')`), Privacy Policy (`TextButton('View Privacy Policy')` → `?doc=privacy`), the not-medical-advice disclaimer and user-responsibility disclaimer (each linking to `?doc=health_disclaimer` per 06-CONTEXT.md's "Health Disclaimer is also linked from the Legal Consent screen" / LEG-02), and the optional 16+ self-declaration row with no document link. A `_canAccept` getter requiring exactly the 4 mandatory booleans (never the 5th). A bottom `FilledButton('Accept and Continue')` with `onPressed: _canAccept ? _onAccept : null`.

    `_onAccept()`: builds `final consentsGiven = ['terms', 'privacy', 'not_medical_advice', 'user_responsibility', if (_age16PlusChecked) 'age_16_plus'];`, calls `await ref.read(consentNotifierProvider.notifier).acceptConsent(consentsGiven: consentsGiven);`, then `if (mounted) context.go('/profile');` (RESEARCH.md Pattern 2 — Profile Setup is the next onboarding step).
  </action>
  <verify>
    <automated>flutter test test/features/legal/legal_consent_screen_test.dart 2>&amp;1 | tail -25</automated>
  </verify>
  <done>`legal_consent_screen_test.dart` is green with zero skips, covering every case in the behavior block above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User checkbox interaction → durable consent write | UI state is transient; `ConsentNotifier.acceptConsent` is the one write side-effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering / Spoofing | Pre-checked or bundled consent boxes making consent legally invalid | mitigate | All 5 booleans hard-coded to `false` at construction, never derived from a stored value; the 4 mandatory checks are independently tracked (never a single "accept all" flag) — verified by the widget test's initial-render assertions |
| T-06-07-02 | Repudiation | User later disputes what exactly they consented to | mitigate | `consentsGiven` sent to `acceptConsent` reflects the exact live checkbox state at the moment of tapping Accept, not a fixed constant — combined with Plan 06-04's durable, timestamped, versioned write |
</threat_model>

<verification>
`flutter test test/features/legal/legal_consent_screen_test.dart` is fully green.
</verification>

<success_criteria>
The Legal Consent screen is legally sound per PITFALLS.md C5's checklist (no pre-checked boxes, no bundled consent, granular tracking, durable versioned write) and reuses the shared document-viewer component per 06-CONTEXT.md.
</success_criteria>

<output>
Create `.planning/phases/06-onboarding-legal-consent-legal-hub-ed-safety-nets-accessibility-pre-submission/06-07-SUMMARY.md` when done
</output>
