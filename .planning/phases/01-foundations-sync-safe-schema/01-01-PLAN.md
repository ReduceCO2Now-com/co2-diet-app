---
phase: 01-foundations-sync-safe-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/color_tokens.dart
  - lib/core/theme/text_tokens.dart
  - lib/core/theme/spacing_tokens.dart
  - assets/fonts/PlusJakartaSans-Regular.ttf
  - assets/fonts/PlusJakartaSans-Medium.ttf
  - assets/fonts/PlusJakartaSans-SemiBold.ttf
  - assets/fonts/PlusJakartaSans-Bold.ttf
  - assets/fonts/Inter-Regular.ttf
  - assets/fonts/Inter-SemiBold.ttf
  - analysis_options.yaml
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05

must_haves:
  truths:
    - "flutter pub get succeeds with all Phase 1 dependency versions pinned"
    - "dart run build_runner build --delete-conflicting-outputs exits 0"
    - "flutter analyze reports zero errors"
    - "ThemeData.light() and ThemeData.dark() are constructed from DESIGN.md tokens verbatim"
    - "Plus Jakarta Sans and Inter fonts are bundled as assets (not loaded from google_fonts network)"
    - "MaterialApp.router runs ProviderScope at root"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies at exact versions from RESEARCH.md"
      contains: "drift: 2.34.2"
    - path: "lib/core/theme/color_tokens.dart"
      provides: "All DESIGN.md color hex values as const Color AppColors"
      contains: "AppColors"
    - path: "lib/core/theme/app_theme.dart"
      provides: "buildLightTheme() and buildDarkTheme() functions"
      exports: ["buildLightTheme", "buildDarkTheme"]
    - path: "lib/main.dart"
      provides: "App entrypoint with ProviderScope root"
      contains: "ProviderScope"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp(ProviderScope(child: Co2DietApp()))"
      pattern: "ProviderScope"
    - from: "lib/app.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "MaterialApp.router theme/darkTheme parameters"
      pattern: "buildLightTheme|buildDarkTheme"
---

<objective>
Scaffold the Flutter project, wire all Phase 1 dependencies in pubspec.yaml at the exact verified versions from RESEARCH.md, bundle Plus Jakarta Sans and Inter fonts as local assets, implement ThemeData (light and dark) from DESIGN.md tokens verbatim, and wire the root MaterialApp.router with ProviderScope.

Purpose: Every subsequent plan in Phase 1 imports from this skeleton. Getting dependency versions, font bundling, and theme tokens exactly right here prevents cascading correctness failures downstream.

Output: Compiling Flutter project with ProviderScope at root, correct pubspec.yaml, and a `lib/core/theme/` module containing the complete token-faithful ThemeData.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/ROADMAP.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/phases/01-foundations-sync-safe-schema/01-RESEARCH.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/docs/design/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flutter project scaffold + pubspec.yaml with all Phase 1 dependencies</name>
  <files>
    pubspec.yaml,
    analysis_options.yaml,
    lib/main.dart,
    lib/app.dart,
    assets/fonts/PlusJakartaSans-Regular.ttf,
    assets/fonts/PlusJakartaSans-Medium.ttf,
    assets/fonts/PlusJakartaSans-SemiBold.ttf,
    assets/fonts/PlusJakartaSans-Bold.ttf,
    assets/fonts/Inter-Regular.ttf,
    assets/fonts/Inter-SemiBold.ttf
  </files>
  <action>
    The repo already contains .claude/, .planning/, docs/ directories but no Flutter project yet. Do NOT run flutter create in a way that overwrites existing directories.

    Step 1 — Create the Flutter project skeleton:
    Run `flutter create --org com.reduceco2now --project-name co2diet .` from the repo root. Verify afterward that android/app/build.gradle contains `applicationId "com.reduceco2now.co2diet"` (no underscore). If flutter create refuses to run in a non-empty directory, use the --overwrite flag. Do NOT let it overwrite .claude/, .planning/, docs/.

    Step 2 — Replace pubspec.yaml entirely with the following exact dependency set (do not use flutter pub add; write pubspec.yaml directly to control exact versions):

    name: co2diet
    description: CO2 Diet — privacy-first nutrition and CO2 footprint tracker
    publish_to: none
    version: 0.1.0+1

    environment:
      sdk: ">=3.12.2 <4.0.0"
      flutter: ">=3.44.6"

    dependencies:
      flutter:
        sdk: flutter
      flutter_localizations:
        sdk: flutter
      drift: 2.34.2
      drift_flutter: 0.3.1
      path_provider: ^2.1.6
      flutter_riverpod: 3.3.2
      riverpod_annotation: 4.0.3
      go_router: 17.3.0
      uuid: 4.6.0
      intl: 0.20.3
      freezed_annotation: 3.1.0

    dev_dependencies:
      flutter_test:
        sdk: flutter
      integration_test:
        sdk: flutter
      drift_dev: 2.34.4
      build_runner: 2.15.2
      riverpod_generator: 4.0.4
      riverpod_lint: 3.1.4
      custom_lint: 0.8.1
      very_good_analysis: 10.3.0
      freezed: 3.2.5
      mocktail: 1.0.5

    flutter:
      uses-material-design: true
      assets:
        - assets/fonts/
      fonts:
        - family: Plus Jakarta Sans
          fonts:
            - asset: assets/fonts/PlusJakartaSans-Regular.ttf
            - asset: assets/fonts/PlusJakartaSans-Medium.ttf
              weight: 500
            - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
              weight: 600
            - asset: assets/fonts/PlusJakartaSans-Bold.ttf
              weight: 700
        - family: Inter
          fonts:
            - asset: assets/fonts/Inter-Regular.ttf
            - asset: assets/fonts/Inter-SemiBold.ttf
              weight: 600

    CRITICAL: Do NOT add sqlite3_flutter_libs — it is EOL. Do NOT add the google_fonts package — fonts must be local assets only (privacy requirement). Do NOT add the hlc package — we hand-roll it in Plan 01-02.

    Step 3 — Download font files. Use curl or wget to download from Google Fonts static URLs:
    - Plus Jakarta Sans weights 400, 500, 600, 700 — save as PlusJakartaSans-Regular.ttf, PlusJakartaSans-Medium.ttf, PlusJakartaSans-SemiBold.ttf, PlusJakartaSans-Bold.ttf
    - Inter weights 400 and 600 — save as Inter-Regular.ttf and Inter-SemiBold.ttf
    Place all six files in assets/fonts/. Create the assets/fonts/ directory if it does not exist.

    Google Fonts download URLs (static, no tracking):
      https://fonts.gstatic.com/s/plusjakartasans/v8/LDIbaomQNQcsA88c7O9yZ4KMCoOg4IA6-91aHEjcWuA_KU7NShXUEKi4Rw.ttf
    Note: Direct .ttf static URLs change. Use the Google Fonts download zip from https://fonts.google.com/specimen/Plus+Jakarta+Sans and https://fonts.google.com/specimen/Inter — unzip and copy the correct weight files. If curl to individual TTF URLs fails, download the zip and extract.

    Step 4 — Write lib/main.dart:
    Import flutter_riverpod. Create main() that calls runApp(const ProviderScope(child: Co2DietApp())). Co2DietApp is imported from lib/app.dart.

    Step 5 — Write lib/app.dart as a minimal StatelessWidget Co2DietApp that returns a MaterialApp (not MaterialApp.router yet — the router is wired in Plan 01-05). For now use MaterialApp with home: Scaffold(body: Center(child: Text('CO2 Diet'))). Import buildLightTheme and buildDarkTheme from lib/core/theme/app_theme.dart. Set theme: buildLightTheme() and darkTheme: buildDarkTheme() and themeMode: ThemeMode.system.

    Step 6 — Write analysis_options.yaml:
    include: package:very_good_analysis/analysis_options.yaml
    analyzer:
      exclude:
        - "**/*.g.dart"
        - "**/*.freezed.dart"
    custom_lint:
      rules:
        - avoid_public_notifier_properties

    Step 7 — Run flutter pub get and verify it exits 0. If dependency conflicts arise, investigate and resolve (do not lower version constraints without checking the RESEARCH.md compatibility table).
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze --no-fatal-warnings 2>&1 | tail -5</automated>
  </verify>
  <done>flutter pub get exits 0; flutter analyze reports 0 errors (warnings about unused imports in generated stubs are acceptable); pubspec.lock exists; assets/fonts/ contains exactly 6 .ttf files</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThemeData light + dark from DESIGN.md tokens verbatim</name>
  <files>
    lib/core/theme/color_tokens.dart,
    lib/core/theme/text_tokens.dart,
    lib/core/theme/spacing_tokens.dart,
    lib/core/theme/app_theme.dart,
    test/core/theme/theme_token_test.dart
  </files>
  <behavior>
    - Test 1: AppColors.primary == const Color(0xFF005222)
    - Test 2: AppColors.onPrimary == const Color(0xFFFFFFFF)
    - Test 3: AppColors.primaryContainer == const Color(0xFF006D2F)
    - Test 4: AppColors.surface == const Color(0xFFF9F9FC)
    - Test 5: AppColors.error == const Color(0xFFBA1A1A)
    - Test 6: buildLightTheme() returns a ThemeData where colorScheme.primary == AppColors.primary
    - Test 7: buildDarkTheme() returns a ThemeData where colorScheme.surface == AppColors.inverseSurface
    - Test 8: buildLightTheme() and buildDarkTheme() both build without throwing
  </behavior>
  <action>
    Write tests first in test/core/theme/theme_token_test.dart. Run dart test — tests must be RED. Then implement the production files until tests are GREEN.

    lib/core/theme/color_tokens.dart — create abstract final class AppColors with one const Color per DESIGN.md color token. Map EVERY color in the DESIGN.md frontmatter. Hex values verbatim:
      surface: Color(0xFFF9F9FC), surfaceDim: Color(0xFFDADADC), surfaceBright: Color(0xFFF9F9FC),
      surfaceContainerLowest: Color(0xFFFFFFFF), surfaceContainerLow: Color(0xFFF3F3F6),
      surfaceContainer: Color(0xFFEEEEF0), surfaceContainerHigh: Color(0xFFE8E8EA),
      surfaceContainerHighest: Color(0xFFE2E2E5), onSurface: Color(0xFF1A1C1E),
      onSurfaceVariant: Color(0xFF3F493F), inverseSurface: Color(0xFF2F3133),
      inverseOnSurface: Color(0xFFF0F0F3), outline: Color(0xFF6F7A6E),
      outlineVariant: Color(0xFFBECABC), surfaceTint: Color(0xFF016D2F),
      primary: Color(0xFF005222), onPrimary: Color(0xFFFFFFFF),
      primaryContainer: Color(0xFF006D2F), onPrimaryContainer: Color(0xFF90EC9F),
      inversePrimary: Color(0xFF7FDA8F), secondary: Color(0xFF0155C7),
      onSecondary: Color(0xFFFFFFFF), secondaryContainer: Color(0xFF336FE2),
      onSecondaryContainer: Color(0xFFFEFCFF), tertiary: Color(0xFF00512D),
      onTertiary: Color(0xFFFFFFFF), tertiaryContainer: Color(0xFF006C3D),
      onTertiaryContainer: Color(0xFF91EAAF), error: Color(0xFFBA1A1A),
      onError: Color(0xFFFFFFFF), errorContainer: Color(0xFFFFDAD6),
      onErrorContainer: Color(0xFF93000A), primaryFixed: Color(0xFF9BF7A9),
      primaryFixedDim: Color(0xFF7FDA8F), onPrimaryFixed: Color(0xFF002109),
      onPrimaryFixedVariant: Color(0xFF005322), secondaryFixed: Color(0xFFD9E2FF),
      secondaryFixedDim: Color(0xFFB0C6FF), onSecondaryFixed: Color(0xFF001945),
      onSecondaryFixedVariant: Color(0xFF00419C), tertiaryFixed: Color(0xFF9CF6BA),
      tertiaryFixedDim: Color(0xFF80D99F), onTertiaryFixed: Color(0xFF00210F),
      onTertiaryFixedVariant: Color(0xFF00522D), background: Color(0xFFF9F9FC),
      onBackground: Color(0xFF1A1C1E), surfaceVariant: Color(0xFFE2E2E5),
      leafGreen: Color(0xFF2EB85C), softMint: Color(0xFF4BB477), skyBlue: Color(0xFF316FE2),
      surfaceLowest: Color(0xFFFFFFFF)

    lib/core/theme/text_tokens.dart — define AppTextTheme with TextStyle constants matching DESIGN.md typography:
      displayLg: Plus Jakarta Sans, 40px, w700, h48, ls -0.02em
      headlineLg: Plus Jakarta Sans, 32px, w700, h40, ls -0.02em
      headlineLgMobile: Plus Jakarta Sans, 28px, w700, h34
      titleMd: Plus Jakarta Sans, 20px, w600, h28
      bodyLg: Plus Jakarta Sans, 16px, w400, h24
      bodySm: Plus Jakarta Sans, 14px, w400, h20
      labelCaps: Inter, 12px, w600, h16, ls 0.05em, uppercase

    lib/core/theme/spacing_tokens.dart — define AppSpacing with const double constants:
      base: 4, xs: 8, sm: 16, md: 24, lg: 32, xl: 48, containerMargin: 20, stackGap: 12

    lib/core/theme/app_theme.dart — implement buildLightTheme() and buildDarkTheme():

    buildLightTheme(): ThemeData(
      useMaterial3: true,
      colorScheme: ColorScheme.light(...) — map every AppColors field to the matching ColorScheme slot.
        Primary group: primary, onPrimary, primaryContainer, onPrimaryContainer, inversePrimary, surfaceTint
        Secondary group: secondary, onSecondary, secondaryContainer, onSecondaryContainer
        Tertiary group: tertiary, onTertiary, tertiaryContainer, onTertiaryContainer
        Error group: error, onError, errorContainer, onErrorContainer
        Surface group: surface, onSurface, onSurfaceVariant, outline, outlineVariant,
          inverseSurface, onInverseSurface (=inverseOnSurface), surfaceContainerLowest,
          surfaceContainerLow, surfaceContainer, surfaceContainerHigh, surfaceContainerHighest
      fontFamily: 'Plus Jakarta Sans',
      textTheme: TextTheme — map AppTextTheme constants to displayLarge, headlineLarge, titleMedium, bodyLarge, bodyMedium, labelSmall etc.
      cardTheme: CardThemeData(shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)))
      inputDecorationTheme: border uses outlineVariant, focusedBorder uses primary 2px
    )

    buildDarkTheme(): ThemeData.dark(useMaterial3: true) — use Material 3 inverse-surface convention per RESEARCH.md discretion note:
      surface = AppColors.inverseSurface (0xFF2F3133)
      onSurface = AppColors.inverseOnSurface (0xFFF0F0F3)
      primary = AppColors.inversePrimary (0xFF7FDA8F)
      onPrimary = AppColors.onPrimaryFixed (0xFF002109)
      Keep error, secondary, tertiary groups the same — they work on dark bg.
      surfaceContainerHighest = Color(0xFF3A3C3E) (slightly lighter than inverseSurface)
      All other tokens: apply Material 3 dark-mode mapping (inverse/on-surface for canvas, primary-fixed-dim for accents).

    The test file tests both theme builds and spot-checks AppColors values against the raw hex constants from DESIGN.md.
  </action>
  <verify>
    <automated>dart test test/core/theme/theme_token_test.dart</automated>
  </verify>
  <done>All 8 theme_token_test cases pass; lib/core/theme/ contains 4 files; flutter analyze reports 0 errors on the theme module</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → pub.dev | Package resolution fetches code from pub.dev CDN; malicious package could introduce tracking SDK |
| Google Fonts download → assets/fonts/ | Font files downloaded from external URL during setup; must be committed to git, not fetched at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | google_fonts network mode | mitigate | BANNED — do not add google_fonts package; fonts must be local assets declared in pubspec.yaml flutter.fonts section |
| T-01-02 | Tampering | pubspec.yaml dependency versions | mitigate | Exact versions pinned (not ^ ranges) for all Phase 1 packages per RESEARCH.md verified list; pubspec.lock committed to git |
| T-01-03 | Information Disclosure | transitive analytics SDK via pub dependency | mitigate | Phase 1 stack has zero analytics transitive deps (verified in RESEARCH.md audit); CI blocklist (Plan 01-06) is the runtime gate |
| T-01-SC | Tampering | pub.dev package installs | mitigate | All Phase 1 packages have Approved disposition in RESEARCH.md Package Legitimacy Audit; no [ASSUMED] or [SUS] packages in the approved set |
</threat_model>

<verification>
After both tasks complete:
1. `flutter pub get` exits 0
2. `flutter analyze --no-fatal-warnings` exits 0
3. `dart test test/core/theme/theme_token_test.dart` exits 0 (8 tests pass)
4. `grep -r "google_fonts" pubspec.yaml` returns empty (forbidden package absent)
5. `grep -r "sqlite3_flutter_libs" pubspec.yaml` returns empty (EOL package absent)
6. `ls assets/fonts/*.ttf | wc -l` returns 6
7. `grep "drift: 2.34.2" pubspec.yaml` returns match
</verification>

<success_criteria>
- pubspec.yaml declares all Phase 1 packages at the exact versions from RESEARCH.md; pubspec.lock is committed
- assets/fonts/ contains PlusJakartaSans-{Regular,Medium,SemiBold,Bold}.ttf and Inter-{Regular,SemiBold}.ttf (6 files total)
- lib/core/theme/color_tokens.dart defines every color token from DESIGN.md frontmatter as a const Color
- lib/core/theme/app_theme.dart exports buildLightTheme() and buildDarkTheme() that construct without throwing
- flutter analyze exits 0 on the theme module
- dart test on the theme test file exits 0
</success_criteria>

<output>
Create `.planning/phases/01-foundations-sync-safe-schema/01-01-SUMMARY.md` when done
</output>
