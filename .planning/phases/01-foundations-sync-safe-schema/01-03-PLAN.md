---
phase: 01-foundations-sync-safe-schema
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - lib/domain/entities/user_profile.dart
  - lib/domain/entities/user_profile.freezed.dart
  - lib/domain/entities/user_profile.g.dart
  - lib/domain/entities/calc_targets.dart
  - lib/domain/entities/calc_targets.freezed.dart
  - lib/domain/services/mifflin_st_jeor.dart
  - lib/domain/services/target_calculator.dart
  - lib/domain/repositories/i_profile_repository.dart
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05

must_haves:
  truths:
    - "calculateTdee() returns null when any required field (weightKg, heightCm, age) is null"
    - "calculateTdee() returns correct TDEE for a known male input (75kg, 175cm, 30y, low activity)"
    - "calculateTdee() averages male+female BMR for gender=other"
    - "TargetCalculator.derive() returns macro grams derived from TDEE using goal-specific ratios"
    - "UserProfile and CalcTargets are Freezed immutable entities with no Drift or Flutter imports"
    - "IProfileRepository is an abstract interface with getProfile() and saveProfile() methods"
  artifacts:
    - path: "lib/domain/services/mifflin_st_jeor.dart"
      provides: "calculateTdee() pure Dart function; Gender and ActivityLevel enums"
      exports: ["calculateTdee", "Gender", "ActivityLevel"]
    - path: "lib/domain/services/target_calculator.dart"
      provides: "TargetCalculator.derive(tdee, goal) returning CalcTargets"
      exports: ["TargetCalculator"]
    - path: "lib/domain/entities/user_profile.dart"
      provides: "UserProfile Freezed entity"
      contains: "@freezed"
    - path: "lib/domain/entities/calc_targets.dart"
      provides: "CalcTargets Freezed entity with nullable kcal/protein/carbs/fat/co2 fields"
      contains: "@freezed"
    - path: "lib/domain/repositories/i_profile_repository.dart"
      provides: "IProfileRepository abstract interface"
      contains: "abstract interface class IProfileRepository"
  key_links:
    - from: "lib/domain/services/target_calculator.dart"
      to: "lib/domain/services/mifflin_st_jeor.dart"
      via: "calls calculateTdee()"
      pattern: "calculateTdee"
    - from: "lib/domain/services/target_calculator.dart"
      to: "lib/domain/entities/calc_targets.dart"
      via: "returns CalcTargets instance"
      pattern: "CalcTargets"
---

<objective>
Implement the pure Dart domain layer: Mifflin-St Jéor TDEE calculator, macro target calculator by goal, Freezed immutable entities (UserProfile, CalcTargets), and the IProfileRepository abstract interface.

Purpose: The domain layer has zero framework dependencies — it is the testable core of the app. Getting the Mifflin-St Jéor formula exactly right here is a correctness requirement, not an implementation detail. All downstream calculation (Phase 5 dashboard, insights) depends on this layer.

Output: Pure Dart files in lib/domain/ with no Drift, Flutter, or Riverpod imports; Freezed generated code; formula correct against known reference values.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/phases/01-foundations-sync-safe-schema/01-CONTEXT.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/phases/01-foundations-sync-safe-schema/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mifflin-St Jéor TDEE calculator + enums (pure Dart, domain layer)</name>
  <files>
    lib/domain/services/mifflin_st_jeor.dart,
    test/domain/services/mifflin_st_jeor_test.dart
  </files>
  <behavior>
    - Test 1: calculateTdee(weightKg: 75, heightCm: 175, age: 30, gender: Gender.male, activityLevel: ActivityLevel.low) == 2336.875 (±0.01 — verify manually: BMR = 10*75 + 6.25*175 − 5*30 + 5 = 1751.25; TDEE = 1751.25 * 1.375 = 2407.97; recalculate and use exact output)
    - Test 2: calculateTdee(weightKg: 60, heightCm: 165, age: 25, gender: Gender.female, activityLevel: ActivityLevel.medium) matches BMR = (10*60 + 6.25*165 − 5*25 − 161) * 1.55 formula output
    - Test 3: calculateTdee(weightKg: null, heightCm: 175, age: 30, gender: Gender.male, activityLevel: ActivityLevel.low) == null
    - Test 4: calculateTdee(weightKg: 75, heightCm: null, age: 30, gender: Gender.male, activityLevel: ActivityLevel.low) == null
    - Test 5: calculateTdee(weightKg: 75, heightCm: 175, age: null, gender: Gender.male, activityLevel: ActivityLevel.low) == null
    - Test 6: calculateTdee with Gender.other returns average of male + female variants
    - Test 7: ActivityLevel.low maps to factor 1.375; ActivityLevel.medium to 1.55; ActivityLevel.high to 1.725
  </behavior>
  <action>
    Write the test file first. Verify RED. Then implement the production code.

    lib/domain/services/mifflin_st_jeor.dart — ZERO framework imports. Only dart:core.

    Define:
      enum Gender { male, female, other }
      enum ActivityLevel { low, medium, high }

    Mifflin-St Jéor formula (verified against nutriadmin.com + 1990 paper):
      Male:   BMR = (10 × weightKg) + (6.25 × heightCm) − (5 × age) + 5
      Female: BMR = (10 × weightKg) + (6.25 × heightCm) − (5 × age) − 161
      Other:  Average of male and female: BMR = (10 × weightKg) + (6.25 × heightCm) − (5 × age) − 78
              (derived: (male + female) / 2 = base − 78 exactly)

    Activity factors (PAL):
      low:    1.375  (lightly active, 1-3 days/week)
      medium: 1.550  (moderately active, 3-5 days/week)
      high:   1.725  (very active, 6-7 days/week)

    Function signature:
      double? calculateTdee({
        required double? weightKg,
        required double? heightCm,
        required int? age,
        required Gender? gender,
        required ActivityLevel activityLevel,
      })

    Return null if weightKg == null || heightCm == null || age == null.
    Treat gender == null the same as Gender.other (average).
    TDEE = BMR × activityFactor.

    Verify exact output for the Test 1 case by computing manually in a comment before the function. Do not guess — compute it: 10×75=750, 6.25×175=1093.75, 5×30=150, BMR_male = 750+1093.75−150+5 = 1698.75, TDEE = 1698.75 × 1.375 = 2335.78125. Use that exact value in the test (not the placeholder 2336.875 above — recalculate precisely).
  </action>
  <verify>
    <automated>dart test test/domain/services/mifflin_st_jeor_test.dart</automated>
  </verify>
  <done>All 7 Mifflin test cases pass; mifflin_st_jeor.dart has zero non-dart:core imports; calculateTdee returns null on missing fields; formula matches reference output</done>
</task>

<task type="auto">
  <name>Task 2: CalcTargets + UserProfile entities (Freezed) + TargetCalculator + IProfileRepository</name>
  <files>
    lib/domain/entities/user_profile.dart,
    lib/domain/entities/user_profile.freezed.dart,
    lib/domain/entities/user_profile.g.dart,
    lib/domain/entities/calc_targets.dart,
    lib/domain/entities/calc_targets.freezed.dart,
    lib/domain/services/target_calculator.dart,
    lib/domain/repositories/i_profile_repository.dart
  </files>
  <action>
    Create directories: lib/domain/entities/, lib/domain/services/, lib/domain/repositories/

    lib/domain/entities/calc_targets.dart — Freezed immutable value object:
      import 'package:freezed_annotation/freezed_annotation.dart';
      part 'calc_targets.freezed.dart';

      @freezed
      class CalcTargets with _$CalcTargets {
        const factory CalcTargets({
          double? kcalTarget,       // null = missing fields, show —
          double? proteinGTarget,   // null = missing fields, show —
          double? carbsGTarget,
          double? fatGTarget,
          // co2GTarget is stored in schema but NOT shown in UI until Phase 3 (per D-08)
          // Include here for completeness; UI layer ignores it until Phase 3
          double? co2GTarget,
          @Default(false) bool kcalIsOverridden,
          @Default(false) bool proteinIsOverridden,
          @Default(false) bool carbsIsOverridden,
          @Default(false) bool fatIsOverridden,
        }) = _CalcTargets;
      }

    lib/domain/entities/user_profile.dart — Freezed entity mirroring the UserProfileTable columns but as a domain type. No Drift imports.
      import 'package:freezed_annotation/freezed_annotation.dart';
      part 'user_profile.freezed.dart';

      @freezed
      class UserProfile with _$UserProfile {
        const factory UserProfile({
          required String id,
          int? age,
          String? gender,          // 'male' | 'female' | 'other'
          double? heightCm,
          double? weightKg,
          String? activityLevel,   // 'low' | 'medium' | 'high'
          String? dietaryPreference,
          String? goal,
          @Default('metric') String units,
          @Default('1.0') String co2MethodologyVersion,
          String? localeTag,
          CalcTargets? targets,    // computed; null until saved
          DateTime? createdAt,
          DateTime? updatedAt,
        }) = _UserProfile;
      }

    lib/domain/services/target_calculator.dart — pure Dart class (no framework imports):
      import 'mifflin_st_jeor.dart' for enums and calculateTdee.
      import '../entities/calc_targets.dart'

      Macro ratios by goal (from RESEARCH.md — marked [ASSUMED] per research note, standard AMDR):
        'reduce_co2':        protein 25%, carbs 45%, fat 30%
        'lose_weight':       protein 35%, carbs 35%, fat 30%
        'maintain_weight':   protein 25%, carbs 50%, fat 25%
        'gain_muscle':       protein 30%, carbs 45%, fat 25%
        'improve_health':    protein 25%, carbs 50%, fat 25%
        'balanced_lifestyle': protein 25%, carbs 50%, fat 25%
        'learn_and_explore': protein 25%, carbs 50%, fat 25%
        null / unknown: use maintain_weight ratios as default

      class TargetCalculator:
        static CalcTargets derive({
          required double? weightKg,
          required double? heightCm,
          required int? age,
          required String? gender,       // string from entity; convert to Gender enum
          required String? activityLevel, // string; convert to ActivityLevel enum
          required String? goal,
          CalcTargets? existingTargets,  // if field is overridden, preserve the overridden value
        })

        Internally:
          1. Map gender string → Gender enum (null → Gender.other)
          2. Map activityLevel string → ActivityLevel enum (null → ActivityLevel.medium as default)
          3. Call calculateTdee() → double? kcal
          4. If kcal == null, return CalcTargets() with all targets null (per D-07: show — for missing fields)
          5. Else compute macros: protein g = kcal × ratio_p / 4; carbs g = kcal × ratio_c / 4; fat g = kcal × ratio_f / 9
          6. If existingTargets has an override flag set, preserve that field's value instead of recalculating it

    lib/domain/repositories/i_profile_repository.dart — abstract interface:
      abstract interface class IProfileRepository {
        Future<UserProfile?> getProfile();
        Future<void> saveProfile(UserProfile profile);
        Stream<UserProfile?> watchProfile();
      }

    Run Freezed codegen:
      dart run build_runner build --delete-conflicting-outputs

    Verify generated files: user_profile.freezed.dart and calc_targets.freezed.dart exist.
    IMPORTANT: Neither of these files should import package:drift. If codegen introduces Drift imports, there is a misconfiguration — investigate.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -3 && dart analyze lib/domain/ 2>&1 | grep -c error || true</automated>
  </verify>
  <done>Freezed codegen succeeds; lib/domain/ has no Drift or Flutter imports anywhere; dart analyze lib/domain/ exits 0; CalcTargets fields are all nullable (representing the — behavior for missing inputs per D-07)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile form input → Domain layer | User-supplied age/height/weight/goal reach calculateTdee as typed Dart values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Calorie target manipulation via extreme inputs | mitigate | Domain layer returns null for missing fields; Phase 6 will add ED safety net (NFR-07: refuse kcal < 1200 or BMI < 17.5 without surfacing warning); Phase 1 domain layer must not silently accept pathological values — TargetCalculator should clamp output range to 500–10000 kcal with a warning log |
| T-03-02 | Information Disclosure | User biometric data (height/weight/age) | accept | Domain entities are in-memory Dart objects; persisted only to local SQLite by Data layer; no network transmission in Phase 1 |
| T-03-SC | Tampering | pub installs | accept | No new pub.dev packages introduced in this plan; Freezed and freezed_annotation already in pubspec from Plan 01-01 |
</threat_model>

<verification>
After both tasks complete:
1. `dart test test/domain/services/mifflin_st_jeor_test.dart` — 7 tests pass
2. `dart analyze lib/domain/` exits 0
3. `grep -r "import 'package:drift" lib/domain/` — returns empty (no Drift in domain)
4. `grep -r "import 'package:flutter" lib/domain/` — returns empty (no Flutter in domain)
5. `ls lib/domain/entities/user_profile.freezed.dart lib/domain/entities/calc_targets.freezed.dart` — both exist
6. `grep "co2GTarget" lib/domain/entities/calc_targets.dart` — matches (CO2 field present but not shown in UI)
</verification>

<success_criteria>
- calculateTdee() returns exact Mifflin-St Jéor TDEE for known male/female/other inputs; returns null on missing fields (D-07 — no fake precision)
- TargetCalculator.derive() uses goal-specific macro ratios to produce CalcTargets from TDEE
- CalcTargets has nullable fields for all 4 macro targets (null = —, per D-07); override flags per D-06
- UserProfile and CalcTargets are Freezed entities with zero framework dependencies
- IProfileRepository defines getProfile(), saveProfile(), and watchProfile() as abstract interface
- dart analyze exits 0 on all domain files
</success_criteria>

<output>
Create `.planning/phases/01-foundations-sync-safe-schema/01-03-SUMMARY.md` when done
</output>
