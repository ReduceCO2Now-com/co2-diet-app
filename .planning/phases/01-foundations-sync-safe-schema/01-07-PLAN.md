---
phase: 01-foundations-sync-safe-schema
plan: 07
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
files_modified:
  - test/domain/services/mifflin_st_jeor_test.dart
  - test/data/local/user_profile_dao_test.dart
  - test/data/local/consent_records_dao_test.dart
  - test/data/local/schema_test.dart
  - test/ci/blocklist_test.dart
  - test/core/theme/theme_token_test.dart
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - PRIV-07
  - CO2-04
  # LEG-04: intentionally deferred to Phase 6 per CONTEXT.md locked decision.
  # LEG-04 ("View Terms / Privacy / Disclaimer links accessible from Legal Consent screen")
  # requires the Legal Consent screen (Phase 6) to be meaningful. The LicensePage widget
  # wired in Plan 01-05 (SettingsScreen) satisfies the open-source disclosure aspect of
  # PRIV-07 in Phase 1. The full LEG-04 Legal Hub is scoped to Phase 6.

must_haves:
  truths:
    - "dart test exits 0 with all 6 test files contributing at least 1 passing test each"
    - "Mifflin test: known male input (75kg/175cm/30y/low) produces correct TDEE within ±0.001 kcal"
    - "Mifflin test: calculateTdee returns null when any required field is null"
    - "Mifflin test: Gender.other averages male+female BMR variants"
    - "DAO test: upsert + retrieve round-trip on in-memory NativeDatabase.memory()"
    - "DAO test: is_overridden flag set correctly after upsertProfile"
    - "Consent DAO test: insert + retrieve round-trip; no SyncSafeTable columns on consent_records"
    - "Schema test: co2_methodology_version column exists and defaults to '1.0'"
    - "Schema test: SyncSafeTable mixin injects all 6 expected columns (id, hlc_millis, hlc_counter, hlc_node_id, dirty, deleted_at)"
    - "Blocklist test: check_privacy_deps.dart subprocess exits 0 for a clean mock pubspec.lock"
    - "Blocklist test: check_privacy_deps.dart subprocess exits 1 when firebase_core is injected"
    - "Theme test: all DESIGN.md color hex values present in ColorScheme.light"
    - "Theme test: buildLightTheme() and buildDarkTheme() both build without throwing"
  artifacts:
    - path: "test/domain/services/mifflin_st_jeor_test.dart"
      provides: "TDEE formula correctness, null-return, gender=other averaging, activity factors"
    - path: "test/data/local/user_profile_dao_test.dart"
      provides: "In-memory Drift DAO: upsert, retrieve, is_overridden flag, locale unit default"
    - path: "test/data/local/consent_records_dao_test.dart"
      provides: "Consent DAO insert+retrieve; verify no SyncSafeTable columns present"
    - path: "test/data/local/schema_test.dart"
      provides: "co2_methodology_version column check; SyncSafeTable 6-column structural check"
    - path: "test/ci/blocklist_test.dart"
      provides: "Subprocess test of check_privacy_deps.dart — exit-0 clean, exit-1 blocked"
    - path: "test/core/theme/theme_token_test.dart"
      provides: "All DESIGN.md color token assertions; ThemeData build-without-throw"
  key_links:
    - from: "test/data/local/user_profile_dao_test.dart"
      to: "lib/data/local/app_database.dart"
      via: "AppDatabase(NativeDatabase.memory())"
      pattern: "NativeDatabase.memory"
    - from: "test/ci/blocklist_test.dart"
      to: "scripts/check_privacy_deps.dart"
      via: "Process.run('dart', ['run', 'scripts/check_privacy_deps.dart', ...])"
      pattern: "check_privacy_deps"
    - from: "test/core/theme/theme_token_test.dart"
      to: "lib/core/theme/color_tokens.dart"
      via: "import AppColors and assert Color values"
      pattern: "AppColors"
---

<objective>
Write and run all 6 Wave 0 test stub files specified in VALIDATION.md. These tests must all pass with `dart test` before this plan is marked complete.

Purpose: VALIDATION.md marks all 6 test files as Wave 0 gaps — they must exist and be green before the phase can be verified. These tests are the automated proof that the schema, domain logic, CI script, and theme are correct.

Output: 6 test files in their specified directories, all green under `dart test`.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/phases/01-foundations-sync-safe-schema/01-CONTEXT.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/phases/01-foundations-sync-safe-schema/01-RESEARCH.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.planning/phases/01-foundations-sync-safe-schema/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mifflin + DAO + Schema + Consent tests (4 of 6 test files)</name>
  <files>
    test/domain/services/mifflin_st_jeor_test.dart,
    test/data/local/user_profile_dao_test.dart,
    test/data/local/consent_records_dao_test.dart,
    test/data/local/schema_test.dart
  </files>
  <behavior>
    mifflin_st_jeor_test.dart:
      - Test 1: calculateTdee(weightKg: 75, heightCm: 175, age: 30, gender: Gender.male, activityLevel: ActivityLevel.low) is approximately 2335.78125 (±0.001). Derived: BMR = 10*75 + 6.25*175 − 5*30 + 5 = 750 + 1093.75 − 150 + 5 = 1698.75; TDEE = 1698.75 * 1.375 = 2335.78125.
      - Test 2: calculateTdee with weightKg: null returns null
      - Test 3: calculateTdee with heightCm: null returns null
      - Test 4: calculateTdee with age: null returns null
      - Test 5: Gender.other — compute average: BMR_male = 1698.75, BMR_female = 1698.75 − 161 − 5 = 1532.75; average = (1698.75 + 1532.75) / 2 = 1615.75. WAIT — recompute correctly: male adds 5, female subtracts 161; for (w=75, h=175, a=30): base = 10*75+6.25*175-5*30 = 750+1093.75-150 = 1693.75; male = 1693.75+5 = 1698.75; female = 1693.75-161 = 1532.75; other = (1698.75+1532.75)/2 = 1615.75; TDEE_other_low = 1615.75 * 1.375 = 2221.65625. Assert: calculateTdee(w:75, h:175, a:30, gender:Gender.other, activity:low) closeTo(2221.65625, 0.001).
      - Test 6: ActivityLevel.medium factor is 1.55: TDEE_male_medium = 1698.75 * 1.55 = 2633.0625. Assert closeTo.
      - Test 7: ActivityLevel.high factor is 1.725: TDEE_male_high = 1698.75 * 1.725 = 2930.34375. Assert closeTo.

    user_profile_dao_test.dart:
      - setUp: db = AppDatabase(DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true)); tearDown: db.close()
      - Test 1: upsert profile with known id='test-uuid-01'; getProfile() returns row where row.id == 'test-uuid-01'. Uses UserProfileTableCompanion with Value() wrappers for all required sync columns.
      - Test 2: upsert profile with kcalIsOverridden: Value(true); getProfile() returns row where row.kcalIsOverridden == true.
      - Test 3: upsert with units: Value('imperial'); getProfile() returns row where row.units == 'imperial'. (Tests that locale-based unit can be stored and retrieved.)
      - Test 4: upsert twice with same id; getProfile() returns only 1 row (single-row table invariant). SELECT COUNT(*) from userProfileTable equals 1.

    consent_records_dao_test.dart:
      - setUp/tearDown: same in-memory pattern.
      - Test 1: insertConsent with id='consent-uuid-01', appVersion='0.1.0+1', policyVersion='2026-07-16', consentsGiven='["terms","privacy","not_medical_advice","user_responsibility"]'; getAllConsents() returns list with length 1 where first.id == 'consent-uuid-01'.
      - Test 2: insertConsent two rows with different UUIDs; getAllConsents() returns 2 rows. (Tests append-only: 2 inserts → 2 rows, not 1.)
      - Test 3: Structural check — ConsentRecordsTable does NOT have a 'dirty' column. Use db.customSelect to run: SELECT sql FROM sqlite_master WHERE type='table' AND name='consent_records'; assert the result string does NOT contain 'dirty'. This proves the SyncSafeTable mixin is absent.

    schema_test.dart:
      - setUp/tearDown: in-memory AppDatabase.
      - Test 1 (CO2-04): co2_methodology_version column exists and defaults to '1.0'. Upsert a UserProfileTableCompanion with minimal required fields (no co2MethodologyVersion specified — let the default apply). getProfile() → assert row.co2MethodologyVersion == '1.0'.
      - Test 2: SyncSafeTable 6-column check. Use db.customSelect('SELECT sql FROM sqlite_master WHERE type="table" AND name="user_profile"').get() and parse the result string. Assert all 6 column names are present as substrings in the CREATE TABLE SQL: 'hlc_millis', 'hlc_counter', 'hlc_node_id', 'dirty', 'deleted_at', 'id'. Use contains() assertions on the SQL string.
      - Test 3: consent_records table does NOT have 'hlc_millis'. Same approach: SELECT sql FROM sqlite_master for consent_records; assert result does NOT contain 'hlc_millis'.
  </behavior>
  <action>
    Create directories: test/domain/services/, test/data/local/, test/ci/, test/core/theme/

    Write all 4 test files following the behavior spec above. Use the dart test framework (package:flutter_test or package:test — both work for pure Dart; for Drift tests use package:drift/native.dart for NativeDatabase.memory()).

    For the DAO tests, the minimum required companion fields for UserProfileTableCompanion.insert() are those without defaults: id, hlcMillis, hlcCounter, hlcNodeId must all be provided with Value() wrappers. All nullable fields can be omitted (they default to null). Fields with .withDefault() can also be omitted.

    Exact import for in-memory Drift tests:
      import 'package:drift/drift.dart';
      import 'package:drift/native.dart';
      import 'package:flutter_test/flutter_test.dart';
      import 'package:co2diet/data/local/app_database.dart';

    For schema_test.dart, use db.customSelect() to query sqlite_master. The return type is Future<List<QueryRow>>. Access the SQL string via row.read<String>('sql').

    For mifflin_st_jeor_test.dart, use closeTo(expected, delta) matcher from package:test or package:flutter_test for floating-point assertions. Import:
      import 'package:flutter_test/flutter_test.dart';
      import 'package:co2diet/domain/services/mifflin_st_jeor.dart';

    Run after writing: dart test test/domain/ test/data/
    All tests in these two directories must be green before moving to Task 2.

    CRITICAL — verify exact TDEE values by computing inline in a comment at the top of each test case rather than trusting the behavior spec above. If the computed value differs from what the production code returns, fix the test assertion (not the production code — the production formula is the reference).
  </action>
  <verify>
    <automated>dart test test/domain/services/mifflin_st_jeor_test.dart test/data/local/user_profile_dao_test.dart test/data/local/consent_records_dao_test.dart test/data/local/schema_test.dart</automated>
  </verify>
  <done>All 4 test files pass; dart test exits 0; mifflin tests assert exact TDEE values with closeTo(expected, 0.001); schema_test asserts all 6 SyncSafeTable columns present in user_profile and absent from consent_records; co2_methodology_version defaults to '1.0'</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Blocklist subprocess test + theme token test (2 of 6 test files)</name>
  <files>
    test/ci/blocklist_test.dart,
    test/core/theme/theme_token_test.dart
  </files>
  <behavior>
    blocklist_test.dart (subprocess tests — verifies the CI script itself):
      - Test 1 (exit-0): Write a temporary clean mock pubspec.lock to a temp file. The mock must look exactly like a real pubspec.lock (YAML format with `packages:` key and package entries indented two spaces). Include 3–5 real Phase 1 package names: 'drift', 'flutter_riverpod', 'go_router', 'uuid', 'freezed'. None of these match any blocked prefix. Run: Process.run('dart', ['run', 'scripts/check_privacy_deps.dart', tempLockPath, '.privacy-blocklist.yaml']). Assert process.exitCode == 0.
      - Test 2 (exit-1): Write a second temp pubspec.lock that includes all the clean packages PLUS 'firebase_core: {version: 2.0.0, ...}'. Run the same Process.run command. Assert process.exitCode == 1. Also assert process.stdout.toString().contains('firebase_core') — the offending package name must appear in output.
      - Test 3: Write a temp pubspec.lock containing 'sentry_flutter'. Run script. Assert exitCode == 1 and stdout contains 'sentry_flutter'.

    theme_token_test.dart (color token spot checks + ThemeData build assertions):
      - Test 1: AppColors.primary == const Color(0xFF005222)
      - Test 2: AppColors.onPrimary == const Color(0xFFFFFFFF)
      - Test 3: AppColors.primaryContainer == const Color(0xFF006D2F)
      - Test 4: AppColors.onPrimaryContainer == const Color(0xFF90EC9F)
      - Test 5: AppColors.surface == const Color(0xFFF9F9FC)
      - Test 6: AppColors.error == const Color(0xFFBA1A1A)
      - Test 7: AppColors.secondary == const Color(0xFF0155C7)
      - Test 8: AppColors.inverseSurface == const Color(0xFF2F3133)
      - Test 9: buildLightTheme().colorScheme.primary == AppColors.primary
      - Test 10: buildLightTheme() does not throw when called (use expect(() => buildLightTheme(), returnsNormally))
      - Test 11: buildDarkTheme() does not throw when called
      - Test 12: buildLightTheme().colorScheme.surface == AppColors.surface
  </behavior>
  <action>
    test/ci/blocklist_test.dart:

    Import dart:io for Process.run and File; import package:flutter_test/flutter_test.dart.

    For the mock pubspec.lock files, write them to dart:io Directory.systemTemp / 'test_pubspec_lock_N.lock'. Use a counter suffix to avoid collisions.

    The format for a valid mock pubspec.lock entry — the script's RegExp matches lines with exactly 2 spaces of indent followed by package name and colon on its own line. A minimal valid mock:

      packages:
        drift:
          dependency: "direct main"
          description:
            name: drift
            url: "https://pub.dev"
          source: hosted
          version: "2.34.2"
        flutter_riverpod:
          dependency: "direct main"
          description:
            name: flutter_riverpod
            url: "https://pub.dev"
          source: hosted
          version: "3.3.2"

    IMPORTANT: the package name line is `  drift:` with exactly 2 spaces. The script extracts names from those lines. Make sure your mock YAML uses exactly 2-space indent for package names.

    For Test 2, add to the mock:
        firebase_core:
          dependency: "transitive"
          description:
            name: firebase_core
            url: "https://pub.dev"
          source: hosted
          version: "2.0.0"

    Use tearDown to delete the temp files after each test.

    Process.run needs the working directory set to the repo root so it can find scripts/check_privacy_deps.dart. Pass workingDirectory: Directory.current.path to Process.run.

    Note: Process.run is async. Use async test bodies and await the result.

    test/core/theme/theme_token_test.dart:

    Import package:flutter/material.dart for Color and ThemeData.
    Import package:flutter_test/flutter_test.dart.
    Import 'package:co2diet/core/theme/color_tokens.dart' for AppColors.
    Import 'package:co2diet/core/theme/app_theme.dart' for buildLightTheme and buildDarkTheme.

    Note: ThemeData construction may require a Flutter test binding. Wrap ThemeData assertions in testWidgets if necessary, or call TestWidgetsFlutterBinding.ensureInitialized() in a setUpAll(). For plain Color assertions (Tests 1–8), no binding is needed. For ThemeData construction (Tests 9–12), use testWidgets with an empty WidgetTester body if ColorScheme construction requires it, OR use test() if it works without a binding.

    Run all tests after writing: dart test test/ci/ test/core/
    Both new test files must be green.

    Then run the full suite: dart test
    All 6 test files across all 4 directories must pass.
  </action>
  <verify>
    <automated>dart test test/ci/blocklist_test.dart test/core/theme/theme_token_test.dart</automated>
  </verify>
  <done>Both test files pass; blocklist_test.dart subprocess tests verify exit-0 for clean lock and exit-1 for firebase_core/sentry_flutter injected; theme_token_test.dart asserts all 12 specific Color values and confirms both ThemeData builders return normally</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green — dart test exits 0</name>
  <files></files>
  <action>
    Run the complete test suite:
      dart test

    This runs all 6 test files across test/domain/, test/data/, test/ci/, test/core/.

    If any test fails:
      1. Read the failure output to identify which assertion failed.
      2. Do NOT change the production code to make a wrong test pass — check whether the test's expected value is correct first.
      3. For TDEE value mismatches: recompute the formula manually in the failure message, update the test closeTo() value to the exact production output.
      4. For DAO tests: check that the companion's required fields (id, hlcMillis, hlcCounter, hlcNodeId) are provided with Value() wrappers — missing required fields cause Drift to throw at insert time.
      5. For subprocess tests: ensure the working directory is set correctly in Process.run and the temp lock files use the exact 2-space indent format.
      6. Fix the issue and re-run dart test.

    After dart test exits 0, also run:
      flutter analyze

    Expected: 0 errors. Warnings about unused members in generated .g.dart files are acceptable.

    Do not proceed to marking this plan complete until BOTH commands exit 0.
  </action>
  <verify>
    <automated>dart test && flutter analyze --no-fatal-warnings 2>&1 | tail -5</automated>
  </verify>
  <done>dart test exits 0 with all tests passing; flutter analyze exits 0 with 0 errors; all 6 Wave 0 test files are green; the VALIDATION.md wave_0_complete field can be updated to true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → subprocess (check_privacy_deps.dart) | blocklist_test.dart spawns a subprocess via Process.run; malformed temp files could cause unexpected behavior |
| test/data/ → in-memory SQLite | DAO tests write to NativeDatabase.memory(); no file system access; isolated per test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Test assertions using wrong TDEE values | mitigate | Behavior spec includes derived computation showing the arithmetic; executor must verify by computing manually in a comment before the assertion |
| T-07-02 | Tampering | Blocklist subprocess test using temp files that persist | mitigate | tearDown deletes temp files; temp files written to dart:io Directory.systemTemp (OS auto-cleans on restart anyway) |
| T-07-03 | Information Disclosure | In-memory Drift test DB | accept | NativeDatabase.memory() exists only during test execution; no data written to disk; no PII in test data |
| T-07-SC | Tampering | pub.dev package installs | accept | mocktail and flutter_test already in pubspec dev_dependencies from Plan 01-01; no new packages needed |
</threat_model>

<verification>
After all tasks complete:
1. `dart test` — exits 0, all tests green
2. `dart test test/domain/services/mifflin_st_jeor_test.dart` — exits 0 (7 tests)
3. `dart test test/data/local/user_profile_dao_test.dart` — exits 0 (4 tests)
4. `dart test test/data/local/consent_records_dao_test.dart` — exits 0 (3 tests)
5. `dart test test/data/local/schema_test.dart` — exits 0 (3 tests)
6. `dart test test/ci/blocklist_test.dart` — exits 0 (3 tests)
7. `dart test test/core/theme/theme_token_test.dart` — exits 0 (12 tests)
8. `flutter analyze --no-fatal-warnings` — exits 0
9. `ls test/domain/services/mifflin_st_jeor_test.dart test/data/local/user_profile_dao_test.dart test/data/local/consent_records_dao_test.dart test/data/local/schema_test.dart test/ci/blocklist_test.dart test/core/theme/theme_token_test.dart` — all 6 files exist
</verification>

<success_criteria>
- All 6 Wave 0 test files exist and pass under `dart test`
- Mifflin test: correct TDEE for male 75kg/175cm/30y/low activity (2335.78125 kcal); null on missing fields; gender=other averages correctly
- DAO test: upsert+retrieve round-trip; kcalIsOverridden=true persisted correctly; two upserts of same id yields 1 row
- Consent DAO test: insert+retrieve works; consent_records table has no 'dirty' column (SyncSafeTable mixin absent)
- Schema test: co2_methodology_version defaults to '1.0' (CO2-04); all 6 SyncSafeTable columns confirmed in user_profile CREATE TABLE SQL
- Blocklist test: script exits 0 for clean pubspec.lock; exits 1 with 'firebase_core' in output when injected; exits 1 for sentry_flutter
- Theme test: 8 specific AppColors hex values asserted; buildLightTheme() and buildDarkTheme() both build without throwing; colorScheme.primary == AppColors.primary
- dart test exits 0; flutter analyze exits 0 with 0 errors
</success_criteria>

<output>
Create `.planning/phases/01-foundations-sync-safe-schema/01-07-SUMMARY.md` when done
</output>
