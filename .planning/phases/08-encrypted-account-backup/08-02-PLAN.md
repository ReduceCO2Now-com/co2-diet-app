---
phase: 08-encrypted-account-backup
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - docs/backend-contracts/encrypted-backup-blob.md
  - docs/backend-contracts/gdpr-account-deletion.md
autonomous: true
requirements:
  - AUTH-09

must_haves:
  truths:
    - "A written proposal exists at docs/backend-contracts/encrypted-backup-blob.md that Tomris can read and either confirm, adjust, or reject -- without needing to ask what is actually being requested"
    - "The proposal names exact push/pull paths, auth scheme, body encoding, a concrete size limit, versioning approach, and the 404-when-none-exists behavior -- nothing left implicit or to be guessed at request time"
    - "The proposal explicitly states what the backend is NOT being asked to do -- no decryption, no inspection, no merge, no per-field access, no server-side key material"
    - "The proposal treats the stored blob as personal data for GDPR retention/erasure purposes and cross-references the existing account-deletion contract, rather than assuming encryption exits the Regulation"
    - "Every field in both contract tables carries an explicit [ASSUMED] status marker, matching the established gdpr-account-deletion.md template -- no field reads as already-agreed"
  artifacts:
    - path: "docs/backend-contracts/encrypted-backup-blob.md"
      provides: "status: ASSUMED frontmatter + assumed request/response contract tables + negative-scope section + retention/deletion section + numbered open-questions list for Tomris, in gdpr-account-deletion.md's established shape"
    - path: "docs/backend-contracts/gdpr-account-deletion.md"
      provides: "A short cross-reference note (new open question) stating that once/if the encrypted-backup blob contract is accepted, account deletion must delete the stored blob in the same operation"
  key_links:
    - from: "docs/backend-contracts/encrypted-backup-blob.md"
      to: "docs/backend-contracts/gdpr-account-deletion.md"
      via: "explicit cross-reference in both directions -- the new doc's retention section points at the deletion contract, and the deletion contract gets a note pointing back"
      pattern: "gdpr-account-deletion\\.md"
    - from: "docs/backend-contracts/encrypted-backup-blob.md"
      to: "the backend's own documented intent"
      via: "the proposed POST/GET /api/v1/backup paths and the 'opaque end-to-end-encrypted blobs only' module description are quoted from CO2Diet_Backend's own docs/backend-architecture.md, not invented"
      pattern: "/api/v1/backup"
---

<objective>
Write the backend contract proposal for pushing and pulling the opaque encrypted backup blob that Plan 08-01 produces, converting the backend's still-open "encrypted blob vs. user-cloud export" decision (SS13 of `docs/backend-architecture.md`, open as of the last verified fetch) into a concrete, reviewable proposal -- the same move that unblocked GDPR account deletion in Phase 7.

Purpose: 08-CONTEXT.md and 08-RESEARCH.md agree this document is arguably the most valuable artifact in the phase -- it is what has been missing since 2026-08-12, and it is valuable regardless of which way Tomris's decision ultimately goes (criteria 4 and 5 of ROADMAP.md's Phase 8 depend directly on it; Plan 08-03's client is built against exactly what this document proposes).

Output: `docs/backend-contracts/encrypted-backup-blob.md` -- a full `[ASSUMED]`-marked proposal for `POST/GET /api/v1/backup`, matching `gdpr-account-deletion.md`'s established template shape -- plus a short cross-reference addition to `gdpr-account-deletion.md` itself.
</objective>

<execution_context>
@/Users/alisafi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-encrypted-account-backup/08-CONTEXT.md
@.planning/phases/08-encrypted-account-backup/08-RESEARCH.md
@docs/backend-contracts/gdpr-account-deletion.md
@lib/domain/services/backend_config.dart
</context>

<interfaces>
<!-- Exact grounding this plan writes against. No further backend exploration needed unless the re-fetch in Task 1 finds something changed. -->

08-RESEARCH.md's "08-02 Contract Research" section (content checklist table) and "Backend Reality Check" section are the authoritative source for this document's content. Key facts already verified there (as of `origin/main` = `d551d0b`, 2026-09-06):

- No `backup` module, no backup controller, no user-data table exists in the backend today.
- The backend's own `docs/backend-architecture.md` "API surface (quick reference)" already lists: `POST /api/v1/backup` and `GET /api/v1/backup`.
- The backend's own module table already reads: **"Encrypted Backup -- Optional, account mode only. Stores opaque end-to-end-encrypted blobs only -- the server cannot read meals/weight/profile."**
- `SecurityConfig` is `oauth2ResourceServer().jwt()` with `.anyRequest().authenticated()` -- a new `/api/v1/backup` endpoint is bearer-JWT-protected by default, zero config change needed on the backend's side for auth.
- A path inconsistency exists: `AuthNotifier.deleteAccount()` and `gdpr-account-deletion.md` both use `{baseUrl}/me/account`, but the backend's actual/documented convention is `/api/v1/...` (`GET /api/v1/me` exists in code). Flag this, do not silently fix it here.

`docs/backend-contracts/gdpr-account-deletion.md`'s exact structure (already read above) is the template to match: YAML frontmatter (`status`, `requested_by`, `owner`, `related_requirement`, `last_updated`), a "this is a proposal, not an agreed contract" paragraph, a legal-requirements section, an assumed request-contract table, an assumed response-contract table (with a `Status` column reading `[ASSUMED]` on every row), a "client-side isolation point" section, and a numbered "Open questions for Tomris" list.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify backend state and draft the contract document</name>
  <files>docs/backend-contracts/encrypted-backup-blob.md</files>
  <action>
    Re-verify the backend's current state with a single fetch before writing, per 08-RESEARCH.md's own instruction ("The backend section should be re-checked with a single git fetch immediately before 08-02 is written -- it changed once already between CONTEXT.md's scan and this one"):

    `git -C /Users/alisafi/Documents/ReduceCO2-Now/CO2Diet_Backend-reference fetch origin && git -C /Users/alisafi/Documents/ReduceCO2-Now/CO2Diet_Backend-reference log -1 origin/main`

    If that path is unreachable or the fetch fails (no network / clone missing), do not block -- proceed using 08-RESEARCH.md's already-verified findings (`origin/main` = `d551d0b`, 2026-09-06) and record in the new document's frontmatter that the backend state reflects that verified commit, not a fresh re-check, so a future reader knows exactly how current the grounding is. If the fetch succeeds and `origin/main` has moved, spot-check whether a `backup/` module or `/api/v1/backup` controller now exists (`git -C &lt;path&gt; ls-tree -r origin/main -- backup/` and a grep for `@PostMapping`/`@GetMapping` referencing `backup`) -- if the backend has moved ahead of this proposal, note that explicitly in the document rather than writing a proposal for something that may already exist.

    Write `docs/backend-contracts/encrypted-backup-blob.md`, matching `gdpr-account-deletion.md`'s exact structure (see `&lt;interfaces&gt;`):

    - **Frontmatter**: `status: ASSUMED -- not yet confirmed with Tomris`, `requested_by: Ali (Flutter client)`, `owner: Tomris (Backend -- Spring Boot + PostgreSQL + Keycloak)`, `related_requirement: AUTH-09`, `last_updated:` the actual execution date, plus a `backend_verified_against:` line naming the commit hash this was written against.
    - **Opening paragraph**: states plainly this is a concrete proposal for review, not a description of an agreed contract -- mirror `gdpr-account-deletion.md`'s opening paragraph's register, and add one sentence noting this proposal deliberately adopts the paths and module description the backend's own `docs/backend-architecture.md` already names, converting an internal sketch into something concrete to confirm or reject.
    - **Legal requirements driving this contract**: AUTH-09 (narrowed to the encrypted-blob case per ROADMAP.md's 2026-08-12 note) and a GDPR Art. 32 / EDPB Guidelines 01/2025 pointer establishing that an encrypted blob tied to an account identifier remains personal data for retention/erasure purposes -- it is a security measure, not an exit from the Regulation (08-RESEARCH.md Pitfall 9).
    - **Assumed request contract table** covering push (`POST /api/v1/backup`) and pull (`GET /api/v1/backup`): method, path, headers (`Authorization: Bearer &lt;access_token&gt;`, `Content-Type: application/octet-stream` for push), body (raw archive bytes for push; none for pull), and a concrete proposed size limit (25 MB) with the `413`-on-exceeded behavior -- every field status `[ASSUMED]`.
    - **Assumed response contract table**: push success (`2xx`), pull success (`200` + raw bytes), pull-when-none-exists (`404` -&gt; client treats as "no server backup yet," an expected state, never an error), non-2xx/timeout -&gt; client surfaces a typed exception. State plainly that the blob's own internal `formatVersion` (from Plan 08-01's manifest) lives inside the archive and the server must never parse it -- propose an opaque `X-Backup-Format` header as one option and ask Tomris which they'd prefer.
    - **Semantics section**: one backup per user, last-write-wins overwrite -- explicitly **not** a version history (CONTEXT.md defers multi-backup UX; a history needs a restore-picker UI that doesn't exist). Note an optional `ETag`/`If-Match` on push as a cheap, low-priority `[ASSUMED]` addition for detecting a concurrent overwrite from another device.
    - **What the backend is explicitly NOT being asked to do**: no decryption, no inspection, no merge, no per-field access, no server-side key material, no analytics on blob contents or timing. Quote the backend's own module-table line verbatim: *"Stores opaque end-to-end-encrypted blobs only -- the server cannot read meals/weight/profile."*
    - **Retention & deletion**: the blob is personal data (per the legal-requirements section above); account deletion must delete it in the same operation as the Keycloak user and any other backend-side state. Cross-reference `gdpr-account-deletion.md` explicitly by filename, and note that document predates this one and says nothing about a backup blob today (that gap is what Task 2 closes).
    - **Client-side isolation point**: name the future single file this would live in on the client (`lib/features/backup/providers/backup_sync_notifier.dart`, per Plan 08-03) -- mirrors `gdpr-account-deletion.md`'s "one-file change" framing.
    - **Open questions for Tomris** (numbered list, at minimum): (1) whether SS13's decision has moved since the last verified fetch and, if resolved toward encrypted blob, whether these proposed paths/limits are acceptable as-is; (2) the `/me/account` vs `/api/v1/...` path-prefix inconsistency between `gdpr-account-deletion.md` and the backend's actual convention -- flagged as a possible latent defect independent of this proposal; (3) whether a 25 MB size limit is right, or whether Tomris has a different number in mind; (4) whether the `X-Backup-Format` header approach is acceptable or whether Tomris prefers the server stay entirely opaque to versioning; (5) whether last-write-wins-with-no-history is acceptable for v1, or whether a lightweight version history is wanted from day one.
  </action>
  <verify>
    <automated>test -f docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; grep -q "status: ASSUMED" docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; grep -q "POST /api/v1/backup" docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; grep -q "GET /api/v1/backup" docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; grep -qi "opaque end-to-end-encrypted blobs only" docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; grep -qi "404" docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; grep -qi "Open questions for Tomris" docs/backend-contracts/encrypted-backup-blob.md &amp;&amp; test "$(grep -c '\[ASSUMED\]' docs/backend-contracts/encrypted-backup-blob.md)" -ge 8</automated>
  </verify>
  <done>docs/backend-contracts/encrypted-backup-blob.md exists with status: ASSUMED frontmatter, both proposed paths, the quoted negative-scope module description, 404 pull-when-none-exists behavior documented, a retention/deletion section, and a numbered open-questions list -- at least 8 fields explicitly marked [ASSUMED].</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference the GDPR account-deletion contract</name>
  <files>docs/backend-contracts/gdpr-account-deletion.md</files>
  <action>
    Add one new numbered item to `gdpr-account-deletion.md`'s existing "Open questions for Tomris" list (append, do not renumber or remove any existing item): note that a new proposed contract, `docs/backend-contracts/encrypted-backup-blob.md`, covers an encrypted backup blob that -- if ever accepted and implemented -- would be additional backend-held personal data tied to the same account, and that account deletion (`DELETE {baseUrl}/me/account`, this document) must delete that blob in the same operation, not leave it orphaned. Keep the addition to a short paragraph; this document's own contract (Keycloak + backend user-data deletion) is otherwise unchanged by this plan -- do not edit any existing table row, only append the new open question and, if useful, one sentence in the existing document's introductory paragraph pointing forward to the new document's existence.

    This closes 08-RESEARCH.md Pitfall 9's explicit requirement that an 08-02 draft not go out "with no retention or deletion section" on either side of the cross-reference.
  </action>
  <verify>
    <automated>grep -qi "encrypted-backup-blob.md" docs/backend-contracts/gdpr-account-deletion.md &amp;&amp; grep -qi "backup" docs/backend-contracts/gdpr-account-deletion.md</automated>
  </verify>
  <done>gdpr-account-deletion.md's Open Questions list carries a new item cross-referencing encrypted-backup-blob.md and stating that account deletion must also delete the backup blob if that contract is ever accepted; no existing content in the document was altered.</done>
</task>

</tasks>

<verification>
`docs/backend-contracts/encrypted-backup-blob.md` exists, follows `gdpr-account-deletion.md`'s established template shape, and every task's `&lt;automated&gt;` grep-based structural check passes. This is a documentation-only plan -- correctness beyond structure is Tomris's review, not a runnable assertion (08-VALIDATION.md: SC-4 is manual-only by nature). No `lib/` or `test/` file is touched by this plan.
</verification>

<success_criteria>
A written backend contract for push/pull exists in `docs/backend-contracts/`, marked `[ASSUMED]` in the established convention, stating explicitly what the backend is not asked to do -- no decryption, inspection, merge, or per-field access (ROADMAP.md Phase 8 criterion 4).
</success_criteria>

<output>
Create `.planning/phases/08-encrypted-account-backup/08-02-SUMMARY.md` when done. Include which backend commit this was verified against, and whether the re-fetch found any change since 08-RESEARCH.md's last check.
</output>
