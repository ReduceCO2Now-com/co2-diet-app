---
phase: 04-meal-logging-core-10s-target
plan: 09
type: execute
wave: 6
depends_on:
  - 04-07
files_modified:
  - lib/features/food_search/widgets/food_detail_sheet.dart
  - lib/features/barcode_scan/screens/barcode_scan_screen.dart
  - lib/features/food_search/widgets/portion_slot_form.dart
  - test/features/food_search/portion_slot_form_test.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-08
  - LOG-11
  - LOG-13

must_haves:
  truths:
    - "Search and barcode-scan entry points render the exact same sheet content — no behavioral divergence (restores Phase 3's original intent)"
    - "Dismissing the sheet from the scanner still resumes the live camera (showFoodDetailSheet's Future<void> is awaited by the caller that needs it)"
    - "Log this food is disabled until quantity is valid (>0) and, on success, shows an Added to [Slot] snackbar with Undo"
    - "Macro and CO2 values displayed in the sheet scale live with the selected quantity"
    - "Every foodRef used for logging/favoriting/overriding is FoodItem.resolvedFoodRef — never item.barcode ?? item.productName or any other productName-based fallback"
    - "The default quantity unit is metric (g) unless the device locale is US/LR/MM, per LOG-06 and ProfileNotifier.setLocaleUnits' existing precedent — and this never bypasses the 'no unit without a configured conversion' rule"
    - "showFoodDetailSheet and PortionSlotForm both accept optional initialSlot/initialQuantity/initialUnit overrides so Plans 04-10 and 04-11 (wave 7, no dependency on each other) can open a pre-filled sheet without either modifying this plan's files"
  artifacts:
    - path: "lib/features/food_search/widgets/portion_slot_form.dart"
      provides: "Meal slot picker + quantity/unit input + live-scaled macro display + Log button (LOG-05/06/13)"
  key_links:
    - from: "lib/features/food_search/widgets/food_detail_sheet.dart"
      to: "lib/features/meal_logging/providers/meal_entry_notifier.dart"
      via: "PortionSlotForm calls ref.read(mealEntryNotifierProvider.notifier).logFood(...)"
      pattern: "logFood"
    - from: "lib/features/barcode_scan/screens/barcode_scan_screen.dart"
      to: "lib/features/food_search/widgets/food_detail_sheet.dart"
      via: "await showFoodDetailSheet(context, item) — real Future<void>, not unawaited"
      pattern: "await showFoodDetailSheet"
    - from: "lib/features/food_search/widgets/portion_slot_form.dart"
      to: "lib/domain/entities/food_item.dart"
      via: "item.resolvedFoodRef used for every MealEntry.foodRef/Favorite.foodRef this plan constructs"
      pattern: "resolvedFoodRef"
---

<objective>
Fix the Phase 3 sheet-duplication bug (CONTEXT.md: `_BarcodeScanDetailSheet` diverged from `FoodDetailBottomSheet` because `showFoodDetailSheet` swallowed its dismissal `Future` in `unawaited()`), then build the core meal-logging UI: the portion/slot form embedded in the now-single shared sheet, plus the favorite star and "Edit this food" action.

Purpose: This is the primary LOG-05/LOG-06/LOG-13 UI surface — the thing a user actually taps through in under 10 seconds. It is the single most requirement-dense plan in the phase. This plan also owns the full `PortionSlotForm`/`showFoodDetailSheet` constructor contract, including the optional `initialSlot`/`initialQuantity`/`initialUnit` overrides that Plans 04-10 and 04-11 (both wave 7, with no dependency declared between each other) need to open a pre-filled sheet for Recent/Favorites/dashboard edit — defining the full contract here means neither of those plans needs to modify this plan's files or depend on the other.

Output: `showFoodDetailSheet` returns a real `Future<void>` and accepts optional pre-fill overrides; `_BarcodeScanDetailSheet` and its private `_MacroRow` are deleted; `_FoodDetailContent` becomes a `ConsumerStatefulWidget` hosting the new `PortionSlotForm`, a favorite star, and an "Edit this food" action.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@lib/features/food_search/widgets/food_detail_sheet.dart
@lib/features/barcode_scan/screens/barcode_scan_screen.dart
@lib/features/barcode_scan/widgets/confidence_chip.dart
@lib/domain/entities/meal_entry.dart
@lib/domain/entities/user_food.dart
@lib/domain/entities/food_item.dart
@lib/features/profile/providers/profile_notifier.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sheet reconciliation — single shared sheet content, pre-fill contract</name>
  <files>
    lib/features/food_search/widgets/food_detail_sheet.dart,
    lib/features/barcode_scan/screens/barcode_scan_screen.dart
  </files>
  <action>
    Edit `lib/features/food_search/widgets/food_detail_sheet.dart`:
    - Change `showFoodDetailSheet` to `Future<void> showFoodDetailSheet(BuildContext context, FoodItem item, {MealSlot? initialSlot, double? initialQuantity, PortionUnit? initialUnit}) { return showModalBottomSheet<void>(context: context, isScrollControlled: true, builder: (context) => _FoodDetailContent(item: item, initialSlot: initialSlot, initialQuantity: initialQuantity, initialUnit: initialUnit)); }` — remove the `unawaited(...)` wrapper entirely. The three new optional params exist so Plans 04-10/04-11 (Recent/Favorites/dashboard edit icons) can open this same sheet pre-filled with an existing entry's slot/quantity/unit without this plan's callers needing to pass anything (all three default to `null`, preserving today's auto-detect/default-chip behavior for the normal search/scan entry points, which never pass them). Callers that don't care about dismissal (the food search screen's `onTap: () => showFoodDetailSheet(context, items[i])`) simply don't await it — Dart does not require awaiting a `Future`-returning call; `flutter analyze`'s `unawaited_futures` lint (if enabled by `very_good_analysis`) may flag the search-screen call site — if so, wrap that one call site in `unawaited(...)` explicitly (import `dart:async`) rather than reintroducing the swallow inside the shared helper itself. Update the file's doc comment: remove the "Log this food deferred to Phase 4" note; this plan is Phase 4.

    Update `_FoodDetailContent` to remove its `TODO(phase-4)` comment at line ~132 (the exact line may have shifted — locate via the `TODO(phase-4)` text) and replace it with the `PortionSlotForm` widget created in Task 2, plus a favorite star `IconButton` in a `Row` alongside the product name, plus an "Edit this food" `TextButton` below the macro table. Convert `_FoodDetailContent` from `StatelessWidget` to `ConsumerStatefulWidget` (needs `ref` for `FavoriteNotifier`/`MealEntryNotifier` and local state for the portion form), widening its constructor to accept the same `initialSlot`/`initialQuantity`/`initialUnit` optional params as `showFoodDetailSheet` and forwarding them unchanged to `PortionSlotForm` (see Task 2 for `PortionSlotForm`'s exact constructor and the widget composition).

    Edit `lib/features/barcode_scan/screens/barcode_scan_screen.dart`:
    - Delete `_BarcodeScanDetailSheet` and its private `_MacroRow` class entirely (lines ~202–307, the two private classes at the bottom of the file).
    - Change `_showItemSheet`'s body from building `_BarcodeScanDetailSheet` directly via `showModalBottomSheet` to calling the shared helper: `Future<void> _showItemSheet(FoodItem item) async { await showFoodDetailSheet(context, item); if (mounted) { setState(() => _processing = false); unawaited(_controller.start()); ref.read(barcodeScanProvider.notifier).resetToIdle(); } }` — import `package:co2diet/features/food_search/widgets/food_detail_sheet.dart`. This preserves the exact resume-camera-on-dismiss behavior since `showFoodDetailSheet` now returns the real dismissal `Future`. The scanner never passes the new `initialSlot`/`initialQuantity`/`initialUnit` params — a fresh scan always uses the default auto-detect/generic-chip behavior.
    - Remove the now-unused `showModalBottomSheet` import if nothing else in the file needs it directly (check remaining usages before removing).
    - Update the class doc comment referencing "the shared `showFoodDetailSheet` helper wraps its ... call in `unawaited()`" — this limitation no longer exists; update or remove the note.
  </action>
  <verify>
    <automated>flutter analyze lib/features/food_search/widgets/food_detail_sheet.dart lib/features/barcode_scan/screens/barcode_scan_screen.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`_BarcodeScanDetailSheet` and its private `_MacroRow` no longer exist anywhere in the codebase. `showFoodDetailSheet` returns `Future<void>` and accepts optional `initialSlot`/`initialQuantity`/`initialUnit`. `BarcodeScanScreen._showItemSheet` awaits it directly. `flutter analyze` reports 0 issues on both files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PortionSlotForm — meal slot, quantity/unit, locale-aware defaults, live scaling, Log button</name>
  <files>
    lib/features/food_search/widgets/portion_slot_form.dart,
    lib/features/food_search/widgets/food_detail_sheet.dart,
    test/features/food_search/portion_slot_form_test.dart
  </files>
  <behavior>
    - The `SegmentedButton<MealSlot>` initial selection matches `initialSlot` when provided (edit-prefill case), otherwise the time-of-day auto-detect rule (e.g. before 11:00 → breakfast, 11:00–15:00 → lunch, 15:00–18:00 → snack, after 18:00 → dinner — exact boundaries are Claude's discretion per CONTEXT.md; pick reasonable cutoffs and document them in a doc comment on the detection function).
    - When `initialQuantity`/`initialUnit` are provided (edit-prefill case), the quantity field and unit dropdown initialize to those exact values, bypassing both the chip-derived default and the locale-aware default described below.
    - When `initialQuantity`/`initialUnit` are NOT provided (fresh-log case) and the food has no configured quick-serving-sizes, the default unit is `PortionUnit.g` and the default quantity chips are `100g`, `200g`, `Custom` for a metric-locale device (`ProfileNotifier.setLocaleUnits(context) == 'metric'`). This is unchanged behavior — LOG-06's "no generic/estimated conversion fallback" rule (CONTEXT.md) means the unit itself cannot switch to a non-weight-based unit (`cup`/`piece`/`portion`) without a configured gram conversion, so an imperial-locale device with no configured servings ALSO defaults to `g`/100g/200g — there is no false-precision-free imperial equivalent to offer in this case, and this is documented explicitly in a code comment referencing LOG-06 and the no-generic-conversion-fallback CONTEXT.md decision.
    - When `initialQuantity`/`initialUnit` are NOT provided and the food DOES have a configured quick-serving-size whose implied unit is `cup`/`piece`/`portion` (a real, non-fabricated conversion), an imperial-locale device (`ProfileNotifier.setLocaleUnits(context) == 'imperial'`, i.e. US/LR/MM) defaults the initial selected chip/unit to that configured non-metric serving size instead of the food's plain gram default; a metric-locale device still defaults to the gram-based chip even when a non-metric serving size is configured (the user can still tap the other chip manually). This is LOG-06's "metric default, imperial from locale" rule applied within the existing unit set (g/ml/piece/cup/portion — the app has no oz/lb units) and never violates the "no unit without a configured conversion" rule, since it only ever selects among chips that are already populated from a real configured `ServingSize`.
    - Tapping a quantity chip pre-fills the numeric field with that chip's value; the field remains directly editable afterward (not locked to the chip's exact value).
    - The unit dropdown offers only `g`/`ml` unless a `UserFood` override exists for this food with at least one `ServingSize` implying a weight-per-unit for `piece`/`cup`/`portion` (this plan's scope: gate the dropdown on serving-size presence; exact per-unit gram mapping is consumed from `ServingSize.grams` when the user picks a serving-size chip that corresponds to a non-gram unit).
    - Changing the quantity field recomputes displayed calories/protein/carbs/fat/CO2 via `MealEntry.scaledMacros` (or the equivalent pure function) — never re-fetches from the database.
    - "Log this food" is disabled while the parsed quantity is `<= 0` or unparseable; becomes enabled otherwise.
    - Every `foodRef` this widget constructs (for `MealEntry.logOrMerge`, `Favorite.toggleFavorite`, and the "Edit this food" override navigation) is `item.resolvedFoodRef` — never `item.barcode ?? item.productName` or any other productName-involving fallback.
  </behavior>
  <action>
    Create `lib/features/food_search/widgets/portion_slot_form.dart`: `ConsumerStatefulWidget` taking `required FoodItem item` (the form reads `item.co2e100g`/`item.confidenceBand`/macros as the 100g-basis snapshot source), `MealSlot? initialSlot`, `double? initialQuantity`, and `PortionUnit? initialUnit` (all three nullable, default `null` — see `<behavior>` for exact precedence rules; a caller providing explicit intent, e.g. a Dashboard quick-add button carrying a slot param (`initialSlot` only), or Plans 04-10/04-11's edit-prefill flow (all three), overrides the corresponding default).

    State: `late MealSlot _selectedSlot` and `late PortionUnit _selectedUnit`, both assigned in `initState` from `initialSlot`/`initialUnit` when provided, else assigned a temporary default (`detectMealSlotForTime(DateTime.now())` / `PortionUnit.g`) that is corrected once locale is available (see below); `TextEditingController _quantityController` (init from `initialQuantity?.toStringAsFixed(0) ?? '100'`); `List<QuantityChip> _chips` (derived from `UserFood?.quickServingSizes` when an override exists for `item`, else the generic `[100g, 200g, Custom]` default — resolve the `UserFood?` via `ref.read(userFoodNotifierProvider.notifier).findOverrideForFoodRef(item.resolvedFoodRef, item.source ?? 'off_ref')` in `initState`).

    Locale-aware default (only applies when `initialQuantity`/`initialUnit` were NOT provided — the fresh-log case): override `didChangeDependencies()` (NOT `initState` — `Localizations.localeOf(context)`/`ProfileNotifier.setLocaleUnits(context)` depend on an `InheritedWidget` lookup that is unsafe to perform in `initState`), guarded by a `bool _localeApplied = false` field so the logic runs exactly once per mount: if `!_localeApplied`, compute `final unitPref = ProfileNotifier.setLocaleUnits(context);` and, only if `unitPref == 'imperial'` and `_chips` contains a configured (non-generic) serving-size chip whose implied unit is `piece`/`cup`/`portion`, call `setState` to select that chip as the initial default (set `_selectedUnit` and `_quantityController.text` accordingly) instead of the plain gram default; set `_localeApplied = true` at the end regardless of branch taken. Add a doc comment on this method citing LOG-06 and CONTEXT.md's "no generic/estimated conversion fallback" rule, per `<behavior>`.

    Use the shared `detectMealSlotForTime(DateTime now)` top-level function from `lib/domain/entities/meal_slot.dart` (Plan 04-03) for the auto-detect rule per `<behavior>` — do not define a private duplicate in this file; Plan 04-10's Recent/Favorites one-tap-log path imports the same shared function.

    Layout (inside the existing `_FoodDetailContent`'s `Column`, replacing the `TODO(phase-4)` marker):
    1. `SegmentedButton<MealSlot>(segments: [...4 slots...], selected: {_selectedSlot}, onSelectionChanged: (s) => setState(() => _selectedSlot = s.first))`.
    2. `Wrap` of quantity chips (`ChoiceChip` per chip, `onSelected` sets `_quantityController.text` and, for a non-"Custom" chip tied to a `ServingSize`, also sets `_selectedUnit` to the size's implied unit) + a `'Custom'` chip that just focuses the field without changing its value.
    3. `Row` of the quantity `TextFormField` (numeric keyboard, `onChanged: (_) => setState(() {})` to trigger live-scaling rebuild) + unit `DropdownButton<PortionUnit>` (items gated per `<behavior>`).
    4. Re-render the existing per-100g macro rows to show scaled values instead: compute `final gramsEquivalent = _resolveGramsEquivalent();` via a private local pure function `double _resolveGramsEquivalent()` defined directly in this file (converts `_selectedUnit`+parsed quantity+the matching `ServingSize.grams` when a non-gram unit is selected, into a grams-equivalent double — this stays a local, file-private helper; it is not added to `MealEntry` since it operates on this form's transient UI state, not a persisted entity), then `final scaled = MealEntry.scaledMacros(entry_or_equivalent, gramsEquivalent: gramsEquivalent);` (or the equivalent direct linear-scale computation against `item`'s 100g-basis fields, since `item` is a `FoodItem` here, not yet a `MealEntry` — apply the same `snapshot * gramsEquivalent / 100` rule from Plan 04-03's `scaledMacros`) and display `scaled.calories`/etc. in place of the raw per-100g values; keep the `co2e100g`/`ConfidenceChip` row logic but feed it `scaled.co2e`.
    5. `FilledButton` "Log this food": `onPressed` is `null` when parsed quantity `<= 0`/unparseable, otherwise builds a `MealEntry` draft (`id: ''`, `mealSlot: _selectedSlot`, `foodRef: item.resolvedFoodRef`, `foodRefSource: item.source ?? 'off_ref'`, `quantity` (parsed), `unit: _selectedUnit`, snapshot fields from `item.*`, `loggedAt: DateTime.now()`, `logDate` (today, `YYYY-MM-DD`)) — calls `ref.read(mealEntryNotifierProvider.notifier).logFood(draft)`, and on success:
       - Pops the sheet (`Navigator.of(context).pop()` or `context.pop()`, matching the existing dismiss pattern).
       - Shows `ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text('Added to ${_selectedSlot.displayLabel}'), action: SnackBarAction(label: 'Undo', onPressed: () { if (result.wasMerge) { ref.read(mealEntryNotifierProvider.notifier).undoMerge(result.entry.id, result.loggedDelta); } else { ref.read(mealEntryNotifierProvider.notifier).deleteEntry(result.entry.id); } })))` — shown via the `context` captured from the screen underneath (the sheet is already dismissed at this point per CONTEXT.md: "sheet auto-dismisses, snackbar appears over whatever screen is now visible").
       - On failure (repository throws): do NOT dismiss the sheet; show an error `SnackBar` with a "Retry" `SnackBarAction` that re-invokes the same log call — wrap the log call in `try`/`on Exception catch (e) { ... }` to implement this.

    Favorite star (added to `_FoodDetailContent`'s title `Row`, next to the product name): `Consumer` or direct `ref.watch` of a small `FutureBuilder`/`isFavorite` check, `IconButton(icon: Icon(isFav ? Icons.star : Icons.star_border), onPressed: () => ref.read(favoriteNotifierProvider.notifier).toggle(Favorite(id: '', foodRef: item.resolvedFoodRef, foodRefSource: item.source ?? 'off_ref', productNameSnapshot: item.productName, brandSnapshot: item.brand, calories100gSnapshot: item.calories100g, co2e100gSnapshot: item.co2e100g, confidenceBandSnapshot: item.confidenceBand, lastQuantity: null, lastUnit: null, favoritedAt: DateTime.now())))`.

    "Edit this food" `TextButton` (below the macro table, above the existing 'per 100g' caption): `onPressed: () => context.push('/custom-food-stub?overrideOf=${Uri.encodeComponent(item.resolvedFoodRef)}&overrideOfSource=${item.source ?? 'off_ref'}')` — navigates to the Plan 04-08 form using the route contract it defines.

    Fill in `test/features/food_search/portion_slot_form_test.dart` (replace Wave 0 skip) with widget tests matching the `<behavior>` block plus the already-stubbed cases (auto-detect selection, chip pre-fill, unit dropdown gating, live scaling, Log-disabled-until-valid, `resolvedFoodRef` used for the logged draft's `foodRef`, locale-aware default chip selection when a non-metric serving size is configured for an imperial-locale test, `initialSlot`/`initialQuantity`/`initialUnit` override behavior for the edit-prefill case).
  </action>
  <verify>
    <automated>flutter test test/features/food_search/portion_slot_form_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`portion_slot_form_test.dart` passes with 0 skips. The sheet (search or scan entry point) shows: slot picker, quantity chips + editable field + unit dropdown, live-scaled macro/CO2 display, disabled-until-valid "Log this food" button, favorite star, "Edit this food" button. Tapping "Log this food" with a valid quantity persists a `MealEntry` using `item.resolvedFoodRef` and shows the Undo snackbar. Passing `initialSlot`/`initialQuantity`/`initialUnit` pre-fills the form exactly, bypassing auto-detect/locale defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-entered quantity → MealEntry.quantity | Free numeric input parsed and validated before reaching the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | Quantity text field parsing | mitigate | `double.tryParse` with explicit `<= 0`/`null` rejection before the Log button is enabled; repository layer (Plan 04-05) does not re-validate quantity but the DAO-level insert only ever receives values that passed this gate — acceptable given quantity has no security-sensitive downstream use beyond arithmetic display |
| T-04-09-02 | Repudiation | Undo action after Log | accept | Local-only, single-user, no audit requirement for this UX convenience feature |
| T-04-09-03 | Tampering | `foodRef` construction for logging/favoriting/overriding | mitigate | Always `item.resolvedFoodRef` (Plan 04-06) — never a raw `productName` string, closing the false-positive-merge risk CONTEXT.md's Merge Semantics decision explicitly warns against |
| T-04-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/features/food_search/ && flutter analyze lib/features/food_search/ lib/features/barcode_scan/screens/barcode_scan_screen.dart
```
</verification>

<success_criteria>
- `_BarcodeScanDetailSheet` deleted; both entry points render `_FoodDetailContent` exclusively
- `showFoodDetailSheet` returns a real `Future<void>` and accepts optional `initialSlot`/`initialQuantity`/`initialUnit`; scanner resume-on-dismiss behavior unchanged
- `PortionSlotForm` implements slot auto-detect+override, chip-driven quantity entry, locale-aware default (metric default, imperial from locale, per LOG-06), unit gating, live scaling, disabled-until-valid Log button, success/error snackbar with Undo/Retry
- Every `foodRef` constructed in this plan is `item.resolvedFoodRef` — zero occurrences of `item.barcode ?? item.productName` or similar
- Favorite star and "Edit this food" action present on the shared sheet
- `portion_slot_form_test.dart` passes with 0 skips
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-09-SUMMARY.md` when done.
</output>
</output>
