---
phase: 04-meal-logging-core-10s-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/data/local/meal_entry_dao_test.dart
  - test/data/local/user_food_dao_test.dart
  - test/data/local/food_catalog_dao_override_test.dart
  - test/data/repositories/meal_entry_repository_test.dart
  - test/data/repositories/user_food_repository_test.dart
  - test/domain/entities/meal_entry_test.dart
  - test/domain/entities/user_food_test.dart
  - test/domain/entities/serving_size_test.dart
  - test/features/meal_logging/meal_entry_notifier_test.dart
  - test/features/my_foods/user_food_notifier_test.dart
  - test/features/food_search/favorites_test.dart
  - test/features/my_foods/custom_food_form_test.dart
  - test/features/meal_logging/offline_logging_test.dart
  - test/features/food_search/portion_slot_form_test.dart
  - test/features/dashboard/meal_entry_row_test.dart
  - integration_test/meal_logging_benchmark_test.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08
  - LOG-09
  - LOG-10
  - LOG-11
  - LOG-12
  - LOG-13

must_haves:
  truths:
    - "flutter test exits 0 on the Wave 0 stub suite (all tests skip with clear labels)"
    - "Every subsequent Phase 4 plan has a named test file to target in its <verify> block"
    - "No production code is modified in this plan"
  artifacts:
    - path: "test/data/local/meal_entry_dao_test.dart"
      provides: "Stubs for LOG-05/LOG-07/LOG-08 DAO tests (insert, merge, recent, favorites)"
    - path: "test/data/local/user_food_dao_test.dart"
      provides: "Stubs for LOG-10/LOG-11 custom food + override DAO tests"
    - path: "test/data/local/food_catalog_dao_override_test.dart"
      provides: "Stub for LOG-11 search-precedence (override replaces original in results)"
    - path: "integration_test/meal_logging_benchmark_test.dart"
      provides: "Stub for LOG-13 tap-to-saved <10s benchmark"
  key_links:
    - from: "test/data/local/meal_entry_dao_test.dart"
      to: "lib/data/local/daos/meal_entry_dao.dart"
      via: "group-level skip referencing production class name"
      pattern: "skip:.*MealEntryDao"
---

<objective>
Create Wave 0 test stubs for all Phase 4 automated test files. Every stub file must compile, run with `flutter test`, and exit 0 — tests skip atomically at the group level when the production class is absent (Phase 2/3 pattern: group-level skip for atomic failure detection; `markTestSkipped()` inside `testWidgets` body for the integration benchmark).

Purpose: Satisfy the Nyquist rule — every execution plan in Phase 4 has a named test file in its `<verify>` block. Stubs prevent a failing wave-2+ execute from producing "file not found" errors, and give the planner a concrete coverage map for LOG-05 through LOG-13.

Output: 16 test/integration_test stub files. Zero production code changes.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@.planning/phases/04-meal-logging-core-10s-target/04-VALIDATION.md

<!-- Phase 3 stub pattern reference: group-level skip -->
@test/features/barcode_scan/barcode_scan_notifier_test.dart
@integration_test/co2_coverage_benchmark_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data-layer test stubs (DAO, repository, domain entity)</name>
  <files>
    test/data/local/meal_entry_dao_test.dart,
    test/data/local/user_food_dao_test.dart,
    test/data/local/food_catalog_dao_override_test.dart,
    test/data/repositories/meal_entry_repository_test.dart,
    test/data/repositories/user_food_repository_test.dart,
    test/domain/entities/meal_entry_test.dart,
    test/domain/entities/user_food_test.dart,
    test/domain/entities/serving_size_test.dart
  </files>
  <action>
    Create the eight files below using the established group-level skip pattern (`group('ClassName', skip: 'ClassName not yet implemented', () { ... })` containing empty-bodied `test()`/`testWidgets()` cases). Import only `package:flutter_test/flutter_test.dart` — no production imports (the classes do not exist yet).

    `test/data/local/meal_entry_dao_test.dart` — `group('MealEntryDao', skip: 'MealEntryDao not yet implemented', ...)`:
    - `test('insert creates a new row with correct logDate computed from loggedAt', () {})` — LOG-05
    - `test('logging same foodRef+slot+day+unit merges into existing row (adds quantity)', () {})` — LOG-05 merge
    - `test('logging same foodRef+slot+day with different unit creates a separate row', () {})` — LOG-05 merge unit-mismatch
    - `test('getRecent returns individually logged items deduped by foodRef, most-recent-first, capped at 15', () {})` — LOG-07
    - `test('re-logging an item already in Recent moves it to the top instead of duplicating', () {})` — LOG-07
    - `test('softDelete sets deletedAt tombstone; row excluded from getEntriesForToday', () {})` — LOG-09
    - `test('toggleFavorite inserts a favorite row; toggling again removes it', () {})` — LOG-08
    - `test('getFavorites returns favorited foods with lastQuantity/lastUnit for one-tap reuse', () {})` — LOG-08

    `test/data/local/user_food_dao_test.dart` — `group('UserFoodDao', skip: 'UserFoodDao not yet implemented', ...)`:
    - `test('insert requires name and calories; throws or rejects when either is missing', () {})` — LOG-10
    - `test('insert with overrideOfRef+overrideOfSource stores an independent override row', () {})` — LOG-11
    - `test('findOverrideByFoodRef returns the override row when one exists for a catalog food', () {})` — LOG-11
    - `test('deleting an override row (revert) leaves the original catalog data untouched', () {})` — LOG-11
    - `test('getAllAlphabetical returns rows sorted by name with optional filter substring', () {})` — LOG-10 My Foods list

    `test/data/local/food_catalog_dao_override_test.dart` — `group('FoodCatalogDao override precedence', skip: 'override-aware search not yet implemented', ...)`:
    - `test('searchLocalFoods returns the override row instead of the original when both match', () {})` — LOG-11
    - `test('lookupByBarcodeWithCo2 resolves to the override when a barcode-keyed override exists', () {})` — LOG-11
    - `test('original catalog row is still queryable directly by barcode when the override is reverted', () {})` — LOG-11

    `test/data/repositories/meal_entry_repository_test.dart` — `group('MealEntryRepository', skip: 'MealEntryRepository not yet implemented', ...)`:
    - `test('logOrMerge captures a macro/CO2 snapshot at write time, not a live reference', () {})` — LOG-05 snapshot data model
    - `test('undoMergeDelta subtracts only the just-added delta, preserving pre-merge quantity', () {})` — LOG-05 undo
    - `test('duplicateEntry always creates a new row, bypassing merge even when merge criteria match', () {})` — LOG-09
    - `test('softDelete + restoreEntry round-trips a deleted entry back to its prior state (Undo)', () {})` — LOG-09

    `test/data/repositories/user_food_repository_test.dart` — `group('UserFoodRepository', skip: 'UserFoodRepository not yet implemented', ...)`:
    - `test('saveCustomFood persists a category-estimate CO2 value with medium confidence band', () {})` — LOG-10
    - `test('saveCustomFood with manual CO2 value stores no confidence band (self-entered, unbacked)', () {})` — LOG-10
    - `test('saveOverride never mutates the original catalog/cache row', () {})` — LOG-11
    - `test('revertOverride deletes the override row and returns void', () {})` — LOG-11

    `test/domain/entities/meal_entry_test.dart` — `group('MealEntry', skip: 'MealEntry entity not yet implemented', ...)`:
    - `test('copyWith replaces only specified fields; sentinel pattern preserves nullable fields', () {})` — entity contract
    - `test('scaled macro calculation: 150g quantity against a 100g-basis snapshot returns 1.5x values', () {})` — LOG-06 live scaling
    - `test('scaled macro calculation returns null fields when the snapshot field is null (no false precision)', () {})` — LOG-06

    `test/domain/entities/user_food_test.dart` — `group('UserFood', skip: 'UserFood entity not yet implemented', ...)`:
    - `test('copyWith replaces only specified fields; sentinel pattern preserves nullable fields', () {})` — entity contract
    - `test('isValid returns false when name is empty or calories is null', () {})` — LOG-10 required-field validation
    - `test('perReferenceAmount macros scale correctly when referenceAmountG differs from 100', () {})` — LOG-10 reference-amount math

    `test/domain/entities/serving_size_test.dart` — `group('ServingSize', skip: 'ServingSize entity not yet implemented', ...)`:
    - `test('toJson/fromJson round-trips a list of {label, grams} pairs', () {})` — LOG-06/LOG-10 quick serving sizes
    - `test('fromJsonList returns an empty list for null or malformed JSON (no crash)', () {})`

    All eight files: no imports beyond `flutter_test`. Do not add `import 'dart:ui'` or any `package:co2diet/...` import — the production classes do not exist yet.
  </action>
  <verify>
    <automated>flutter test test/data/local/meal_entry_dao_test.dart test/data/local/user_food_dao_test.dart test/data/local/food_catalog_dao_override_test.dart test/data/repositories/meal_entry_repository_test.dart test/data/repositories/user_food_repository_test.dart test/domain/entities/meal_entry_test.dart test/domain/entities/user_food_test.dart test/domain/entities/serving_size_test.dart 2>&amp;1 | tail -8</automated>
  </verify>
  <done>All eight test files compile and report 0 failures (tests are skipped, not failing). `flutter test` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create feature/UI/integration test stubs</name>
  <files>
    test/features/meal_logging/meal_entry_notifier_test.dart,
    test/features/my_foods/user_food_notifier_test.dart,
    test/features/food_search/favorites_test.dart,
    test/features/my_foods/custom_food_form_test.dart,
    test/features/meal_logging/offline_logging_test.dart,
    test/features/food_search/portion_slot_form_test.dart,
    test/features/dashboard/meal_entry_row_test.dart,
    integration_test/meal_logging_benchmark_test.dart
  </files>
  <action>
    Create `test/features/meal_logging/`, `test/features/my_foods/`, and `test/features/dashboard/` directories. Write eight more stub files using the same group-level skip pattern (unit/widget) or `markTestSkipped()` body pattern (integration).

    `test/features/meal_logging/meal_entry_notifier_test.dart` — `group('MealEntryNotifier', skip: 'MealEntryNotifier not yet implemented', ...)`:
    - `test('build() returns entries for today grouped implicitly by logging them with slots', () {})` — LOG-05
    - `test('logFood merges into an existing same-slot/same-day/same-unit entry', () {})` — LOG-05
    - `test('editEntry updates slot/quantity/unit and recomputes snapshot scaling only (not the stored snapshot values)', () {})` — LOG-09
    - `test('deleteEntry soft-deletes; undoDelete restores the exact prior entry', () {})` — LOG-09
    - `test('duplicateEntry creates a new row bypassing merge', () {})` — LOG-09

    `test/features/my_foods/user_food_notifier_test.dart` — `group('UserFoodNotifier', skip: 'UserFoodNotifier not yet implemented', ...)`:
    - `test('saveCustomFood persists and refreshes the My Foods list', () {})` — LOG-10
    - `test('saveOverride persists an override tied to overrideOfRef/overrideOfSource', () {})` — LOG-11
    - `test('revertOverride removes the override and the original reappears', () {})` — LOG-11

    `test/features/food_search/favorites_test.dart` — `group('FavoriteNotifier', skip: 'FavoriteNotifier not yet implemented', ...)`:
    - `test('toggleFavorite flips favorite state for a foodRef', () {})` — LOG-08
    - `test('one-tap log from a favorite uses last-used quantity and auto-detected slot', () {})` — LOG-08
    - `testWidgets('favorite star renders filled when isFavorite is true', (tester) async {})` — LOG-08

    `test/features/my_foods/custom_food_form_test.dart` — `group('CustomFoodFormScreen', skip: 'CustomFoodFormScreen not yet implemented', ...)`:
    - `testWidgets('Save button disabled until name and calories are provided', (tester) async {})` — LOG-10
    - `testWidgets('adding a quick serving size row appends a label+grams field pair', (tester) async {})` — LOG-06/LOG-10
    - `testWidgets('manual CO2 entry shows "user-provided" label and no ConfidenceChip', (tester) async {})` — LOG-10
    - `testWidgets('Revert to original button is shown only when editing an override', (tester) async {})` — LOG-11

    `test/features/meal_logging/offline_logging_test.dart` — `group('Offline core logging', skip: 'offline logging path not yet verifiable', ...)`:
    - `test('logFood, editEntry, deleteEntry, duplicateEntry never call OffApiClient or Connectivity check', () {})` — LOG-12

    `test/features/food_search/portion_slot_form_test.dart` — `group('PortionSlotForm', skip: 'PortionSlotForm not yet implemented', ...)`:
    - `testWidgets('meal slot segmented control pre-selects based on time of day', (tester) async {})` — LOG-05
    - `testWidgets('tapping a preset quantity chip pre-fills an editable numeric field', (tester) async {})` — LOG-06
    - `testWidgets('unit dropdown only offers g/ml when no weight-per-unit is configured', (tester) async {})` — LOG-06
    - `testWidgets('displayed macro values scale live when quantity changes', (tester) async {})` — LOG-06
    - `testWidgets('Log this food button is disabled until quantity is valid (>0)', (tester) async {})` — LOG-06

    `test/features/dashboard/meal_entry_row_test.dart` — `group('MealEntryRow', skip: 'MealEntryRow not yet implemented', ...)`:
    - `testWidgets('swipe reveals Edit, Duplicate, Delete actions', (tester) async {})` — LOG-09
    - `testWidgets('row shows name, quantity, calories, and CO2 (not just name+quantity)', (tester) async {})` — LOG-09
    - `testWidgets('a slot with zero entries hides its header entirely', (tester) async {})` — LOG-05/LOG-09

    `integration_test/meal_logging_benchmark_test.dart` (in `integration_test/`, not `test/`): Use the Phase 2/3 pattern for integration benchmark stubs — `testWidgets` body calls `markTestSkipped()` to exit cleanly without a device. Import `package:flutter_test/flutter_test.dart` and `package:integration_test/integration_test.dart`. Body:
    ```
    IntegrationTestWidgetsFlutterBinding.ensureInitialized();
    testWidgets('LOG-13: tap "Add Breakfast" to saved+visible on dashboard completes in under 10s', (tester) async {
      markTestSkipped('Meal logging benchmark: run after meal logging flow is fully wired (Plan 04-09 onward)');
    });
    ```
  </action>
  <verify>
    <automated>flutter test test/features/meal_logging/ test/features/my_foods/ test/features/food_search/favorites_test.dart test/features/food_search/portion_slot_form_test.dart test/features/dashboard/ 2>&amp;1 | tail -8</automated>
  </verify>
  <done>All eight stub files exist. The seven unit/widget stub files run and exit 0. The integration test stub exists and compiles (not run here — requires device). `flutter test test/` exits 0 (full suite including all 16 new stubs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production | Test files must not import production code that does not exist yet — stubs use only flutter_test/integration_test imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Wave 0 stub plan — no new dependencies |
</threat_model>

<verification>
Run the full test suite after both tasks complete:

```
flutter test test/
```

Expected output: all Phase 1–3 tests still green; sixteen new Phase 4 stubs skipped (not failing). Exit code 0.

Also run flutter analyze to confirm no lint violations in the new stub files:

```
flutter analyze test/data/local/ test/data/repositories/ test/domain/entities/ test/features/meal_logging/ test/features/my_foods/ test/features/food_search/ test/features/dashboard/
```
</verification>

<success_criteria>
- Sixteen stub files exist at exact paths listed in files_modified
- `flutter test test/` exits 0
- No production files modified
- All unit/widget stubs use group-level skip; the integration stub uses `markTestSkipped()` in body
- No imports beyond flutter_test/integration_test in stub files
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-01-SUMMARY.md` when done.
</output>
