---
phase: 04-meal-logging-core-10s-target
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - lib/data/repositories/meal_entry_repository.dart
  - lib/data/repositories/user_food_repository.dart
  - lib/core/di/meal_logging_providers.dart
  - lib/domain/entities/meal_entry.dart
  - lib/domain/entities/favorite.dart
  - lib/domain/entities/user_food.dart
  - test/data/repositories/meal_entry_repository_test.dart
  - test/data/repositories/user_food_repository_test.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08
  - LOG-09
  - LOG-10
  - LOG-11

must_haves:
  truths:
    - "MealEntryRepository.logOrMerge constructs the snapshot from the FoodItem/UserFood passed in at call time, never re-reads it later"
    - "MealEntryRepository implements both IMealEntryRepository and the favorite-related methods (single class, per CONTEXT.md's connected Recent/Favorites/logging flow)"
    - "UserFoodRepository.saveCustomFood/saveOverride reject invalid UserFood drafts before hitting the DAO"
    - "MealEntry.fromRow/Favorite.fromRow/UserFood.fromRow are owned exclusively by this plan (Plan 04-03 defines the entities but deliberately never imports package:drift, so the Drift-row-to-entity mapping direction lives here instead)"
  artifacts:
    - path: "lib/data/repositories/meal_entry_repository.dart"
      provides: "Concrete IMealEntryRepository — merge/undo/duplicate/favorites orchestration over MealEntryDao"
    - path: "lib/data/repositories/user_food_repository.dart"
      provides: "Concrete IUserFoodRepository — custom food/override orchestration over UserFoodDao"
    - path: "lib/core/di/meal_logging_providers.dart"
      provides: "Riverpod DI: mealEntryDao/userFoodDao/mealEntryRepository/userFoodRepository providers"
  key_links:
    - from: "lib/core/di/meal_logging_providers.dart"
      to: "lib/data/repositories/meal_entry_repository.dart"
      via: "mealEntryRepositoryProvider returns IMealEntryRepository"
      pattern: "IMealEntryRepository"
---

<objective>
Implement `MealEntryRepository` (satisfying `IMealEntryRepository`, including the favorite-related methods) and `UserFoodRepository` (satisfying `IUserFoodRepository`), each backed by the DAOs from Plan 04-04. Wire Riverpod DI providers in a new `meal_logging_providers.dart`, following `app_providers.dart`'s established `@Riverpod(keepAlive: true)` pattern.

Purpose: This is the orchestration layer between raw DAO rows (`MealEntryRow`, `FavoriteRow`, `UserFoodRow`) and domain entities (`MealEntry`, `Favorite`, `UserFood`) that notifiers (Plan 04-07) will consume. Row↔entity mapping lives here, not in the DAO (DAOs stay Drift-row-typed; repositories are the domain-interface boundary, matching `FoodCatalogRepository`'s existing role). This plan is also the single, exclusive owner of the `MealEntry.fromRow`/`Favorite.fromRow`/`UserFood.fromRow` factory constructors: Plan 04-03 defines the entities but deliberately never imports `package:drift`, so the Drift-row-to-entity mapping direction cannot live there — it belongs here, where `MealEntryRow`/`FavoriteRow`/`UserFoodRow` (Plan 04-02's generated Drift data classes, available transitively via Plan 04-04) are already in scope.

Output: Two repository files, one DI provider file, three entity files extended with `fromRow` factory constructors, and both Wave 0 repository test stubs filled in.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@lib/data/repositories/food_catalog_repository.dart
@lib/core/di/app_providers.dart
@lib/core/di/providers.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MealEntryRepository</name>
  <files>
    lib/data/repositories/meal_entry_repository.dart,
    lib/domain/entities/meal_entry.dart,
    lib/domain/entities/favorite.dart,
    test/data/repositories/meal_entry_repository_test.dart
  </files>
  <behavior>
    - `logOrMerge(draft)` where `draft` carries a fresh snapshot (name/macros/CO2 as of "now") always persists exactly those values, regardless of what the source food's current DB row contains — verified by mocking `MealEntryDao` and asserting the Companion/row passed to `insertOrMerge` matches `draft`'s snapshot fields byte-for-byte.
    - `undoMergeDelta(id, delta)` calls `MealEntryDao.adjustQuantity(id, delta)` and nothing else (no re-fetch of the source food).
    - `duplicateEntry(id)` calls `MealEntryDao.duplicate(id, newId: ...)`, never `insertOrMerge`.
    - `deleteEntry(id)` calls `MealEntryDao.softDelete` and returns the pre-delete row (fetched via `getById` before deleting) for the Undo snackbar to hold onto.
    - `MealEntry.fromRow(MealEntryRow)` and `Favorite.fromRow(FavoriteRow)` map every column 1:1 onto the corresponding entity field (no lossy conversions); round-tripping a row through `fromRow` then back through the repository's row-building step reproduces the original row's field values.
  </behavior>
  <action>
    Add factory constructors `MealEntry.fromRow(MealEntryRow row)` to `lib/domain/entities/meal_entry.dart` and `Favorite.fromRow(FavoriteRow row)` to `lib/domain/entities/favorite.dart` — this plan is the exclusive, committed owner of both (see `<objective>`; Plan 04-03 does not add them because it never imports `package:drift`, and only this plan's Drift-row types are available at this point in the dependency chain). Each factory maps every table column onto the matching entity field 1:1 (e.g. `MealEntryRow.mealSlot` → `MealEntry.mealSlot`, `MealEntryRow.calories100gSnapshot` → `MealEntry.calories100gSnapshot`, etc.) with no lossy conversions.

    Create `lib/data/repositories/meal_entry_repository.dart`: `final class MealEntryRepository implements IMealEntryRepository`. Constructor `MealEntryRepository(this._dao);` taking a `MealEntryDao`. Add a private `Uuid` const instance (`static const _uuid = Uuid();`) matching `FoodCatalogRepository`'s pattern for `id` generation (`.v7()`).

    Implement every `IMealEntryRepository` method by:
    1. Converting the incoming domain entity (`MealEntry`/`Favorite`) to the corresponding Drift row/Companion using the sync-safe HLC placeholder convention already established (`hlcNodeId: 'local'`, `hlcCounter: 0`, `hlcMillis: BigInt.from(DateTime.now().millisecondsSinceEpoch)`, `dirty: true` — Phase 7 replaces these).
    2. Calling the matching `MealEntryDao` method.
    3. Mapping the returned Drift row back to the domain entity using `MealEntry.fromRow(MealEntryRow)` / `Favorite.fromRow(FavoriteRow)` (added to the entity files as described above).

    `logOrMerge(MealEntry draft)`: generate `id: _uuid.v7()` for the row if `draft.id` is empty/placeholder (the UI constructs a draft without a real `id` before the DB assigns one), build the Companion from `draft`'s already-populated snapshot fields (the caller — the portion/slot form's notifier, Plan 04-07 — is responsible for populating the snapshot from the currently-displayed `FoodItem`/`UserFood`/`Favorite` at the moment "Log this food" is tapped; this repository never re-fetches source data), call `_dao.insertOrMerge(draft: companion)`, map back to `MealEntry`.

    `getEntriesForToday()`: compute `logDate` as today's local `YYYY-MM-DD` (`DateTime.now()` formatted, not UTC), call `_dao.getEntriesForToday(logDate)`, map rows to entities.

    `deleteEntry(String id)`: `final row = await _dao.getById(id); if (row == null) throw StateError('...'); await _dao.softDelete(id); return MealEntry.fromRow(row);` (returns the pre-delete state for Undo).

    `toggleFavorite(Favorite draft)`: build a `FavoriteTableCompanion` from `draft`, call `_dao.toggleFavorite`, then re-check `_dao.isFavorite(draft.foodRef, draft.foodRefSource)` to determine the resulting state and return an appropriately-constructed `Favorite` (or rely on `_dao.toggleFavorite`'s own row-presence check — document the exact contract clearly in a doc comment since the DAO already does the presence check internally).

    Implement the remaining methods (`editEntry`, `restoreEntry`, `duplicateEntry`, `undoMergeDelta`, `isFavorite`, `getFavorites`, `touchFavoriteUsage`, `getRecent`) as thin pass-throughs to the corresponding `MealEntryDao` method with row↔entity mapping.

    Fill in `test/data/repositories/meal_entry_repository_test.dart` (replace Wave 0 skip) using `mocktail` to mock `MealEntryDao` (matching the mocking style already used in `test/features/barcode_scan/barcode_scan_notifier_test.dart` or `test/data/repositories/food_catalog_repository_test.dart` — read whichever exists for the exact mocktail setup pattern), covering the `<behavior>` cases plus the already-stubbed snapshot/undo/duplicate/soft-delete-restore cases.
  </action>
  <verify>
    <automated>flutter test test/data/repositories/meal_entry_repository_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`meal_entry_repository_test.dart` passes with 0 skips. `MealEntryRepository` implements every `IMealEntryRepository` method with row↔entity mapping and zero direct SQL (all queries delegated to `MealEntryDao`). `MealEntry.fromRow`/`Favorite.fromRow` exist on the entity classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserFoodRepository + DI providers</name>
  <files>
    lib/data/repositories/user_food_repository.dart,
    lib/domain/entities/user_food.dart,
    test/data/repositories/user_food_repository_test.dart,
    lib/core/di/meal_logging_providers.dart
  </files>
  <behavior>
    - `saveCustomFood(food)` where `food.isValid == false` throws `ArgumentError` before calling `UserFoodDao.insert` (repository-level guard mirrors the DAO-level guard — defense in depth, not redundant given DAOs and repositories can each be called independently in tests/future code paths).
    - `saveOverride(override)` where `override.overrideOfRef == null` throws `ArgumentError` (an override must reference what it overrides).
    - `revertOverride(id)` calls `UserFoodDao.revert(id)` and nothing else — no side effect on any other table.
    - `UserFood.fromRow(UserFoodRow)` maps every column 1:1 onto the entity, including `quickServingSizes` (already decoded to `List<ServingSize>` at the column level by the table's `TypeConverter.json2` from Plan 04-02 — assigned directly, not re-decoded).
  </behavior>
  <action>
    Add a factory constructor `UserFood.fromRow(UserFoodRow row)` to `lib/domain/entities/user_food.dart` — this plan is the exclusive, committed owner of it (same reasoning as Task 1: Plan 04-03 never imports `package:drift`, so the Drift-row-to-entity mapping lives here). Maps every column 1:1 (`row.name` → `name`, `row.co2e100g` → `co2e100g`, etc.); `row.quickServingSizes` is already `List<ServingSize>` (decoded at the column level by Drift's `TypeConverter.json2`, Plan 04-02) — assign it directly to `quickServingSizes`, do not re-decode.

    Create `lib/data/repositories/user_food_repository.dart`: `final class UserFoodRepository implements IUserFoodRepository`. Constructor `UserFoodRepository(this._dao);` taking a `UserFoodDao`.

    `saveCustomFood(UserFood food)`: `if (!food.isValid) throw ArgumentError('UserFood requires a name and calories');` then build a `UserFoodTableCompanion` (id via `_uuid.v7()` if new, `quickServingSizes` via `ServingSize.encodeList`), call `_dao.insert` (new) or `_dao.update` (existing — branch on whether `food.id` already exists via `_dao.getById`), map the returned row back to `UserFood` via the `UserFood.fromRow` factory added above.

    `saveOverride(UserFood override)`: same validation plus `if (override.overrideOfRef == null) throw ArgumentError('An override must set overrideOfRef');`, then delegates to the same insert/update path as `saveCustomFood` (both are the same underlying table — CONTEXT.md's "single table for both" decision).

    `revertOverride(String userFoodId)` → `_dao.revert(userFoodId)`.
    `findOverrideForFoodRef(String foodRef, String foodRefSource)` → `_dao.findOverrideByFoodRef`, map to `UserFood?`.
    `getAllAlphabetical({String? filter})` → `_dao.getAllAlphabetical`, map list.
    `getById(String id)` → `_dao.getById`, map to `UserFood?`.

    Fill in `test/data/repositories/user_food_repository_test.dart` (replace Wave 0 skip) using `mocktail` to mock `UserFoodDao`, covering the `<behavior>` cases plus the already-stubbed category-estimate/manual-CO2/override-independence cases.

    Create `lib/core/di/meal_logging_providers.dart` (new file — mirrors `app_providers.dart`'s doc-comment style: "These providers are intentionally separate ... to keep the base DI file focused"):
    - `@Riverpod(keepAlive: true) MealEntryDao mealEntryDao(Ref ref) => ref.watch(appDatabaseProvider).mealEntryDao;`
    - `@Riverpod(keepAlive: true) UserFoodDao userFoodDao(Ref ref) => ref.watch(appDatabaseProvider).userFoodDao;`
    - `@Riverpod(keepAlive: true) IMealEntryRepository mealEntryRepository(Ref ref) => MealEntryRepository(ref.watch(mealEntryDaoProvider));`
    - `@Riverpod(keepAlive: true) IUserFoodRepository userFoodRepository(Ref ref) => UserFoodRepository(ref.watch(userFoodDaoProvider));`
    - Import `appDatabaseProvider` from `package:co2diet/core/di/providers.dart` (matching `app_providers.dart`'s existing import).
    - Add `part 'meal_logging_providers.g.dart';` and run `dart run build_runner build --delete-conflicting-outputs`.
  </action>
  <verify>
    <automated>flutter test test/data/repositories/user_food_repository_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`user_food_repository_test.dart` passes with 0 skips. `meal_logging_providers.dart` compiles and exposes four keep-alive providers. `UserFood.fromRow` exists on the entity class. `flutter analyze lib/data/repositories/ lib/core/di/meal_logging_providers.dart` reports 0 issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notifier layer → Repository | Domain entities cross this boundary; repositories are the last point before raw SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | `UserFoodRepository.saveCustomFood`/`saveOverride` bypassing validation | mitigate | Repository-level `ArgumentError` guard mirrors the DAO-level guard (defense in depth per RESEARCH.md Security Domain V5) |
| T-04-05-02 | Information Disclosure | HLC placeholder fields (`hlcNodeId: 'local'`) | accept | Matches the Phase 1/2/3 established placeholder convention; Phase 7 replaces with real device UUID — no new risk introduced here |
| T-04-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/data/repositories/ && flutter analyze lib/data/repositories/ lib/core/di/
```
</verification>

<success_criteria>
- `MealEntryRepository` implements `IMealEntryRepository` completely (meal-entry + favorite methods)
- `UserFoodRepository` implements `IUserFoodRepository` completely
- `MealEntry.fromRow`/`Favorite.fromRow`/`UserFood.fromRow` exist, owned exclusively by this plan
- `meal_logging_providers.dart` exposes `mealEntryDaoProvider`, `userFoodDaoProvider`, `mealEntryRepositoryProvider`, `userFoodRepositoryProvider`
- Both repository test files pass with 0 skips
- Neither repository issues raw SQL directly — all queries go through the DAO layer
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-05-SUMMARY.md` when done.
</output>
</output>
