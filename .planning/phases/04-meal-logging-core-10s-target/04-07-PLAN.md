---
phase: 04-meal-logging-core-10s-target
plan: 07
type: execute
wave: 5
depends_on:
  - 04-05
  - 04-06
files_modified:
  - lib/features/meal_logging/providers/meal_entry_notifier.dart
  - lib/features/food_search/providers/favorite_notifier.dart
  - lib/features/my_foods/providers/user_food_notifier.dart
  - test/features/meal_logging/meal_entry_notifier_test.dart
  - test/features/food_search/favorites_test.dart
  - test/features/my_foods/user_food_notifier_test.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-07
  - LOG-08
  - LOG-09
  - LOG-10
  - LOG-11

must_haves:
  truths:
    - "MealEntryNotifier exposes today's entries and re-runs build() after every mutation (log/edit/delete/duplicate)"
    - "Deleting an entry keeps enough state client-side for an Undo action to restore it without re-fetching"
    - "FavoriteNotifier and UserFoodNotifier follow the same @riverpod class AsyncNotifier pattern as ProfileNotifier"
  artifacts:
    - path: "lib/features/meal_logging/providers/meal_entry_notifier.dart"
      provides: "MealEntryNotifier — the UI-facing mutation surface for LOG-05/07/09"
    - path: "lib/features/food_search/providers/favorite_notifier.dart"
      provides: "FavoriteNotifier — LOG-08 toggle + one-tap-log support"
    - path: "lib/features/my_foods/providers/user_food_notifier.dart"
      provides: "UserFoodNotifier — LOG-10/11 custom food + override mutation surface"
  key_links:
    - from: "lib/features/meal_logging/providers/meal_entry_notifier.dart"
      to: "lib/core/di/meal_logging_providers.dart"
      via: "ref.watch(mealEntryRepositoryProvider)"
      pattern: "mealEntryRepositoryProvider"
---

<objective>
Implement the three Riverpod `@riverpod class` AsyncNotifiers that the UI layer (Plans 04-08 through 04-11) will consume: `MealEntryNotifier` (today's entries + log/edit/delete/duplicate/undo), `FavoriteNotifier` (toggle + one-tap-log), and `UserFoodNotifier` (custom food/override save/revert + My Foods list). Every notifier follows `ProfileNotifier`'s exact codegen shape — `build()` returns the current state, mutation methods call the repository then `ref.invalidateSelf()`.

Purpose: This is the UI-facing state layer. No widget code is written in this plan — Plans 04-08 through 04-11 consume these notifiers exclusively through `ref.watch`/`ref.read(...).notifier`, never touching the repository or DAO layers directly.

Output: Three notifier files and their three Wave 0 test stubs filled in.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@lib/features/profile/providers/profile_notifier.dart
@lib/features/food_search/providers/food_search_notifier.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MealEntryNotifier</name>
  <files>
    lib/features/meal_logging/providers/meal_entry_notifier.dart,
    test/features/meal_logging/meal_entry_notifier_test.dart
  </files>
  <behavior>
    - `build()` returns today's entries via `IMealEntryRepository.getEntriesForToday()`.
    - `logFood(draft)` calls `logOrMerge`, then `ref.invalidateSelf()` so the dashboard list picks up the change.
    - `deleteEntry(id)` calls the repository's `deleteEntry(id)` (which returns the pre-delete row), stores that row internally for one pending-undo slot, then `ref.invalidateSelf()`; a subsequent `undoDelete()` call restores it via `restoreEntry` and clears the pending-undo slot.
    - `logMerge(draft)` returning a merged (not new) row also exposes the merge `delta` to the caller so the UI's Undo snackbar can call `undoMerge(entryId, delta)` later.
    - `duplicateEntry(id)` calls the repository's `duplicateEntry(id)`, then `ref.invalidateSelf()`.
  </behavior>
  <action>
    Create `lib/features/meal_logging/providers/meal_entry_notifier.dart` following `ProfileNotifier`'s exact structure:
    ```
    @riverpod
    class MealEntryNotifier extends _$MealEntryNotifier {
      @override
      Future<List<MealEntry>> build() =>
          ref.watch(mealEntryRepositoryProvider).getEntriesForToday();
      ...
    }
    ```
    Methods:
    - `Future<MealLogResult> logFood(MealEntry draft)` — calls `ref.read(mealEntryRepositoryProvider).logOrMerge(draft)`, compares the returned row's `quantity` against `draft.quantity` to detect whether this was a merge (returned quantity > draft quantity means an existing row absorbed the delta) or a fresh insert; returns a small `MealLogResult` value object (`{MealEntry entry, bool wasMerge, double loggedDelta}`) defined in this same file so the UI's Undo button knows whether to call `undoMerge` (merge case) or `deleteEntry` (fresh-insert case) on Undo. `ref.invalidateSelf()` after the write.
    - `Future<void> undoMerge(String entryId, double delta)` — `ref.read(mealEntryRepositoryProvider).undoMergeDelta(entryId, delta)`, then `ref.invalidateSelf()`.
    - `Future<MealEntry> editEntry(MealEntry updated)` — `ref.read(mealEntryRepositoryProvider).editEntry(updated)`, then `ref.invalidateSelf()`.
    - `MealEntry? _pendingUndoDelete;` (private field) + `Future<void> deleteEntry(String id) async { _pendingUndoDelete = await ref.read(mealEntryRepositoryProvider).deleteEntry(id); ref.invalidateSelf(); }` + `Future<void> undoDelete() async { final entry = _pendingUndoDelete; if (entry == null) return; await ref.read(mealEntryRepositoryProvider).restoreEntry(entry); _pendingUndoDelete = null; ref.invalidateSelf(); }`.
    - `Future<void> duplicateEntry(String id) async { await ref.read(mealEntryRepositoryProvider).duplicateEntry(id); ref.invalidateSelf(); }`.
    - `Future<List<MealEntry>> getRecent() => ref.read(mealEntryRepositoryProvider).getRecent();` — NOT part of `build()`'s state (Recent is a separate concern queried on-demand by the food search empty state, not the dashboard's today-list); document why this method exists outside the `build()`/`invalidateSelf()` cycle.

    Fill in `test/features/meal_logging/meal_entry_notifier_test.dart` (replace Wave 0 skip) using `ProviderContainer` + a mocked `IMealEntryRepository` (mocktail), following the `ProviderContainer.listen` + `Completer` pattern already established in `test/features/food_search/food_search_notifier_test.dart` for awaiting AsyncNotifier `build()`. Cover the `<behavior>` cases plus the already-stubbed merge/edit/delete-undo/duplicate cases.
  </action>
  <verify>
    <automated>flutter test test/features/meal_logging/meal_entry_notifier_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`meal_entry_notifier_test.dart` passes with 0 skips. `MealEntryNotifier` exposes `logFood`, `undoMerge`, `editEntry`, `deleteEntry`, `undoDelete`, `duplicateEntry`, `getRecent`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FavoriteNotifier + UserFoodNotifier</name>
  <files>
    lib/features/food_search/providers/favorite_notifier.dart,
    lib/features/my_foods/providers/user_food_notifier.dart,
    test/features/food_search/favorites_test.dart,
    test/features/my_foods/user_food_notifier_test.dart
  </files>
  <behavior>
    - `FavoriteNotifier.build()` returns the current favorites list.
    - `FavoriteNotifier.toggle(favorite)` flips favorite state and `ref.invalidateSelf()`s.
    - `FavoriteNotifier.logFromFavorite(favorite)` builds a `MealEntry` draft from the favorite's snapshot + `lastQuantity`/`lastUnit` (falling back to 100g when `lastQuantity` is null — a favorite that has never been logged) and the time-of-day auto-detected slot, then delegates the actual write to `MealEntryNotifier.logFood` (this notifier reads `ref.read(mealEntryNotifierProvider.notifier)` to perform the write, keeping the "one tap = instant log" behavior in one place) and finally calls `touchFavoriteUsage` to update `lastQuantity`/`lastUnit`.
    - `UserFoodNotifier.saveCustomFood`/`saveOverride`/`revertOverride` each `ref.invalidateSelf()` so the My Foods list and any override-precedence search results refresh.
  </behavior>
  <action>
    Create `lib/features/food_search/providers/favorite_notifier.dart`:
    ```
    @riverpod
    class FavoriteNotifier extends _$FavoriteNotifier {
      @override
      Future<List<Favorite>> build() =>
          ref.watch(mealEntryRepositoryProvider).getFavorites();
      ...
    }
    ```
    Methods:
    - `Future<void> toggle(Favorite draft) async { await ref.read(mealEntryRepositoryProvider).toggleFavorite(draft); ref.invalidateSelf(); }`.
    - `Future<bool> isFavorite(String foodRef, String foodRefSource) => ref.read(mealEntryRepositoryProvider).isFavorite(foodRef, foodRefSource);`.
    - `Future<MealLogResult> logFromFavorite(Favorite favorite, {required MealSlot autoDetectedSlot}) async` — builds a `MealEntry` draft (no `id`, `mealSlot: autoDetectedSlot`, `quantity: favorite.lastQuantity ?? 100`, `unit: favorite.lastUnit ?? PortionUnit.g`, snapshot fields copied from `favorite.*Snapshot`, `loggedAt`/`logDate` computed now), calls `ref.read(mealEntryNotifierProvider.notifier).logFood(draft)`, then `ref.read(mealEntryRepositoryProvider).touchFavoriteUsage(favorite.foodRef, favorite.foodRefSource, quantity: draft.quantity, unit: draft.unit)`, returns the `MealLogResult` from the log call so the caller's Undo snackbar works identically to a sheet-initiated log.

    Create `lib/features/my_foods/providers/user_food_notifier.dart`:
    ```
    @riverpod
    class UserFoodNotifier extends _$UserFoodNotifier {
      @override
      Future<List<UserFood>> build({String? filter}) =>
          ref.watch(userFoodRepositoryProvider).getAllAlphabetical(filter: filter);
      ...
    }
    ```
    Note: if `@riverpod` family parameters (`{String? filter}`) complicate the generated provider signature, an acceptable alternative consistent with existing codebase patterns is to keep `build()` parameterless (returns the full unfiltered list) and let the My Foods screen (Plan 04-08) filter client-side with a local `TextEditingController` — choose whichever keeps the generated provider name predictable; document the choice in the SUMMARY.
    Methods:
    - `Future<UserFood> saveCustomFood(UserFood food) async { final saved = await ref.read(userFoodRepositoryProvider).saveCustomFood(food); ref.invalidateSelf(); return saved; }`.
    - `Future<UserFood> saveOverride(UserFood override) async { ... }` — same shape.
    - `Future<void> revertOverride(String userFoodId) async { await ref.read(userFoodRepositoryProvider).revertOverride(userFoodId); ref.invalidateSelf(); }`.
    - `Future<UserFood?> findOverrideForFoodRef(String foodRef, String foodRefSource) => ref.read(userFoodRepositoryProvider).findOverrideForFoodRef(foodRef, foodRefSource);` — used by the sheet's "Edit this food" flow to pre-fill the form with an existing override's values if one is already being edited a second time.

    Fill in `test/features/food_search/favorites_test.dart` and `test/features/my_foods/user_food_notifier_test.dart` (replace Wave 0 skips) using `ProviderContainer` + mocked repositories, matching `meal_entry_notifier_test.dart`'s established pattern from Task 1.
  </action>
  <verify>
    <automated>flutter test test/features/food_search/favorites_test.dart test/features/my_foods/user_food_notifier_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Both test files pass with 0 skips. `FavoriteNotifier` and `UserFoodNotifier` compile and expose the methods above. `flutter analyze lib/features/meal_logging/ lib/features/food_search/providers/favorite_notifier.dart lib/features/my_foods/` reports 0 issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget layer → Notifier | User-entered quantity/slot/CO2 values cross this boundary before reaching the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Denial of Service | Rapid repeated `logFood`/`toggle` taps | accept | Local SQLite writes are near-instant; no rate limiting needed for a single-user offline app (consistent with Phase 2/3 precedent) |
| T-04-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/features/meal_logging/ test/features/food_search/favorites_test.dart test/features/my_foods/
```
</verification>

<success_criteria>
- `MealEntryNotifier`, `FavoriteNotifier`, `UserFoodNotifier` all follow the `@riverpod class` AsyncNotifier pattern
- Every mutation method calls `ref.invalidateSelf()` after a successful write
- `MealEntryNotifier` maintains exactly one pending-undo-delete slot (no unbounded undo history — matches CONTEXT.md's single-level Undo pattern)
- All three notifier test files pass with 0 skips
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-07-SUMMARY.md` when done.
</output>
