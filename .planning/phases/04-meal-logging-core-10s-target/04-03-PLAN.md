---
phase: 04-meal-logging-core-10s-target
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/domain/entities/meal_slot.dart
  - lib/domain/entities/portion_unit.dart
  - lib/domain/entities/meal_entry.dart
  - lib/domain/entities/favorite.dart
  - lib/domain/entities/serving_size.dart
  - lib/domain/entities/user_food.dart
  - lib/domain/repositories/i_meal_entry_repository.dart
  - lib/domain/repositories/i_user_food_repository.dart
  - test/domain/entities/meal_entry_test.dart
  - test/domain/entities/user_food_test.dart
  - test/domain/entities/serving_size_test.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-08
  - LOG-10
  - LOG-11

must_haves:
  truths:
    - "MealEntry's live macro-scaling function is a pure Dart function testable without a database"
    - "UserFood.isValid enforces name + calories as the only required fields (LOG-10)"
    - "ServingSize round-trips through JSON without data loss"
  artifacts:
    - path: "lib/domain/entities/meal_entry.dart"
      provides: "MealEntry entity + pure live-scaling calculation (LOG-06)"
    - path: "lib/domain/entities/user_food.dart"
      provides: "UserFood entity with required-field validation (LOG-10)"
    - path: "lib/domain/repositories/i_meal_entry_repository.dart"
      provides: "Domain contract consumed by Plan 04-05 (repository) and Plan 04-07 (notifiers)"
  key_links:
    - from: "lib/domain/repositories/i_meal_entry_repository.dart"
      to: "lib/data/repositories/meal_entry_repository.dart"
      via: "implements IMealEntryRepository (Plan 04-05)"
      pattern: "implements IMealEntryRepository"
---

<objective>
Define the pure-Dart domain layer for meal logging: `MealSlot`/`PortionUnit` enums, the `MealEntry`/`Favorite`/`ServingSize`/`UserFood` entities (sentinel `copyWith` pattern matching `FoodItem`), and the `IMealEntryRepository`/`IUserFoodRepository` domain interfaces. This plan runs in parallel with Plan 04-02 (Drift schema) — nothing here imports `package:drift`.

Purpose: Interface-first ordering — Plan 04-04 (DAOs) and Plan 04-05 (repositories) implement against the contracts defined here, and Plan 04-02's `MealEntryTable`/`FavoriteTable` reference `MealSlot`/`PortionUnit` from this plan. Fill in the Wave 0 stub tests for the domain entities.

Output: Seven new domain files plus real (unskipped) tests replacing the three Wave 0 entity stubs.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@.planning/phases/04-meal-logging-core-10s-target/04-RESEARCH.md
@lib/domain/entities/food_item.dart
@lib/domain/repositories/i_food_catalog_repository.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MealSlot, PortionUnit enums, MealEntry entity, IMealEntryRepository</name>
  <files>
    lib/domain/entities/meal_slot.dart,
    lib/domain/entities/portion_unit.dart,
    lib/domain/entities/meal_entry.dart,
    lib/domain/repositories/i_meal_entry_repository.dart,
    test/domain/entities/meal_entry_test.dart
  </files>
  <behavior>
    - `MealEntry.scaledMacros(gramsEquivalent)`: given a 100g/100ml-basis snapshot (e.g. `calories100gSnapshot: 150`) and `gramsEquivalent: 150`, returns `calories: 225` (linear scale: `snapshot * gramsEquivalent / 100`).
    - `MealEntry.scaledMacros`: when a snapshot field is `null` (e.g. `co2e100gSnapshot: null`), the corresponding scaled field is also `null` — never `0` or a fabricated number (no false precision).
    - `MealEntry.copyWith`: sentinel pattern — omitting a nullable field preserves the original value; explicitly passing `null` clears it.
  </behavior>
  <action>
    `meal_slot.dart`: `enum MealSlot { breakfast, lunch, dinner, snack }`. Doc comment: append-only — `textEnum<MealSlot>()` in `MealEntryTable` stores `Enum.name`; renaming a member after rows exist breaks deserialization (RESEARCH.md Pitfall 4). Add a `displayLabel` extension getter (`'Breakfast'`, `'Lunch'`, `'Dinner'`, `'Snack'`) for UI use. Also add a top-level function `MealSlot detectMealSlotForTime(DateTime now)` implementing the time-of-day auto-detect rule referenced throughout CONTEXT.md (e.g. before 11:00 → breakfast, 11:00–15:00 → lunch, 15:00–18:00 → snack, after 18:00 → dinner — exact boundaries are Claude's discretion per CONTEXT.md; pick reasonable cutoffs and document the chosen boundaries in a doc comment on the function). This single shared implementation lives here — the earliest wave both `PortionSlotForm` (Plan 04-09) and the Recent/Favorites one-tap-log path (Plan 04-10) transitively depend on — specifically so neither of those two wave-6/wave-7 plans needs to define its own private copy or modify the other plan's files to share one.

    `portion_unit.dart`: `enum PortionUnit { g, ml, piece, cup, portion }`. Same append-only doc-comment warning. Add a `displayLabel` extension getter (`'g'`, `'ml'`, `'piece'`, `'cup'`, `'portion'`) and an `isWeightBased` getter (`true` for `g`/`ml`, `false` for `piece`/`cup`/`portion` — the portion/slot form uses this to decide whether a weight-per-unit conversion is required before offering the unit).

    `meal_entry.dart`: `@immutable class MealEntry` mirroring `FoodItem`'s structure exactly (plain Dart, no Freezed, sentinel `copyWith`):
    - Constructor fields: `id` (String, required — unlike `FoodItem`, `MealEntry` rows are individually addressable for edit/delete/duplicate so `id` is required, not optional), `mealSlot` (MealSlot, required), `foodRef` (String, required), `foodRefSource` (String, required — `'off_ref'`|`'user_food_cache'`|`'user_foods'`), `quantity` (double, required), `unit` (PortionUnit, required), `productNameSnapshot` (String, required), `brandSnapshot` (String?), `calories100gSnapshot` (double?), `protein100gSnapshot` (double?), `carbs100gSnapshot` (double?), `fat100gSnapshot` (double?), `co2e100gSnapshot` (double?), `confidenceBandSnapshot` (String?), `loggedAt` (DateTime, required), `logDate` (String, required).
    - `copyWith` using the `_sentinel` pattern for every nullable field, exactly like `FoodItem.copyWith`.
    - A static pure function `ScaledMacros scaledMacros(MealEntry entry, {required double gramsEquivalent})` (or an instance method `entry.scaled(gramsEquivalent)`) returning a small `ScaledMacros` value object (`{double? calories, double? protein, double? carbs, double? fat, double? co2e}`) computed as `snapshot == null ? null : snapshot * gramsEquivalent / 100`. Define `ScaledMacros` as a small `@immutable` class in the same file (no need for a separate file — it has no independent identity).
    - `MealEntry` equality: based on `id` (unlike `FoodItem`, which has no `id` — meal entries are always individually addressable rows).
    - Add a doc comment restating the "snapshot, not reference" principle from CONTEXT.md directly on the class. In the same doc comment, note explicitly: the snapshot intentionally captures calories/protein/carbs/fat/co2e only (not sugar/fiber/salt, which `UserFood` captures) — this is a deliberate Phase 4 scope boundary, flagged here for Phase 5 nutrition-rollup planning rather than expanded now.

    `i_meal_entry_repository.dart`: `abstract interface class IMealEntryRepository` covering both meal-entry and favorite concerns (one cohesive "meal logging" domain interface — CONTEXT.md treats Recent/Favorites/logging as one connected flow):
    - `Future<MealEntry> logOrMerge(MealEntry draft)` — inserts a new row, or merges (adds `quantity`) into an existing same-`foodRef`+`foodRefSource`+`mealSlot`+`logDate`+`unit` row; returns the resulting persisted row (new or merged).
    - `Future<List<MealEntry>> getEntriesForToday()` — excludes soft-deleted rows, ordered for grouped-by-slot display.
    - `Future<List<MealEntry>> getRecent({int limit = 15})` — individually logged items, deduped by `foodRef`+`foodRefSource`, most-recent-first.
    - `Future<MealEntry> editEntry(MealEntry updated)` — updates slot/quantity/unit on an existing row by `id`; does NOT touch the snapshot fields.
    - `Future<MealEntry> deleteEntry(String id)` — soft-deletes (tombstone) and returns the pre-delete row for the Undo snackbar.
    - `Future<void> restoreEntry(MealEntry entry)` — clears the tombstone, restoring the exact row (Undo for delete).
    - `Future<MealEntry> duplicateEntry(String id)` — creates a new row copying `id`'s fields into the same slot; bypasses `logOrMerge`'s merge rule entirely (CONTEXT.md: Duplicate must never be a no-op via accidental merge).
    - `Future<void> undoMergeDelta(String entryId, double delta)` — subtracts `delta` from `entryId`'s `quantity` (Undo after a merge restores the pre-merge quantity without deleting the row).
    - `Future<bool> isFavorite(String foodRef, String foodRefSource)`.
    - `Future<Favorite> toggleFavorite(Favorite draft)` — inserts if absent, deletes if present (unique on `foodRef`+`foodRefSource`); returns the resulting state (or a sentinel indicating removal — document the exact contract in a doc comment, e.g. return the row that now exists, and the caller checks `isFavorite` separately for the boolean).
    - `Future<List<Favorite>> getFavorites()`.
    - `Future<void> touchFavoriteUsage(String foodRef, String foodRefSource, {required double quantity, required PortionUnit unit})` — updates `lastQuantity`/`lastUnit` after a one-tap log from Favorites.

    Fill in `test/domain/entities/meal_entry_test.dart`, replacing the Wave 0 skip with real tests matching the `<behavior>` block above plus the `copyWith` sentinel test already stubbed.
  </action>
  <verify>
    <automated>flutter test test/domain/entities/meal_entry_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`meal_entry_test.dart` passes with 0 skips. `flutter analyze lib/domain/entities/meal_slot.dart lib/domain/entities/portion_unit.dart lib/domain/entities/meal_entry.dart lib/domain/repositories/i_meal_entry_repository.dart` reports 0 issues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Favorite, ServingSize, UserFood entities, IUserFoodRepository</name>
  <files>
    lib/domain/entities/favorite.dart,
    lib/domain/entities/serving_size.dart,
    lib/domain/entities/user_food.dart,
    lib/domain/repositories/i_user_food_repository.dart,
    test/domain/entities/user_food_test.dart,
    test/domain/entities/serving_size_test.dart
  </files>
  <behavior>
    - `ServingSize.encodeList`/`decodeList`: a `List<ServingSize>` of `{label: 'Slice', grams: 30}` round-trips through `jsonEncode`/`jsonDecode` unchanged; `decodeList(null)` and `decodeList('not json')` both return `[]` rather than throwing.
    - `UserFood.isValid`: `false` when `name.trim().isEmpty`; `false` when `calories` is `null`; `true` when both are present regardless of every other field being null.
    - `UserFood.caloriesPer100g` (and equivalent per-100g getters for protein/carbs/fat/sugar/fiber/salt): correctly rescale when `referenceAmountG != 100` (e.g. `calories: 250, referenceAmountG: 50` → `caloriesPer100g == 500`).
  </behavior>
  <action>
    `favorite.dart`: `@immutable class Favorite` — fields: `id` (String, required), `foodRef` (String, required), `foodRefSource` (String, required), `productNameSnapshot` (String, required), `brandSnapshot` (String?), `calories100gSnapshot` (double?), `co2e100gSnapshot` (double?), `confidenceBandSnapshot` (String?), `lastQuantity` (double?), `lastUnit` (PortionUnit?), `favoritedAt` (DateTime, required). Sentinel `copyWith`. Equality on `id`.

    `serving_size.dart`: `@immutable class ServingSize { final String label; final double grams; }` with `toJson()`/`ServingSize.fromJson(Map)`. Add static helpers `String encodeList(List<ServingSize> sizes)` (→ `jsonEncode(sizes.map((s) => s.toJson()).toList())`) and `List<ServingSize> decodeList(String? raw)` (→ `[]` on `null`/empty/malformed input via `on FormatException catch (_) { return []; }`, otherwise `(jsonDecode(raw) as List).map(...)`). These are the functions `UserFoodTable.quickServingSizes`'s `TypeConverter.json2` delegates to (Plan 04-02 imports this file).

    `user_food.dart`: `@immutable class UserFood` mirroring `FoodItem`'s sentinel `copyWith` pattern:
    - Fields: `id` (String, required), `name` (String, required), `brand` (String?), `category` (String?), `referenceAmountG` (double, default 100), `calories` (double?, nullable in the constructor to allow constructing an in-progress form draft, but see `isValid`), `protein` (double?), `carbs` (double?), `sugar` (double?), `fat` (double?), `fiber` (double?), `salt` (double?), `co2e100g` (double?), `confidenceBand` (String?), `co2Source` (String? — `'category_estimate'`|`'manual'`), `barcode` (String?), `quickServingSizes` (`List<ServingSize>`, default `const []`), `overrideOfRef` (String?), `overrideOfSource` (String?).
    - `bool get isValid => name.trim().isNotEmpty && calories != null;` (LOG-10: "name + at least a calorie value are required to save").
    - `bool get isOverride => overrideOfRef != null;`
    - Per-100g getters (`caloriesPer100g`, `proteinPer100g`, etc.) computing `value == null ? null : value * 100 / referenceAmountG` for each macro — used when a `UserFood` needs to appear in search results alongside `FoodItem`'s implicit per-100g convention.
    - Equality on `id`.

    `i_user_food_repository.dart`: `abstract interface class IUserFoodRepository`:
    - `Future<UserFood> saveCustomFood(UserFood food)` — throws/rejects when `!food.isValid`.
    - `Future<UserFood> saveOverride(UserFood override)` — same validation; `override.overrideOfRef` must be non-null.
    - `Future<void> revertOverride(String userFoodId)` — deletes the override row.
    - `Future<UserFood?> findOverrideForFoodRef(String foodRef, String foodRefSource)` — used by the search/barcode integration (Plan 04-06) to check override precedence.
    - `Future<List<UserFood>> getAllAlphabetical({String? filter})` — for the My Foods list.
    - `Future<UserFood?> getById(String id)`.

    Fill in `test/domain/entities/user_food_test.dart` and `test/domain/entities/serving_size_test.dart`, replacing the Wave 0 skips with real tests matching the `<behavior>` block plus the already-stubbed `copyWith` sentinel test.
  </action>
  <verify>
    <automated>flutter test test/domain/entities/user_food_test.dart test/domain/entities/serving_size_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Both test files pass with 0 skips. `flutter analyze lib/domain/entities/favorite.dart lib/domain/entities/serving_size.dart lib/domain/entities/user_food.dart lib/domain/repositories/i_user_food_repository.dart` reports 0 issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain layer | Pure Dart, no I/O — no direct trust boundary; validation rules defined here (`UserFood.isValid`) are the contract the data layer must enforce again (defense in depth, RESEARCH.md Security Domain V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | `UserFood.isValid` client-side-only check | mitigate | Plan 04-04/04-05 must re-validate at the DAO/repository boundary before insert — this plan only defines the rule, not the enforcement point |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/domain/entities/ && flutter analyze lib/domain/
```
</verification>

<success_criteria>
- Seven new domain files exist matching the field/method contracts above
- `test/domain/entities/meal_entry_test.dart`, `user_food_test.dart`, `serving_size_test.dart` all pass with 0 skips
- No file in this plan imports `package:drift` or any Flutter widget package
- `IMealEntryRepository`/`IUserFoodRepository` are the exact contracts Plan 04-05 implements
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-03-SUMMARY.md` when done.
</output>
</output>
