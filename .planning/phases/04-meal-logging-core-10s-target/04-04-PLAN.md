---
phase: 04-meal-logging-core-10s-target
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - lib/data/local/daos/meal_entry_dao.dart
  - lib/data/local/daos/user_food_dao.dart
  - lib/data/local/app_database.dart
  - test/data/local/meal_entry_dao_test.dart
  - test/data/local/user_food_dao_test.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08
  - LOG-09
  - LOG-10
  - LOG-11

must_haves:
  truths:
    - "MealEntryDao merge-check uses logDate string equality, never SQL date functions (RESEARCH.md Pattern 3)"
    - "All raw SQL uses Variable.withString/parameterized customStatement — no string interpolation of user input"
    - "UserFoodDao insert rejects rows missing name or calories at the DAO boundary (defense in depth beyond domain-layer isValid)"
  artifacts:
    - path: "lib/data/local/daos/meal_entry_dao.dart"
      provides: "MealEntryDao — insert/merge, recent query, soft-delete, favorites (LOG-05/07/08/09)"
    - path: "lib/data/local/daos/user_food_dao.dart"
      provides: "UserFoodDao — custom food/override CRUD, My Foods list (LOG-10/11)"
  key_links:
    - from: "lib/data/local/daos/meal_entry_dao.dart"
      to: "lib/data/local/tables/meal_entry_table.dart"
      via: "@DriftAccessor(tables: [MealEntryTable, FavoriteTable])"
      pattern: "DriftAccessor"
---

<objective>
Implement `MealEntryDao` (handling both `MealEntryTable` and `FavoriteTable`, following the `FoodCatalogDao` precedent of one DAO covering multiple related tables) and `UserFoodDao` (handling `UserFoodTable`). Register both in `AppDatabase`'s `daos:` list.

Purpose: This is the query layer for LOG-05, LOG-07, LOG-08, LOG-09, LOG-10, and LOG-11. Every query here is raw `customSelect`/`customStatement` with parameterized variables, matching `FoodCatalogDao`'s established security pattern (T-02-03-01/T-03-02-01 precedent).

Output: Two DAO files, `AppDatabase` daos registration, and both DAO test stubs filled in with real tests against an in-memory database.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@.planning/phases/04-meal-logging-core-10s-target/04-RESEARCH.md
@lib/data/local/daos/food_catalog_dao.dart
@test/data/local/food_catalog_dao_test.dart
@lib/data/local/app_database.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MealEntryDao (meal entries + favorites)</name>
  <files>
    lib/data/local/daos/meal_entry_dao.dart,
    test/data/local/meal_entry_dao_test.dart
  </files>
  <behavior>
    - Logging `foodRef=X, slot=lunch, unit=g, quantity=100` then logging `foodRef=X, slot=lunch, unit=g, quantity=50` on the same `logDate` merges into one row with `quantity == 150`.
    - Logging the same food+slot+day but `unit=cup` the second time creates a second, separate row (no merge across units).
    - `getRecent()` returns items most-recent-`loggedAt`-first, deduped by `(foodRef, foodRefSource)`, capped at the given limit.
    - `softDelete(id)` sets `deletedAt`; `getEntriesForToday()` excludes it; a subsequent `restore(id)`-style clear of `deletedAt` brings it back.
    - `toggleFavorite` on an unfavorited `(foodRef, foodRefSource)` inserts a row; calling it again on the same pair removes the row (the `UNIQUE(food_ref, food_ref_source)` constraint from Plan 04-02 backs this).
  </behavior>
  <action>
    Create `lib/data/local/daos/meal_entry_dao.dart`: `@DriftAccessor(tables: [MealEntryTable, FavoriteTable]) class MealEntryDao extends DatabaseAccessor<AppDatabase> with _$MealEntryDaoMixin`. Constructor `MealEntryDao(super.attachedDatabase);`. Methods (all raw `customSelect`/`customStatement`/`customUpdate` with `Variable.withString`/`Variable.withReal`/`Variable.withBool` parameterization — never string interpolation of `foodRef`, `productNameSnapshot`, `brand`, or any other free-text value):

    - `Future<MealEntryRow> insertOrMerge({required MealEntryRow draft})`: `SELECT` for an existing row matching `food_ref = ? AND food_ref_source = ? AND meal_slot = ? AND log_date = ? AND unit = ? AND deleted_at IS NULL`. If found: `UPDATE meal_entry_table SET quantity = quantity + ?, dirty = 1 WHERE id = ?` (parameterized), then re-select and return the updated row. If not found: `INSERT INTO meal_entry_table (...)` with all `draft` fields (via the generated `MealEntryTableCompanion.insert(...)` — prefer the typed Companion `into(mealEntryTable).insert(...)` over raw SQL for the insert path, matching `FoodCatalogRepository`'s `insertOnConflictUpdate` precedent for typed writes; use raw `customSelect`/`customUpdate` only for the merge-check `SELECT` and the delta `UPDATE`).
    - `Future<List<MealEntryRow>> getEntriesForToday(String logDate)`: `SELECT * FROM meal_entry_table WHERE log_date = ? AND deleted_at IS NULL ORDER BY meal_slot, logged_at`, parameterized.
    - `Future<List<MealEntryRow>> getRecent({int limit = 15})`: `SELECT *, MAX(logged_at) FROM meal_entry_table WHERE deleted_at IS NULL GROUP BY food_ref, food_ref_source ORDER BY MAX(logged_at) DESC LIMIT ?`, parameterized limit.
    - `Future<MealEntryRow?> getById(String id)`.
    - `Future<void> updateSlotQuantityUnit({required String id, required String mealSlot, required double quantity, required String unit})` — parameterized `UPDATE`; does not touch snapshot columns (LOG-09 edit).
    - `Future<void> softDelete(String id)` — `UPDATE meal_entry_table SET deleted_at = ? WHERE id = ?`, parameterized with `DateTime.now()` millis.
    - `Future<void> restore(String id)` — `UPDATE meal_entry_table SET deleted_at = NULL WHERE id = ?`.
    - `Future<void> adjustQuantity(String id, double delta)` — `UPDATE meal_entry_table SET quantity = quantity - ? WHERE id = ?` (undo-merge).
    - `Future<MealEntryRow> duplicate(String id, {required String newId})` — reads the row by `id`, inserts a new row with `newId` and all other fields copied (typed Companion insert), bypassing `insertOrMerge` entirely.
    - `Future<void> toggleFavorite(FavoriteRow draft)` — `SELECT` by `(food_ref, food_ref_source)`; if present, `DELETE`; if absent, typed Companion `INSERT`.
    - `Future<bool> isFavorite(String foodRef, String foodRefSource)`.
    - `Future<List<FavoriteRow>> getFavorites()` — `SELECT * FROM favorite_table ORDER BY favorited_at DESC`.
    - `Future<void> touchFavoriteUsage(String foodRef, String foodRefSource, {required double quantity, required String unit})` — parameterized `UPDATE favorite_table SET last_quantity = ?, last_unit = ? WHERE food_ref = ? AND food_ref_source = ?`.

    Every method wraps its query in `try`/`on Exception catch (e) { debugPrint('[MealEntryDao] ... error: $e'); rethrow; }` OR follows `FoodCatalogDao`'s pattern of returning a safe default on failure where the caller can tolerate it — use judgement per method matching the existing DAO's per-method style (queries that MUST succeed for data integrity, like insert/merge, should propagate the exception rather than silently swallow it, unlike the read-path fallback pattern in `FoodCatalogDao.searchLocalFoods`).

    Fill in `test/data/local/meal_entry_dao_test.dart` (replace Wave 0 skip) with real tests against `AppDatabase(NativeDatabase.memory())` (no `offRefPath` needed — these tables are entirely in `co2diet.sqlite`), matching the `<behavior>` block plus the already-stubbed cases (merge, recent dedup/cap, soft-delete, favorite toggle).
  </action>
  <verify>
    <automated>flutter test test/data/local/meal_entry_dao_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`meal_entry_dao_test.dart` passes with 0 skips, exercising merge, unit-mismatch non-merge, recent dedup/cap/reorder, soft-delete exclusion, and favorite toggle round-trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserFoodDao + AppDatabase DAO registration</name>
  <files>
    lib/data/local/daos/user_food_dao.dart,
    test/data/local/user_food_dao_test.dart,
    lib/data/local/app_database.dart
  </files>
  <behavior>
    - Inserting a `UserFoodRow` with an empty `name` or `null` calories is rejected by the DAO (throws or returns an error result — defense in depth beyond the domain-layer `UserFood.isValid` check, per RESEARCH.md Security Domain V5).
    - `findOverrideByFoodRef('123456', 'off_ref')` returns the matching row when a `UserFoodRow` exists with `override_of_ref = '123456' AND override_of_source = 'off_ref'`.
    - Deleting an override row via `revert(id)` is a plain `DELETE` — no cascading writes to any other table.
    - `getAllAlphabetical(filter: 'oat')` returns only rows whose `name` contains `'oat'` (case-insensitive), sorted by `name`.
  </behavior>
  <action>
    Create `lib/data/local/daos/user_food_dao.dart`: `@DriftAccessor(tables: [UserFoodTable]) class UserFoodDao extends DatabaseAccessor<AppDatabase> with _$UserFoodDaoMixin`. Methods:

    - `Future<UserFoodRow> insert(UserFoodTableCompanion draft)`: guard `if (draft.name.value.trim().isEmpty || draft.calories.value == null) throw ArgumentError('UserFood requires a name and a calorie value');` before calling `into(userFoodTable).insertReturning(draft)` (typed Companion insert — required fields are enforced by Dart types plus this explicit guard for the empty-string case Drift's type system can't catch).
    - `Future<UserFoodRow> update(String id, UserFoodTableCompanion changes)` — typed Companion `(update(userFoodTable)..where((t) => t.id.equals(id))).write(changes)`.
    - `Future<UserFoodRow?> getById(String id)`.
    - `Future<UserFoodRow?> findOverrideByFoodRef(String foodRef, String foodRefSource)` — parameterized `SELECT * FROM user_food_table WHERE override_of_ref = ? AND override_of_source = ? AND deleted_at IS NULL LIMIT 1`.
    - `Future<void> revert(String id)` — `DELETE FROM user_food_table WHERE id = ?`, parameterized. (Hard delete, not a tombstone — CONTEXT.md: "deletes the override row; the original reappears in search immediately.") Use a real `DELETE`, not `softDelete`, because the override must vanish from all queries immediately, not linger as a tombstone for sync purposes; document this deliberate deviation from the `SyncSafeTable` soft-delete convention in a doc comment (Phase 7 sync will need to propagate hard deletes for this table specifically — flag as a note for Phase 7, not a blocker now).
    - `Future<List<UserFoodRow>> getAllAlphabetical({String? filter})` — parameterized `SELECT * FROM user_food_table WHERE deleted_at IS NULL AND (? IS NULL OR LOWER(name) LIKE '%' || LOWER(?) || '%') ORDER BY name COLLATE NOCASE`, both `?` bound to `filter` (or pass `null`/`null` when no filter — Drift's `Variable` supports nullable binds).

    Fill in `test/data/local/user_food_dao_test.dart` (replace Wave 0 skip) with real tests matching the `<behavior>` block plus the already-stubbed cases (missing name/calories rejection, override independent-row storage, revert leaves original untouched, alphabetical + filter).

    Edit `lib/data/local/app_database.dart`: add `MealEntryDao` and `UserFoodDao` to the `@DriftDatabase(daos: [...])` list (alongside the existing `UserProfileDao`, `ConsentRecordsDao`, `FoodCatalogDao`). Run `dart run build_runner build --delete-conflicting-outputs` to regenerate `app_database.g.dart` with `db.mealEntryDao` and `db.userFoodDao` accessors.
  </action>
  <verify>
    <automated>flutter test test/data/local/user_food_dao_test.dart test/data/local/schema_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`user_food_dao_test.dart` passes with 0 skips. `AppDatabase` exposes `mealEntryDao` and `userFoodDao`. `flutter test test/data/local/schema_test.dart` still passes (no regression to Phase 1/2 schema assertions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App UI → MealEntryDao/UserFoodDao | Free-text fields (product names, brand, custom-food name/category/serving-size labels) enter raw SQL here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Raw customSelect/customStatement in MealEntryDao/UserFoodDao | mitigate | Every query uses `Variable.withString`/typed Companion parameterization — never string interpolation, matching T-02-03-01/T-03-02-01 precedent |
| T-04-04-02 | Tampering | UserFoodDao.insert bypassing required-field validation | mitigate | DAO-level guard (`ArgumentError` on empty name/null calories) enforces LOG-10 required fields even if a caller skips `UserFood.isValid` |
| T-04-04-03 | Repudiation | `revert()` hard-deletes without an audit trail | accept | Overrides are user-owned, low-value data; CONTEXT.md explicitly requires immediate disappearance from search, which a tombstone would not satisfy; flagged as a Phase 7 sync follow-up |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/data/local/ && flutter analyze lib/data/local/daos/
```
</verification>

<success_criteria>
- `MealEntryDao` and `UserFoodDao` exist, registered in `AppDatabase.daos`
- `meal_entry_dao_test.dart` and `user_food_dao_test.dart` pass with 0 skips
- Merge-check uses `log_date` string equality, never SQL date functions
- All queries parameterized — zero string-interpolated SQL
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-04-SUMMARY.md` when done.
</output>
