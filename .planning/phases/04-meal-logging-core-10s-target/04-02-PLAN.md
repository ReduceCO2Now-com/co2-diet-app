---
phase: 04-meal-logging-core-10s-target
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/data/local/tables/meal_entry_table.dart
  - lib/data/local/tables/favorite_table.dart
  - lib/data/local/tables/user_food_table.dart
  - lib/data/local/migrations/migration_strategy.dart
  - lib/data/local/app_database.dart
  - tool/generate_schema_v1.dart
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08
  - LOG-10
  - LOG-11

must_haves:
  truths:
    - "co2diet.sqlite schemaVersion is 3 with three new tables created on both fresh install and upgrade from schemaVersion 2"
    - "No cross-attached-database foreign keys are attempted (off_ref reference columns are plain text, per RESEARCH.md Pattern 2)"
  artifacts:
    - path: "lib/data/local/tables/meal_entry_table.dart"
      provides: "MealEntryTable — sync-safe, snapshot columns, logDate for same-day merge queries"
    - path: "lib/data/local/tables/favorite_table.dart"
      provides: "FavoriteTable — sync-safe, unique(food_ref, food_ref_source)"
    - path: "lib/data/local/tables/user_food_table.dart"
      provides: "UserFoodTable — sync-safe, custom foods + overrides, JSON quick-serving-sizes"
  key_links:
    - from: "lib/data/local/migrations/migration_strategy.dart"
      to: "lib/data/local/app_database.dart"
      via: "onUpgrade from<3 branch creates the three new tables"
      pattern: "from < 3"
---

<objective>
Add the three new Drift tables Phase 4 needs (`MealEntryTable`, `FavoriteTable`, `UserFoodTable`), bump `AppDatabase.schemaVersion` from 2 to 3, and wire the schemaVersion 2→3 migration branch. Every new table uses the `SyncSafeTable` mixin for Phase 7 sync readiness, matching the pattern already established by `UserFoodCacheTable`.

Purpose: This is the foundational data layer for the entire phase — DAOs, repositories, and notifiers in later plans all depend on these table definitions existing and being correctly migrated. Per RESEARCH.md Pattern 2, SQLite cannot enforce foreign keys across the `ATTACH`ed `off_ref` database, so food references are plain text columns validated only at the application layer — this is also required by the "snapshot, not reference" data model decision (CONTEXT.md).

Output: Three new table files, an updated migration strategy, and `AppDatabase` registered at schemaVersion 3.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@.planning/phases/04-meal-logging-core-10s-target/04-RESEARCH.md
@lib/data/local/mixins/sync_safe_table.dart
@lib/data/local/tables/user_food_cache_table.dart
@lib/data/local/app_database.dart
@lib/data/local/migrations/migration_strategy.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MealEntryTable, FavoriteTable, UserFoodTable</name>
  <files>
    lib/data/local/tables/meal_entry_table.dart,
    lib/data/local/tables/favorite_table.dart,
    lib/data/local/tables/user_food_table.dart
  </files>
  <action>
    Follow `UserFoodCacheTable`'s exact style: `with SyncSafeTable`, `@DataClassName('...Row')`, getter-syntax columns, doc comments explaining each column group.

    `meal_entry_table.dart` — `@DataClassName('MealEntryRow') class MealEntryTable extends Table with SyncSafeTable`:
    - `TextColumn get mealSlot => textEnum<MealSlot>()();` (import the `MealSlot` enum from `package:co2diet/domain/entities/meal_slot.dart` — created in Plan 04-03; this table file compiles once that file exists, which is fine since both plans are Wave 2 and this table plan `depends_on: [04-01]` only for the schema portion — note in a doc comment that `MealSlot`/`PortionUnit` come from the parallel Plan 04-03 domain layer)
    - `TextColumn get foodRef => text()();` — barcode (off_ref/user_food_cache) or `UserFoodTable.id` (user_foods). Never a Drift `.references()` FK (RESEARCH.md Pitfall 1 — cross-attached-DB FK is impossible and would fight the snapshot data model).
    - `TextColumn get foodRefSource => text()();` — one of `'off_ref'`, `'user_food_cache'`, `'user_foods'`.
    - `RealColumn get quantity => real()();`
    - `TextColumn get unit => textEnum<PortionUnit>()();`
    - `TextColumn get productNameSnapshot => text()();`
    - `TextColumn get brandSnapshot => text().nullable()();`
    - `RealColumn get calories100gSnapshot => real().nullable()();`
    - `RealColumn get protein100gSnapshot => real().nullable()();`
    - `RealColumn get carbs100gSnapshot => real().nullable()();`
    - `RealColumn get fat100gSnapshot => real().nullable()();`
    - `RealColumn get co2e100gSnapshot => real().nullable()();`
    - `TextColumn get confidenceBandSnapshot => text().nullable()();`
    - `DateTimeColumn get loggedAt => dateTime()();`
    - `TextColumn get logDate => text()();` — `'YYYY-MM-DD'` local calendar day, computed once in Dart at write time (RESEARCH.md Pattern 3 / Pitfall 2 — never derived via SQL `strftime`). Used for the same-slot/same-day merge check and for grouping the dashboard entries list.

    Add a doc comment on the table class explaining: all `*Snapshot` fields are captured at log time and never re-joined to the source food at read time (CONTEXT.md "snapshot, not reference" — the single biggest data-model decision in this phase); creating/editing a personal override must never retroactively change these values.

    `favorite_table.dart` — `@DataClassName('FavoriteRow') class FavoriteTable extends Table with SyncSafeTable`:
    - `TextColumn get foodRef => text()();`
    - `TextColumn get foodRefSource => text()();`
    - `TextColumn get productNameSnapshot => text()();`
    - `TextColumn get brandSnapshot => text().nullable()();`
    - `RealColumn get calories100gSnapshot => real().nullable()();`
    - `RealColumn get co2e100gSnapshot => real().nullable()();`
    - `TextColumn get confidenceBandSnapshot => text().nullable()();`
    - `RealColumn get lastQuantity => real().nullable()();` — null until the favorite has been logged at least once via the one-tap path; falls back to a generic default (100g) when null.
    - `TextColumn get lastUnit => textEnum<PortionUnit>().nullable()();`
    - `DateTimeColumn get favoritedAt => dateTime()();`
    - Override `@override List<String> get customConstraints => ['UNIQUE(food_ref, food_ref_source)'];` — one favorite row per distinct food; toggling twice on the same food must update/delete the existing row, never insert a duplicate.

    `user_food_table.dart` — `@DataClassName('UserFoodRow') class UserFoodTable extends Table with SyncSafeTable`:
    - `TextColumn get name => text()();` — required (LOG-10).
    - `TextColumn get brand => text().nullable()();`
    - `TextColumn get category => text().nullable()();` — fixed AGRIBALYSE category tag, not freeform (CONTEXT.md decision — guarantees the CO2 category-estimate always resolves).
    - `RealColumn get referenceAmountG => real().withDefault(const Constant(100))();` — the amount the nutrition fields below are measured against.
    - `RealColumn get calories => real()();` — required (LOG-10: "name + at least a calorie value are required to save").
    - `RealColumn get protein => real().nullable()();`
    - `RealColumn get carbs => real().nullable()();`
    - `RealColumn get sugar => real().nullable()();`
    - `RealColumn get fat => real().nullable()();`
    - `RealColumn get fiber => real().nullable()();`
    - `RealColumn get salt => real().nullable()();`
    - `RealColumn get co2e100g => real().nullable()();` — kg CO2e per kg product, same unit convention as `FoodItem.co2e100g` (independent of `referenceAmountG`, which only scopes the nutrition fields).
    - `TextColumn get confidenceBand => text().nullable()();` — `'medium'` when `co2Source == 'category_estimate'`; always `null` when `co2Source == 'manual'` (a self-entered number has no methodology backing it — CONTEXT.md decision, no confidence chip for manual CO2).
    - `TextColumn get co2Source => text().nullable()();` — `'category_estimate'` or `'manual'`.
    - `TextColumn get barcode => text().nullable()();` — optional; pre-filled from the barcode no-match flow so a future scan of that barcode resolves to this custom food.
    - `TextColumn get quickServingSizes => text().map(servingSizeListConverter)();` — JSON column via `TypeConverter.json2`/`JsonTypeConverter2` (RESEARCH.md Pitfall 6 — use `json2`, not the older `.json()` helper, to avoid double-encoding when a future export feature serializes the row). Define `servingSizeListConverter` in this file as a top-level `final` using `TypeConverter.json2` with `fromJson`/`toJson` delegating to `ServingSize.decodeList`/`ServingSize.encodeList` (static helpers Plan 04-03 adds to `lib/domain/entities/serving_size.dart`; import it here).
    - `TextColumn get overrideOfRef => text().nullable()();` — non-null only when this row is an override of an existing catalog/cache food.
    - `TextColumn get overrideOfSource => text().nullable()();` — `'off_ref'` or `'user_food_cache'`, paired with `overrideOfRef`.

    Add a doc comment: a single table serves both brand-new custom foods (`overrideOfRef == null`) and personal overrides (`overrideOfRef != null`) per CONTEXT.md's "single `user_foods` table for both" decision. The original catalog/cache row is never mutated — reverting an override means deleting this row.
  </action>
  <verify>
    <automated>flutter analyze lib/data/local/tables/meal_entry_table.dart lib/data/local/tables/favorite_table.dart lib/data/local/tables/user_food_table.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Three table files exist, each `with SyncSafeTable`, matching the column layout above. `flutter analyze` on these three files reports only expected "undefined class MealSlot/PortionUnit/ServingSize" errors if Plan 04-03 has not landed yet in the same execution session — otherwise zero issues.</done>
</task>

<task type="auto">
  <name>Task 2: Wire schemaVersion 2→3 migration and register tables in AppDatabase</name>
  <files>
    lib/data/local/migrations/migration_strategy.dart,
    lib/data/local/app_database.dart,
    tool/generate_schema_v1.dart
  </files>
  <action>
    Edit `lib/data/local/app_database.dart`:
    - Add `MealEntryTable`, `FavoriteTable`, `UserFoodTable` to the `@DriftDatabase(tables: [...])` list (do NOT add DAOs yet — those are created in Plan 04-04; the `daos:` list stays as-is for now, updated later).
    - Bump `@override int get schemaVersion => 3;` (from 2).
    - Update the class doc comment's `schemaVersion` note to describe the Phase 4 addition, following the existing style ("schemaVersion: 3 (Phase 4 adds MealEntryTable, FavoriteTable, UserFoodTable).").
    - Run `dart run build_runner build --delete-conflicting-outputs` (per this project's pinned `build_runner: 2.15.1`) to regenerate `app_database.g.dart` with the new table getters (`db.mealEntryTable`, `db.favoriteTable`, `db.userFoodTable`) that the migration strategy below needs.

    Edit `lib/data/local/migrations/migration_strategy.dart`:
    - Inside `onUpgrade`, after the existing `if (from < 2) { ... }` branch, add:
      ```
      if (from < 3) {
        await m.createTable(db.mealEntryTable);
        await m.createTable(db.favoriteTable);
        await m.createTable(db.userFoodTable);
      }
      ```
    - `onCreate: (m) async => m.createAll();` already covers fresh installs — no change needed there.
    - Update the file's top doc comment to mention the new schemaVersion 2→3 branch, matching the existing style for the 1→2 branch.

    Edit `tool/generate_schema_v1.dart`:
    - Add a doc comment note (functional behavior unchanged — RESEARCH.md Pitfall 5, option (b)): this script is documentation-only, hardcoded to schema_version 1, and is explicitly OUT OF SCOPE for tracking the live schemaVersion (now 3 as of Phase 4). No test consumes `schema_v1.json`. State this explicitly so a future phase does not assume the file reflects the current schema.
  </action>
  <verify>
    <automated>flutter test test/data/local/schema_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>`AppDatabase.schemaVersion` is 3. `flutter test test/data/local/schema_test.dart` passes (existing Phase 1/2 schema assertions still hold; the test file is not expected to assert on the new tables yet — that lands with the DAO plan). A fresh in-memory `AppDatabase` opens without error and `sqlite_master` contains `meal_entry_table`, `favorite_table`, and `user_food_table`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ local SQLite (co2diet.sqlite) | Trusted — single-user local device, no network input at this layer |
| App ↔ off_ref (ATTACHed, read-only) | Read-only reference data; no writes ever issued against `off_ref.*` from these new tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `foodRef`/`overrideOfRef` text columns | mitigate | Never interpolated into raw SQL directly in this plan (no queries yet); DAOs in Plan 04-04 must use `Variable.withString` parameterization per T-02-03-01/T-03-02-01 precedent |
| T-04-02-02 | Integrity | Cross-attached-DB FK misuse | mitigate | No `.references()` used against `off_ref` tables; RESEARCH.md Pitfall 1 documented in code comments to prevent future regressions |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/ && flutter analyze lib/data/local/
```
Expected: full suite still green (new tables exist but nothing queries them yet); zero analyzer issues once Plan 04-03's `MealSlot`/`PortionUnit`/`ServingSize` land.
</verification>

<success_criteria>
- `AppDatabase.schemaVersion == 3`
- Three new tables exist with `SyncSafeTable` columns + the fields specified above
- `onUpgrade` creates all three tables when migrating from schemaVersion 2
- `onCreate` (fresh install) creates all three tables via `createAll()`
- No `.references()` FK used against any `off_ref.*` table
- `tool/generate_schema_v1.dart` carries an explicit out-of-scope note
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-02-SUMMARY.md` when done.
</output>
