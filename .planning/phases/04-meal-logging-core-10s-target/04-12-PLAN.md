---
phase: 04-meal-logging-core-10s-target
plan: 12
type: execute
wave: 8
depends_on:
  - 04-10
  - 04-11
files_modified:
  - integration_test/meal_logging_benchmark_test.dart
  - test/features/meal_logging/offline_logging_test.dart
autonomous: true
requirements:
  - LOG-12
  - LOG-13

must_haves:
  truths:
    - "The benchmark test uses Stopwatch + bounded pump(), never pumpAndSettle(), to avoid the Undo-snackbar auto-dismiss timer hang (RESEARCH.md Pitfall 3)"
    - "The benchmark self-skips cleanly when required fixtures (off_reference.sqlite) are absent, matching the Phase 2/3 precedent"
    - "The offline test asserts zero OffApiClient/Connectivity calls occur anywhere in the log/edit/delete/duplicate code path"
  artifacts:
    - path: "integration_test/meal_logging_benchmark_test.dart"
      provides: "LOG-13 Dart-level tap-to-saved timing proxy (real-device verification happens in Plan 04-13)"
    - path: "test/features/meal_logging/offline_logging_test.dart"
      provides: "LOG-12 automated zero-network-dependency assertion"
---

<objective>
Fill in the two remaining Wave 0 stubs that could not be completed until the full logging flow existed end-to-end: the LOG-13 tap-to-saved `<10s` integration benchmark and the LOG-12 offline-path assertion.

Purpose: Per RESEARCH.md, this integration test is a Dart-level proxy for the literal LOG-13 requirement, not a substitute for it — Plan 04-13's real-device human-verify checkpoint is what actually closes LOG-13. This plan produces the regression-guard automation that runs on every future CI pass.

Output: Two test files with real (unskipped) implementations.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@.planning/phases/04-meal-logging-core-10s-target/04-RESEARCH.md
@integration_test/food_search_benchmark_test.dart
@integration_test/co2_coverage_benchmark_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOG-13 tap-to-saved benchmark</name>
  <files>
    integration_test/meal_logging_benchmark_test.dart
  </files>
  <action>
    Rewrite `integration_test/meal_logging_benchmark_test.dart` (replacing the Wave 0 `markTestSkipped` stub) following `food_search_benchmark_test.dart`'s established structure: `setUpAll` that checks for required fixtures (`off_reference.sqlite` decompressed, matching whatever setup `food_search_benchmark_test.dart`/`co2_coverage_benchmark_test.dart` already do — read both files to copy the exact fixture-detection `try`/`on Object catch` self-skip pattern) and self-skips via `markTestSkipped(...)` when absent, exactly like the two existing benchmark files.

    Test body (per RESEARCH.md Pitfall 3 — never use unbounded `pumpAndSettle()` here, the Undo snackbar's auto-dismiss timer and the swipe-reveal animations will defeat it):
    1. Pump the app (`app.main()` equivalent used by the existing benchmark tests, or `pumpWidget` with a `ProviderScope` wrapping the router — match whichever bootstrap pattern `food_search_benchmark_test.dart` uses).
    2. Navigate to `/food-search`, enter a known query (e.g. a food guaranteed present in the bundled seed DB — reuse a query already used in `food_search_benchmark_test.dart` for consistency), wait for results via bounded `tester.pump(const Duration(milliseconds: 100))` calls in a loop (not `pumpAndSettle`) until the result list appears or a max iteration count is hit.
    3. Start `final stopwatch = Stopwatch()..start();` immediately before tapping the first result row.
    4. Tap the row → sheet opens → tap a quantity chip (or accept the default) → tap "Log this food" — each step followed by a bounded `pump()` loop (not `pumpAndSettle()`), watching for the specific next expected widget (sheet content, then its disappearance, then the "Added to Breakfast"-style snackbar `Text` finder).
    5. Stop the stopwatch the moment the snackbar text is found: `stopwatch.stop();`.
    6. `expect(stopwatch.elapsedMilliseconds, lessThan(10000));` — LOG-13's literal threshold, applied to the automated Dart-level proxy (documented as a proxy, not the full manual-testing requirement, in the test's doc comment).
    7. Optionally also verify the entry is visible on `/dashboard` by navigating there and finding the logged item's row — completing the literal "food saved and visible on dashboard" chain from LOG-13's wording.

    Add a doc comment at the top of the file explicitly stating: this benchmark is a regression guard, not a substitute for the mandatory real-device user-testing checkpoint (Plan 04-13) — matching RESEARCH.md's Environment Availability table finding.
  </action>
  <verify>
    <automated>flutter analyze integration_test/meal_logging_benchmark_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>File compiles and self-skips cleanly when fixtures are absent (verify via `flutter analyze`; running the test itself requires a device/emulator and is not required to pass this plan's gate — device execution happens in Plan 04-13).</done>
</task>

<task type="auto">
  <name>Task 2: LOG-12 offline logging assertion</name>
  <files>
    test/features/meal_logging/offline_logging_test.dart
  </files>
  <action>
    Rewrite `test/features/meal_logging/offline_logging_test.dart` (replacing the Wave 0 skip). Using `mocktail`, construct a `MockOffApiClient` and a mocked `Connectivity`-channel setup (matching `test/features/food_search/food_search_notifier_test.dart`'s `TestDefaultBinaryMessengerBinding.setMockMethodCallHandler` pattern for `connectivity_plus`, per STATE.md's documented Phase 02-07 decision) that both throw/fail loudly (`fail('unexpected network call')`) if invoked.

    Test cases:
    - `logFood` (via `MealEntryNotifier` backed by a real in-memory `AppDatabase`-based `MealEntryRepository`, not mocked at the repository level — the point is to prove the *real* implementation never reaches for the network, not just that a mock wasn't called) completes successfully without touching the mocked `OffApiClient`/`Connectivity` handler.
    - `editEntry`, `deleteEntry`, `duplicateEntry` — same assertion.
    - `UserFoodNotifier.saveCustomFood`/`saveOverride`/`revertOverride` — same assertion (LOG-10/LOG-11 flows are also core logging and must be offline-safe).

    Implementation approach: since `MealEntryRepository`/`UserFoodRepository`/their DAOs never import `OffApiClient` or call `Connectivity()` at all (verify this is true by inspection — Plans 04-04/04-05 as specified do not reference either), the strongest test here is a static/structural one plus a runtime one: (a) a runtime test that runs the full log/edit/delete/duplicate/save/override/revert sequence against an in-memory DB with the mocked network channel installed and set to throw on any invocation, asserting the sequence completes without error; (b) optionally, a simple `grep`-style compile-time signal is out of scope for a Dart test file — rely on (a) as the automated proof.
  </action>
  <verify>
    <automated>flutter test test/features/meal_logging/offline_logging_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`offline_logging_test.dart` passes with 0 skips, running the full core-logging + custom-food/override sequence against an in-memory database with network channels mocked to fail loudly on any invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → mocked platform channels | No production trust boundary — test-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/features/meal_logging/offline_logging_test.dart && flutter analyze integration_test/meal_logging_benchmark_test.dart
```
</verification>

<success_criteria>
- `meal_logging_benchmark_test.dart` uses `Stopwatch` + bounded `pump()`, never `pumpAndSettle()`
- `meal_logging_benchmark_test.dart` self-skips cleanly without a device/fixtures
- `offline_logging_test.dart` passes with 0 skips, proving the core logging path never touches the network
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-12-SUMMARY.md` when done.
</output>
