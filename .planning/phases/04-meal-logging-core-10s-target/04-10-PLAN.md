---
phase: 04-meal-logging-core-10s-target
plan: 10
type: execute
wave: 7
depends_on:
  - 04-08
  - 04-09
files_modified:
  - lib/features/food_search/widgets/search_prompt_widget.dart
  - lib/features/food_search/widgets/recent_favorites_list.dart
  - lib/features/food_search/widgets/no_results_widget.dart
  - lib/features/food_search/screens/food_search_screen.dart
autonomous: true
requirements:
  - LOG-07
  - LOG-08
  - LOG-10

must_haves:
  truths:
    - "The food search screen's empty state shows Recent and Favorites instead of a plain search prompt"
    - "Typing a query replaces Recent/Favorites with search results, unchanged from Phase 2 behavior"
    - "One tap on a Recent/Favorite row logs it instantly using the last-used quantity and auto-detected slot — no sheet"
    - "The genuine no-results state offers an Add as custom food link; offline and network-error states do not"
  artifacts:
    - path: "lib/features/food_search/widgets/recent_favorites_list.dart"
      provides: "Recent + Favorites sections with one-tap log and edit-icon-opens-sheet (LOG-07/LOG-08)"
  key_links:
    - from: "lib/features/food_search/widgets/recent_favorites_list.dart"
      to: "lib/features/food_search/providers/favorite_notifier.dart"
      via: "ref.watch(favoriteNotifierProvider) / ref.watch(mealEntryNotifierProvider.notifier).getRecent()"
      pattern: "favoriteNotifierProvider"
    - from: "lib/features/food_search/widgets/no_results_widget.dart"
      to: "lib/features/my_foods/screens/custom_food_form_screen.dart"
      via: "genuine variant navigates to /custom-food-stub?name=<query>"
      pattern: "?name="
---

<objective>
Replace the food search screen's plain `SearchPromptWidget` empty state with Recent + Favorites sections (LOG-07, LOG-08), and close the search no-results "Add as custom food" gap Phase 2's CONTEXT.md explicitly deferred to Phase 4 (LOG-10).

Purpose: This is where a returning user actually experiences the <10s promise most often — re-logging something they've eaten before should be a single tap, not a full search-and-sheet round trip.

Output: A new `RecentFavoritesList` widget replacing the empty-state prompt, and a genuine-no-results-only "Add as custom food" link.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@lib/features/food_search/screens/food_search_screen.dart
@lib/features/food_search/widgets/no_results_widget.dart
@lib/features/food_search/widgets/food_result_row.dart
@lib/features/food_search/providers/food_search_state.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecentFavoritesList widget + search screen wiring</name>
  <files>
    lib/features/food_search/widgets/recent_favorites_list.dart,
    lib/features/food_search/screens/food_search_screen.dart,
    lib/features/food_search/widgets/search_prompt_widget.dart
  </files>
  <behavior>
    - When both Recent and Favorites are empty, the original "Search for a food..." prompt still renders (no empty-section clutter).
    - Tapping a Recent/Favorite row body (not its edit icon) logs it immediately: no sheet opens, a snackbar with Undo appears, matching `PortionSlotForm`'s success feedback.
    - Tapping the small edit icon on a row opens the full `FoodDetailBottomSheet`/`PortionSlotForm` pre-filled with that row's last-used slot/quantity/unit instead of logging immediately.
  </behavior>
  <action>
    Create `lib/features/food_search/widgets/recent_favorites_list.dart`: `ConsumerWidget` (or `ConsumerStatefulWidget` if a `FutureBuilder`-style one-shot `getRecent()` call needs local caching — prefer converting `MealEntryNotifier.getRecent()` usage through a small dedicated `FutureProvider` if that's cleaner than ad-hoc `FutureBuilder` state; keep whichever approach avoids re-fetching Recent on every rebuild). Renders two sections:
    - "Recent" — `ref.watch` the recent-entries source (via a lightweight wrapper provider or direct `FutureBuilder<List<MealEntry>>(future: ref.read(mealEntryNotifierProvider.notifier).getRecent(), ...)`), each row: leading food name + calories(scaled)/CO2 summary text, trailing edit `IconButton(Icons.edit_outlined, size: 18)`, `onTap` (row body) calls a one-tap-log helper (see below).
    - "Favorites" — `ref.watch(favoriteNotifierProvider)`, each row: leading filled `Icons.star` + food name, trailing edit `IconButton`, `onTap` calls `ref.read(favoriteNotifierProvider.notifier).logFromFavorite(favorite, autoDetectedSlot: detectMealSlotForTime(DateTime.now()))`, importing `detectMealSlotForTime` directly from `lib/domain/entities/meal_slot.dart` (Plan 04-03 already defines this shared top-level function specifically so this plan and Plan 04-09 both consume one implementation without either modifying the other's files — no extraction or cross-plan refactor needed here).

    For the Recent one-tap-log path (recent items don't have a dedicated "log from recent" repository method — CONTEXT.md describes the same one-tap behavior for both Recent and Favorites): construct a `MealEntry` draft from the tapped `MealEntry`'s own snapshot fields (reusing its `foodRef`/`foodRefSource`/snapshot columns, `quantity`/`unit` from that same row as the "previously used quantity"), new `mealSlot: detectMealSlotForTime(DateTime.now())` (same shared function as above), `loggedAt`/`logDate` now, and call `ref.read(mealEntryNotifierProvider.notifier).logFood(draft)` directly from this widget (or add a thin `logFromRecent(MealEntry recent)` convenience method to `MealEntryNotifier` in a small edit to `lib/features/meal_logging/providers/meal_entry_notifier.dart` if that keeps the widget simpler — prefer the notifier method for consistency with `FavoriteNotifier.logFromFavorite`'s shape).

    Both one-tap paths show the same "Added to [Slot]" + Undo snackbar pattern established in Plan 04-09.

    Both edit-icon paths open the shared sheet pre-filled: convert the tapped `MealEntry`/`Favorite` into a `FoodItem` via the `toFoodItem()` extension methods already defined in `lib/domain/entities/meal_entry_food_item_mapping.dart` (Plan 04-06 — import that file; do not redefine the conversion here), then call `showFoodDetailSheet(context, entryOrFavorite.toFoodItem(), initialSlot: entry.mealSlot /* or the auto-detected slot for a Favorite with no prior log */, initialQuantity: entry.quantity /* or favorite.lastQuantity ?? 100 */, initialUnit: entry.unit /* or favorite.lastUnit ?? PortionUnit.g */)`. `showFoodDetailSheet` and `PortionSlotForm` already accept these three optional params as of Plan 04-09 — this plan only calls them with values, it does not modify `lib/features/food_search/widgets/portion_slot_form.dart` or `food_detail_sheet.dart` at all.

    Edit `lib/features/food_search/screens/food_search_screen.dart`: in `_buildBody`, change the `FoodSearchPrompt()` case from `const SearchPromptWidget()` to `const RecentFavoritesList()`. `SearchPromptWidget` itself is kept unchanged (still used inside `RecentFavoritesList` for the both-empty case per `<behavior>`).
  </action>
  <verify>
    <automated>flutter test test/features/food_search/favorites_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Opening `/food-search` with an empty query shows Recent + Favorites sections (or the original prompt when both are empty). Tapping a row body logs instantly with Undo; tapping the edit icon opens the pre-filled sheet.</done>
</task>

<task type="auto">
  <name>Task 2: "Add as custom food" link on genuine no-results</name>
  <files>
    lib/features/food_search/widgets/no_results_widget.dart
  </files>
  <action>
    Edit `lib/features/food_search/widgets/no_results_widget.dart`: add a new optional constructor param `final String? query` is already present — reuse it. Inside `build()`, when `variant == NoResultsVariant.genuine`, render an additional `FilledButton.tonal(onPressed: () => context.push('/custom-food-stub?name=${Uri.encodeComponent(query)}'), child: const Text('Add as custom food'))` below the existing message text (requires `import 'package:go_router/go_router.dart';`). Do NOT add this button for `NoResultsVariant.offline` or `NoResultsVariant.networkError` — CONTEXT.md is explicit: "appears only on the genuine no-match state — not on the offline-with-0-results or network-failure states, since those haven't actually confirmed there's no match."
  </action>
  <verify>
    <automated>flutter analyze lib/features/food_search/widgets/no_results_widget.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Genuine no-results state shows "Add as custom food" navigating to `/custom-food-stub?name=<query>`. Offline and network-error variants are unchanged (no new button).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search query text → route query param | The raw search query is URL-encoded before being placed in the route path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Tampering | Search query placed into `/custom-food-stub?name=...` | mitigate | `Uri.encodeComponent(query)` before insertion into the route path — matches the existing `BarcodeScanNoMatchScreen` barcode-encoding precedent |
| T-04-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
flutter test test/features/food_search/ && flutter analyze lib/features/food_search/
```
</verification>

<success_criteria>
- Empty-query state shows Recent + Favorites (or the original prompt when both empty)
- One-tap log from Recent/Favorites works with Undo feedback
- Edit icon opens the pre-filled sheet instead of instant-logging
- "Add as custom food" appears only on the genuine no-results variant
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-10-SUMMARY.md` when done.
</output>
