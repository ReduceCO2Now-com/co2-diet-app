---
phase: 04-meal-logging-core-10s-target
plan: 11
type: execute
wave: 7
depends_on:
  - 04-09
files_modified:
  - pubspec.yaml
  - lib/core/router/app_router.dart
  - lib/features/dashboard/screens/placeholder_dashboard_screen.dart
  - lib/features/dashboard/widgets/meal_entry_row.dart
  - test/features/dashboard/meal_entry_row_test.dart
autonomous: false
requirements:
  - LOG-05
  - LOG-09

must_haves:
  truths:
    - "The dashboard shows today's meal entries grouped under Breakfast/Lunch/Dinner/Snack; a slot with zero entries hides its header entirely"
    - "Swiping a row reveals Edit, Duplicate, Delete actions"
    - "Delete has no confirmation dialog — immediate delete with a Deleted — Undo snackbar"
    - "flutter_slidable is installed only after the package-legitimacy human-verify checkpoint is approved"
  artifacts:
    - path: "lib/features/dashboard/widgets/meal_entry_row.dart"
      provides: "Slidable-wrapped row: name + quantity + calories + CO2, Edit/Duplicate/Delete actions (LOG-09)"
    - path: "lib/features/dashboard/screens/placeholder_dashboard_screen.dart"
      provides: "Real grouped-entries list replacing the Phase-1-through-3 'Dashboard coming in Phase 5' placeholder text"
  key_links:
    - from: "lib/features/dashboard/widgets/meal_entry_row.dart"
      to: "lib/features/meal_logging/providers/meal_entry_notifier.dart"
      via: "onDelete/onDuplicate/onEdit call MealEntryNotifier methods"
      pattern: "mealEntryNotifierProvider"
---

<objective>
Give `PlaceholderDashboardScreen` a real body: today's logged meal entries grouped by slot, each row swipeable to reveal Edit/Delete/Duplicate (LOG-09), satisfying LOG-13's "visible on dashboard" requirement without building Phase 5's full dashboard. Phase 5 extends this same list rather than starting from scratch (CONTEXT.md).

Purpose: `flutter_slidable` is a new external package (RESEARCH.md Package Legitimacy Audit: `[ASSUMED]` — pub.dev is not a slopcheck-supported ecosystem, so independent verification requires a human checkpoint per the Package Legitimacy Protocol). This plan isolates that checkpoint immediately before the one task that needs the package, keeping every other Phase 4 plan fully autonomous.

Output: `flutter_slidable` installed (post-approval), `MealEntryRow` widget, dashboard screen extracted from `app_router.dart` into its own file with a real grouped list.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-logging-core-10s-target/04-CONTEXT.md
@.planning/phases/04-meal-logging-core-10s-target/04-RESEARCH.md
@lib/core/router/app_router.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Nothing yet — this checkpoint gates the `flutter_slidable` package install per the Package Legitimacy Protocol. RESEARCH.md's Package Legitimacy Audit found strong independent signals (pub.dev score 150/160, `is:flutter-favorite`, 626,026 downloads/30 days, 6,130 likes, MIT license, verified publisher `romainrastel.com`, source at `github.com/letsar/flutter_slidable`) but could not run `slopcheck` because pub.dev/Dart is not one of its supported ecosystems (`{pypi, npm, crates.io, go, rubygems, maven, packagist}`). Per the protocol, this makes `flutter_slidable` `[ASSUMED]`, not verified — it requires a blocking human-verify checkpoint before install, regardless of how strong the independent signals are.
  </what-built>
  <how-to-verify>
    1. Visit https://pub.dev/packages/flutter_slidable and confirm:
       - Latest version is `4.0.3` or newer, published by `publisher:romainrastel.com`
       - Pub score is high (140+/160) and the `flutter-favorite` badge is present
       - The "Repository" link points to `github.com/letsar/flutter_slidable` and the repo has recent commits/releases, not an abandoned/typosquat-looking project
    2. Optionally cross-check on https://github.com/letsar/flutter_slidable — confirm star count, no recent "this package is deprecated/compromised" notices in the README or Issues.
    3. Confirm there is no similarly-named package with a suspiciously higher download count that this plan should have used instead (e.g. search "slidable" on pub.dev and sanity-check `flutter_slidable` is the dominant, correct choice).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `flutter pub add flutter_slidable`, or describe what looks wrong (e.g. "publisher mismatch" or "repo looks abandoned") to halt the install.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install flutter_slidable, build MealEntryRow + real dashboard screen</name>
  <files>
    pubspec.yaml,
    lib/core/router/app_router.dart,
    lib/features/dashboard/screens/placeholder_dashboard_screen.dart,
    lib/features/dashboard/widgets/meal_entry_row.dart,
    test/features/dashboard/meal_entry_row_test.dart
  </files>
  <behavior>
    - `MealEntryRow` renders name, quantity+unit, scaled calories, and scaled CO2 (not just name+quantity) — reusing the same `scaledMacros` pure function from `MealEntry` that `PortionSlotForm` (Plan 04-09) already uses.
    - Swiping the row (end action pane) reveals three actions in order: Edit, Duplicate, Delete (Delete rendered in the error color, per RESEARCH.md Pattern 4).
    - Tapping Delete removes the row immediately (no confirmation dialog) and shows a "Deleted — Undo" snackbar.
    - A meal slot section with zero entries renders no header and no empty placeholder — it simply does not appear in the list.
  </behavior>
  <action>
    Run `flutter pub add flutter_slidable` (only after the checkpoint above is approved) — this updates `pubspec.yaml`.

    Create `lib/features/dashboard/widgets/meal_entry_row.dart`: `ConsumerWidget` wrapping a `Slidable(key: ValueKey(entry.id), endActionPane: ActionPane(motion: const ScrollMotion(), children: [SlidableAction(onPressed: (_) => onEdit(entry), icon: Icons.edit_outlined, label: 'Edit'), SlidableAction(onPressed: (_) => onDuplicate(entry), icon: Icons.copy_outlined, label: 'Duplicate'), SlidableAction(onPressed: (_) => onDelete(entry), backgroundColor: Theme.of(context).colorScheme.errorContainer, icon: Icons.delete_outline, label: 'Delete')]), child: ListTile(title: Text(entry.productNameSnapshot), subtitle: Text('${entry.quantity.toStringAsFixed(0)}${entry.unit.displayLabel} • ${scaled.calories?.round() ?? '—'} kcal${scaled.co2e != null ? ' • ${formatCo2Display(scaled.co2e)}' : ''}')))` — reuse `formatCo2Display` from `lib/features/barcode_scan/utils/co2_formatter.dart` for CO2 text consistency with the rest of the app. Take `onEdit`/`onDuplicate`/`onDelete` as constructor callbacks (caller — the dashboard screen — wires them to `MealEntryNotifier` methods) rather than calling the notifier directly from this widget, keeping it a presentation-only component per RESEARCH.md's Architectural Responsibility Map ("Swipe-to-reveal actions — Client (Flutter UI widget) — Pure presentation/gesture concern").

    Create `lib/features/dashboard/screens/placeholder_dashboard_screen.dart`: move `PlaceholderDashboardScreen`'s class definition out of `app_router.dart` into this new file (keep the exact class name `PlaceholderDashboardScreen` — CONTEXT.md: "Phase 5 extends/replaces this same list rather than starting from scratch," and the class name signals its Phase-5-provisional nature intentionally). New body: `ConsumerWidget` watching `mealEntryNotifierProvider`; on data, group entries by `mealSlot` (iterate `MealSlot.values` in `breakfast, lunch, dinner, snack` order, filter entries per slot, skip rendering a `ListTile`/header + `MealEntryRow` list for any slot with zero entries — per `<behavior>`), render each non-empty group as a `Text` slot header (`Theme.of(context).textTheme.titleMedium`) followed by its `MealEntryRow`s. When the entire day has zero entries, show a centered empty state: icon + "No meals logged yet" (CONTEXT.md — pre-empts Phase 5's DASH-07 wording, honest-empty-state philosophy matching `NoResultsWidget`). Wire `onEdit` to open the pre-filled sheet (same `PortionSlotForm`-via-`FoodItem`-conversion approach as Plan 04-10's Recent/Favorites edit icon — reuse the `toFoodItem()` extension added there), `onDuplicate` to `ref.read(mealEntryNotifierProvider.notifier).duplicateEntry(entry.id)`, `onDelete` to `ref.read(mealEntryNotifierProvider.notifier).deleteEntry(entry.id)` followed immediately by a "Deleted — Undo" `SnackBar` whose Undo action calls `.undoDelete()`.

    Edit `lib/core/router/app_router.dart`: remove the inline `PlaceholderDashboardScreen` class definition, add `import 'package:co2diet/features/dashboard/screens/placeholder_dashboard_screen.dart';`, keep the `GoRoute(path: '/dashboard', builder: (context, state) => const PlaceholderDashboardScreen())` reference unchanged (same class name, new import location).

    Fill in `test/features/dashboard/meal_entry_row_test.dart` (replace Wave 0 skip) with widget tests matching the `<behavior>` block plus the already-stubbed cases (swipe reveals actions, row content, empty-slot-header-hidden — the last case belongs more naturally in a dashboard screen test, but since only `meal_entry_row_test.dart` was stubbed in Wave 0, cover the row-level assertions here and note in the SUMMARY if a screen-level test would strengthen coverage for a future gap-closure pass).
  </action>
  <verify>
    <automated>flutter test test/features/dashboard/meal_entry_row_test.dart 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`flutter_slidable` is a `pubspec.yaml` dependency. `meal_entry_row_test.dart` passes with 0 skips. `/dashboard` shows grouped entries with swipe actions, or the "No meals logged yet" empty state. Deleting shows an Undo snackbar; duplicate creates a new row; edit opens the pre-filled sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev package registry → app | New third-party dependency (`flutter_slidable`) — supply-chain trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-SC | Tampering | `flutter_slidable` pub.dev install | mitigate | Blocking human-verify checkpoint before install (pub.dev not slopcheck-covered — package tagged `[ASSUMED]` per RESEARCH.md Package Legitimacy Audit); CI privacy-blocklist (Phase 1) still runs against this dependency on every subsequent build |
| T-04-11-01 | Tampering | Delete action has no confirmation | accept | Undo snackbar provides an equivalent safety net without an extra tap, per CONTEXT.md's explicit no-confirmation-dialog decision; data is local-only and low-stakes |
</threat_model>

<verification>
```bash
flutter test test/features/dashboard/ && flutter analyze lib/features/dashboard/ lib/core/router/app_router.dart
```
</verification>

<success_criteria>
- Human-verify checkpoint approved before `flutter_slidable` is added to `pubspec.yaml`
- `PlaceholderDashboardScreen` extracted to its own file, real grouped-entries body
- Swipe reveals Edit/Duplicate/Delete; Delete is immediate + Undo; empty slots hide their headers
- `meal_entry_row_test.dart` passes with 0 skips
</success_criteria>

<output>
Create `.planning/phases/04-meal-logging-core-10s-target/04-11-SUMMARY.md` when done.
</output>
