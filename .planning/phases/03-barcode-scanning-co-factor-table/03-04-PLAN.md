---
phase: 03-barcode-scanning-co-factor-table
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - lib/core/theme/color_tokens.dart
  - lib/features/barcode_scan/widgets/confidence_chip.dart
  - lib/features/food_search/widgets/food_detail_sheet.dart
  - lib/features/barcode_scan/screens/methodology_screen.dart
  - docs/CO2_METHODOLOGY.md
  - integration_test/co2_coverage_benchmark_test.dart
autonomous: true
requirements:
  - CO2-01
  - LEG-05

must_haves:
  truths:
    - "FoodDetailBottomSheet shows a CO₂ row when food_item.co2e100g is non-null"
    - "The CO₂ value is formatted to 1-2 significant figures with a ~ prefix (e.g. '~2.3 kg CO₂e/kg')"
    - "High confidence → green ConfidenceChip; Medium confidence → amber ConfidenceChip"
    - "ConfidenceChip is tappable and opens an explanation bottom sheet"
    - "Explanation sheet contains a 'Full methodology' link button"
    - "Tapping 'Full methodology' either navigates to MethodologyScreen or launches GitHub URL via url_launcher"
    - "MethodologyScreen displays: AGRIBALYSE v3.1.1 attribution, ADEME credit, Etalab Open Licence v2.0, confidence band definitions, and a GitHub link button"
    - "docs/CO2_METHODOLOGY.md committed with attribution and methodology explanation"
    - "CO₂ row is hidden (not shown as '—') when co2e100g is null"
    - "warningAmber token added to color_tokens.dart for Medium chip background"
    - "NFR-06(b) benchmark integration test replaces stub with real query against off_reference.sqlite"
  artifacts:
    - path: "lib/core/theme/color_tokens.dart"
      provides: "warningAmber = Color(0xFFF59E0B) token"
      contains: "warningAmber"
    - path: "lib/features/barcode_scan/widgets/confidence_chip.dart"
      provides: "ConfidenceChip widget for High/Medium bands"
      exports: ["ConfidenceChip"]
    - path: "lib/features/food_search/widgets/food_detail_sheet.dart"
      provides: "CO₂ row enabled with ConfidenceChip"
      contains: "co2e100g"
    - path: "lib/features/barcode_scan/screens/methodology_screen.dart"
      provides: "Offline LEG-05 methodology documentation screen"
    - path: "docs/CO2_METHODOLOGY.md"
      provides: "Public methodology documentation for CO₂ estimates"
    - path: "integration_test/co2_coverage_benchmark_test.dart"
      provides: "Real NFR-06(b) benchmark — ≥90% products have CO₂e estimate"
  key_links:
    - from: "lib/features/food_search/widgets/food_detail_sheet.dart"
      to: "lib/features/barcode_scan/widgets/confidence_chip.dart"
      via: "widget import when co2e100g is non-null"
      pattern: "ConfidenceChip"
    - from: "lib/features/barcode_scan/widgets/confidence_chip.dart"
      to: "lib/features/barcode_scan/screens/methodology_screen.dart"
      via: "tappable explanation sheet 'Full methodology' button navigation"
      pattern: "MethodologyScreen"
---

<objective>
Wire the CO₂ display layer: add `warningAmber` token, build `ConfidenceChip`, enable the CO₂ row in `FoodDetailBottomSheet`, build `MethodologyScreen` (LEG-05 offline surface), commit `docs/CO2_METHODOLOGY.md`, and activate the NFR-06(b) coverage benchmark integration test.

Purpose: Every food in the app — whether found by search or barcode scan — now shows a CO₂e value and confidence band when data is available, satisfying CO2-01 and LEG-05.

Output: CO₂ display live in FoodDetailBottomSheet; MethodologyScreen accessible; docs committed; NFR-06(b) benchmark activated.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-barcode-scanning-co-factor-table/03-CONTEXT.md
@.planning/phases/03-barcode-scanning-co-factor-table/03-RESEARCH.md

@lib/core/theme/color_tokens.dart
@lib/features/food_search/widgets/food_detail_sheet.dart
@lib/domain/entities/food_item.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: warningAmber token, ConfidenceChip widget, CO₂ row in FoodDetailBottomSheet, MethodologyScreen</name>
  <files>
    lib/core/theme/color_tokens.dart,
    lib/features/barcode_scan/widgets/confidence_chip.dart,
    lib/features/food_search/widgets/food_detail_sheet.dart,
    lib/features/barcode_scan/screens/methodology_screen.dart,
    test/features/barcode_scan/confidence_chip_test.dart,
    test/features/barcode_scan/co2_formatting_test.dart,
    test/features/barcode_scan/methodology_screen_test.dart
  </files>
  <behavior>
    confidence_chip_test.dart (remove skip, implement real tests):
    - ConfidenceChip(band: 'high') renders a chip with green background (AppColors.primaryContainer or similar green token)
    - ConfidenceChip(band: 'medium') renders a chip with amber background (AppColors.warningAmber)
    - Tapping ConfidenceChip triggers the explanation bottom sheet (verify onTap callback invoked or sheet shown)

    co2_formatting_test.dart (remove skip, implement real tests):
    - formatCo2Display(4.732) == '~4.7 kg CO₂e/kg'
    - formatCo2Display(12.04) == '~12 kg CO₂e/kg'
    - formatCo2Display(0.45) == '~0.45 kg CO₂e/kg'
    - formatCo2Display(null) == null

    methodology_screen_test.dart (remove skip, implement real widget tests):
    - MethodologyScreen contains text 'AGRIBALYSE v3.1.1'
    - MethodologyScreen contains text 'Source ADEME'
    - MethodologyScreen contains text 'Licence Ouverte / Open Licence v2.0'
    - MethodologyScreen contains a button/link that triggers URL launch (verify via mock url_launcher)
  </behavior>
  <action>
    **`lib/core/theme/color_tokens.dart`** — add `warningAmber` token. In the `AppColors` class (or equivalent token class), add:
    `static const Color warningAmber = Color(0xFFF59E0B);`
    This is Tailwind amber-500 — distinct from the green primary chip and error red. It satisfies ACC-04 (color-blind friendly alongside green — not relying on red/green alone). Add a doc comment: `/// Amber warning color for Medium CO₂ confidence chip. Tailwind amber-500. [RESEARCH.md Pitfall 8]`.

    **`lib/features/barcode_scan/widgets/confidence_chip.dart`** — `ConfidenceChip` is a `StatelessWidget` accepting `String band` ('high' or 'medium') and `VoidCallback? onTap`. Renders as an `InkWell`-wrapped `Container` with:
    - `border-radius: 12px` (pill shape)
    - Background: `band == 'high' ? AppColors.primaryContainer : AppColors.warningAmber` (where `primaryContainer` = `Color(0xFF006D2F)` — the green from color_tokens.dart). The text color must contrast: white for dark green, dark for amber.
    - Label: `'High' | 'Medium'` with a small confidence icon prefix (`Icons.check_circle_outline` for high, `Icons.info_outline` for medium)
    - Padding: horizontal 8px, vertical 4px
    - `onTap` callback — caller provides the sheet-open logic

    Also implement a `ConfidenceExplanationSheet` widget (can be in the same file or a separate `confidence_explanation_sheet.dart`): a `showModalBottomSheet` content widget that shows:
    - `Text('CO₂ Confidence: High / Medium')` as title
    - Explanation text for the band: High = "Product-specific LCA value from AGRIBALYSE v3.1.1 (direct barcode match)"; Medium = "Category-average estimate from AGRIBALYSE v3.1.1 (your product's category median)"
    - `TextButton(child: Text('Full methodology'), onPressed: () => context.push('/methodology'))` — where `/methodology` is a new route (see router addition in app_router.dart)

    Add a static helper `ConfidenceChip.showExplanation(BuildContext context, String band)` that calls `showModalBottomSheet(...)` with `ConfidenceExplanationSheet(band: band)`.

    **`lib/features/food_search/widgets/food_detail_sheet.dart`** — enable the CO₂ row. Read the existing file first to understand the sheet layout. Locate the comment about the CO₂ row being hidden (CONTEXT.md: "CO₂ row is currently hidden with a comment"). Remove the comment guard and enable the row.

    Add a top-level private function `String? _formatCo2Display(double? value)` that formats kg CO₂e/kg to 1-2 significant figures with a `~` prefix. Implementation: if `value == null` return null. Use `toStringAsPrecision(2)` on values < 10, or `value.round().toString()` for values ≥ 10. Prepend `~` and append ` kg CO₂e/kg`. Examples: `4.732 → '~4.7 kg CO₂e/kg'`, `12.04 → '~12 kg CO₂e/kg'`, `0.45 → '~0.45 kg CO₂e/kg'`.

    In the sheet body, conditionally render the CO₂ row when `item.co2e100g != null`:
    - A `Row` with `Text('CO₂e estimate:')` label, spacer, `Text(_formatCo2Display(item.co2e100g)!)`, and `ConfidenceChip(band: item.confidenceBand!, onTap: () => ConfidenceChip.showExplanation(context, item.confidenceBand!))` — only rendered when `item.confidenceBand != null`
    - When `item.co2e100g == null`: the CO₂ row is completely absent (no '—' placeholder, no "data unavailable" text — per CONTEXT.md: "No CO₂ shown rather than a Low-confidence guess")

    Add `/methodology` route to `app_router.dart` as a sibling to `/barcode-scan`:
    `GoRoute(path: '/methodology', builder: (context, state) => const MethodologyScreen())`

    **`lib/features/barcode_scan/screens/methodology_screen.dart`** — `MethodologyScreen` is a `StatelessWidget`:
    - `Scaffold` with `AppBar(title: Text('CO₂ Methodology'))`
    - Scrollable body with `Padding` and `Column` containing:
      - `Text('About CO₂e estimates', style: textTheme.titleMedium)` section header
      - Plain-language explanation: "CO₂e (carbon dioxide equivalent) measures the climate impact of food production per kg of product. Values include production, processing, and packaging — not transport to your home."
      - `Text('Data source', style: textTheme.titleMedium)` section header
      - Attribution: `'Source ADEME, données AGRIBALYSE v3.1.1'` (exact attribution string from CONTEXT.md)
      - `'INRAE/ADEME, 2023. AGRIBALYSE v3.1.1. Licence Ouverte / Open Licence v2.0 (Etalab). Available at: data.ademe.fr'`
      - `Text('Confidence bands', style: textTheme.titleMedium)` section header
      - Two-row explanation of High (product-specific LCA) and Medium (category-average estimate)
      - Note on why no Low tier: "The app shows no CO₂ estimate rather than display a poorly-sourced guess."
      - `Text('Full documentation', style: textTheme.titleMedium)` section header
      - `OutlinedButton.icon(icon: Icon(Icons.open_in_new), label: Text('View full methodology on GitHub'), onPressed: () => unawaited(launchUrl(Uri.parse('https://github.com/ReduceCO2Now/Co2-diet-app/blob/main/docs/CO2_METHODOLOGY.md'))))` using `url_launcher` package (add `url_launcher: ^6.3.1` to pubspec.yaml — use `url_launcher` not `webview_flutter` per RESEARCH.md Open Questions #2 recommendation; check if it is already a transitive dependency before adding explicitly).

    The `launchUrl` call opens the system browser (LaunchMode.externalApplication). Import `package:url_launcher/url_launcher.dart`. If `url_launcher` is not already in `pubspec.yaml`, add it: `url_launcher: ^6.3.1` with comment `# url_launcher — opens GitHub methodology link in system browser (privacy-preferred over embedded WebView). [RESEARCH.md Open Questions #2]`.

    Add CI blocklist check after adding url_launcher to verify no blocklisted transitive deps.
  </action>
  <verify>
    <automated>flutter test test/features/barcode_scan/confidence_chip_test.dart test/features/barcode_scan/co2_formatting_test.dart test/features/barcode_scan/methodology_screen_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - `AppColors.warningAmber` exists in `color_tokens.dart`
    - `ConfidenceChip` compiles with `band` and `onTap` parameters
    - `ConfidenceChip.showExplanation()` opens explanation sheet
    - CO₂ row visible in `FoodDetailBottomSheet` when `item.co2e100g != null`
    - CO₂ row absent (not '—') when `item.co2e100g == null`
    - CO₂ value formatted to 1-2 significant figures with `~` prefix
    - `MethodologyScreen` contains "AGRIBALYSE v3.1.1", "Source ADEME", "Licence Ouverte"
    - `/methodology` route added to `app_router.dart`
    - `flutter test test/features/barcode_scan/` exits 0 with ≥3 passing tests
    - `flutter analyze` clean on all modified files
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit docs/CO2_METHODOLOGY.md and activate NFR-06(b) integration benchmark</name>
  <files>
    docs/CO2_METHODOLOGY.md,
    integration_test/co2_coverage_benchmark_test.dart
  </files>
  <action>
    **`docs/CO2_METHODOLOGY.md`** — create this documentation file committed to the repo. It is the public methodology document linked from `MethodologyScreen` and from the Legal Hub in Phase 6 (LEG-05 requires it to be publicly accessible). Content:

    ```markdown
    # CO₂ Methodology: CO₂ Diet App

    ## Overview
    CO₂ Diet estimates the climate impact of food in kg CO₂e (carbon dioxide equivalent) per kg of product. Estimates cover the full production lifecycle including farming, processing, and packaging — not transport from store to home.

    ## Data Source
    **AGRIBALYSE v3.1.1** — a French LCA (Life Cycle Assessment) database covering ~2,500 food categories.

    - **Producer:** INRAE / ADEME (Agence de la transition écologique)
    - **Version:** 3.1.1 (2023)
    - **License:** Licence Ouverte / Open Licence v2.0 (Etalab) — commercial use permitted, redistribution permitted, attribution required
    - **Citation:** Source ADEME, données AGRIBALYSE v3.1.1
    - **DOI:** 10.57745/B5DTRR (DATAVERSE)

    Only precomputed impact score CSV files are used — NOT the raw LCI/ecoinvent-linked formats (which carry an ecoinvent license dependency).

    ## Confidence Bands

    | Band | Meaning |
    |------|---------|
    | **High** | Product-specific LCA value: the exact barcode was matched to a CIQUAL product code with a measured lifecycle assessment |
    | **Medium** | Category-average estimate: the product's food category was matched to an AGRIBALYSE food group; the displayed value is the median CO₂e for that group |
    | *(none)* | No AGRIBALYSE coverage: no CO₂ estimate is shown — the app never displays a poorly-sourced guess |

    ## Category Mapping
    Products without a direct AGRIBALYSE barcode match are assigned CO₂e values using a category mapping file (`tools/off_to_agribalyse_map.csv`). This file maps Open Food Facts `categories_tags` values to AGRIBALYSE food groups. The mapping is hand-curated and committed to this repository.

    ## Display Format
    CO₂e values are always displayed with a `~` prefix and rounded to 1–2 significant figures (e.g., `~2.3 kg CO₂e/kg`, not `2.345 kg CO₂e/kg`). This reflects honest uncertainty — even "High" confidence values are LCA model outputs, not physical measurements.

    ## What Is Not Included
    - Transport from store to home
    - Home cooking energy
    - Food waste after purchase
    - Packaging disposal

    These factors are scoped to the CO₂ Calculation Settings screen (Phase 5), which lets users optionally configure regional transport, cooking method, and waste level.

    ## Out of Scope
    - **Poore & Nemecek 2018:** Under AAAS copyright with non-commercial restrictions — not included. A CC0 permission request is a deferred action item.
    - **Clark et al. 2022 (PLOS ONE, CC-BY 4.0):** Identified as a potential future enhancement (Phase 8).

    ## Version History

    | Version string | Data | Date |
    |----------------|------|------|
    | AGRIBALYSE-3.1.1-v1 | AGRIBALYSE v3.1.1 synthesis CSV | Phase 3 (2026) |
    ```

    **`integration_test/co2_coverage_benchmark_test.dart`** — replace the Wave 0 stub with the real benchmark. The stub used `markTestSkipped()` — replace with a real integration test that:

    1. Calls `IntegrationTestWidgetsFlutterBinding.ensureInitialized()`
    2. In `testWidgets('NFR-06(b): ≥90% of products in off_reference.sqlite have a CO₂e estimate')`:
       - Loads `off_reference.sqlite` from the app's asset (using the same `FirstLaunchExtractor` pattern from Phase 2, or by directly accessing the asset bundle)
       - Opens an in-memory SQLite connection via `NativeDatabase.memory()` and ATTACHes the extracted DB path
       - Runs: `SELECT COUNT(*) FROM products WHERE primary_category_tag IS NOT NULL OR barcode IN (SELECT barcode FROM food_co2_overrides)` — this is the count of products with CO₂e coverage
       - Runs: `SELECT COUNT(*) FROM products` — total product count
       - Asserts: `coveredCount / totalCount >= 0.90`
       - Prints the coverage percentage for visibility: `debugPrint('CO₂ coverage: ${(coverage * 100).toStringAsFixed(1)}%')`

    The benchmark runs on a connected device (`flutter test integration_test/co2_coverage_benchmark_test.dart`). It should pass on the Galaxy Tab S7 FE (Android) used for the Phase 3 verification checkpoint.

    Note: This integration test requires `drift_flutter` for `NativeDatabase` — already in pubspec. Import `package:drift/native.dart`. Use `on Exception catch (e)` for any DB error handling (very_good_analysis rule).
  </action>
  <verify>
    <automated>test -f docs/CO2_METHODOLOGY.md &amp;&amp; grep -c "AGRIBALYSE v3.1.1" docs/CO2_METHODOLOGY.md &amp;&amp; grep -v "^#" docs/CO2_METHODOLOGY.md | grep -c "Licence Ouverte"</automated>
  </verify>
  <done>
    - `docs/CO2_METHODOLOGY.md` exists and contains all required sections: AGRIBALYSE v3.1.1, Licence Ouverte, confidence band definitions, citation, DOI
    - `integration_test/co2_coverage_benchmark_test.dart` contains real query logic (not markTestSkipped stub)
    - `flutter analyze integration_test/co2_coverage_benchmark_test.dart` reports no issues
    - `flutter test test/features/barcode_scan/methodology_screen_test.dart` exits 0 (all methodology screen tests pass)
    - Complete phase 3 test suite: `flutter test test/` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → GitHub (url_launcher) | URL opened in system browser — no data sent from app to GitHub beyond the URL itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | url_launcher GitHub link | accept | URL opens docs/CO2_METHODOLOGY.md on GitHub — no PII transmitted, public repo. System browser handles privacy. |
| T-03-04-02 | Tampering | CO₂ value display | mitigate | `_formatCo2Display` always rounds to 1-2 significant figures. False precision (NFR-05) is prevented by the formatter. Null input → null output → row hidden. |
| T-03-04-SC | Tampering | url_launcher package install | mitigate | url_launcher is a Flutter ecosystem standard (flutter.dev publisher, transitive dep of many Flutter apps). Run CI blocklist check after adding. |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. Full test suite
flutter test test/

# 2. Analyze all Phase 3 files
flutter analyze lib/features/barcode_scan/ lib/features/food_search/widgets/food_detail_sheet.dart lib/core/theme/color_tokens.dart docs/

# 3. Verify methodology doc
grep "AGRIBALYSE v3.1.1" docs/CO2_METHODOLOGY.md
grep "Licence Ouverte" docs/CO2_METHODOLOGY.md

# 4. Verify warningAmber token
grep "warningAmber" lib/core/theme/color_tokens.dart

# 5. Confidence chip renders correctly (widget test)
flutter test test/features/barcode_scan/confidence_chip_test.dart -v
```
</verification>

<success_criteria>
- `AppColors.warningAmber = Color(0xFFF59E0B)` added to `color_tokens.dart`
- `ConfidenceChip` widget compiles with `band` (String) and `onTap` (VoidCallback?) params
- Explanation sheet opens on chip tap with "Full methodology" link
- CO₂ row renders in `FoodDetailBottomSheet` for items with `co2e100g != null`
- CO₂ row absent entirely when `co2e100g == null` (no placeholder '—')
- CO₂ values formatted to 1-2 significant figures with `~` prefix (NFR-05)
- `MethodologyScreen` contains AGRIBALYSE v3.1.1, Source ADEME, Licence Ouverte text
- `/methodology` route added to `app_router.dart`
- `docs/CO2_METHODOLOGY.md` committed with all required fields (LEG-05)
- NFR-06(b) benchmark integration test contains real query (not stub)
- `flutter test test/` exits 0
- `flutter analyze` clean on all modified files
</success_criteria>

<output>
Create `.planning/phases/03-barcode-scanning-co-factor-table/03-04-SUMMARY.md` when done.
</output>
