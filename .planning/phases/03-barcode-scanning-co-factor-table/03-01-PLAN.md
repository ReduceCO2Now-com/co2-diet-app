---
phase: 03-barcode-scanning-co-factor-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/features/barcode_scan/barcode_scan_notifier_test.dart
  - test/features/barcode_scan/confidence_chip_test.dart
  - test/features/barcode_scan/co2_formatting_test.dart
  - test/features/barcode_scan/methodology_screen_test.dart
  - test/data/local/food_catalog_dao_barcode_test.dart
  - integration_test/co2_coverage_benchmark_test.dart
autonomous: true
requirements:
  - LOG-03
  - LOG-04
  - CO2-01
  - LEG-05

must_haves:
  truths:
    - "flutter test exits 0 on the Wave 0 stub suite (all tests skip with clear labels)"
    - "Every subsequent plan in Phase 3 has a named test file to target in its <verify> block"
    - "No production code is modified in this plan"
  artifacts:
    - path: "test/features/barcode_scan/barcode_scan_notifier_test.dart"
      provides: "Stubs for LOG-03 lookup-chain tests and LOG-04 no-match test"
    - path: "test/features/barcode_scan/confidence_chip_test.dart"
      provides: "Stub for CO2-01 confidence chip widget test"
    - path: "test/features/barcode_scan/co2_formatting_test.dart"
      provides: "Stub for CO2-01 significant-figures formatting test"
    - path: "test/features/barcode_scan/methodology_screen_test.dart"
      provides: "Stub for LEG-05 methodology screen attribution test"
    - path: "test/data/local/food_catalog_dao_barcode_test.dart"
      provides: "Stub for FoodCatalogDao barcode lookup with CO₂ fields"
    - path: "integration_test/co2_coverage_benchmark_test.dart"
      provides: "Stub for NFR-06(b) ≥90% CO₂ coverage benchmark"
  key_links:
    - from: "test/features/barcode_scan/barcode_scan_notifier_test.dart"
      to: "lib/features/barcode_scan/providers/barcode_scan_notifier.dart"
      via: "group-level skip referencing production class name"
      pattern: "skip:.*BarcodeScanNotifier"
---

<objective>
Create Wave 0 test stubs for all Phase 3 automated test files. Every stub file must compile, run with `flutter test`, and exit 0 — tests skip atomically at the group level when the production class is absent (Phase 2 pattern: group-level skip for atomic failure detection).

Purpose: Satisfy the Nyquist rule — every execution plan in Phase 3 has a named test file in its `<verify>` block. Stubs prevent a failing wave-1 execute from producing "file not found" errors.

Output: Six test/integration_test stub files. Zero production code changes.
</objective>

<execution_context>
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alisafi/Documents/ReduceCO2-Now/Co2-diet-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-barcode-scanning-co-factor-table/03-CONTEXT.md
@.planning/phases/03-barcode-scanning-co-factor-table/03-VALIDATION.md

<!-- Phase 2 stub pattern reference: group-level skip with markTestSkipped() inside testWidgets body -->
@test/features/food_search/food_search_notifier_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create barcode_scan_notifier_test, confidence_chip_test, co2_formatting_test stubs</name>
  <files>
    test/features/barcode_scan/barcode_scan_notifier_test.dart,
    test/features/barcode_scan/confidence_chip_test.dart,
    test/features/barcode_scan/co2_formatting_test.dart
  </files>
  <action>
    Create `test/features/barcode_scan/` directory. Write three test stub files using the Phase 2 group-level skip pattern (established in Phase 02-01 — Wave 0 unit stubs use `group` with `skip: 'BarcodeScanNotifier not yet implemented'`).

    `barcode_scan_notifier_test.dart`: Import `package:flutter_test/flutter_test.dart`. One `group('BarcodeScanNotifier', skip: 'BarcodeScanNotifier not yet implemented', () { ... })` block containing:
    - `test('lookup chain — high confidence path returns FoodItem with co2e100g and confidenceBand high', () {})` — covers LOG-03
    - `test('lookup chain — medium confidence path returns FoodItem with confidenceBand medium', () {})` — covers LOG-03
    - `test('lookup chain — no-match path returns null', () {})` — covers LOG-03
    - `test('no-match state is emitted when all three lookup steps return null', () {})` — covers LOG-04

    `confidence_chip_test.dart`: Import `package:flutter_test/flutter_test.dart`. One `group('ConfidenceChip', skip: 'ConfidenceChip widget not yet implemented', () { ... })` block containing:
    - `testWidgets('renders green chip for high confidence band', (tester) async {})` — covers CO2-01
    - `testWidgets('renders amber chip for medium confidence band', (tester) async {})` — covers CO2-01
    - `testWidgets('chip is tappable and opens explanation sheet', (tester) async {})` — covers CO2-01

    `co2_formatting_test.dart`: Import `package:flutter_test/flutter_test.dart`. One `group('CO₂ formatting', skip: 'formatCo2Display function not yet implemented', () { ... })` block containing:
    - `test('formats 4.732 as "~4.7 kg CO₂e/kg" (1 decimal, ~prefix)', () {})` — covers CO2-01/NFR-05
    - `test('formats 12.04 as "~12 kg CO₂e/kg" (2 significant figures)', () {})` — covers CO2-01
    - `test('returns null or empty string for null input', () {})` — covers CO2-01

    All three files: no imports beyond flutter_test (the production classes do not exist yet). The `specify_nonobvious_property_types` lint rule does not apply here (no property declarations in stubs). Do not add `import 'dart:ui'` — flutter_test is sufficient.
  </action>
  <verify>
    <automated>flutter test test/features/barcode_scan/barcode_scan_notifier_test.dart test/features/barcode_scan/confidence_chip_test.dart test/features/barcode_scan/co2_formatting_test.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All three test files compile and report 0 failures (tests are skipped, not failing). `flutter test` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create methodology_screen_test, food_catalog_dao_barcode_test, co2_coverage_benchmark_test stubs</name>
  <files>
    test/features/barcode_scan/methodology_screen_test.dart,
    test/data/local/food_catalog_dao_barcode_test.dart,
    integration_test/co2_coverage_benchmark_test.dart
  </files>
  <action>
    Write three more stub files.

    `methodology_screen_test.dart`: Import `package:flutter_test/flutter_test.dart`. One `group('MethodologyScreen', skip: 'MethodologyScreen not yet implemented', () { ... })` block containing:
    - `testWidgets('displays AGRIBALYSE v3.1.1 attribution string', (tester) async {})` — covers LEG-05
    - `testWidgets('displays Source ADEME credit text', (tester) async {})` — covers LEG-05
    - `testWidgets('shows GitHub link button for full methodology doc', (tester) async {})` — covers LEG-05

    `food_catalog_dao_barcode_test.dart`: This is a new file alongside the existing `food_catalog_dao_test.dart` (do NOT modify the existing file). Import `package:flutter_test/flutter_test.dart`. One `group('FoodCatalogDao.lookupByBarcodeWithCo2', skip: 'barcode lookup not yet implemented', () { ... })` block containing:
    - `test('returns FoodItem with co2e100g and confidenceBand="high" for a barcode in food_co2_overrides', () {})` — covers LOG-03
    - `test('returns FoodItem with confidenceBand="medium" for a barcode in products with category match', () {})` — covers LOG-03
    - `test('returns null when barcode not found in any local source', () {})` — covers LOG-03

    `integration_test/co2_coverage_benchmark_test.dart` (in integration_test/, not test/): Use the Phase 02-01 pattern for integration benchmark stubs — `testWidgets` body calls `markTestSkipped(...)` to exit cleanly without a device. Import `package:flutter_test/flutter_test.dart` and `package:integration_test/integration_test.dart`. Body:
    ```
    IntegrationTestWidgetsFlutterBinding.ensureInitialized();
    testWidgets('NFR-06(b): ≥90% of products in off_reference.sqlite have a CO₂e estimate', (tester) async {
      markTestSkipped('CO₂ coverage benchmark: run after ingest pipeline produces co2_factors table');
    });
    ```

    For the integration test stub, `markTestSkipped()` inside the `testWidgets` body exits cleanly without requiring a device — this is the established Phase 2 pattern (documented in STATE.md: "Integration benchmark stubs use markTestSkipped() inside testWidgets body (not skip: arg) to exit cleanly without device").
  </action>
  <verify>
    <automated>flutter test test/features/barcode_scan/methodology_screen_test.dart test/data/local/food_catalog_dao_barcode_test.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All three stub files exist. The two unit stub files run and exit 0. The integration test stub exists and compiles (not run here — requires device). `flutter test test/` exits 0 (full suite including new stubs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production | Test files must not import production code that does not exist yet — stubs use only flutter_test imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Wave 0 stub plan — no new dependencies |
</threat_model>

<verification>
Run full test suite after both tasks complete:

```
flutter test test/
```

Expected output: all Phase 1 and Phase 2 tests still green; six new Phase 3 stubs skipped (not failing). Exit code 0.

Also run flutter analyze to confirm no lint violations in the new stub files:

```
flutter analyze test/features/barcode_scan/ test/data/local/food_catalog_dao_barcode_test.dart
```
</verification>

<success_criteria>
- Six stub files exist at exact paths listed in files_modified
- `flutter test test/` exits 0
- No production files modified
- All stubs use group-level skip (unit tests) or markTestSkipped() in body (integration test) — per Phase 2 established patterns
- No imports beyond flutter_test and integration_test in stub files
</success_criteria>

<output>
Create `.planning/phases/03-barcode-scanning-co-factor-table/03-01-SUMMARY.md` when done.
</output>
